# Implementation TODO

Consumer: the user and Codex executing the 2026-09-04 request to implement the reality-check backlog and GitHub voice-leading issue #2. Gate: do not declare this campaign complete while a requested item remains unresolved. Observed defect: the audit found 51 closed records with unfulfilled obligations. Retirement: retire this checklist when the campaign is complete; lasting commands/results belong on the Beads. This inventory is process and earns no implementation credit.

## Current order

- [ ] Complete all fifteen September ideas requested by the owner; full ownership/dependencies remain in `PRODUCT_IDEAS_2026_09.md` and Beads. Start with the independent entry-repair/focus improvements while preserving existing engine owners and prerequisites.
- [x] Entry repair (`jcpe-entry-repair-0h1g`): all three phases and epic closed. Final69 scenarios pass; six focused reduced-motion profile rechecks also pass. This is one of fifteen ideas complete, not an aggregate or deployment claim.
  - [x] Freeze UTF-16 source selection, stale refusal, IME and Escape behavior with independent fixtures (six literal spans, six refusal ranges, twelve interaction obligations; specification-only test 1/0).
  - [x] Share diagnostic prose; retain multiline drafts; add exact token selection and bounded Next error on both entry surfaces.
  - [x] Preserve surrounding text and caret; add explicit Keep/Cancel repair and composition-safe Enter/Escape. First browser run found camel-cased Preact composition handlers did not subscribe to native events; fixed with native lowercase listeners and cleanup. Original assertions retained; command and Library IME/stale tests now pass 2/0.
  - [x] Show exact existing duration/insertion labels; retain all U1 parser/input/render limits and explicit recovery semantics.
  - [x] Prove current controller draft-only edits, Insert/Undo, stale ranges, Unicode and boundary cases: focused unit/controller 44/0 (236 assertions), existing overlay/U1 edit-channel 56/0 (280 assertions); four production-source mutations killed with an honest baseline pass. The first integration test called a nonexistent snapshot property; corrected to the public previewQuickEntryDraft method and manually recounted the UTF-16 offset. No production expectation was regenerated.
  - [x] Replace the raw-barlines pristine insertion workaround with one parsed atomic edit. Retain the first measure ID, comments/quoted barlines, intentional empty bars, exact fractions and one-step Undo/Redo. The committed implementation fails all three independent regression cases; the replacement passes them plus existing atomic/editing laws (334 tests, 7163 assertions).
  - [x] Repair two acceptance-discovered input lifecycle defects: native multiline paste must not erase a limit refusal, and Cancel after a rejected own edit must restore the original while preserving edits accepted from another entry surface. Focused current three-engine proof 6/0; pre-fix Firefox cancellation fails its exact original-draft assertion.
  - [x] Build and run real file/HTTP three-engine desktop/phone/zoom/IME proof; retain failures, diagnostics and artifact hash. The first complete matrix was 34/2: an out-of-band stale-input control could be reset before activation, and WebKit exceeded Clear's real five-second confirmation window. Make the stale control atomic and use real keyboard confirmation; retain every assertion. A subsequent Firefox combined-flow timeout is under investigation; it is not a pass.
  - [x] Keep native IME listeners installed from layout commit until unmount; complete owned lint and application/E2E typechecks. The global typecheck still has 17 pre-existing U2 test type errors; no release claim until aggregate gates pass.
  - [x] Record the completed63-case build matrix:0failures,0skips,0retries;36 file/HTTP diagnostic reports and three native listener traces each[2,4,2,4,2], bound to artifact786aabe1.
  - [x] Independently test phone keyboard space: the first run exposed WebKit fields above the viewport after Cancel (-137/-116px). Explicit repair actions now reveal the field; all six reduced-viewport tests pass, preserving selected source, real chord IDs, revision and Undo/Redo availability.
  - [x] Complete the combined69-case verification packet on artifact462f8155 (8324728bytes):69 pass,0failed/skipped/flaky,one attempt each; six real reduced-motion profile rechecks pass. App/E2E/tools types, owned lint, static standalone and isolated exact build pass. Five selection mutants and a real premature-Insert source fault are killed. Exact reports/results are in the verification Bead; the17 pre-existing U2 test-type errors remain open.

- [ ] Chart focus (`jcpe-chart-focus-7iz1`): specification closed; build leaf released without closure after failed acceptance (Bead comment359). Feature code is present; full browser proof is not green.
  - [x] Freeze keyboard entry/exit, draft/caret, scroll, transport, recovery and preference-failure semantics; independently calculate ten boundary fixtures (specification1/0; production anchor11/0 including packet check).
  - [ ] Add one session-only toggle and preserve existing chart DOM, rails, document/history and audio ownership.
  - [ ] Expose existing Library/Harmony sheets at focused desktop widths; retain short-phone transport trigger ownership and focus return.
  - [ ] Preserve reading anchor and raw input selection; keep explicit Exit/Stop and title reachable.
  - [ ] Verify file/loopback, three engines, desktop/two touch phones,200% layout and reduced motion with actual adapters and error/request logs.
  - [ ] Retain recovery/entry regressions, prove source faults are detected, run owned types/lint/standalone checks, and close only green phases.
  - [x] Retain12 unit/fixture passes (22 assertions) and four killed production arithmetic faults, including a strengthened fractional-output case after the original rounding mutant survived.
  - [x] Preserve the local4/7 first run and subsequent startup-failing/interrupted controls. Both preceding/current artifacts exceeded the same10s startup deadline; exploratory sampling also observed3.369s startup, with no stable code-hotspot or speedup claim.
  - [x] Prepare an isolated hz3 browser copy with exact input manifests, identical Bun1.3.14/Node26/browser binaries, native system libraries/audio and an exclusive browser lock. RCH scheduling refused the generic job's16-slot estimate; direct SSH execution is explicitly not an RCH receipt.
  - [x] Record remote3/0 focused controls (23.4s) and first full32/13 matrix (596451.428ms;0skips/flakes). Reports and before/after hashes are in `/tmp/jcpe-focus-remote-controls` and `/tmp/jcpe-focus-remote-first-full`. Do not treat the focused controls as the full gate.
  - [ ] Investigate navigation/input deadlines, Firefox pending recovery/export settlement and native-audio control delays. Retain failures; no timeout/expectation relaxation. Complete final isolated proof and existing recovery/entry regressions before closure.


- [x] Restore Cloudflare publication under `jcpe-0bjj`: the committed build is deployed and verified on both hosts.
- [ ] U5 remains open and unassigned; resume its remaining lifecycle acceptance. Automatic recovery, phone chart space, title preservation and replacement/playback fixes are verified on both hosts.
- [x] Voice-leading: reproduce raw and actually sounded discontinuities; specify a compatible versioned policy; implement; prove small-oracle, real chart, fixed-voicing, browser audio and MIDI behavior.
- [ ] U5: connect the ready lifecycle package; fix observed recovery ordering defects before adding import/export and replacement workflows.
- [ ] H0: implement all three accepted operations, full scale/readings evidence and independent proof; then close the parent.
- [ ] U2 / idea2: specification leaf `jcpe-milestone-reliable-studio-l3a.11.1` closed with exact gates; build `.11.2` claimed by CodexWorkflow. No build/browser/aggregate closure yet.
  - [x] Reconcile policy2 with Foundation: 1..16 ordered exact notes, duplicate unisons, MIDI0..127, inert2000-code-point annotations and all21 accepted family/bass combinations. Frozen copies generated pitches with actual provenance; it does not invent metadata for arbitrary Manual notes.
  - [x] Replace hash-only validation with independent arithmetic/preservation/count/reference checks; correct impossible E5/A5 MIDI coordinates and C# pitch class; add16/17, astral2000/2001, unison/order/boundary, and12 revision/preview/Undo workflow obligations.
  - [x] Catch five actual domain source faults (order, dedupe, register clamp, cap12, accept17) and four actual annotation faults (strip markup, UTF-16 counting, erase draft, reject2000), each with a passing baseline. Preserve reports under `/tmp/jcpe-u2-{source,annotation}-mutations/`.
  - [x] Preserve annotation source in the application projection, including literal markup/controls and invalid drafts. Correct contradictory unit expectations under accepted Foundation authority; retain the custom formula/pitch mismatch as a negative publication test. Focused61/0,239assertions; native rendering remains a build/verification obligation.
  - [x] Repair the17 test-type errors using real constructors and valid measure completion; fix the UI contract's private layer import without widening the production entry point.
    - [x] Publish test documents through real F2/F3; remove the17 original type errors and private import. Full refreshed typecheck passes once, then is repeated against the final frozen inputs after annotation cleanup.
    - [x] Final full typecheck/lint pass against1340 unchanged hash/mtime inputs; `/tmp/jcpe-u2-{typecheck,lint}-final.log` and `/tmp/jcpe-u2-gates-identity.json`. Initial failures retained. These gates precede the new build changes and must run again before build closure.
  - [ ] Replace unused inspector placeholder pitches/timing with real V0/playback realizations and exact document timing before wiring the surface. Fix custom-to-Auto availability, exact enharmonic octave projection and preview lifecycle; X1/X0 permit16 notes but the controller currently caps arbitrary previews at10.
  - [ ] Bind a small choice/hear/keep workflow to the existing V0/X1/application commands; retain advanced structure/timing/exact-note/annotation editing.
    - [x] Add bounded current/balanced/shell proposals from real whole-chart V2 selection; never invent notes for an unavailable family. Service6/0; real-pitch/exact-time projection14/0. No browser proof yet.
    - [ ] Complete command bridge and regression proof: SetVoicing, SetChord, annotation and exact duration; no-change reads, stale source/selection, explicit stored-note replacement, single Undo/Redo.
    - [ ] Complete preview bridge: serialized release by exact preview ID, 16-note support, preparation cancellation, adapter failure/retry and protection of band/newer preview ownership.
    - [ ] Mount an accessible inspector from Harmony Lens at desktop/phone widths; staged exact-note/Auto controls, dirty Apply/Discard/Continue, truthful preview feedback and global Stop.
    - [ ] Complete synchronized semantic structure/symbol drafts and actual Harmony/Motion facts; remove remaining projection placeholders rather than labeling them implemented.
  - [ ] Verify Manual/Frozen order, duplicates,16/17 notes, offscreen MIDI,2000/2001 and astral/markup annotations, mode confirmation, stale/cancelled preview, one Undo and global Stop.
  - [ ] Complete real browser/audio/download/storage acceptance and named aggregate gates; close only phases whose requirements actually pass.
- [ ] Shared discovery execution: accepted types/fixtures, actual bounded execution and atomic Apply, then proof.
- [ ] H1 and G0/G1: exact transforms, plural tonal paths, correct hashes/rights/compiler/query behavior and independent proof.
- [ ] G2/G3/G4: real context/route/constraint search and current web/native wiring, with all-slot constraints and proof.
- [ ] G5/G6/G7/G8/G9: lawful reharmonization, registered guide paths/color evidence, exact rhythm/tension, lawful motifs and correctly graded practice.
- [ ] M1: resolve the symmetric-input key-equivariance contradiction, preserve uncertainty in actual import and prove.
- [ ] U3/U4/U5/U6/U7/U8/U9/U10/U11: complete existing musician workflows in dependency order; preserve active assignees.
- [ ] D0/D1: reviewed Atlas and teaching content with actual compiler/engine proof.
- [ ] Cross-surface conformance: real inspect/audition/apply/undo/recovery/export and scoped native proof.
- [ ] Existing audio/native liabilities and release blockers: preserve ownership, obtain real required acceptance, fix global failures and pass Q0/R0. Human-only gates remain unresolved until real observations exist.

## Voice-leading active subtasks

- [x] Capture baseline raw voicings, alignment/gaps/span and actually sounded comp voices for all nine Deacon transitions and three reference progressions.
- [x] Author independent arithmetic/ordering fixtures and policy amendment; prove legacy specification unchanged.
- [x] Implement version-selected aggregation, identity/resume checks, deterministic bounded ranking and studio binding.
- [x] Compare with exhaustive small independent oracles; test actual V0/V1 candidate chains and all relevant fixed/section/time/spelling invariants.
- [x] Inspect and repair any demonstrated downstream register-placement regression.
- [x] Run actual browser playback/Stop and MIDI notes; verify artifact reproducibility.
- [x] Fresh acceptance review; attach exact results and close only satisfied phases.

## H0 active subtasks

- [x] Claim `jcpe-milestone-musical-intelligence-qqy.2.2`; read the complete H0 contract, public types, and inherited Bead context.
- [x] Inspect the independent corpus before using it as implementation authority. Found identical context inputs with contradictory expected dispositions, reversed tier ordering, and scale evidence absent from the public request.
- [x] Implement the `deriveLiteralFacts` production operation and public export, preserving selected T1 identity, spelling, roles, exact weights, Custom duplicates, immutable snapshots, and typed refusals. This alone does not complete H0/build.
- [x] Exercise literal fixtures, all altered selections, twelve written roots, input mutation, and version/selection precedence against the production callable: 50 pass, 2 fail. Both failures expose H0's omission of degree 11 from C13/C13sus4 despite their cited T1 formula requiring it. Keep those comparisons failing until the authority is reconciled.
- [x] Preserve executable context/scale contract witnesses in `scripts/audit-h0-contract-consistency.ts`: five findings. Reopen H0/spec and mark H0/build blocked by that dependency; retain all fixture assertions and valid T1 laws.
- [x] Run the literal-only honest control and four actual source mutants: all four killed (weights, selection, spelling, mutable alias). Recheck the generated artifact with 6/0 voice-leading browser cases, 36/0 standalone cases, and distinct-root/mtime reproducibility. These checks do not certify the missing context or scale algorithms.
- [ ] Reconcile contradictory H0 authority and publish the missing semantic context-evidence boundary. Independently agree the exact accounting recipe, then prove every reachable valid cap and source mutant.
- [ ] Implement all contextual rules and plural scale mappings after the authority is consistent.
- [ ] Replay all independent H0 corpora, execute production mutants, and record complete package gates; close only satisfied phases.

## Completed Cloudflare delivery: jcpe-0bjj

- [x] Reclaim Cloudflare delivery at the owner's 2026-09-06 request; pause U5 without marking its remaining acceptance complete. Independently query the live API: the shell token is active and names the expected account, but the exact Pages project returns HTTP 403 / `10000`. Stored OAuth cannot refresh. The other available deployment tokens also fail the same project check.
- [x] Repair deployment credential selection: preflight the exact Pages account/project before expensive gates, then check saved OAuth when environment credentials fail. Scope the credential override to Cloudflare subprocesses and preserve the shell token for other projects. Run the complete public `predeploy:check` command, retain the native playback gate, isolate public assets from the playback ledger, allowlist Vercel uploads, and execute staging cleanup on failure.
- [x] Complete the owner-authorized device sign-in and verify saved OAuth against the actual Pages project (HTTP 200). The shell token still lacks Pages access; the repaired command selects the verified login only for Cloudflare subprocesses. Credentials were never printed or committed.
- [x] Verify the repair's local release path: `bun scripts/deploy.ts --check` through the required-UA launcher passes model acceptance for 11 shipping models, mechanical quality for nine instruments, and all 15 real Chromium playback instruments with zero console/page errors. The staged bytes match the current committed artifact and staging is removed on success. The recovery subcontrol is explicitly vacuous, not fault-recovery proof. Full output: `/tmp/jcpe-cf-repair-check-all.log`; native report: `/tmp/jcpe-cf-repair-playback.json`.
- [x] Prove cleanup on a nonzero child exit after real acceptance/quality checks. The repaired command removes its stage; restoring the original `process.exit` failure path in a scratch copy leaves the stage behind and fails the same cleanup oracle. The diagnostic then removes only its own leaked stage. Retain `/tmp/jcpe-cf-repair-cleanup-{exit,mutant}-summary.json`. An earlier missing-executable control covered thrown exceptions only; these paired controls specifically exercise the repaired failure path. Neither control claims native playback or deployment success.
- [x] Commit the deployment repair as `9a6ce4790fb7c43a48364b75d8e98a71bb21bb5a`; strict tools TypeScript, changed-file ESLint, the final real-API refusal check and the immediate precommit build pass their intended assertions. All 295 source/test snapshot entries remain unchanged.
- [x] Publish committed `55ac81e31b281b489d8e9f92a29b5ec0e1fc80aa` at 2026-09-06 19:54 UTC: `bun /tmp/jcpe-ship-deploy-entry.ts` runs the canonical production script with the required user agent, Bun 1.3.14 and real Node 26. Exit 0; 11 accepted shipping models, nine quality instruments with zero failing findings, and all 15 native playback instruments with zero console/page errors. Existing warnings and the explicitly vacuous recovery subcontrol remain recorded.
- [x] Verify Cloudflare deployment `8f5d110b-8b89-4601-a5ca-9186448cfd4d` has a successful deploy stage and Vercel `dpl_CJrmKLvJZABYeJfxo5UQLXNuxcrV` is production READY. Both hosts serve SHA-256 `f7685e9ebf752fb8ce83ef907648690ed768a05839b39860e1490675cab15b3e` (8,319,874 bytes); the new stage was removed. Receipts/logs: `/tmp/jcpe-cf-after-auth-{receipt,playback,pages-status}.json`, `/tmp/jcpe-cf-after-auth-deploy.log`, `/tmp/jcpe-cf-after-auth-vercel-status.log`.
- [x] Independently compare both public HTTPS responses with `git show HEAD:jazz_chord_progression_editor.html`: exact byte/hash matches at 20:20 UTC. Preserve `/tmp/jcpe-cf-after-auth-final-host-hashes.json`. A different client receives the identical application plus exactly 367 bytes of the documented Cloudflare analytics script; preserve the insertion-only diff and raw response. Real browsers block that script under the unchanged CSP. Do not claim every response variant is byte-identical or that the host injects no script.
- [x] Prove 40 distinct real Chromium live cases: 28 recovery/layout/native-audio/import-owner-loss cases, eight Organ/Ukulele admission-delay cases and four replacement/Undo cases across the two hosts. All pass with zero page errors or unexpected console errors. The 40 expected beacon CSP messages in the 28-case matrix remain in its diagnostics. Retain eight viewport screenshots and inspect desktop/phone views on both hosts. Reports: `/tmp/jcpe-cf-after-auth-live/{results,summary}.json`, `/tmp/jcpe-cf-after-auth-live-clock-results.json`, `/tmp/jcpe-cf-after-auth-live-replacement-results.json`.
- [x] Recheck the playback failure mechanism on both hosts at 1280/390px: all eight Organ/Ukulele cases survive one real 40 ms busy wait at native engine admission (observed clock advances 32–42.67 ms), start native sources, reach zero sounding/future sources after Stop and allow fresh Play/Stop. New/lesson Cancel/Confirm restores focus and preserves exact JSON through Undo/Redo; confirmed New during playback retires the transport.
- [x] Correct and retain verification failures: the first temporary launcher failed module resolution before running any tests; the first actual run passed 25 and failed three layout cases because Escape preceded export-marker settlement. Trace evidence shows the dialog still busy. Test commit `d567bc7` adds completed-handoff and dialog-absent assertions before the existing chart hit test; all original checks and deadlines remain. E2E TypeScript and changed-file ESLint pass. Preserve `initial-module-resolution-*`, `first-browser-*` and `adaptations.json` in the live report directory.
- [x] Distinguish browser results from wrapper status: the corrected full live run has 28 passes, zero failures/skips/flakes (353,746.47 ms), but its wrapper exits 1 because invocation without explicit spec filenames requests unrelated standalone evidence. Preserve `unrequested-offline-merge-failure.json`; no F0/aggregate pass is claimed. The public runner with the two explicit live layout specs and separate `focused.config.ts` passes eight cases with zero failures/skips/flakes, 92,089.452 ms, exit 0. These repeated cells add no unique-case credit.
- [x] Rebuild before committing the test correction: root/dist/committed HTML remain identical at the hash above. Of 295 source/test snapshot entries, only the explicit four-line test correction changed; production sources are unchanged. Close `jcpe-0bjj` with the exact commands and completed live obligations recorded in its final comment. U5, aggregate findings, independent verification and human acceptance remain open. This is solo verification, not independent certification. Older artifact attempts below are historical and superseded by this deployment.

- [x] Confirm restored Vercel authentication and reproduce the committed `4887a39` artifact with unchanged source hashes/mtimes. Cloudflare account identification alone did not establish Pages authorization; the separate project access failure is recorded below.
- [x] Run the unchanged production gate: 13 instruments pass; Organ and Ukulele refuse with `transport.engine_refusal`. Preserve `/tmp/jcpe-u5-production-playback-failure.json`; no upload occurred.
- [x] Read and atomically claim the existing bug; release the U5 claim while preserving all unfinished acceptance.
- [x] Diagnose against a scratch copy of the real engine: a single 40 ms admission delay produces `audio.start_time_invalid` for both instruments; no-delay controls play. Diagnostic globals exist only in that scratch build.
- [x] Specify an explicit bounded engine-entry catch-up policy that preserves gates and leaves strict absolute-time requests unchanged.
- [x] Author independent positive, strict-negative, malformed, future-boundary, transposition and transport/preview/click regression cases. Corrected pre-fix control: 14 pass/22 fail; restored implementation: 36 pass/0 fail, 172 assertions. Initial invalid test inputs and phone selectors remain recorded with their corrections.
- [x] Implement engine admission and X1 opt-in, preserving strict absolute-time admission outside the explicit policy. Source/contracts/tests and guarded artifact are committed in `8caf56e`.
- [x] Fix the independently exposed Stop-in-ready shortcut: an active preview is retired before Stop promises no future attack. The regression exercises public release-preview refusal and repeated idle Stop behavior.
- [x] Kill five source mutations (no catch-up, unshifted release, lost strict admission, missing transport opt-in, ready Stop shortcut). Focused matrix: 356 pass/0 fail, 3,533 assertions across 32 files. App/browser types, changed-file lint and X0/X1 contract validators pass; full test types retain exactly the 17 existing U2 errors.
- [x] Prove a 40 ms injected admission-clock advance in Chromium, Firefox and WebKit on desktop/phone: 12 pass/0 fail/0 skipped/0 flaky. All 12 cases observe started native sources then zero sounding/future sources; all 24 diagnostic attachments are checked. The separate scratch diagnostic uses a real 40 ms delay; the checked-in cross-browser regression injects the clock value. The original artifact fails all four corrected Chromium counterfactual cases.
- [x] Verify the static standalone envelope and different-root/mtime reproducibility: SHA-256 `7206a79d2c5bc8227311628c058282299c42516d1181845823e93621b2f58010`, 8,318,570 bytes. All 289 source hashes/mtimes remained unchanged through native verification.
- [x] Complete the serial 204-case lifecycle matrix: 204 pass, zero unexpected/skipped/flaky, 1,757,756.944 ms. Every attached top-level console/page-error array is empty. All 289 source hashes/mtimes remain unchanged; report `/tmp/jcpe-0bjj-browser-lifecycle-serial-results.json`. The first attempt was interrupted after an accidental overlapping `verify:standalone` browser launch; both reports remain under `/tmp/jcpe-0bjj-overlap-retained`. Its initial file-navigation timeout predates that overlap and remains unexplained, not attributed to it. Static-only verification uses the documented `--static-only` argument.
- [x] Rebuild immediately before deployment: unchanged `7206a79d` bytes. The original deployment script at `a2ac0bd` passes model acceptance and all 15 real playback instruments with zero console/page errors. Its recovery control is explicitly vacuous; the separate native failure cases supply actual reconciliation proof. Preserve `/tmp/jcpe-0bjj-production-playback.json` and `/tmp/jcpe-0bjj-production-deploy.log`.
- [x] Publish the exact gated stage to Vercel after Cloudflare stops the combined script: deployment `dpl_AgnrMbV34A2bZ98UHuj2YBBQrWGt`, production READY. The mirror returns HTTP 200 with committed SHA-256 `7206a79d2c5bc8227311628c058282299c42516d1181845823e93621b2f58010`, 8,318,570 bytes. The staging upload allowlist contains only `index.html` and `og-image.png`; link-created local environment files and diagnostics are excluded. No gate was skipped or source changed.
- [x] Verify Vercel in real desktop/phone browsers: four Organ/Ukulele cases survive a real 40 ms busy wait at native engine admission, with started sources followed by zero sounding/future sources and fresh playback. Two replacement cases prove New/lesson Cancel/Confirm focus and exact Undo/Redo. Two import/recovery cases prove all six owner-loss paths, exact JSON preservation/download/Undo, and cold Keep/Discard. All eight cases have zero console/page errors. Both screenshots are inspected and neither viewport overflows. Reports: `/tmp/jcpe-0bjj-live-vercel-{clock,replacement,import-recovery}-results.json`.
- [x] Resolve the delivery obligation from the failed 02:08 UTC attempt by publishing the current artifact above. At that earlier attempt Pages returned `10000`, saved OAuth was expired, and the custom domain served `5b8678cde3e005f522d96fab1e3eacffa43f22b66501a0aa2fbf324d4db2b475` (8,204,122 bytes). That historical `7206a79d` target is superseded, not retroactively deployed to Cloudflare. Old Vercel-linked stages remain excluded from uploads.
- [x] Record exact results and push implementation/proof commits through `a2ac0bd`. At that historical point this Bead, full U5, aggregate-gate findings and independent/human acceptance stayed open. The delivery obligation is now closed above; full U5 and the other acceptance work remain open. The scoped Vercel receipt is `/tmp/jcpe-0bjj-partial-deploy-receipt.json`; the combined production script exited 1 at Cloudflare authentication, while Vercel's continuation exited 0 and is independently verified.
- [x] Resolve the automatic-recovery delivery obligation with the current artifact above. The `675fe5a` target (`b6105ad410c5e70bd9d1959683b462e93dcc517452b68af8849bf66b10da10b1`, 8,319,722 bytes) passed its gates but failed Pages authorization at 05:25 UTC; the 05:27 UTC custom-domain read still returned `d7a2cb2f8ab61573d9a9566b2d8ec38ca6d150425576a63fc23b08918e1c1bbb` (8,203,755 bytes). That target is superseded, not retroactively deployed. The successful fresh stage and automatic-recovery live proof are recorded above.

## U5 active subtasks

- [x] Claim `jcpe-milestone-reliable-studio-l3a.13.2`; read the accepted U5 contract and inherited context. Basic recovery is already wired; the README's broader disconnected claim is stale.
- [x] Preserve existing recovery bytes throughout startup and the Keep/Discard decision; serialize admitted writes with Discard so recovery cannot resurrect after successful removal.
- [x] Seed recovery's document/revision dedupe without a boot write; focus, export-preview, and other ephemeral notifications must not overwrite stored copies. A subsequent document edit still persists.
- [x] Bind write completion to document ID, revision, and write generation; an older document with the same numeric revision cannot become clean.
- [x] Replace the UI's unknown recovery token and unchecked cast with an application-owned choice; refuse stale Keep after edits and retain actionable failures.
- [x] Render previous-envelope fallback and corrupt startup diagnostics. Await actual Discard, retaining the offer and bytes when removal fails, with a working retry.
- [x] Render pending/current recovery and exact export status; show quota/denied diagnostics with the pending state and Export JSON action. Keep storage failure independent from chart editing.
- [x] Connect canonical JSON preparation, immutable preview, exact browser handoff and A0 marker settlement. Cancellation, malformed/wrong-byte receipts, allocation failure, and stale completion advance no marker.
- [x] Persist A1's complete export binding through the actual storage adapter, including the delivered file hash/length; verify a fresh service reads it. Quota after handoff must not claim durable marker persistence.
- [x] Cover Blob/URL/anchor/append/click failures with exact cleanup accounting, including cleanup refusal. Preserve synchronous gesture activation and the existing CSP.
- [x] Keep the U0 modal/focus lease across dismissibility/committing changes, with A0 history locking and exact focus return.
- [x] Connect bounded canonical/legacy file and paste preview, grouped reports, inert cancel and revision-bound commit through E0/A0/X1.
- [x] Allocate the import preview identity in the composition-owned replacement workflow; do not start retirement on preview or silently rebind a displayed confirmation to a later request identity. Restore the frozen six-member composition return; the unchanged A0/E0 static policy caught the widened boundary.
- [x] Implement bounded File/clipboard reading, independently decoded canonical data, legacy migration with grouped diagnostics, exact non-Undoable acknowledgement and single-use candidate authority. Focused integration proof passes; normal file/clipboard browser acceptance passes. Oversized history has focused proof; its browser matrix remains below.
- [x] Refuse stale previews, late cancelled reads, removed dialog hosts and oversized input without reusing an earlier candidate; clear import draft authority on failure.
- [x] Fix import command logical time against the existing history. The first real-browser run caught a backwards-time refusal missed by the initial zero-clock harness; the harness now uses prior timed edits.
- [x] Prove exact imported document equality through actual browser JSON download, Cancel/Undo, file and native clipboard paths, Manual/Frozen/Custom values and different-ID cold recovery on desktop/phone in all three browsers.
- [x] Prove serialized confirmed replacement during real playing and paused audio, with no publication from file selection or preview. Connect genuine receipt settlement so controls stop too; retain A0's notification-only generation law. Repeated imports use only the adapter's proven epoch chain; unrelated advances still refuse.
- [x] Connect lossy chart-text export, independently compare its supported projection, render loss disclosures, and preserve the canonical marker law.
  - [x] Compare every supported document/chord/timing field against actual reparsed values; retain the reviewed golden bytes and kill semantic mutations.
  - [x] Prepare text through E0, disclose exact loss counts, and hand single-use bytes to the real browser adapter through the application service.
  - [x] Prove success, cancellation, stale preparation, adapter failure and repeated clicks preserve document/history and canonical export/recovery bindings.
  - [x] Fix the encoder's unescaped section names, invalid annotated repeats, and unordered loss rows. Preserve annotated repeats as literal chords under the unchanged T0 grammar; keep the old goldens and all T0 negative cases.
  - [x] Reproduce and fix mutable delivery authority: freeze the text request and binding so the adapter cannot substitute its own expected filename. Retain the failing counterexample and the interrupted browser-run status; repeat the real browser gates on the corrected artifact.
  - [x] Verify real downloads, loss disclosures, errors, focus and phone layout in all three browsers; rebuild and bind final evidence to unchanged source/artifact bytes. Final source: 275 focused tests / 1,899 assertions; 156 broad browser passes plus six standalone passes after correcting the launcher invocation, on the same artifact. The initial broad invocation's six missing-run-ID failures remain recorded; it was not a green aggregate run.
  - [x] Retain seven killed production-source mutants, the literal fractional-duration/annotation control, exact source/hash evidence, and the existing U2/source/cast gate failures. Full U5 completion and independent verification remain below.
- [ ] Complete New/lesson replacement confirmation. Import confirmation, export-first, focus return, and single-commit ownership are connected; oversized-history browser acceptance remains open. Do not route New/lesson through a falsely labeled canonical-import origin.
  - [x] Prepare a complete validated New/lesson candidate before touching the current chart; replace the library loader's clear/stage/insert/title/groove/tempo sequence with one committed replacement. Production source committed in `7f37d88`; integration proof passes. Phone dialog handoff remains below.
  - [x] Carry the exact `new` or `lesson` origin through the application workflow and serialized retirement/publication boundary. The shared adapter preserves the real local origin and the unchanged import source-format contract; New → JSON import → New has focused proof.
  - [ ] Bind the displayed candidate, dirty/transport confirmation decision, export-first action and oversized-history acknowledgement to the exact current revision; cancel and stale confirmation must preserve every authoritative field.
  - [ ] Wire New and library gestures to the application-owned dialog, retaining focus and selection without publishing a candidate from a preview or lesson pick.
  - [ ] Prove one Undoable replacement after real retirement in ready/playing/paused/preview states; add cancel, retirement failure, repeated-confirm, stale and oversized browser cases with exact document/history/marker observations.
  - [x] Fix phone lesson confirmation ownership by carrying the launching surface's stable owner through the UI/application boundary. Preserve U0 refusal checks and show their diagnostics. Consume chart-focus requests only when confirmation owns focus; immediate empty-chart replacement retains chart focus. Final expanded matrix: 60 passes, zero failures/skips/flakes across Chromium/Firefox/WebKit, including both viewport widths, exact Cancel/Confirm focus, missing-owner refusal, and the empty-chart path. The earlier nine phone failures remain recorded in the Bead.
  - [x] Keep Export current chart first available even after native recovery finishes; recommendation status controls its explanation, not action availability. The browser case now waits for real recovery before using the action.
  - [x] Fix the audio-clock retirement race: a caller timestamp that elapsed before engine entry retires immediately at the captured engine time. Negative/nonfinite/out-of-horizon values still refuse, and future times remain exact. The restored old guard fails the corrected clock-advance regression; final audio/transport/lifecycle suite: 179 tests / 1,860 assertions. X0 contract validation passes.
  - [x] Complete the New/lesson host-removal, exact no-effect evidence-envelope, and automatic retirement-reconciliation slice. Source/tests are committed in `b02bf6b` and the guarded artifact in `be4cfb4`. The full U5 package remains open.
    - [x] Reject a late publication after its exact A0 confirmation host or captured UI owner is removed or replaced; close the invalidated host, preserve chart/history/bookmarks/markers and allow a new valid request after proven retirement. Also recover the next gesture after a host disappears before Confirm.
    - [x] Require the exact no-effect envelope before unlocking; malformed success/refusal data and thrown readers discard the publication capability and demand reconciliation. Extra fields cannot turn an uncertain effect into a no-effect refusal.
    - [x] Reconcile uncertain New/lesson retirement with one real serialized stop over the current physical epoch. Release the editing lock only after request-bound generation/ready/no-future-attack proof; never publish the failed candidate. Invalid, refused or throwing reconciliation stays visibly locked. A fresh replacement and playback work after successful reconciliation.
    - [x] Prove 196 focused tests / 1,989 assertions, 72 desktop/phone replacement/accessibility/offline browser cases, and six additional instrumented native recovery cases. All final cases pass with zero failures/skips/flakes; each native case observes started sources followed by zero sounding sources and zero future attacks. Three source mutants are killed. App/browser types, changed-file lint, U5/X1 contracts, standalone static and two-root reproducibility pass.
    - [x] Finish the import/recovery reconciliation slice and its final browser/build evidence; full U5 remains open.
      - [x] Reproduce five unsafe unlocks from ambiguous effects, rejected retirement, and throwing evidence readers; preserve the 33-pass/5-fail baseline.
      - [x] Share application-owned E0 failure handling and the real X1 stop adapter. Discard request authority before reconciliation, validate the actual echoed request and generation/ready/no-future-attack receipt, and retain the lock on unproven stops. Accepted E0 driver and fixtures remain unchanged.
      - [x] Keep import Cancel/source changes and recovery Keep/Discard disabled while reconciliation is unresolved. After a proven safe stop, preserve the current chart/history/bookmarks/markers and require a fresh preview or Keep gesture. Focused 301-test/2,994-assertion matrix passes; two source mutants fail the intended assertions and the restored 74-test control passes.
      - [x] Add native clock fault, actual source counts, exact JSON download, fresh replacement/Keep/playback/Undo, and recovery-storage preservation cases for desktop/phone in all three engines.
      - [x] Complete the 186-case U5 import/recovery/local replacement/accessibility/offline browser matrix: 186 pass, zero unexpected/skipped/flaky, 805419.593ms. All 289 source hashes/mtimes remained unchanged. Final native helper correction passes another 18 overlapping cases (123060.283ms), each with real started sources followed by zero sounding/future sources and zero console/page errors. Reports: `/tmp/jcpe-u5-import-reconcile-browser-matrix-results.json` and `/tmp/jcpe-u5-import-reconcile-native-final-results.json`.
      - [x] Verify the phone recovery-layout repair: the first Chromium probe passed five cases and exposed the fixed recovery banner intercepting Export JSON on the sixth. Place recovery inside the shell layout and modal background; retain real pointer/focus assertions and strict zero-error checks.
      - [x] Finish changed-file type/lint, U5/X1 contract, standalone static and distinct-root/mtime reproducibility checks; rebuild immediately before explicit commits. Source/proof commits: `b2cbc17`, `c6fafee`, `169aac3`, `5b6f161`, `2db99d2`; artifact commit `e8df0de`, SHA-256 `ad03e604f90c1a72cb5652a36aa137dc1381ca4ef4b08cbdb83e9a135e5604f1`, 8,316,364 bytes. Full test typecheck retains exactly 17 U2 errors; source policy retains the U2 private import and accepted E0 bridge pin validation still fails. Full U5 and independent verification remain open.
      - [x] Check the committed `e8df0de` artifact through unchanged deployment gates: `jcpe.deploy.v1`, check-only, modelAcceptance=pass, playback=pass for all 15 instruments. Predeploy quality covers nine instruments with zero failing findings and existing warnings retained (evidence `2fd8f75c5e4ddfb1`). The deployment gate's recovery control is explicitly vacuous; the 18 separate native fault cases supply actual reconciliation evidence.
      - [x] Finish custom-domain synchronization through the current `f7685e9e` artifact on both hosts; see the completed `jcpe-0bjj` checklist above. Earlier intermediate artifacts remain historical evidence, not current production targets.
  - [x] Commit and push the unchanged source, regression tests, and regenerated artifact in separate groups (`7f37d88`, `ddb1389`, `1a81d4a`). Focused checks: 51 tests / 513 assertions; app and browser-test typechecks, changed-file lint, static standalone, and two-root reproducibility pass. Artifact SHA-256 `53321c21c6a01d1b9e18c99de9fa03687d58003899f77f3e5892b84e69fed76c`, 8,314,227 bytes. No production upload.
  - [x] Commit and push the phone/focus, export-first, and clock-retirement fixes in logical groups: `aa13c18` (audio and contract), `65a1edd` (studio and regressions), `43f9902` (guarded artifact). Final artifact SHA-256 `b735a5d050272b6841fa35090a52a15b0af6c4578eb3e7a4344bd5a78c08c21d`, 8,314,648 bytes. All 287 source hashes/mtimes stayed unchanged through the final gates. App/browser typechecks, changed-file lint, X0 contract, standalone static and two-root reproducibility pass. Committed-byte deploy check at `43f9902`: model acceptance passes and all 15 instruments pass real playback; the no-refusal recovery control remains explicitly vacuous. Predeploy instrument quality also passes with zero failing findings; existing warnings remain. Exact commands and earlier failed/interrupted runs are recorded in the U5/build Bead.
  - [x] Rebuild the final reconciliation source with Bun 1.3.14: artifact SHA-256 `d853215c84b5409c4bb9e45700b210ba8e4b1e007ce0dffb5c77f0960bc7cd76`, 8,317,523 bytes, byte-identical root and dist. Source hashes/mtimes stayed stable during the 72-case matrix. The subsequent type-only callback correction emits identical bytes; the instrumented native cases and final type/lint/build checks use the corrected source. Committed-byte deploy check at `be4cfb4` passes model acceptance and all 15 real playback instruments. Predeploy instrument quality passes for nine instruments with zero failing findings; existing warnings remain. The separate playback recovery control is explicitly vacuous, while the six new native fault cases provide actual reconciliation evidence.
  - [x] Finish custom-domain synchronization through the current `f7685e9e` artifact on both hosts; see the completed `jcpe-0bjj` checklist above. Earlier intermediate artifacts remain historical evidence, not current production targets.
  - [ ] Resolve the pre-existing A0/E0 bridge documentation-pin mismatch (`BRIDGE_ACCEPTED_E0_DRIFT`, `docs/E0_INTERCHANGE_CONTRACT.md`) through its authority owner; the current E0 validator itself passes. Do not rewrite an accepted pin to manufacture a pass.
- [x] Verify cold startup discovery after replacing with a different document ID, through native browser storage, reload, Keep and exact JSON export.
- [x] Connect a bounded durable location to the actual recovery adapter, preserving imported IDs. Location-write failure must not certify clean recovery; Discard retains location without chart payloads so an older document is not resurrected. Real A1 service and three-browser cold-start/Discard proof pass.
- [x] Complete the auto-open recovery explanation and Discard/New surface. Automatic-current presentation follows the A1 matrix without overwriting edits or unapplied drafts entered while startup was pending.
  - [x] Reconcile A1/U5 startup fixtures with the starter-chart task: seed only when no recovery exists; render immediately and preserve edits during the probe.
  - [x] Capture failing automatic-open/pending-draft controls before changing production behavior: three application regressions fail at baseline. A later native-browser control also proves that an unapplied title draft must downgrade automatic recovery.
  - [x] Defer the demo until an untouched, empty recovery result; preserve explicit shared charts and session changes during the probe. Raw UI input now supplies an application intent because title drafts do not yet advance document revision or application state.
  - [x] Retain a visible automatic-open explanation; wire Discard to local recovery deletion and New to the existing serialized replacement confirmation, including Cancel and focus restoration. Discard leaves the open chart in memory and cancels its queued recovery write.
  - [x] Prove automatic success, conflicting edits/drafts, previous-copy fallback, Discard failure/success, no boot audio, and real reload/storage/download behavior on desktop and phone. All 36 new cases pass across Chromium, Firefox and WebKit; their diagnostics contain no console/page errors and share the tested artifact hash. All six audio cells observe started native sources, then zero sounding sources and future attacks after both Stop cycles.
  - [x] Run focused gates and production-source mutation controls; rebuild, commit explicit paths, push, and record exact results without closing unfinished U5 acceptance.
    - [x] Retain the full 261-case run: 260 passed and one WebKit import/Undo case exceeded its unchanged 30-second deadline. The stylesheet CSP message began after teardown; Playwright's WebKit screenshot preparation inserts a stylesheet. No CSP or assertion was relaxed.
    - [x] Compare the unchanged failing scenario with the default Mesa renderer and four workers on this 128-CPU host: 19.4/20.3s default controls versus 15.5s with four workers; observed llvmpipe workers fell from 32 to 4. The runner now supplies a bounded Linux default while honoring explicit overrides.
    - [x] Complete the full matrix with the documented renderer default: 261 pass (87 per engine), zero failures/skips/flakes, 2190977.417ms. The original full-run timeout remains retained, not silently converted to a pass. Report: `/tmp/jcpe-u5-auto-browser-bounded-renderer-results.json`; summary: `/tmp/jcpe-u5-auto-browser-bounded-renderer-summary.json`.
    - [x] Pass 259 focused tests / 2,033 assertions across 17 files; kill five production-source mutants; pass app/browser/tools typechecks, changed-file lint, A1/U5 contracts, static standalone and two-root/mtime reproducibility. All 289 source files plus the runner retain their hashes and nanosecond mtimes throughout the final browser gate. The 17 existing U2 test-type errors and other aggregate liabilities remain open.
    - [x] Rebuild immediately and push four logical groups: production/application proof `53535e8`, native browser proof `e2fdac0`, measured renderer resource policy `eb079b0`, and guarded artifact/docs/tracker `675fe5a`. Remote main was verified at `675fe5a03843ac0b8dcca4be3b0c102157a68b20`; GitHub reported no Actions runs for that commit. Root/dist/committed artifact bytes match SHA-256 `b6105ad410c5e70bd9d1959683b462e93dcc517452b68af8849bf66b10da10b1` (8,319,722 bytes).
    - [x] Run the unchanged deployment script against committed bytes: model acceptance and all 15 native playback instruments pass; the recovery subcontrol explicitly reports no reproducible refusal and earns no fault-recovery credit. Cloudflare then refuses authentication. Continue Vercel with the exact gated file and an upload allowlist for only HTML/OG assets; production deployment `dpl_7Azk6bKrWHujdWS1hBerXLe8b5FN` is READY and its public HTTPS bytes match the commit. Logs: `/tmp/jcpe-u5-auto-committed-deploy.log`, `/tmp/jcpe-u5-auto-committed-playback-gate.json`, `/tmp/jcpe-u5-auto-vercel-deploy.log`.
    - [x] Verify the deployed HTTPS application at 1280x900 and 390x844: 12 pass, zero failures/skips/flakes, 139441.149ms. All 12 artifact-bound attachments have zero console/page errors; both native audio cases retain one context after trusted Play and reach zero sounding/future sources after each Stop. Exact JSON, Discard failure/retry/durable reload, New/Cancel focus/Undo, delayed native IndexedDB draft preservation and explicit-share priority pass. Both screenshots were inspected; recovery explanations/actions render and neither viewport overflows horizontally. Report and summary: `/tmp/jcpe-u5-auto-live/{results,summary}.json`.
    - [x] Improve chart visibility at 390x844 and 320x568 while preserving the recovery explanation and precise Discard semantics. The original collapsed chart and completed layout/title repairs are recorded below; current live proof covers both hosts.
      - [x] Measure the smaller supported phone before editing: at 320x568 the recovered chart scrollport collapses to exactly zero pixels, and scrolling a chord cannot make it usable. Preserve the native storage/reload measurements and screenshots in `/tmp/jcpe-u5-phone-before*`.
      - [x] Add failing real-browser regression cases for short fine/coarse phones, tall phone and desktop; measure usable chart space and actual pointer hit targets. Corrected Chromium baseline: one pass / three failures; restored control: four pass. The first draft's invalid corner probes on the circular Play button are retained and corrected to interior axis points.
      - [x] Give compact layouts a scrollable shell frame and a minimum usable chart area while preserving all recovery copy/actions and the persistent transport. The short touch scrollport grows from 0 to 265px and remains 213px after selection. Both CSS mutations fail their intended assertion. The first no-scroll mutant survived programmatic scrolling; the strengthened test now requires measured movement from native wheel input, and that earlier survivor remains recorded.
      - [x] Diagnose the first full matrix honestly: 275 passed / 7 failed, zero skips/flakes. Earlier tail-only progress updates missed Firefox failures and were corrected in the operator update and Bead comment. Preserve `/tmp/jcpe-u5-phone-first-matrix-results.json` and its browser artifacts.
      - [x] Repair Firefox's added scroll-wrapper tab stop so the existing skip link remains first. Remove the new optional screenshot call that makes Playwright inject a WebKit animation-sync stylesheet; retain CSP and every behavior/error assertion.
      - [x] Reproduce the startup title overwrite with native input; synchronize external titles before paint and preserve focused text selections. Add controlled real-UI/application tests for untouched, selected and dirty fields with delayed passive effects: nine engine/case controls pass. The first partial fix still changed an active selection and was not accepted; the completed native quota diagnostic preserves all 12 exact edits.
      - [x] Catch and repair the over-broad focus guard: confirmed New deliberately focuses the title field, so the guard retained the old chart's title. Bind preservation to document identity and reset on replacement, including equal-title replacements. The first expanded run was stopped after two New failures (37 passed, two failed, 252 not run); retain `/tmp/jcpe-u5-phone-interrupted-title-replacement*` as an interrupted diagnostic, not a release pass.
      - [x] Prove title scheduling counterfactuals after the document-boundary fix: each removed safeguard fails its intended case (two pass / one fail), with exact source/mtime restoration.
      - [x] Diagnose the next full run (290 passed / one failed, zero skips/flakes): the export-refusal test captured the initial blank title, but the trace shows the demo and revision 3 already published before its first Export click. Add an explicit default-startup precondition before that baseline; preserve all original cancellation/failure/marker/title/focus assertions. All six engine/viewport controls pass. Retain `/tmp/jcpe-u5-phone-export-baseline-failure*` and `/tmp/jcpe-u5-export-baseline-control-results.json`.
      - [x] Complete the fresh 291-case browser gate: 97 pass per engine, zero failures/skips/flakes, 1285188.407ms. All 295 source/test snapshot entries remain unchanged; all 12 viewport diagnostics bind SHA-256 `f7685e9ebf752fb8ce83ef907648690ed768a05839b39860e1490675cab15b3e` with zero console/page errors. Reports: `/tmp/jcpe-u5-phone-final-matrix-{results,summary}.json`. Earlier failed/interrupted runs remain retained.
      - [x] Verify exact recovered JSON, chord selection, recovery action reachability, New/Cancel/Undo, title scheduling and native storage/audio through the responsive/recovery/accessibility/offline matrix. App/browser types, changed-file lint, static standalone and reproducibility pass. The immediate precommit rebuild reproduces root/dist at 8,319,874 bytes and the same SHA-256 above.
      - [x] Commit and push explicit groups: layout `87d7161`, title timing/selection/document boundary `b993806`, export-test startup precondition `1f99b8f`, artifact/docs/tracker `11ef504`. Remote main matched `11ef50462a70f97a5c72c23d7f898d134177bce2`; GitHub reported zero Actions runs (no workflow directory). Ephemeral reports/caches remain untracked.
      - [x] Pass predeploy acceptance (11 shipping models), mechanical quality (nine rendered instruments, zero failing findings) and native playback (15 instruments) against committed bytes. The playback recovery subcontrol is explicitly vacuous and earns no fault-recovery credit. The immediate predeploy rebuild and gated stage match `git show HEAD` exactly. Logs: `/tmp/jcpe-u5-phone-{predeploy-check,committed-deploy}.log`; native ledger: `/tmp/jcpe-u5-phone-committed-playback-gate.json`.
      - [x] Deploy the same gated stage to Vercel: `dpl_FEpsdUV6mKzhhu9dKWJqCj4vNMsv` is production READY; public HTTPS returns the exact committed SHA-256 above. Upload allowlist includes only HTML/OG assets; the new stage's CLI-created token file was removed afterward. The combined deploy command failed at Cloudflare authentication `10000`, and the authorized mirror upload was continued separately.
      - [x] Verify six live Chromium cases (four viewport/input cells plus desktop/phone New/Cancel/Undo): six pass, zero failures/skips/flakes, 27357.244ms. All six diagnostics bind committed bytes with zero console/page errors; phone and desktop screenshots were inspected. Reports: `/tmp/jcpe-u5-phone-live/{results,summary}.json`. The first live launcher failed before running tests because its Git output buffer was too small; the temporary adapter's capture bound was corrected, with the original setup failure retained and all assertions unchanged.
      - [x] Complete Cloudflare publication under `jcpe-0bjj`: both hosts serve the committed `f7685e9e` artifact, with successful provider receipts and live desktop/phone/recovery/audio proof recorded above. The former `10000` blocker is resolved; full U5 acceptance remains separate.
  - [x] Validate checksum-accepted recovery candidates through F2/F3 before offering them, in current/previous order; retain slot-specific refusal details and never modify storage during selection.
  - [x] Prove structurally/semantically invalid current, invalid previous, both invalid, valid-current precedence, no automatic previous opening, stale Keep and actual successful previous Keep. The 19-case orchestrator suite passes; five temporary production-source mutants fail the intended assertions and the unchanged control passes.
  - [x] Verify native browser storage, checksum-valid semantic corruption, exact recovered JSON, pending-offer byte preservation and cold reload in all three browsers. Final matrix: 123 pass, zero failures/skips/flakes, including all 12 new cases; final focused suite: 92 pass / 761 assertions. Artifact SHA-256 `bc42718621e69cb42f3261ce836a44405fe57f6f6b67c53157ce7e5e0f433c81`; source unchanged throughout, standalone/static/reproducibility checks pass. Full U5 and the existing aggregate failures remain open.
- [x] Keep chart-text import on the insertion lane; preserve MIDI's separate workflows. Section-headed text reaches the real Quick entry draft on desktop and phone without replacing the document.
- [x] Exercise recovery write/discard races and native browser recovery/JSON downloads at desktop and phone widths across Chromium, Firefox and WebKit, including allocation failure and focus restoration.
- [x] Finish the import slice gates: 113 focused tests / 852 assertions; 144 real-browser cases across Chromium/Firefox/WebKit and desktop/phone; app typecheck, all 24 touched TypeScript files linted, standalone static check and two-root reproducibility. Exact commands, artifact hash and prior failure dispositions are in the U5/build Bead. No deployment.
- [x] Replace Firefox's empty constructed clipboard event with native clipboard/keyboard proof. Fix the observed unsafe ephemeral port in the existing offline negative-control server with bounded high-port allocation; preserve every negative assertion, zero retries and CSP.
- [ ] Exercise the remaining New/lesson, oversized consent, active-preview replacement, storage fallback and complete lifecycle fixture matrix after implementation.
  - [x] Finish the import confirmation-owner lifetime slice and final browser evidence; the complete U5 package remains open.
    - [x] Reproduce removed/replaced A0 hosts and false/non-boolean/throwing UI consent: 24 passing import tests and five new failing regressions before the fix.
    - [x] Bind publication to the exact committing A0 descriptor, captured DOM trigger and dialog, and explicit invalidation. Check consent only at the synchronous real owner port after proven X1 retirement; cancel the actual request and let A0 consume/refuse its stale capability. Preserve the accepted E0 driver and result/receipt protocols.
    - [x] Preserve document/history/bookmarks/export/recovery state and the next valid import. A failed old attempt cannot close/update a later same-ID host; unknown retirement still requires real reconciliation. Pristine direct import uses the same guard, and busy reopening cannot replace its consent.
    - [x] Pass 309 focused tests / 3,057 assertions across 28 files. Kill three temporary source mutants (omit request revocation, loosen exact host identity, ignore host invalidation); restore bytes/mtimes and pass the 55-test/397-assertion control.
    - [x] Add real playing-audio browser cases for hidden/replaced triggers and hidden dialogs, exact downloads/Undo, visible focus, actual native source retirement and a fresh import. Initial Chromium desktop/phone probe: six passes.
    - [x] Complete the final 144-case import/recovery/accessibility/offline matrix: 144 pass, zero unexpected/skipped/flaky, 672755.314ms across Chromium 149.0.7827.55, Firefox 151.0 and WebKit 26.5 at desktop/phone widths. All 18 new owner-loss cases observe started native sources followed by zero sounding/future sources. All 132 attached error diagnostics are clean, and all 289 source hashes/nanosecond mtimes remained unchanged. Report: `/tmp/jcpe-u5-import-owner-browser-matrix-results.json`.
    - [x] Pass app/browser typechecks and changed-file lint after correcting closure-state narrowing and validating the cloned DOM node type. Full test types retain only the 17 existing U2 errors; source policy retains the existing U2 private import. Standalone/static and separate-root/mtime reproducibility pass.
    - [x] Rebuild immediately before logical commits and push application/regression source (`06be5c3`), UI/native-browser proof (`34869aa`) and guarded root artifact (`7f224ee`). Root/dist/committed bytes match SHA-256 `9803b439c4f38ba6405e16b18ba641d294974dabdfd484647215a7cfcdd9b017`, 8,318,093 bytes. The original deploy script's committed-byte `--check` passes model acceptance and all 15 real playback instruments at `7f224ee`; its no-refusal recovery control is explicitly vacuous. Log: `/tmp/jcpe-u5-import-owner-committed-deploy-check.log`. Remote main was verified at the artifact commit before recording this evidence.
    - [x] Finish custom-domain synchronization through the current `f7685e9e` artifact on both hosts; see the completed `jcpe-0bjj` checklist above. Earlier intermediate artifacts remain historical evidence, not current production targets.
- [ ] Resolve the repository-wide gate findings through their owned tasks: 17 U2 test type errors, the existing U2 private application import, and 33 existing cast-policy findings. Focused import evidence does not certify the aggregate gate.
- [ ] Run named build gates, update evidence and tracker, then perform the separate verification phase. Do not close U5 or flip human acceptance flags based on partial wiring.

## Complete unresolved tracker inventory

Statuses below are the campaign-start snapshot. Use br show and br ready before claiming; an existing assignee is not an invitation to take over.

- [ ] `jcpe-bass-vibes-temporal-character-9g4t` — Bass+vibes physical models: temporal character rejected by owner (thump/bloom, shimmer/beating) (in_progress; P0; CreamBass)
- [ ] `jcpe-cast-policy-compatibility-np17` — P0 compatibility: restore honest cast-policy conformance (open; P0; ubuntu)
- [ ] `jcpe-darl` — Native Apple correctness: theory closure, audible inversions, and promised platform paths (blocked; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1` — Epic: Release Proof — audited standalone artifact (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2` — R0: Produce the standalone artifact, documentation, and release evidence (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2.1` — R0/spec: Specify interfaces, limits, and independent fixtures (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2.2` — R0/build: Implement the complete production package (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2.3` — R0/verify: Prove conformance, integration, and evidence (open; P0; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a` — Epic: Reliable Studio — author, recover, import, play, and stop (open; P0; unassigned)
- [ ] `jcpe-playback-gate-ship-failures-u90y` — Ship candidate: dreadnought mid-chart refusal + archtop/ukulele no pitch lock (in_progress; P0; unassigned)
- [ ] `jcpe-winds-quality-triangulation-drga` — Winds quality: triangulated fix for owner-rejected flute@2/clarinet@2 timbre (in_progress; P0; unassigned)
- [ ] `jcpe-winds-reference-quality-qb68` — Winds: reference-judged realism gate for clarinet v2 + flute (in_progress; P0; TurquoiseBluff)
- [ ] `jcpe-2nky` — Disable Cloudflare Web Analytics beacon injection for jazzchords.org (CSP correctly blocks it; edge modifies released bytes) (open; P1; unassigned)
- [ ] `jcpe-60xy` — X0 human listening session: author release-evidence/audio/listening/x0-listening-v1.json (open; P1; unassigned)
- [ ] `jcpe-765f` — M1: Idiot-proof automated MIDI import — one gesture from .mid to playable chart with matched groove (open; P1; unassigned)
- [ ] `jcpe-bv2b` — FrankenSim-powered next-gen instrument physics: ultra-realism campaign (owner criteria 2026-08-09) (in_progress; P1; unassigned)
- [ ] `jcpe-discovery-execution-proof-gdcw` — Discovery execution: truthful bounded jobs and atomic proof validation (open; P1; unassigned)
- [ ] `jcpe-discovery-execution-proof-gdcw.1` — Discovery execution: truthful bounded jobs and atomic proof validation / spec (open; P1; unassigned)
- [ ] `jcpe-discovery-execution-proof-gdcw.2` — Discovery execution: truthful bounded jobs and atomic proof validation / build (open; P1; unassigned)
- [ ] `jcpe-discovery-execution-proof-gdcw.3` — Discovery execution: truthful bounded jobs and atomic proof validation / verify (open; P1; unassigned)
- [ ] `jcpe-espf` — HUMAN: audition Upright Bass + Concert Vibes on the live studio (jcpe-1miv acceptance) (open; P1; unassigned)
- [ ] `jcpe-ionn` — M1/spec: Freeze the automated-import laws — track roles, harmonic-rhythm segmentation, contextual resolution, settings transfer, groove match (open; P1; ubuntu)
- [ ] `jcpe-ios-quality-verification-uo47` — FrankenJazz iOS: quality, honesty, and recorded verification (in_progress; P1; unassigned)
- [ ] `jcpe-j4hj` — Instrument selection requires manual refresh: newly selected physical instruments do not take effect live (in_progress; P1; SwiftBridge)
- [ ] `jcpe-milestone-advanced-craft-ulj` — Epic: Advanced Craft — routes, constraints, reharmonization, and practice (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy` — Epic: Musical Intelligence — explainable continuation and Harmonic Atlas (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2` — H0: Implement evidence-tier context readings and chord-scale tables (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2.1` — H0/spec: repair five executable authority conflicts (reopened; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2.2` — H0/build: Implement the complete production package (blocked by H0/spec; P1; CodexImplementation; literal projection implemented, full package incomplete)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2.3` — H0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3` — H1: Build proof-carrying transformation laws and spelled transposition (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3.1` — H1/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3.2` — H1/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3.3` — H1/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4` — G0: Implement phrase analysis and k-best Tonal Journey paths (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4.1` — G0/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4.2` — G0/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4.3` — G0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5` — G1: Build Atlas schema/compiler, provenance, facets, and fingerprints (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5.1` — G1/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5.2` — G1/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5.3` — G1/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6` — G2: Implement the explainable Contextual Continuation Engine (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6.1` — G2/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6.2` — G2/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6.3` — G2/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7` — D0: Author and compile the reviewed 250-seed Harmonic Atlas (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7.1` — D0/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7.2` — D0/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7.3` — D0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8` — U3: Build the evidence-first Harmony Lens and motion explanations (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8.1` — U3/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8.2` — U3/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8.3` — U3/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9` — U8: Build Continuation, Atlas, Similarity, and Tonal Journey UI (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9.1` — U8/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9.2` — U8/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9.3` — U8/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1` — Q0: Run the complete regression, accessibility, visual, and performance proof (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1.1` — Q0/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1.2` — Q0/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1.3` — Q0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11` — U2: Build semantic chord inspector, exact piano, and real preview (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11.1` — U2/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11.2` — U2/build: Implement the complete production package (in_progress; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11.3` — U2/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.12` — U4: Build transport controls and truthful audio status (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.12.2` — U4/build: Implement the complete production package (in_progress; P1; IvoryBluff)
- [ ] `jcpe-milestone-reliable-studio-l3a.12.3` — U4/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.13` — U5: Build document, recovery, and import/export lifecycle dialogs (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.13.2` — U5/build: Implement the complete production package (in_progress; P1; CodexImplementation)
- [ ] `jcpe-milestone-reliable-studio-l3a.13.3` — U5/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-mnsc` — Epic: Gesture-driven physical instrument realism (open; P1; unassigned)
- [ ] `jcpe-mnsc.4` — PHS2: Clarinet v2 dynamic reed, tongue, tone holes, and continuous bore (open; P1; unassigned)
- [ ] `jcpe-mnsc.4.3` — PHS2/verify: Prove conformance, integration, and evidence independently (in_progress; P1; ubuntu)
- [ ] `jcpe-mnsc.5` — PHS3: Flute v2 embouchure, tone-hole lattice, fingering, and continuous air column (open; P1; unassigned)
- [ ] `jcpe-mnsc.5.2` — PHS3/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.5.3` — PHS3/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.6` — PHS4: Coupled guitar family: strings, bridge/body, excitation, pickups, and amps (open; P1; unassigned)
- [ ] `jcpe-mnsc.6.2` — PHS4/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.6.3` — PHS4/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.7` — PHS5: Trumpet outward-striking lips, valves, bore, bell, and brass nonlinearity (open; P1; unassigned)
- [ ] `jcpe-mnsc.7.2` — PHS5/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.7.3` — PHS5/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.8` — PHS6: Physical or hybrid vibraphone: bars, mallets, resonators, fans, and pedal (open; P1; unassigned)
- [ ] `jcpe-mnsc.8.2` — PHS6/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.8.3` — PHS6/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.9` — PHS7: Whole-system physical synthesis acceptance, documentation, and release evidence (open; P1; unassigned)
- [ ] `jcpe-mnsc.9.2` — PHS7/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.9.3` — PHS7/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.9.4` — HUMAN: audition the complete gesture-driven physical instrument matrix (open; P1; unassigned)
- [ ] `jcpe-qbvz` — M1/verify: Independent proof over real multi-track MIDI — conformance, ear-check, and one-undo evidence (open; P1; ubuntu)
- [ ] `jcpe-rh3a` — U0 human accessibility session: attach the six manual observations to the evidence ledger (open; P1; unassigned)
- [ ] `jcpe-sample-elimination-physical-qzgo` — Replace CC0 sample recipes with physical wasm models (vibes, bass; scope piano) (in_progress; P1; unassigned)
- [ ] `jcpe-sample-elimination-physical-qzgo.6` — Replace Salamander piano attack with cooperative physical WASM (blocked; P1; unassigned)
- [ ] `jcpe-semantic-surface-conformance-32w2` — Semantic conformance: real engine wiring across studio and native bridge (open; P1; unassigned)
- [ ] `jcpe-semantic-surface-conformance-32w2.1` — Semantic conformance: real engine wiring across studio and native bridge / spec (open; P1; unassigned)
- [ ] `jcpe-semantic-surface-conformance-32w2.2` — Semantic conformance: real engine wiring across studio and native bridge / build (open; P1; unassigned)
- [ ] `jcpe-semantic-surface-conformance-32w2.3` — Semantic conformance: real engine wiring across studio and native bridge / verify (open; P1; unassigned)
- [ ] `jcpe-upbz` — M1/build: Implement the automated pipeline and the two-mode import UI (Automatic default, Advanced disclosure) (open; P1; ubuntu)
- [x] `jcpe-voice-leading-continuity-0rgn` — Fix audible voice-leading discontinuities and gap-free scoring exploit (GH #2) (closed; P1; unassigned)
- [x] `jcpe-voice-leading-continuity-0rgn.1` — Voice-leading continuity / spec (closed; P1; CodexImplementation)
- [x] `jcpe-voice-leading-continuity-0rgn.2` — Voice-leading continuity / build (closed; P1; CodexImplementation)
- [x] `jcpe-voice-leading-continuity-0rgn.3` — Voice-leading continuity / verify (closed; P1; CodexImplementation)
- [ ] `jcpe-wyw5` — M1 human listening notes: audition the two imported reference charts under the matched groove (open; P1; unassigned)
- [ ] `jcpe-z62r` — What a Fool Believes + syncopated-sixteenths@1 landed; owner listening is the remaining gate (in_progress; P1; unassigned)
- [ ] `jcpe-0bjj` — Playback-gate flake under CPU contention: first instrument (mellow-keys) intermittently refuses (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10` — U6: Progression voice-leading workbench (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10.1` — U6/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10.2` — U6/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10.3` — U6/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.11` — U7: MIDI export workflow (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.11.2` — U7/build: Implement the complete production package (in_progress; P2; SwiftBridge)
- [ ] `jcpe-milestone-advanced-craft-ulj.11.3` — U7/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12` — U9: Route, Constraint, and Reharmonization workbench UI (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12.1` — U9/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12.2` — U9/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12.3` — U9/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13` — U10: Guide, color, rhythm, tension, sequence, and nonfunctional discovery UI (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13.1` — U10/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13.2` — U10/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13.3` — U10/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14` — U11: Practice Laboratory UI (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14.1` — U11/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14.2` — U11/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14.3` — U11/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15` — D1: Curate lessons, practice prompts, palette, help, and keyboard guide (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15.1` — D1/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15.2` — D1/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15.3` — D1/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3` — G3: Goal-directed Harmonic Route Planner (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3.1` — G3/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3.2` — G3/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3.3` — G3/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4` — G4: Constraint Harmonization Workbench engine (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4.1` — G4/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4.2` — G4/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4.3` — G4/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5` — G5: Proof-carrying reharmonization branches (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5.1` — G5/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5.2` — G5/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5.3` — G5/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6` — G6: Guide-tone paths and contextual Color Laboratory engine (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6.1` — G6/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6.2` — G6/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6.3` — G6/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7` — G7: Cadence, phrase, harmonic-rhythm, and tension-curve transforms (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7.1` — G7/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7.2` — G7/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7.3` — G7/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8` — G8: Motif, sequence, and nonfunctional transformation atlas (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8.1` — G8/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8.2` — G8/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8.3` — G8/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9` — G9: Deterministic Chart-to-Practice Laboratory engine (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9.1` — G9/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9.2` — G9/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9.3` — G9/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-dsp-flute-c7-44k1-regime-c4p1` — Flute MIDI 93/96 at 44.1/48 kHz: sub-14-sample jets refuse fractional tuning (regime flips) (open; P4; unassigned)
- [ ] `jcpe-zw47` — Batch local MIDI import: score user-supplied candidate files and auto-select the best (open; P4; unassigned)
