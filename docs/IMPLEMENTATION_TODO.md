# Implementation TODO

## Legacy E0 delivery boundary — jcpe-4po2

- [x] Trace remaining cast-policy sites to unchecked delivery receipts; read E0 sections10–11 and reuse existing normalization design. Claim/reserve exact paths.
- [x] Original-source strict RCH baseline6pass/13fail; all five honest terminal controls pass. Malformed receipts/envelopes incorrectly accepted. Preserve `test-results/legacy-delivery-baseline-20260920/`; split getter/retained-mutation witnesses and strengthen reflection case afterward.
- [x] Reuse `normalizeExportDelivery` against private binding; require a plain own-data single completion promise, reject getters/custom prototypes, snapshot typed outcomes and retire exactly once. No new normalizer or validator exceptions.
- [x] Strict RCH hz2:319 tests/1318 assertions across eight affected E0/export suites, full types, scopedlint, boundaries and guarded build pass in `test-results/legacy-delivery-checked-20260920/`. Earlier runs had only new-fixture type errors (literal widening then branded hash), now corrected; reports fixed/final retained. Unchanged global cast-policy remains1pass/1fail with6 legacy findings vs9; hostile control passes.
- [x] Fresh replay114tests/438assertions, native downloads12/12 across Chromium/Firefox/WebKit in16.359s, guarded rebuild identical. Preserve hz2 memory admission refusal, hz4 source-drift refusal (concurrent merge markers), and hz4 SSH timeout; no local fallback. Recovered hz2 browser wrapper exited1 after successful tests/build because JSON reporter path nested under config directory. Actual report recovered and verified12expected/0unexpected/skipped/flaky/global errors, no rerun or assertion relaxation. Evidence `test-results/legacy-delivery-review-recovered-20260920/`.
- [x] Concurrent peer committed newer ef5202e and merged older origin draft into two reserved files. Restored both to exactly checked hashes and merge c22cf7b preserves both histories; conflict snapshots retained. Independently checked all1,842 frozen remote and local input hashes; source mtime changed only by the owned restoration, rebaselined after exact hash proof. Final restored368 mtimes unchanged. Guarded candidate promoted only after prior root matched Git; root/dist equal SHA2562facafaf2240e6db0cbf34024bd339e9ec653fd90e4df0d4e61530d588a76754,8,646,325bytes.
- [x] Close only this bounded boundary repair, record unchanged global cast-policy6 findings on original np17 and commit/push owned artifact/TODO/tracker. Fresh solo proof; aggregate/human acceptance and production deployment remain outstanding. No CSP, normalizer, fixture expectation, deadline or retry relaxation.


## Current register and motion review — jcpe-6ujg.13.3

- [x] Read original contract, spec/build receipts and current operator/selector/inspector code; claim existing verification leaf and reserve this TODO.
- [x] Preserve exact saved pitches, bounded occurrence pairs and existing V1 admission; freeze1,841 inputs and368 source mtimes. No defect identified by source review yet.
- [x] Strict RCH hz2:54 tests/556 assertions across six operator/controller/inspector suites; all four semantic mutants killed and honest control passes; full types, scoped lint, source boundaries and guarded build pass. Evidence: `test-results/register-review-20260920/`.
- [x] Real-browser12/12 in73.059s across Chromium149.0.7827.55/Firefox151.0/WebKit26.5,320x568 and1280x900,light/dark. Zero skipped/flaky/unexpected/global errors; all12 attachments bind committed SHA256a98361533e022eefaeaed0ad344ebc1c1da23f4f65d04406ea2e1ad154f80f6b and contain zero console/page errors or forbidden requests. Native Hear/Release, exact note order/duplicates,28-pair disclosure, unsupported V1 witness, keyboard/Axe/layout and unchanged revision assertions pass.
- [x] Fresh source review found no demonstrated defect; no production/test/fixture/gate changes. Reproduced deployed artifact; final1,841 input hashes/368 mtimes unchanged. No new feature or deployment claimed.
- [ ] Human musical/usability acceptance remains required by `docs/REGISTER_OBSERVATIONS.md`; return this same verification leaf open/unassigned with exact automated proof, without closing the package or inventing a substitute acceptance task.


## Deploy MIDI export ownership repair — jcpe-ue9p

- [x] Claim deployment, reserve TODO and coordinate browser lane; pin committed2d241d4 and preserve peer panels.
- [x] Freeze1,841 committed input hashes and368 source mtimes; artifact SHA256a98361533e022eefaeaed0ad344ebc1c1da23f4f65d04406ea2e1ad154f80f6b,8,646,325bytes.
- [x] Strict RCH hz4 guarded build reproduced committed bytes. Model11 and instrument-quality9 pass (0 fail findings,16 warnings); native playback15/15 pass, recovery explicitly vacuous. `test-results/midi-dismissal-release-gates-20260920/`.
- [x] All1,841 hashes/368 mtimes unchanged; final strict RCH rebuild reproduces the same artifact (`test-results/midi-dismissal-release-rebuild-20260920/`). Initial upload guard stopped because peer committed metadata-only328ed4a/4cab596; verified exact source/artifact equality and repinned4cab596. Uploaded git-show bytes:Cloudflare c82dac02; Vercel dpl_6xtC1bXfFs8n6wvLJtdFdjjVAXbR READY/production. Both HTML/social-image hashes match on first poll; social image SHA256b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12,1,295,227bytes. Vercel staging env remains ignored and excluded; upload contained only2 assets.
- [x] Real browser desktop/phone boot, MIDI download/cancel, delayed fingerprint and cleanup-error recovery on both hosts. Alternate Python response still injects beacon (+367bytes,SHA256251c2f13fa89105dedecde704ad0c28ad01acc0bb26b51b58e2d0040a6e428d1); keep jcpe-2nky open and CSP strict. Initial live run passed16/18 Cloudflare cases; two WebKit cleanup cases failed the console assertion. Trace timestamps place each stylesheet CSP error inside an explicit screenshot call; installed Playwright coreBundle.js:20678 injects an inline `body {}` style when synchronizing WebKit screenshots. Removed optional explicit screenshot calls, retained native video and every behavior/error assertion; full36-case verification follows. Preserve `test-results/midi-dismissal-release-live-20260920/`. Corrected hz4 run17/18: all WebKit pass; Firefox320 pending-hash case stalled before first click was dispatched (5s native click timeout). IO full avg60 measured21.44%; pressure is a suspect, not a proven cause. Preserve `test-results/midi-dismissal-release-live-final-20260920/`; unchanged suite moved to quieter hz2 (full avg60=2.23%) without retries or deadline changes. Final strict RCH hz2 passes36/36: Cloudflare18/18 in82.674s; Vercel18/18 in65.416s, Chromium149.0.7827.55/Firefox151.0/WebKit26.5. All36 diagnostics have exact committed response hashes, zero console/page errors and forbidden requests; no skipped/flaky/unexpected/global errors (`test-results/midi-dismissal-release-live-hz2-20260920/`). Native MIDI headers/download hashes, unchanged chart revision, focus return and zero outstanding blob URLs pass. Initial-run phone/desktop screenshots inspected; capture and video retain-on-failure policy unchanged.
- [x] Reviewed receipts and all36 diagnostic attachments; final1,841 input hashes/368 source mtimes unchanged. Close only deployment leaf with exact results; commit/push owned tracker/TODO and release reservation. Same-author fresh verification, not independent-person or aggregate/human acceptance. Known Cloudflare injection and16 quality warnings remain disclosed.

## MIDI export dismissal ownership — jcpe-ls5c

- [x] Trace U7 contract and real compact-sheet/service cancellation defect; claim and reserve exact paths.
- [x] Original-source strict RCH baseline:20 U7 passes/3 ownership failures;12/12 browser cells fail at cancelled-hash registry leakage or enabled Close during delivery. Desktop busy was also aria-only, so both surfaces need blocked dismissibility. Evidence: `test-results/midi-dismissal-baseline-20260920/`.
- [x] Keep committed delivery owned until completion on phone and desktop; suppress dismissed preview replies and release their prepared bytes (2d241d4; regression evidence below).
- [x] Strict RCH focused units, full typecheck, scoped lint, guarded build and three-engine desktop/phone browser proof. hz2 refused critical memory admission; switched to hz4. Cancelled one queued attempt before execution to finish cleanup handling; subsequent input guard rejected App drift before tests. Initial final run:23 U7 unit passes, scoped lint/build pass,18/18 browser pass; typecheck failed on Playwright reducedMotion option placement. Corrected to contextOptions; no assertion/deadline/retry relaxation. Preserve `test-results/midi-dismissal-final-20260920/`. Repinned1,841 inputs/368 source mtimes; Complete run passed:23 U7 tests/173 assertions, full typecheck, scoped lint, guarded build and24/24 browser cases in101.604s across Chromium149.0.7827.55/Firefox151.0/WebKit26.5. Zero skipped/flaky/unexpected/global errors; all24 source-bound attachments have zero console/page errors or forbidden requests. `test-results/midi-dismissal-complete-20260920/`. Fresh replay:23 units/173 assertions and24/24 browser cases in119.705s, same clean diagnostic counts (`test-results/midi-dismissal-fresh-20260920/`).
- [x] Fresh same-author review/replay, final strict RCH precommit rebuild and1,841 input hashes/368 source mtimes unchanged. Complete/fresh/rebuild artifacts identical:SHA256a98361533e022eefaeaed0ad344ebc1c1da23f4f65d04406ea2e1ad154f80f6b,8,646,325bytes. Prior root equaled git-showHEAD before generated artifact promotion; root/dist now identical. Evidence: `test-results/midi-dismissal-rebuild-20260920/`. Close only this repair leaf and commit owned paths; no independent-person, aggregate verify or deployment claim.
- [x] Deployed and verified on both hosts under jcpe-ue9p. U7 independent/human acceptance and Cloudflare injection remain open.

## Deploy MIDI Stop acknowledgement — jcpe-p1ip

- [x] Claim deployment leaf, reserve TODO, coordinate browser lane and pin cab1f38; preserve three peer panels.
- [x] Freeze1,839 committed input hashes and368 source mtimes.
- [x] Strict RCH: initial hz4 build failed because tmpfs dependencies were absent; restored cached dependencies. Preserved `test-results/midi-stop-release-gates-20260920/`. Restored hz4 guarded build/model11/quality9 pass with16 warnings; native playback14/15, Mellow keys measured master peak0 despite Playing. Preserve full failure in `test-results/midi-stop-release-gates-restored-20260920/`; observed I/O full avg60 near65%, not proven sole cause.
- [x] Exact same committed artifact and unchanged native gate on lower-I/O hz2:15/15 playback pass, recovery explicitly vacuous. `test-results/midi-stop-release-playback-hz2-20260920/`. Uploader checks model/quality and fresh playback receipts separately against identical1,839 inputs/artifact/gate-source hashes; no failed playback counted as pass.
- [x] Source368 mtimes and1,839 hashes unchanged; final strict RCH hz2 guarded rebuild reproduces SHA2565bd775bcfc587e6a407002790b309029241853c0d7c4ab2fd4e3c606ad65ff72,8,645,682bytes (`test-results/midi-stop-release-rebuild-20260920/`). Stage git-show2ae7bb9 bytes only, no runtime edits. Cloudflare ef797165 and Vercel dpl_8MvZJainNqnjd37LntSxHFbyL34C READY/production; excluded Vercel staging env remains ignored.
- [x] Both curl HTML/image hashes match on first poll; image b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12,1,295,227bytes. Alternate Python response still includes beacon (+367bytes, SHAcedb9d72fec56a42f3a9f86cb73c9ed02269bb977c23c33e6848000680d5c166). Keep jcpe-2nky open; no CSP relaxation or injection-removal claim.
- [x] Native MIDI audition Stop/Discard at320/1280px across three engines on each host. Initial live35/36:Cloudflare18/18, Vercel17/18; Firefox320px piano file-input timeout before playback. Trace showed immediate Sound close→Library click without confirming dismissal/focus return, then no Library dialog. Add positive closed-dialog/focus-return/panel-visible assertions; final full36-case run passes:Cloudflare18/18 in148.434s, Vercel18/18 in130.630s. Chromium149.0.7827.55,Firefox151.0,WebKit26.5; no skips/flakes/unexpected/global errors. All36 hash-bound diagnostic attachments have zero console/page errors and unexpected requests (`test-results/midi-stop-release-live-final-20260920/`). Four desktop/phone screenshots inspected across hosts: stopped feedback readable, controls fit. Preserve `test-results/midi-stop-release-live-20260920/`, no deadline/retry/assertion relaxation.
- [x] Reviewed complete receipts, diagnostics and screenshots; final1,839 input hashes/368 source mtimes unchanged. Close only deployment leaf; commit/push owned tracker/TODO and release reservation. Human/aggregate and Cloudflare injection obligations stay open.

## Acknowledge MIDI audition stop — jcpe-e6ui

- [x] Trace explicit stop UI: it ignored the asynchronous release result, declared success early and removed retry.
- [x] Claim/reserve UI, browser harness/spec, TODO and generated artifact; coordinate and preserve peer panels.
- [x] Corrected original-source baseline:6 pass/6 fail across Chromium/Firefox/WebKit; refusal/rejection cases reproduce the missing Stop button while release is pending, success/replacement controls pass (`test-results/midi-stop-red-fixed-20260920/`). Preserve the earlier12 setup failures: fake clock selected rendered piano without its renderer; corrected fixture selects real analog-poly through the controller. The first misnamed scratch baseline ran existing tests only and is not defect proof.
- [x] Retain Stop while pending/failed; announce success only after acknowledgement; suppress stale replies and cancel timers immediately. Source/tests were committed by the concurrent session as69bfc56/230cb95; preserve that history.
- [x] Strict RCH hz2 final:32 controller tests/423 assertions, full typecheck, scoped ESLint and guarded build pass. Browser27/27 in144.260s:12 controlled-release cases and15 unchanged native MIDI-source/organ/piano audition checks across Chromium149.0.7827.55,Firefox151.0,WebKit26.5; zero skipped/flaky/unexpected/global errors. All12 release attachments bind App SHA8dbd36ebb244d7ee6ede876100527fbf7a8eb71d7e2080976cf2bf753829e90d and have zero errors/requests. Native checks retain exact chart and audio Stop/Discard assertions. `test-results/midi-stop-final-20260920/`. Preserve earlier scoped lint failure, corrected callback syntax; positive active-voice assertion added, no gate relaxation.
- [x] Fresh strict RCH hz2 rebuild and12/12 regression replay in46.239s; zero skips/flakes/unexpected/global errors (`test-results/midi-stop-fresh-20260920/`). Same-author fresh verification, not independent-person acceptance. Generated root/dist match both earlier builds:SHA2565bd775bcfc587e6a407002790b309029241853c0d7c4ab2fd4e3c606ad65ff72,8,645,682bytes. All1,839 input hashes/368 source mtimes unchanged; prior root equaled git-showHEAD before promotion. Bounded honesty audit found no gate weakening; disclosed setup/invocation/lint failures retained. Commit only owned test corrections/artifact/TODO/tracker; preserve peer panels.
- [x] Deployed and verified under jcpe-p1ip; human/aggregate acceptance stays open.

## Deploy audition release repairs — jcpe-drbr

- [x] Claim deployment leaf; reserve TODO and coordinate browser lane. Pin ec8448d; preserve three peer-owned panels.
- [x] All1,837 inputs matched committed bytes;368 source mtimes frozen including untouched peer panels. Strict RCH hz4 fresh guarded build/predeploy pass:11 shipping models,9 instruments,0 failures/16 retained warnings; native Node26 playback15/15. Recovery row explicitly vacuous, not failure-recovery proof. RCH report transfer timed out after remote exit0; exact reports recovered via rsync without rerunning or relaxing gates (`test-results/audition-release-gates-20260920/`).
- [x] hz2 had zero admissible slots (strict refusal103); final strict RCH hz4 rebuild passes and matches committed89987ee (runtime ec8448d), SHA256 a0398a0bbe62b362d5560331710f1b221108869b414a07a52985ac52d68a4e50,8,645,438bytes. Peer commits changed only tracker/TODO. Inputs/mtimes rechecked before upload.
- [x] Staged git-show89987ee bytes only; Cloudflare d84e4f1d and Vercel dpl_2TYekCmchotMYHhzyDnuKQheUDVt READY. Raw curl HTML/image hashes match both hosts on first poll; image b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12. Alternate Python HTTP response still contains Cloudflare beacon (+367bytes; SHA e601c70abf762dc19e2d7d2980e97761ab86986bfe12dbecc6fdc636bfce3a21). jcpe-2nky remains open; CSP unchanged. Vercel-created staging env file stays ignored and excluded by upload allowlist.
- [x] Preserve initial hz4 live failures:15/18 (three WebKit timeouts; disk IO full avg60 45.98%), then17/18 after moving profiles/cache/artifacts to tmpfs (all WebKit passed, one Firefox click timeout). No assertions/timeouts/retries changed. Reports `test-results/audition-release-live{,-ram}-20260920/`; disk pressure alone not proven as complete cause.
- [x] Recovered hz2 admission with zero IO/memory pressure: Cloudflare18/18,140.837s,all18 diagnostics clean. Wrapper still exited1 because scratch invocation omitted explicit spec filenames and mistakenly triggered the unrelated standalone-offline evidence merger. Preserve `test-results/audition-release-live-hz2-20260920/`; correct only command arguments to name the same three specs.
- [x] Final strict RCH hz2 command explicitly names all three specs:36/36 live checks,18 per host at320/1280px across Chromium149.0.7827.55,Firefox151.0,WebKit26.5; clean command receipts,zero skipped/flaky/unexpected/global errors. Cloudflare136.384s,Vercel121.648s. All36 hash-bound diagnostics have zero console/page errors or forbidden requests; real audition release, exact JSON/undo/redo/recovery and MIDI-source preservation pass. Four desktop/phone Chromium screenshots inspected: source notes/Stop focus visible and controls fit. `test-results/audition-release-live-final-20260920/`.
- [x] Final1,837 input hashes/368 source mtimes unchanged; no runtime source edits, assertion/time-limit changes or CSP relaxation. Close only deployment leaf; preserve all failed/harness-error runs and outstanding human/physical-device/aggregate obligations. Commit/push owned TODO/tracker and release exact reservation.

## Retry note-first and MIDI-import audition release — jcpe-t3v8

- [x] Reproduce refusal/throw retries in note entry, MIDI pitch sets and MIDI playback plans: strict RCH baseline 6 pass / 18 fail; six cross-panel isolation controls pass.
- [x] Keep independent exact-submission retirement ownership for the two panels, while immediately cancelling pending preparations.
- [x] Strict RCH hz2: 96 focused tests/989 assertions across six suites, all TypeScript projects, scoped ESLint and guarded build pass (`test-results/audition-retire-final-20260919/`).
- [x] Strict RCH hz2 fresh replay:24 tests/324 assertions. Native note-first exact audio/Manual roundtrip and MIDI source review:12/12 at320/1280px across Chromium149.0.7827.55, Firefox151.0, WebKit26.5 in104.515s. Zero skips/retries/unexpected/global errors; all12 hash-bound diagnostics show zero console/page errors or forbidden requests. MIDI source voice counts4→0 with no future attacks. Note-first export/undo/redo/local recovery preserves exact Manual notes. `test-results/audition-retire-browser-20260919/`.
- [x] Final strict RCH guarded rebuild matches both earlier builds: SHA256 `a0398a0bbe62b362d5560331710f1b221108869b414a07a52985ac52d68a4e50`,8,645,438bytes. All1,837 input hashes and368 source mtimes unchanged (including three untouched peer panels). Root matched HEAD before guarded artifact promotion; dist is byte-identical. Close only jcpe-t3v8 after exact gate receipts; same-author replay is not independent-person or physical-device acceptance.
- [x] Deployed with required committed-byte gates and both-host browser checks under jcpe-drbr; broader human and package acceptance remains open.

## Retry inspector audition release — jcpe-j3vf

- [x] Inspect U2 preview contract and current source; claim exact controller/test/artifact/TODO paths and coordinate.
- [x] Add8 real controller/X1 cases over fake audio platform: refusal/throw crossed with retry, repeated retry, newer preview and new preparation. Preserve voice-count, exact identity and unchanged chart controls.
- [x] Original-source strict RCH hz2 baseline17pass/6fail. Strengthen preparation-race case to explicitly refuse the background release; corrected baseline against f25801b remains17pass/6fail,143 assertions. Existing15 tests and2 newer-owner controls pass. Preserve both `test-results/inspector-retire-baseline{,-corrected}-20260919/`.
- [x] Retain source-bound submitted identity separately from pending preparation; clear only after acknowledged retirement or observed replacement. Retry exact owner without cancelling newer unrelated previews.
- [x] Strict RCH hz2 focused64tests/566assertions across inspector/register/comping/pads, all4 TypeScript projects, scoped ESLint and guarded build pass (`test-results/inspector-retire-final-20260919/`).
- [x] Fresh solo strict RCH hz2 replay23tests/179assertions passes. Native inspector54/54 in302.699s, file/HTTP ×1280/390/320px ×3engines ×3original preview tests. Chromium149.0.7827.55, Firefox151.0, WebKit26.5; zero skipped/flaky/unexpected/global errors. All54 hash-bound diagnostics have zero console/page errors or forbidden requests. Real pointer/keyboard held-source retirement, exact chart preservation, independent band playback and global Stop pass. `test-results/inspector-retire-browser-20260919/`.
- [x] Final strict RCH guarded rebuild matches static/browser builds: SHA256 `ce4fb767b6346e12b9349a38c8f1fbdb10358205b96ca8214760b7ebb56a09d3`,8,645,217bytes. All1,836 input hashes/365 source mtimes unchanged; root matched HEAD before generated artifact promotion. Source/tests committed by peer as613d59e; preserve its history. Close only bounded bug; commit owned artifact/TODO/tracker and release reservations. No deployment, independent-human, physical-device or aggregate acceptance claim; existing gaps stay open.

## Deploy export repairs — jcpe-7med

- [x] Claim release leaf; reserve TODO, coordinate browser lane, preserve three peer panels. Pin committed ee3fee3 artifact f27164fa (8,645,119 bytes) and 1,836 input hashes/365 source mtimes.
- [x] Strict RCH hz4 guarded build and fresh hz2 rebuild reproduce committed SHA256 `f27164fa2aa7b4f0c003c1884c989019e283ee00452d90bed44db08e04131fe7`,8,645,119 bytes. `bun run predeploy:check` passes11 shipping models/9 instruments with zero fail findings and16 retained warnings; native Node26 playback15/15, enforced recovery explicitly vacuous. Reports `test-results/export-release-{gates,rebuild}-20260919/`.
- [x] All1,836 input hashes/365 source mtimes unchanged. Stage exact ee3fee3 git-show HTML/image; Cloudflare5e608166 and Vercel dpl_EqijmbT4RXpbmZJKk3DWxLjs7iTb READY. Both raw curl HTML/image hashes match on first poll. Image SHA256 b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12. Python HTTP response still has injected beacon (+367bytes); jcpe-2nky remains open, no CSP relaxation. Manual orchestration preserves both required deploy gates while executing them through strict RCH.
- [x] Strict RCH hz2 live checks24/24:12 per host, two original U5 JSON tests at1280/390px across Chromium149.0.7827.55, Firefox151.0 and WebKit26.5. Actual downloads12, exact durable binding, cancellation, injected native URL-allocation failure, chart preservation and focus restoration pass. Both JSON reports show zero skipped/flaky/unexpected/global errors;24 diagnostics have zero console/page errors and only top-level host requests. Durations47.664s/35.687s. Four desktop/phone successful-export screenshots inspected; controls fit and restored Export JSON focus is visible. `test-results/export-release-live-20260919/`.
- [x] Fresh solo release verification; no independent-person, physical-device, full aggregate or natural malformed-completion injection claim. No failed test, assertion relaxation, retry or deadline change in this release run. Nine legacy cast findings, human acceptance and Cloudflare injection remain open. DCG refused deletion of Vercel-created ignored staging `.env.local`; left excluded from upload/Git, with no alternate deletion attempt. Close only release leaf and commit/push owned TODO/tracker; exact reservation released after commit.

## Export completion containment — jcpe-uc4p

- [x] Trace active click/lifecycle consumers. Ordinary malformed receipts already cannot publish a marker; accessor/proxy inspection can instead reject the operation and leave the dialog delivering. No marker-bypass claim.
- [x] Claim/reserve exact paths; notify peer and preserve unrelated panels.
- [x] Add real lifecycle failure-to-fresh-success regressions and15 valid terminal/12 malformed registry cases.
- [x] Implement exact own-data snapshots, expected artifact binding, correlated cleanup outcomes and frozen published receipts. Unknown/reflection-failed results retain unknown cleanup knowledge.
- [x] Strict RCH pre-fix driver replay:25pass/2fail. Accessor throws from shallow outcome validation; proxy with valid visible values is admitted without inspecting its throwing prototype trap (not a demonstrated marker-data bypass). Both new regressions pass with normalization. `test-results/delivery-normalization-baseline-20260919/`.
- [x] Strict RCH hz4:150 tests/684 assertions across6 delivery/marker/lifecycle files, all4 types, scoped lint and guarded build pass. Unchanged cast-policy1pass/1fail retains9legacy findings; hostile controls pass. `test-results/delivery-normalization-final-20260919/`. Initial149pass/1fail exposed an invalid success fixture with artifact:null; corrected to the actual binding and added an exact binding assertion. New test matcher typing and2 lint mistakes corrected without suppression. Initial logs retained in `test-results/delivery-normalization-checked-20260919/`.
- [x] Fresh solo hz2 replay119tests/529assertions. Native adapter12/12 in16.100s; actual studio JSON export/cancellation/failure12/12 in36.261s at1280/390px across Chromium149.0.7827.55, Firefox151.0, WebKit26.5. No skipped/flaky/unexpected/report errors;12 hash-bound diagnostics have zero console/page errors and only file requests. `test-results/delivery-normalization-browser-hz2-20260919/`.
- [x] Preserve interrupted hz4 browser run separately: RCH job30027234903327490 cancelled after several minutes with severe IO stalls; Firefox cancellation trace reports Channel closed/target closed. No passing denominator claimed. Exact process absence checked before whole unchanged matrix moved to hz2. Raw logs/traces in `test-results/delivery-normalization-browser-cancelled-20260919/`; original deadlines/assertions/retries untouched.
- [x] All1,836 input hashes/365 source mtimes unchanged. Final strict RCH hz2 guarded rebuild is identical to hz4/final and hz2/browser builds: SHA256 `f27164fa2aa7b4f0c003c1884c989019e283ee00452d90bed44db08e04131fe7`,8,645,119bytes. Root matched HEAD before guarded artifact promotion to root/dist. `test-results/delivery-normalization-rebuild-20260919/`.
- [x] Implementation/artifact/evidence committed and pushed as311a5dd; peer integration-regression commit206e589 retained. Closed only jcpe-uc4p with exact results and interruption disclosure. Human/aggregate obligations and9legacy cast findings remain open; no deployment claim.

## Typed browser export capabilities — jcpe-dqge

- [x] Inspect E0 section10, architecture, existing delivery tests and parent cast-policy findings; claim one leaf and reserve exact source/test paths.
- [x] Replace unchecked global/Blob assertions with structurally checked optional native globals, callable guards whose outputs remain unknown, and an owned ArrayBuffer Blob part. Preserve picker/writer receivers and synchronous activation.
- [x] Add ordinary/shared sliced-byte snapshot checks and exact native-method receiver checks; existing cancellation, fallback and cleanup tests remain intact.
- [x] First strict RCH focused tests/types/build pass; lint finds10 errors in the initial implementation. Preserve that failed run in `test-results/export-types-checked-20260919/`; correct the source without lint suppression or assertion weakening.
- [x] Corrected strict RCH hz4:85 tests/321 assertions, all4 TypeScript projects, scoped lint and guarded build pass. Unchanged cast-policy reports1pass/1fail with9 legacy application findings (down from11); hostile controls pass, original parent stays open. `test-results/export-types-final-20260919/`.
- [x] Fresh solo delivery replay65 tests/215 assertions; existing12 native download/activation checks pass across Chromium, Firefox and WebKit in16.001s with zero failed/skipped/flaky cases or report errors. Golden downloads, exact cleanup, activation and zero console/page errors asserted. `test-results/export-types-browser-20260919/`; JSON reporter nested its output under that directory. No physical-device or independent-review claim.
- [x] Final strict RCH guarded rebuild matches SHA256 `344c216f3b457e95b2f776096ae93c8bdb862894da35de3ecb3203ea2bc2fb8d`,8,640,754 bytes. All1,835 input hashes/364 source mtimes unchanged; generated root/dist promoted only after checking prior root against HEAD. `test-results/export-types-rebuild-20260919/`.
- [x] Source/tests/initial tracker committed by peer session as7812b22/1c94681/a25428e; source lint correction21c2a80 and final artifact/evidence1586818 preserved and pushed. Closed only jcpe-dqge with exact gate results; parent np17 retains9findings and human/aggregate obligations. No deployment claim for this slice.

## Deploy committed repairs — jcpe-m6m8

- [x] Recover source/tracker/ownership; confirm both hosts serve the older artifact and Cloudflare modifies some response types. Pin 0a2c284 and preserve peer files.
- [x] Strict RCH hz4 guarded build reproduces committed HTML SHA256 `ad5b28ba2ada28adf160ecf9774cc957667c110745f35c82acfe34a54fd7e90c`,8,640,237 bytes. `bun run predeploy:check`:11 model rows and9 instruments/zero fail findings; retained quality warnings. Real Node26 playback15/15, enforced recovery explicitly vacuous (no refusal reproduced). Original gate assertions unchanged; isolated copy sets required User-Agent and resolves its admission import. Evidence `test-results/studio-release-gates-20260919/`.
- [x] All1,835 input hashes/364 source mtimes unchanged; git-show root and image staged. Metadata-only HEAD `a70b682` carries runtime fix `0a2c284`. Cloudflare Pages `2f81ac8d` and Vercel `dpl_2sVRVHc18siP4hUsNfwV9qJBoMKx` READY/production. Both raw curl HTML and image responses match committed SHA256 on first poll; social image `b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12`,1,295,227 bytes. Required User-Agent throughout. `.tmp/studio-release-20260919/{upload.py,host-hashes.json}`.
- [x] Final strict RCH hz2 named-spec live matrix36/36 (`test-results/studio-release-live-final-20260919/`):18/18 Cloudflare in107.387s and18/18 Vercel in93.935s, Chromium149.0.7827.55/Firefox151.0/WebKit26.5, one attempt per case in this run, no skipped/flaky/unexpected/global errors. All36 hash-bound attachments have zero app/console errors or unexpected requests;12 actual JSON downloads preserve exact spelling/order/duplicates and the Unicode Custom label. Pad mouse/keyboard release, release-success feedback, page-change retirement, 320/1280px dark/light layout and axe checks pass. No beacon exception was used by these browser runs, but Python still observes injection. Preserve hz4 first Cloudflare16/18 (181.555s): native file selection and command-lane click exceeded unchanged5s deadlines; no code defect or sole infrastructure cause established. All trace/console/request/download data retained in `test-results/studio-release-live-20260919/`. The next hz2 Cloudflare18/18 run exited1 because my scratch launcher omitted named spec arguments, triggering unrelated standalone-evidence merging. Preserve `test-results/studio-release-live-hz2-20260919/`; correct only the invocation to name both specs, then execute the complete final36 cases. No production runner, assertion, deadline or retry-policy changes.
- [x] Keep Cloudflare injection issue `jcpe-2nky` open: Pages OAuth works, RUM list returns403/10000 for both credentials. Raw Python response still contains injected beacon while curl matches clean bytes; do not strip responses or infer absence from curl. Owner dashboard action requested; CSP unchanged.
- [x] Separate RCH Chromium visual capture4/4 in11.787s (`test-results/studio-release-visual-20260919/`) retains desktop/320px screenshots from both hosts; all four inspected. Narrow pads fit in two columns with normal vertical scrolling; desktop controls remain readable. Final source hashes/mtimes and three receipts agree; both full curl response bodies still equal git-show HEAD. Close only release leaf, commit/push TODO/tracker and release reservation. No app code changed. Human/physical-device/aggregate acceptance and Cloudflare injection remain open.

## Retry chord-pad release — jcpe-kvak

- [x] Review shipped controller/UI and touch-pad contract; claim one bug, reserve exact files and coordinate.
- [x] Add real composition/X0/X1 refusal/throw tests for retry, repeated failure, unrelated newer preview and pending replacement; preserve positive voices/ownership/no-mutation controls.
- [x] Corrected original-source strict RCH baseline10pass/6fail,96 assertions: six failures reproduce lost retirement ownership; existing cases and newer-preview controls pass (`test-results/pad-retire-baseline-corrected-20260919/`). Preserve initial8/8 setup failure: tests incorrectly read timed-plan status for one-shot pads; corrected to capture actual startPreview IDs without relaxing voice assertions.
- [x] Retain the exact failed retirement identity, cancel pending preparation first and protect newer previews. Show release outcomes and explicit retry, guarding against obsolete UI notices.
- [x] Strict RCH hz2: `bun test tests/integration/studio-pads.test.ts tests/unit/studio-pads.test.ts tests/integration/studio-comping.test.ts`47pass/0fail,441 assertions; `bun run typecheck` all4 projects, scoped ESLint and `bun run build` pass (`test-results/pad-retire-hz2-20260919/`). Preserve hz4 first run46pass/1fail: unchanged comping MIDI case6284ms against5000ms deadline. Same frozen source on lower-I/O worker passes; I/O is not a proven sole cause. No deadline/assertion/retry changes.
- [x] Native studio-pads matrix27/27, one attempt each, Chromium149.0.7827.55/Firefox151.0/WebKit26.5,136.651s (`test-results/pad-retire-browser-20260919/`). No skipped/flaky/unexpected/global errors; all27 attachments have no page/console errors or unexpected requests. Actual audio retirement, keyboard/pointer/focus behavior, narrow/wide dark/light layouts and new success feedback pass. UI failure feedback is source/type reviewed; injected refusal/throw coverage uses the real composition/X0/X1 over fake audio platform, not native failure injection.
- [x] Fresh solo strict RCH hz4 `bun test tests/integration/studio-pads.test.ts`16pass/0fail,126 assertions and guarded build pass (`test-results/pad-retire-review-20260919/`). hz2 replay admission was refused by RCH-I002 before execution; no bypass/local fallback. All three receipts/current1,835 input hashes/364 source mtimes agree. Generated artifacts identical SHA256 `ad5b28ba2ada28adf160ecf9774cc957667c110745f35c82acfe34a54fd7e90c`,8,640,237 bytes; root matched HEAD before root/dist promotion.
- [x] Close only bounded leaf and commit/push owned source/UI/tests/artifact/TODO/tracker; release exact reservations. Physical-phone/human parent acceptance stays open. No independent reviewer, aggregate release-gate or deployment claim.

## Checked literal snapshots — jcpe-46gc

- [x] Read H0/T1/architecture/plan/theory guidance; retain np17 ownership and human dependencies; claim one leaf and reserve exact source/test/TODO.
- [x] Add 1–16 Custom length, duplicate/enharmonic order, frozen nonaliasing and exact accounting controls; add empty/misaligned internal T1 invariant negatives.
- [x] Original-source strict RCH hz4 baseline68pass/3fail,4,742 assertions across71 cases: all52 prior tests and16 length controls pass; empty/missing-class/extra-class Custom inputs incorrectly publish success (`test-results/literal-snapshot-baseline-20260919/`).
- [x] Replace generic tuple cast with checked bounded/nonempty snapshots and explicit one/four realization ID tuples; guard Custom pitch-array alignment. Preserve exact spellings, duplicate/order, immutable nonaliasing and work counts.
- [x] Strict RCH hz4 final160pass/0fail,94,276 assertions over8 H0/T1 suites; all4 TypeScript projects, scoped lint and guarded build pass (`test-results/literal-snapshot-final-20260919/`). Initial run had test-only inferred-type/template-name errors; correct annotations and formatting without changing fixture values/assertions. Keep `literal-snapshot-checked-20260919/`. Unchanged cast-policy is1pass/1fail:11 remaining findings vs12, hostile controls pass. Parent remains open.
- [x] Fresh solo RCH literal replay71pass/0fail,4,742 assertions and guarded build pass (`test-results/literal-snapshot-review-20260919/`). Both receipts/current1,835 inputs and364 source mtimes agree. Both artifacts equal committed root/dist SHA256 `7634f80337cb35ffa87f6734609b2a248d1a820a9b84a87790429fe7ec4df5db`,8,639,671 bytes: H0 deriveLiteralFacts is currently exported but has no shipped UI caller, so this is source/gate repair with no visible app change. Existing source/test commits32675ce/da1f468 preserved. Close only this leaf, update np17 evidence and finish owned test-typing/TODO/tracker commit; release leases11043–11045/11054. No aggregate, independent reviewer, human, browser or deployment claim.

## Isolate save-picker capability — jcpe-l3cf

- [x] Read architecture, E0 section10 and plan15.2; claim leaf and reserve adapter/test/TODO.
- [x] Add public/prepared probes and synchronous/asynchronous cancellation/error cases, retaining existing successful downloads and owner-binding controls.
- [x] Original-source strict RCH hz4 baseline:50pass/12fail,163 assertions across62 cases; all42 existing cases and8 new controls pass. Preserve `test-results/picker-probe-baseline-20260919/`. The hz3 attempt was stopped while queued, before any tests ran.
- [x] Isolate picker probing to the preferred channel; contain probe/name-access failures and classify synchronous cancellation. Preserve receiver binding, byte encoding, marker settlement and resource cleanup.
- [x] Strict RCH hz4 final:82pass/0fail,305 assertions across4 delivery suites; all4 TypeScript projects, scoped lint and guarded build pass (`test-results/picker-probe-final-20260919/`). Earlier run had only two test-fixture lint errors; use a real Error with the same throwing name getter, without altering assertions. Retain `picker-probe-checked-20260919/` failure logs.
- [x] Fresh solo RCH replay62pass/0fail,199 assertions and guarded build pass (`test-results/picker-probe-review-20260919/`). Both receipts/current1,835 input hashes and364 source mtimes match; generated bytes identical SHA256 `7634f80337cb35ffa87f6734609b2a248d1a820a9b84a87790429fe7ec4df5db`,8,639,671 bytes. Promote root/dist after verifying root matched HEAD; commit owned source/test/artifact/TODO/tracker and release leases11025–11027/11035. No new native browser, human acceptance, aggregate or deployment claim. The cast-policy follow-up exposed this runtime defect; its remaining findings were not changed or waived.

## Total malformed import-request diagnostics — jcpe-3ae7

- [x] Read E0v2 RES-01/04, owner boundary and plan; claim one bug and reserve driver/test/TODO paths10994–10996.
- [x] Add17 malformed-envelope cases with exact refusal, no getter/owner/X1 calls, immutable request-derived identity and unchanged real composition state; retain existing valid commit/consent controls.
- [x] Original-source strict RCH baseline32pass/15fail,265 assertions across47 cases. All30 existing cases and2 malformed controls pass;15 new cases reproduce throws, owner calls or mutable diagnostics (`test-results/request-boundary-baseline-20260919/`).
- [x] Inspect exact plain own-data fields and contain reflection failures; construct frozen request-derived primitive identity snapshots without either double assertion.
- [x] Strict RCH hz3 focused E0 suites179pass/0fail,1,376 assertions; all4 TypeScript projects, scoped lint and guarded build pass (`test-results/request-boundary-checked-20260919/`). Preserve first production type failure and second test-only typing failure. Correct malformed diagnostic typing with a narrow unvalidated-identity result variant and document the unchanged wire fields; annotate raw test identity without changing values/assertions. Unchanged global cast-policy remains1pass/1fail, now12 findings versus14; hostile controls pass and parent np17 remains open.
- [x] Fresh solo RCH driver replay47pass/0fail,363 assertions plus guarded build (`test-results/request-boundary-review-20260919/`). Both receipts/current1,835 inputs/364 source mtimes agree; identical HTML SHA256 `213eefeb1f9f0d234b142d2215104a3b41d6c1a496147af58f19d16ed5a6a3c5`,8,639,211 bytes. Promote root/dist only after root matched HEAD. Runtime fixd843eb7, diagnostic typing7207cfa and test annotation4a02b88 are already committed/pushed; finish owned artifact/TODO/tracker commit and release leases10994–10998. Close bounded bug and update np17 evidence only; aggregate/human gates remain open. Solo verification, no new browser or deployment claim.

## Retry comping preview retirement — jcpe-twtz

- [x] Read authored-comping ownership and preview-retirement contracts; claim one bug and reserve controller/integration/TODO paths10983–10985.
- [x] Add eight cases using real composition/X0/X1 over the fake audio platform: refused/throwing releases, repeated refusal/retry, preserving a newer unrelated preview, and cancelling new preparation while retrying an older failed retirement. Check actual owned nonreleasing voice counts, exact preview IDs and unchanged document/revision.
- [x] Original-source RCH baseline16pass/4fail,194 assertions: all four release-retry cases fail; existing tests and both newer-preview controls pass. `test-results/comping-retire-baseline-20260919/`.
- [x] Preserve the exact submitted preview reference until retirement succeeds; cancel pending comping preparation first, retry any still-owned submission, and discard obsolete ownership when a newer preview replaces it.
- [x] Strict RCH hz4 checks:73pass/0fail,878 assertions across9 files; all4 TypeScript projects, scoped lint and guarded build pass (`test-results/comping-retire-checked-20260919/`). Preserve earlier MIDI adapter timeout (7,629ms against unchanged5,000ms), own test-vector lint error and nullable-ID type error. Correct only test typing; no assertion, deadline or retry changes. The unchanged MIDI case passed in both subsequent full focused runs; worker I/O pressure was observed but is not a proven cause.
- [x] Native comping matrix30/30 in147.322s on hz3 (`test-results/comping-retire-native-cache-20260919/`), Chromium149.0.7827.55/Firefox151.0/WebKit26.5, one attempt each, zero skipped/flaky/unexpected/global errors. All30 diagnostic attachments bind the same artifact and show no page/console errors or unexpected requests. Real audio Stop, MIDI/recipe downloads, chart ownership, exact notes and accessibility pass. Preserve hz4 initial27/30 and hz3 cold-native-cache29/30: Firefox navigation/download/title timeouts, including a download delivered6.155s after its click vs unchanged5s deadline. Lower-load worker plus reuse of only Mesa shader/fontconfig caches gives final green; fresh browser profiles, source/assertions/deadlines unchanged. This is not cold-cache performance or proof of a sole timeout cause. Two ovh-b RCH-I003 admission refusals ran no tests; worker config untouched.
- [x] Fresh solo strict RCH replay22pass/0fail,240 assertions plus guarded build (`test-results/comping-retire-review-20260919/`). Final static/browser/review receipts and current1,835 input hashes/364 source mtimes match; all three generated artifacts are identical SHA256 `9273a5efda6629e5ae464f8c655268c8ec9831a06911936c7f5e5312eef42ce9`,8,638,949 bytes. Promote root/dist only after tracked root matched HEAD. Source/test commits8454b29 and2a88904 are already pushed; close only this bounded bug and finish the owned artifact/TODO/tracker commit and lease release. No independent-human, cold-cache performance, aggregate or deployment claim; original human/physical-device package evidence remains open.

## Import confirmation provenance — jcpe-ezhp

- [x] Read accepted E0v2 RES-04 and real owner consent checks; claim bug and reserve exact driver/test/TODO paths10980–10982.
- [x] Add25 cases: reordered requirement/identity/impact/all, each15 changed scalar fields, owner acknowledgement replacement, serialization hooks, getter, reflection trap and extra symbol. Positive cases use the real application owner and scripted transport receipt; no native audio claim.
- [x] Strict RCH original-source baseline5pass/25fail,140 assertions (`test-results/consent-baseline-20260919/`). Existing5 cases pass; baseline rejects reordered consent, accepts swapped owner consent and invokes owner identity read on provenance failures.
- [x] Replace JSON serialization comparison with fixed own-data field comparison; bind both submitted acknowledgements to displayed consent and keep provenance diagnostics request-derived before any owner call.
- [x] Final strict RCH hz4:162pass/0fail,1,239 assertions across7 integration/conformance files; all4 TypeScript projects, scoped lint and guarded build pass (`test-results/consent-final-20260919/`). Preserve first ovh-b missing-dependency-cache failures and first hz4 test-harness never-type errors. Correct the test-only request assertion to its declared union; no expected values or assertions changed.
- [x] Fresh solo review/re-execution30pass/0fail,226 assertions plus guarded build (`test-results/consent-review-20260919/`). Both receipts/current1,835 inputs and364 source mtimes agree. Identical artifact SHA256 `8b5e6ecb363a14475d6321717e555ad041a58351ac8e159563ac15c4681cb634`,8,638,694 bytes. Root/dist promoted after tracked root matched HEAD. No independent-human or aggregate claim.
- [x] Close bounded consent bug with exact results, preserving wider acceptance obligations. Commit/push only owned source/test/TODO/tracker/generated artifact and release exact leases; no deployment claim.

## Save-picker receiver — jcpe-3rla

- [x] Inspect activation-safe browser adapter and section10 contract; claim bug and reserve source/test/TODO paths10962–10964.
- [x] Add receiver-sensitive global/window picker cases through public/prepared delivery, success/cancellation, synchronous activation, exact bytes and cleanup.
- [x] Original-source RCH baseline34pass/8fail,103 assertions: all receiver-sensitive cases fail with activation_failed; all existing delivery controls pass. Reports `test-results/picker-owner-baseline-20260919/`.
- [x] Capture the global picker once, preserve global/window owner choice and invoke with call(owner); keep activation ordering, channel preference and cleanup.
- [x] Initial strict RCH62/0 and all types pass; lint rejects redundant null comparison. Replace it with a function check, no suppression. Final `test-results/picker-owner-browser-final-20260919/`: all4 TypeScript projects, scoped lint,62 tests /208 assertions and guarded build pass. Preserve first logs and unchanged14 global cast-policy findings.
- [x] Native download suite:12/12 across Chromium/Firefox/WebKit, zero skipped/flaky/unexpected/global errors, one worker, explicit required User-Agent. Preserve EDQUOT workspace-copy failure and failed helper upload/parser/RCH-E309 attempt; neither ran tests. Move scratch/helpers to separate /tmp after checking capacity and remote/local helper hashes. No peer cleanup or local fallback.
- [x] Fresh solo review42/0,123 assertions (`test-results/picker-owner-review-20260919/`). Both final receipts and current1,835 inputs match; guarded artifacts identical SHA256 `aa8b2cc0884693c20157d8a2b61d59dff209e8b7247432b103d1db53fb5bea70`,8,637,477 bytes. Root/dist promoted only after root matched HEAD. Close bounded bug, commit/push owned files and release leases. No native picker-dialog, independent-human, aggregate or deployment claim.

## Typed chart-text chord construction — jcpe-haci

- [x] Read E0 section8.3 and domain parsed/slash voicing construction; claim slice and reserve exact source/test/TODO paths (10954–10956).
- [x] Replace two double assertions and widened AutoVoicing cast with explicit bass-discriminated frozen chord snapshots. Validate the fixed default range once through the domain makeMidiRange constructor; preserve all literal settings.
- [x] Add four literal root/slash spelling cases, fractional durations, annotations, IDs and exact text export through the real T0/F3/export pipeline.
- [x] Preserve initial RCH417/4 and typecheck failure: four new charts omitted required section headers; raw default range lacked branded MidiPitch values. Add section headers without changing assertions and construct the validated range in production. Reports `test-results/chart-chords-first-20260919/`.
- [x] Preserve second RCH417/4 (`test-results/chart-chords-final-20260919/`): production types pass; new tests incorrectly read published duration.value and widened pitch literals. Correct field access and literal typing, retaining exact rational/spelling/default expectations. No production change after validated-range fix.
- [x] Corrected strict RCH on hz4:421 tests /2,396 assertions across16 files pass; all4 TypeScript projects, scoped ESLint and guarded build pass. Reports `test-results/chart-chords-corrected-hz4-20260919/`. Unchanged cast policy falls16 to14 findings; hostile controls pass, global gate remains red.
- [x] Fresh solo replay on ovh-b:16 chart/preview/full-production-loop tests /136 assertions pass, cast policy14 unchanged, guarded build matches SHA256 `a77eaac9978bcce25d8bea6ce74713379f15a7b9633d31ea9a5da9714f7bcbf7` (8,637,438 bytes). Both receipts and current1,835 inputs match; promote root/dist only after root matched HEAD. Reports `test-results/chart-chords-review-ovh-20260919/`. Retain first ovh-b queue timeout; cancel only own queued hz4 review before moving to free ovh-b, verify no orphan job. Close bounded slice and commit/push owned paths; aggregate/human obligations stay open, no independent-human or deployment claim.

## Typed E0 envelope normalization — jcpe-v0f6

- [x] Inspect remaining cast-policy findings and accepted E0 contract; claim only this bounded slice, leaving np17 and its aggregate obligations open.
- [x] Replace seven double assertions with four exact envelope predicates and one typed guarded normalizer; preserve existing checks and valid raw identity.
- [x] Add25 independently literal refusal-code/near-miss cases with exact reference and frozen-wrapper assertions. All412 tests across15 normalization, driver, owner, full-loop and static contract files pass (2,300 assertions); no existing assertion/fixture changed.
- [x] Strict RCH `test-results/typed-ports-final-20260918/`: all4 TypeScript projects, scoped ESLint and guarded build pass. Unchanged cast policy:16 findings versus23 previously; hostile controls pass. Its production check remains red and the helper honestly exits1.
- [x] Fresh solo RCH re-execution (`test-results/typed-ports-review-20260918/`):412/0,2,300 assertions; cast policy16 unchanged; guarded build identical SHA256 `1f04d49b18cd9c5b1336f5db3cd53fe8aa9cd5a1b28e85479e55b3bf452caa01`,8,637,265 bytes. Both receipts and current1,835 inputs match; root/dist promoted only after root matched HEAD. Diff reviewed without gate/contract relaxation. Close only jcpe-v0f6, prepare owned paths for commit/push; original np17 remains open. No independent-human, aggregate or deployment claim. Mail41912 delivered; reservation listing blocked by shared mailbox lock, no lease claim.

## Frozen E0 port envelopes — jcpe-l8a0

- [x] Read accepted E0 v2 section3, existing owner implementation and conformance materializers; claim defect. Inbox read succeeds; reservation fails mailbox snapshot conflict, no lease claim.
- [x] Freeze JSON fixture materialization per original contract; add legal-envelope controls plus mutable/accessor/prototype/hidden/symbol/proxy and nested-container attacks.
- [x] Strict RCH baseline35pass/85fail across120 normalization cases; legal controls pass, all85 new hostile cases fail on original code. Reports `test-results/frozen-ports-baseline-20260918/`.
- [x] Validate frozen plain records and dense frozen arrays with own enumerable data descriptors before reads; reject hidden/symbol fields and contain reflection/get-trap failures. Retain valid raw identity, including plain null-prototype records.
- [x] Final strict RCH346pass/0fail,2,074 assertions across12 E0 v2/owner/full-loop/contract files; all four TypeScript projects, scoped lint and guarded build pass. Initial334/5 revealed five mutable scripted returns; those and negative controls now freeze without changing any assertion or fixture value. Initial test matcher type mismatch corrected with unknown-typed observations. Three driver cleanup and four nested/positive checks added.
- [x] Separate E0 v2/owner-boundary static contract and production-policy suites:41pass/0fail,126 assertions (`test-results/frozen-ports-contracts-ovh-20260918/`).
- [x] Fresh solo repeat on hz4:346/0,2,074 assertions; final normalization tests against original source36pass/88fail. All1,835 inputs match final/review/contract receipts and current source. Three guarded builds match SHA256 `88a3046ec010f507f7fd4cade2769cb63848cc131800cc3beaca4f06717b79a7`,8,638,026 bytes; root/dist promoted only after root matched HEAD. Close only jcpe-l8a0 and prepare owned paths for commit/push. No independent-human/browser/deployment or aggregate claim; unchanged cast-policy23 findings remains open. Retain RCH-I002 memory refusal and RCH-I001 queue timeout; no local fallback or relaxed admission.

## Publication effect validation — jcpe-h29m

- [x] Inspect E0 v2 exact-result contract and claim the concrete effect vocabulary/sparse-array defect. Agent Mail reservation failed during database initialization; preserve peer files and check exact source hashes before promotion.
- [x] Add independent cases for all five legal kinds, empty effects, four illegal kinds, missing and prototype-provided occurrences.
- [x] Strict RCH baseline:29pass/6fail,74 assertions (`test-results/publication-effects-baseline-20260918/`). All six invalid receipts were incorrectly accepted; all legal/empty controls passed.
- [x] Validate against APPLICATION_EFFECT_KINDS and require each own effect occurrence; original fixtures and assertions unchanged. Initial100/0 behavioral pass had test-only TypeScript/lint failures; corrected matcher observation typing and prototype construction, retained failed logs.
- [x] Strict RCH final:100pass/0fail,811 assertions across five normalization/workflow/owner/full-loop/contract files; all four types projects, scoped lint and guarded build pass. Fresh solo replay reproduces100/0 and the final tests fail29/6 against original production. Reports `test-results/publication-effects-{checked,review}-20260918/` bind1,835 unchanged inputs. Guarded artifact SHA256 `326e626f5e859d2d8b395fe2a1500d88f8c2530319eed6a971a9dd9108ba7295`,8637191 bytes, identical across both builds and root/dist.
- [x] Record bounded proof and prepare exact owned files for commit/push. Unchanged cast policy remains1pass/1fail with23 findings; hostile controls pass. Original np17, broader normalization and aggregate/human obligations remain open. Solo review only, no browser or deployment claim.
- [x] Follow-up implemented and verified separately in jcpe-l8a0 above: frozen containers, safe data descriptors, prototype/key checks and contained normalization errors. The earlier effect-only fix did not itself satisfy this obligation. Original cast-policy and release gates remain open.

## File writer cleanup accounting — jcpe-gpge

- [x] Read E0 cleanup contract, claim bounded defect, attempt lease renewal/handoff (Agent Mail unavailable; existing source leases retained).
- [x] Add12 write/close × successful/rejected/absent-abort cases across public/prepared entry points. RCH baseline28pass/6fail proves missing-abort false cleanliness and invented close failures; successful-abort controls pass.
- [x] Track whether close was attempted; require successful abort before clean failure, otherwise report one unresolved writer with exact cleanup failure kinds.
- [x] Final RCH76pass/0fail,560 assertions; all four TypeScript projects, exact lint and guarded build pass (`test-results/writer-cleanup-final-20260918/`). Global cast policy remains1pass/1fail with23 findings, hostile controls pass. Preserve initial union-construction type failure and fix with explicit existing result variants, no cast/contract change.
- [x] Native Chromium/Firefox/WebKit download/activation:12/12 passed, zero skipped/flaky/unexpected and zero console/page errors (`test-results/writer-cleanup-browser-final-20260918/`). Tests bundle final source directly; writer fault injection is separately covered by scripted adapters.
- [x] Fresh solo review repeats76/0,560 assertions and guarded build; all1,835 static/browser/review input hashes match current files. Both builds reproduce SHA256 `690481498827286c7f102ec7c2a8e3cb0d065445aaf870a9d8f1ce5cbaffa477`,8,637,096 bytes. Copied guarded artifact to root/dist after confirming root still matched HEAD. Record exact tracker proof and prepare owned paths for commit/push; no aggregate/deploy claim. Mailbox remains unavailable; queued handoff is not delivered.

## Public export cleanup — jcpe-j45m

- [x] Claim defect and reserve exact export/unit/browser paths. Read E0 cleanup contract.
- [x] Add public API regressions: baseline17pass/5fail after correcting one newly authored cancellation expectation (contract uses ok:true for cancellation). Retain both original16/6 and corrected17/5 logs. Five failures prove false counters, missing anchor cleanup, hidden cleanup errors, and missing activation refusal.
- [x] Delegate public delivery to the existing prepared primitive; remove duplicate cleanup and its unchecked global cast.
- [x] Extend native download/activation tests to both public and prepared entry points under style-src none.
- [x] RCH final static packet (`test-results/export-cleanup-types-fixed-20260918/`):64pass/0fail,536 assertions; all four TypeScript projects and exact lint pass; guarded build passes. Global cast policy remains1pass/1fail with23 findings (down from24), hostile controls pass. Earlier runs retained: raw unknown adapter completion required a private typed implementation; one new-test literal/brand comparison required an unknown observation, without assertion changes.
- [x] Native Chromium/Firefox/WebKit:12/12 cases passed once, zero skipped/flaky/unexpected and zero page/console errors (`test-results/export-cleanup-browser-20260918/`); both APIs deliver exact golden bytes and filenames under style-src none.
- [x] Fresh solo review repeats64/0,536 assertions and guarded build;1,835 inputs match static/browser/review receipts. Identical build SHA256 `4091a46a8186d2f97eed15f2e0579c637195e4b28ffb469a8ea0324827948359`,8,636,971 bytes. Copy only generated bytes to root/dist after checking root still equals HEAD. No independent-human or aggregate claim.
- [x] Record exact proof for jcpe-j45m and prepare owned paths for commit/push. Agent Mail extension/artifact reservation/handoff failed during mailbox initialization; existing source leases remain valid, unchanged source/root hashes checked. No deploy.

## Recovery Keep undo-disposition typing — jcpe-j6bv

- [x] Read replacement/recovery source and A0/E0 request contracts; claim one bounded task and reserve exact paths.
- [x] Preserve input undo disposition in the workflow result type and remove recovery Keep as-never. Retain validation, oversized-history refusal, consent and transport semantics.
- [x] Strict RCH focused proof (`test-results/recovery-typing-20260918/`):77pass/0fail,767 assertions across five existing recovery/replacement/import suites.
- [x] All four TypeScript projects, exact lint and guarded build pass. App bytes equal HEAD:8,636,958 bytes, SHA256 `e621a568febf11d7baeaa89f122e16124cb020e8d44c65dcd889396ded499d68`. All1,835 inputs unchanged. Cast policy remains1pass/1fail with24 findings (down from25); hostile controls pass.
- [x] Fresh solo re-execution reproduces every result (`test-results/recovery-typing-review-20260918/`); no independent-human claim. Review exact diff and all1,835 hashes, record named commands, close only jcpe-j6bv, and prepare owned paths for commit/push. Original np17 and human/aggregate gates remain open; no deployment.

## Chart import factory ID typing — jcpe-k3to

- [x] Read E0 section 8.3 and stable-ID contracts; preserve original np17 owner and aggregate/human prerequisites.
- [x] Claim the bounded implementation task and reserve exact files.
- [x] Preserve the generic factory result in the collision/budget wrapper; remove four unchecked ID escapes and the redundant document-ID assertion without changing allocation behavior.
- [x] Run original chart roundtrip, defaults, refusal/collision, E0 preview and contract checks through strict RCH: 35pass/0fail,456 assertions on 2026-09-18 (`test-results/chart-id-20260918/`). Earlier admission failures retained: ovh-b queued for five minutes then exited103 (`RCH-I001 queue_timeout`); prepared alternate vmi1152480 exited103 (`RCH-I004 alias_wrong_target` pointing at another project scratch directory). Neither executed tests. No shared alias changes or admission bypass; ovh-b later admitted the unchanged packet.
- [x] App typecheck and exact lint pass. Unchanged cast policy is 1pass/1fail: hostile controls pass, production findings decrease from29 to25. The global gate and original np17 remain open.
- [x] Guarded remote build reproduces committed HTML exactly: 8,636,958 bytes, SHA256 `e621a568febf11d7baeaa89f122e16124cb020e8d44c65dcd889396ded499d68`; all1,835 frozen inputs unchanged.
- [x] Fresh solo review repeats all named checks (`test-results/chart-id-review-20260918/`): same35/0,456 assertions, app types/lint/build pass, same25 global findings, identical artifact and1,835 inputs. This is self-review, not independent human verification. Close only this bounded task; np17 remains open.
- [x] Complete owned source/TODO/tracker change for commit and push; no deployment for unchanged app bytes.

## Inspector containment and print focus — jcpe-dymb

- [x] Read source/contract and reconcile original U2 ownership; claim this defect and preserve peer files.
- [x] RCH committed baseline: both 320/1280px cases fail horizontal containment; 320px content is 1,885px inside 286px. Exact inspector close restores chart focus; entering print mode causes the command-lane stale-owner refusal. Preserve `test-results/inspector-baseline-20260917/`.
- [x] Add native regression assertions for tab/piano containment and hit testing, dirty-draft retention, exact focus return and retained print preview without visible refusal. All six new/strengthened Chromium cases fail on the committed baseline: two geometry failures and four missing retained print panels; preserve `test-results/inspector-regressions-red-20260917/`.
- [x] Fix the inspector grid with one zero-minimum fluid column. Observe native print-media changes so print CSS does not dismiss connected screen owners; still invalidate removed or explicitly hidden owners and recheck CSS-only invisibility on return to screen. Retire the media listener with its overlay lease.
- [x] Prove genuine owner removal and responsive disappearance still dismiss correctly, including removed/hidden owners during print and CSS-only disappearance on screen return; original diagnostics, exact focus and listener retirement remain asserted.
- [x] Retain first static run failures: an existing observer double lacked `matchMedia`; two new E2E typing errors; scratch Bun selection mistakenly imported the Playwright-only U2 evidence file. Extend the double with original stale-owner assertions and print-listener cleanup, fix test typing, and select the browser file only through native Playwright. No production fallback or weakened assertion.
- [x] Final RCH static proof (`test-results/inspector-static-final-20260917/`): U0/U1/U2 contracts, 124/124 tests (637 assertions), full four-project typecheck, lint/boundaries (349 files), and guarded build pass. HTML is 8,636,921 bytes, SHA256 `9806877b0228701d7b1699d2db4383b9e9deabfd184b8136f4d0e1c810a11204`. No aggregate verify claim.
- [x] First Chromium 53/53 and final Firefox 53/53 pass with one attempt each, zero skipped/flaky/unexpected. Original print/PDF, exact draft/notes, preview audio, accessibility, responsive stale owners and new containment/print cases are included. First Chromium precedes two test typing corrections; its production bytes are identical, and those eleven affected test cases are covered by the final Chromium rerun below. Inspect actual first/last-key screenshots at 320/1280px; controls are visible and hit-testable.
- [x] Retain first WebKit worker run (`test-results/inspector-webkit-20260917/`): 20/23 pass, three download/action deadline failures; the following 30 inspector cases did not run. The trace records an SVG download after its timeout. Worker I/O pressure was 80–98%; this correlation alone does not prove the root cause. Move the entire unchanged 53-case set to the quiet worker; no threshold, retry or assertion changes.
- [x] Retain second WebKit run on quiet-I/O ovh-b (`test-results/inspector-webkit-quiet-20260917/`): 19/23 pass, four action/total-test deadline failures; following 30 inspector cases did not run. This disproves a disk-pressure-only explanation. No specific runtime/root cause is established. Prepare the same pinned toolchain on hz4 with more available memory; keep source/assertions/deadlines unchanged.
- [x] Final Chromium correction check (`test-results/inspector-chromium-final-20260917/`): 11/11 pass, 27.634s, zero skipped/flaky/unexpected. Only the two typing-corrected test files differ from the earlier 53-case Chromium snapshot.
- [x] Retain third WebKit run on hz4 (`test-results/inspector-webkit-hz4-20260917/`): 21/23 pass; both desktop print-current-source cases exceed 30 seconds. All corresponding phone cases pass around 12 seconds. Meter-hidden, backdrop-blur-disabled and foreground diagnostics do not provide a clean cause/fix; no production changes follow from them. Native timing still measures roughly 0.6s to the second frame with capture disabled, so disabling capture is neither the remedy nor release proof.
- [x] Verify the pinned Desktop Safari profile uses deviceScaleFactor 2 and the worker exposes 64 CPUs. The scratch helper had capped Mesa rasterization at two threads. Change only `LP_NUM_THREADS` to eight: both original desktop cases pass in 17.897s/17.433s with full trace/video, unchanged assertions and deadlines (`test-results/inspector-print-lp8-20260917/`). This supports a software-renderer resource bottleneck; no Mac/physical-phone or cold-start performance claim.
- [x] Complete the unchanged WebKit matrix (`test-results/inspector-webkit-lp8-20260917/`): 23/23 surface cases (139.083s) and 30/30 original inspector cases (191.379s), each one passed attempt, zero skipped/flaky/unexpected. All three accepted engines bind to the same 1,805 inputs and HTML hash. Check every JSON diagnostic attachment; 159 unique native cases plus eleven final Chromium typing-correction reexecutions pass. All earlier failures and diagnostic-only runs remain recorded; no claim they passed.
- [x] Fresh-review every changed source/test diff and source hashes/mtimes. Complete bounded anti-ceremony/honesty review: no weakened assertions, regenerated goldens, skipped cases or independent-person claim; retain disclosed harness errors. CASS timed out without results, so no historical-search claim.
- [x] Guarded RCH rebuild (`test-results/inspector-rebuild-20260917/`, 5.307s remote) reproduces the same HTML. Recheck every input hash/mtime and copy only guarded generated bytes to root/dist; preserve other processes' source/test commits and the three peer UI files. Artifact/tracker commit `f626840` was pushed and used for the gated deployment below.
- [x] Commit/push generated artifact and owned tracker updates as `f626840`. Initial hz3 admission refused with zero test execution; move to hz4 with the same standard Bun and original UIowa corpus. Committed-byte RCH gates (`test-results/inspector-committed-20260917/`, 258.071s remote) pass 11 shipping models, nine quality profiles (zero fail findings; 16 existing warnings), all 15 native playback instruments, and exact final rebuild. Recovery is enforced but vacuous; no human-listening claim.
- [x] Publish committed `f626840` bytes to Cloudflare Pages `0a5df82e` and Vercel `dpl_Eqb64QEQ8qj5bwBn1LXF2LDhmtvc` (READY). Both canonical hosts return HTTP 200 and exact committed HTML/social-image hashes on the first poll at 2026-09-17 06:36:44–45 UTC. Preserve raw responses and upload receipts in `.tmp/inspector-20260917/`; HTML hash is `9806877b0228701d7b1699d2db4383b9e9deabfd184b8136f4d0e1c810a11204`, social-image hash is unchanged `b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12`.
- [x] Final native Chromium live checks (`test-results/inspector-live-final-20260917/`) pass both hosts at 320/1280px: JazzChords.org 2/2 and mirror 2/2, all single attempts, zero skipped/flaky/unexpected, application/console errors or forbidden requests. Prove edge-key viewport/hit testing, exact draft retention/discard, close/reopen focus, retained print preview and unchanged revision. Strengthen the initial 4/4 live run with an explicit SVG viewport assertion; inspect the actual printable chart in phone and desktop screenshots. No Cloudflare beacon error was observed with the required User-Agent; this is not a global no-injection claim. Every source hash/mtime and Git/staged/root/dist artifact still matches.
- [x] Record final commands/hashes/receipts and preserved failure history in jcpe-dymb; close only this repair after fresh author-run review/reexecution and live proof. Flush Beads and publish the explicit closure checkpoint; release exact reservations. Original U2/human/device acceptance, broader regression work, runtime portability and Cloudflare analytics permissions remain open.



## Preserve piano, print and QR colors — jcpe-milestone-reliable-studio-l3a.12.4

- [x] Reproduce the unchanged U0 token failure via RCH: 10 pass, 1 fail, ten literal-color findings; original contrast and mutation checks stay intact.
- [x] Freeze nine named color values from the existing component CSS; mirror their definitions in the U0 contract and independent exact-map transcription before replacing consumers.
- [x] Replace only the ten color literals; keep these physical piano/paper/QR colors invariant across light, OS-dark and pinned-dark themes.
- [x] Unmask the pre-existing responsive-token inventory mismatch: retain the original coarse-pointer witness and pin all five existing media/selector/value combinations, with 19 adverse scope/value/missing/duplicate controls. No transport CSS or geometry changes.
- [x] RCH final static `test-results/token-static-complete2-20260916/`: U0 contract, 27/27 focused tests (607 assertions), full types, full lint, 349-file boundaries and guarded build pass. The palette and responsive inventory changes are explicitly reviewed; no claim that the old gate is unchanged. HTML is 8,636,715 bytes, SHA256 `a4fa47b59d56125e4d0ad72e79b7aa4a08af7048dc6665447d73fb852ec8830d`. Retain the earlier digest/inventory/lint failures and the input-transfer restart that ran no tests.
- [x] Prepare an admitted second RCH worker after ovh-a memory refusal; verify the official Bun 1.3.14 baseline archive for ovh-b's older CPU, retain Node 26 / Playwright 1.61.1 / pinned browser bytes, native PulseAudio and real PDF/QR dependencies. The initial source transfer resumes after a timeout; no local fallback or admission bypass.
- [x] Retain the first browser run: 19 passed, six driver failures, 41 interrupted/not-run cases, zero flaky. The new comparison assumed the command lane remained open after native `afterprint`; restore it through normal controls just as the existing print suite does. No product bytes, color/byte assertions, deadlines or retry settings changed; the full 66-case replacement run is required.
- [x] Complete the corrected 66-case run: Chromium 22/22 and Firefox 22/22 pass; all 22 WebKit cases fail at native browser launch because the new worker lacks `libwoff2dec.so.1.0.2`. Keep the entire red report. Install the required system libraries and run the full WebKit group with unchanged source, assertions and deadlines; the two passing engine groups remain source-bound evidence, not a green full-run claim.
- [x] Retain WebKit's post-library run on ovh-b: 10 passed, 3 deadline failures and 9 interrupted/not-run. Inspect the first trace/screenshot: app boot and present controls are visible, but the root cause is not proven. Move the complete unchanged WebKit group to the original ovh-a platform after fresh RCH admission and no-browser-contention checks; no timeout widening or cold-start performance claim.
- [x] Verify all three complete engine groups against the same 1,804 inputs and HTML hash: Chromium 22/22 and Firefox 22/22 in `test-results/token-browser-complete-20260916/`; WebKit 22/22 (206.136s) in `test-results/token-webkit-ovha-20260916/`. Each accepted case passes once; zero skipped/flaky cases in the accepted groups. Check all 84 diagnostic attachments and 18 exact old/new comparisons, including every piano key style, print-media colors, exported SVG bytes and QR paths across 320/1280px and light/OS-dark/pinned-dark. Original native PDF and independent QR pixel-decode tests pass. The earlier full run remains red; there is no aggregate `bun run verify`, cold-platform timing or original U4/human acceptance claim.
- [x] Fresh-review every changed source/test/validator path; guarded rebuild on ovh-b reproduces the same bytes with all source hashes/mtimes retained. Commit owned files as `a22a1da`; merge and preserve upstream native build 3 as `4e555cc`, then push. The merge changes only native metadata plus the Apple build guard; all web code/artifact bytes remain identical. Rebind committed predeploy inputs to that reviewed merge.
- [x] Diagnose the first committed predeploy refusal on ovh-b without changing the gate: flute PCM/input bindings match and all musical checks pass, but 52 numeric measurements differ by at most `1.4210854715202004e-14`, changing eight evidence hashes. Record the supported-runtime portability defect as open `jcpe-uid4`; preserve `test-results/token-flute-baseline-20260916/` and the red predeploy log. CPU and Bun-build contributions are not separately proven.
- [x] Complete the original committed-byte gates on standard Bun 1.3.14 through RCH on hz3: `bun run predeploy:check` passes all 11 shipping models, nine instrument-quality profiles have zero fail findings (16 existing warnings retained), and native Chromium playback passes all 15 instruments. Recovery is enforced but vacuous; no exercised-refusal or human-listening claim. The final guarded rebuild matches committed HTML and all 1,804 release input hashes. Evidence: `test-results/token-committed-hz3-20260916/`, 195.133s remote. No ledger, threshold, evidence-equality or model-routing relaxation.
- [x] Publish committed `4e555cc` bytes to Cloudflare Pages `36eb3d51` and Vercel `dpl_Et6msUV6fms2scJTzfm8EgWsTxAc` (READY). Both canonical hosts return HTTP 200 and exact committed HTML/social-image hashes on the first poll; preserve unmodified responses and CLI receipts in `.tmp/runtime-audit-20260916/`. HTML SHA256 is `a4fa47b59d56125e4d0ad72e79b7aa4a08af7048dc6665447d73fb852ec8830d`; social-image SHA256 is `b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12`.
- [x] Complete native Chromium live comparisons on both hosts across 320/1280px and all three theme modes: JazzChords.org 6/6 (66.662s), mirror 6/6 (63.321s), all single-attempt passes with zero skipped/flaky/unexpected. All 24 diagnostic attachments match the committed artifact and local surface/SVG captures; no console/page errors or forbidden requests were observed with the required User-Agent. This does not establish that Cloudflare beacon injection is globally gone. Evidence: `test-results/token-live-20260916/`.
- [x] Inspect four representative phone/desktop screenshots. They reveal independent horizontal inspector clipping and a visible stale-focus refusal after the inspector/print/share path; the piano screenshot is not a viewport-visibility proof. Record the exact screenshots and a granular repair checklist in open P1 `jcpe-dymb`. Passing color/console checks do not supply responsive-layout or full-dialog acceptance.
- [x] Recheck all 1,804 release source hashes and mtimes after deployment; root/dist/staged/Git HTML remain byte-identical. Close only this color defect, preserving original U4 ownership/acceptance and all human/device work. Keep `jcpe-dymb`, baseline-Bun portability `jcpe-uid4`, and the broader regression audit open.
- [x] Retain original U4 ownership and full acceptance; this isolated defect cannot close its parent.


## Flute replay portability — jcpe-uid4 (2026-09-17 UTC)

- [x] Claim the original portability bug; preserve exact canonical evidence and all model thresholds.
- [x] Recover both official Bun 1.3.14 binaries and verify their recorded SHA-256 values.
- [x] Same-host RCH hz4: standard and baseline Bun each exactly reproduce all 12 cells and the complete accepted report (zero differences), `test-results/flute-portability-same-host-20260917/`.
- [x] Same baseline binary across hz4/ovh-b: all 12 cell bindings equal, including PCM. ovh-b retains 52 numeric and eight derived-hash differences. Original and instrumented replays agree per host. Trace 2,048 sine / 6,143 cosine distinct inputs: one sine and five cosine outputs differ for identical arguments. Matching-input log10/hypot/pow/sqrt outputs agree. Reports `flute-portability-{math,trig}{,-ovh}-20260917`; CPU and system-library contributions were still unisolated at this stage.
- [x] Document explicit numerical-runtime admission in `docs/DEPLOY_GATE.md`. Add six real FFT/window probes before machine-delegated flute replay; preserve original complete canonical equality, all musical thresholds, accepted report bytes and source/WASM/corpus/PCM binding. This diagnoses/refuses incompatible hosts; it does not solve portable analysis.
- [x] RCH hz4 final: 40/40 tests, 149 assertions (8.85s), four type projects, full lint/boundaries, and original `bun run predeploy:check` PASS; receipt `test-results/flute-portability-final-20260917/` binds 1,805 inputs. RCH ovh-b actual CLI exits 1 with all six precise runtime findings, `test-results/flute-portability-refusal-20260917/`; that is expected refusal, not deploy acceptance.
- [x] Isolate glibc CPU-feature dispatch on one unchanged host/binary/library set. RCH hz4 default replay has zero differences; `GLIBC_TUNABLES=glibc.cpu.hwcaps=-FMA,-FMA4` and separately `-AVX,-AVX2` each reproduce the complete ovh-b report exactly (52 numeric fields plus eight hashes). All 1,805 input hashes remain stable. Receipt: `test-results/flute-runtime-dispatch-20260917/`, three completed replays, 20.625s remote. These are diagnostic reproductions, not passing deployment gates.
- [x] Evaluate a runtime-switch shortcut without changing source: Node 26.0.0 differs from two of the six admitted probe values on hz4. Reject it as an exact drop-in replacement. Scalar probe receipt: `test-results/flute-runtime-math-hz4-20260917/`.
- [x] Choose the original Bead's explicit checked-runtime alternative: opt-in `JCPE_EVIDENCE_QEMU` verifies pinned Linux x64 QEMU and official Bun binaries, then executes the same model gate on a software CPU. No analyzer substitution, epsilon, rounded metric, probe replacement or approval-report rewrite. Implementation `5a4cad7`.
- [x] Prove complete replay on the old worker: native baseline retains 60 differences; the identical Bun under QEMU has zero differences from the full accepted 12-cell report. `test-results/flute-emulation-replay-complete-20260917/`, 73.978s remote. Initial helper failed because its replay file was absent; preserve `flute-emulation-replay-20260917/` and do not count it as musical evidence.
- [x] Exercise real CLI success and adverse paths on ovh-b: full emulated predeploy passes 11 model and nine instrument-quality checks; red model and tampered report each exit 1; wrong emulator digest exits 2; unconfigured native gate still reports its six math mismatches. `test-results/flute-emulation-acceptance-20260917/`, 187.815s remote. Mutations occurred only in a frozen RAM checkout and were restored before the input check.
- [x] Preserve native behavior on hz4: 43 tests / 154 assertions (8.89s), all four type projects, full lint/source boundaries, and native predeploy pass. `test-results/flute-emulation-static-20260917/`, 281.087s remote.
- [x] Verify the separate native Node 26 Chromium playback gate on ovh-b: 15/15 instruments and enforced recovery pass, `test-results/flute-emulation-playback-20260917/`, 202.518s remote. Only the required downloader user-agent was added in the temporary gate copy; assertions and recovery enforcement were unchanged.
- [x] Fresh review and input verification: all three final receipts match 1,807 frozen files, with zero drift before/after and locally. Original evidence/math/gates remain intact; only the execution launcher was added. Close `jcpe-uid4` for this documented checked-runtime solution. Native math still varies by CPU; this is solo automated proof, not human/device acceptance. App HTML remains SHA-256 `9806877b0228701d7b1699d2db4383b9e9deabfd184b8136f4d0e1c810a11204`; no deployment.

## Broader regression audit — 2026-09-16 UTC

- [x] Preserve the RCH baseline at `test-results/runtime-audit-20260916/`: 3,892 pass, six failures and two unhandled errors; this is not a full-suite pass.
- [x] Repeat the full baseline on RCH hz4 with normal `bun test` and the pinned original UIowa corpus: 5,831 pass, 10 fail including two errors, 599,672 assertions across 523 files (1,278.56s). Retain `test-results/full-tests-20260917/`; this frozen pre-fix snapshot is not aggregate acceptance.
- [x] Clarinet CLI and exact flute replay pass with the corpus present. The earlier absent-corpus failures did not establish source defects.
- [x] Fix normal Bun routing under `jcpe-cs5z`: exclude only the two Playwright-only U2/U4 producers, matching the existing TS split. Real Node 26 collection of each unchanged dedicated configuration still finds its original test in Chromium/Firefox/WebKit (six collected, zero executed; not browser acceptance). Final Bun discovery loads all 521 eligible files, executes six selected staleness/Rust-window tests, filters 5,833 others, and exits zero with no import errors. This is focused discovery proof, not a full-suite pass. Reports: `test-results/test-routing-final-20260917/` and `test-results/test-routing-discovery-20260917/`.
- [x] Repair three standalone-staleness controls under `jcpe-0b8b`: corrupt exactly one byte of `JazzChords.org` without shortening it. Original assertions remain intact. RCH ovh-b `bun test tests/static/standalone-staleness.test.ts`: four passes (clean real build plus all three intended guard refusals); scoped ESLint passes. Receipt `test-results/staleness-final-20260917/`, all 1,807 bound input hashes unchanged. No production/artifact change; native branding acceptance remains open.
- [ ] Review the six stale U0 evidence source hashes against actual acceptance evidence; do not regenerate approval pins merely to turn the test green.
- [x] Repair `U5-MUT-009` under `jcpe-cs5z` by binding its needle to the ordinary MIDI export hash/delivery pair; the performed-MIDI callback is untouched. All 14 source mutation controls plus the ordinary MIDI success test pass (15 tests, 85 assertions, 21.95s); inspect all 14 baseline/mutant diagnostic pairs. MUT-009 fails at the intended exportRevision assertion (null versus 4). Test-project types and scoped ESLint pass; no killer assertion or injected behavior changed.
- [x] Rerun the unchanged semantic-surface suite with the iOS source closure included: 49 tests, 136 assertions pass (0.84s), `test-results/semantic-inputs-final-20260917/`. The earlier missing-file finding was a RAM-closure defect. All three routing/semantic receipts bind the same 1,835 unchanged inputs. Preserve the first discovery failure: this worker lacked Git objects for a top-level committed Rust-source read. Final probe installs six authentic objects from 78bbea5, verifies their original hashes, and executes real git show; no Git shim, synthetic commit, test change or assertion waiver. Production source and generated HTML remain unchanged; no deployment.
- [x] Under original `jcpe-6ujg.5.3`, replace authored comping's double assertion with a statically checked nonempty tuple of frozen pitch copies. Keep note order, duplicates, spelling and source immutability; no test or fixture changes.
- [x] Unchanged cast-policy rerun on RCH hz4 removes exactly the comping finding; 29 findings remain elsewhere and the global gate still fails (one pass, one fail). Guarded build passes, producing 8,636,958 bytes, SHA256 `e621a568febf11d7baeaa89f122e16124cb020e8d44c65dcd889396ded499d68`. Receipt: `test-results/comping-cast-policy-20260917/`.
- [x] RCH ovh-b final comping proof: 187/187 tests, 5,052 assertions (11.80s), P0 validator, all four TypeScript projects and full lint/boundaries pass. Its guarded build exactly matches hz4 above. `test-results/comping-cast-final-20260917/` remains overall red because the unchanged global cast gate still fails; no assertion relaxation. Initial hz4 focused job hit admission queue timeout without executing; no local fallback.
- [x] Fresh-review the tuple change and all 1,807 bound inputs, preserve peer files, and copy only the two-worker identical guarded artifact to root/dist for the source checkpoint. No browser, human, deployment or aggregate pass claim; original comping acceptance stays open.
- [x] Review the original P0 production inventory against `src/playback/authored-comping.ts` and prove its real layer ownership under the original comping acceptance.
  - [x] Claim `jcpe-6ujg.5.3`; read the comping/P0 ownership contracts and real source.
  - [x] Reproduce on RCH ovh-b with pinned Bun 1.3.14: 82 pass / 1 fail; exact failure is the omitted `authored-comping.ts` inventory entry (`test-results/comping-ownership-baseline-20260917/`).
  - [x] Include the compiler in the unchanged ownership scanner; apply all three existing theory-import/generation mutation controls to both loop projection and comping, with clean-source success twins.
  - [x] RCH ovh-b final proof: P0 contract PASS; 187 tests / 5,052 assertions across seven complete suites PASS (10.64s); all four TypeScript projects and full lint/source-boundary checks PASS. Receipts in `test-results/comping-ownership-final-20260917/`, all 1,805 input hashes stable.
  - [x] Review final diff and bound source hashes; stage only the ownership test and tracker/TODO record. Production source and committed HTML remain unchanged; no rebuild/deployment required for this test-only repair. Original `jcpe-6ujg.5.3` remains open for independent/human acceptance.
- [ ] Investigate the G5 expected-child mismatch under the reopened original G5 task; preserve independently authored branch expectations and keep full advanced-discovery acceptance open.

## Play-along start cues — jcpe-6ujg.1.3 (CyanCove, 2026-09-16 UTC)

Consumer: the requested granular implementation list. Retire this section after original play-along acceptance; preserve exact results in the Bead.

- [x] Re-read the repository agreement, README and architecture; reconcile ready Beads, original play-along spec/build/verify context, current source and peer ownership. Only CyanCove is active in Agent Mail; preserve all three untracked application UI copies.
- [x] Claim the original ready verification leaf and inspect the source timeline, controller authority, Focus lifecycle and current browser coverage.
- [x] Reproduce stopped-seek disagreement: the scrubber stores the next start while the cue reads the old audio position. Author exact starting/arriving and unchanged-state expectations through the real controller.
- [x] Reproduce source-edit leakage through the actual next Play: after seeking beat 5 and changing the title, the baseline still started on Dm7 at 5 instead of Cmaj7 at 0.
- [x] Verify pending intent across Undo/Redo, edit-and-Undo, real lesson replacement-and-Undo, refused edits, setting receipts and accepted/ignored notifications: 26/26 focused tests, 204 assertions, 1.07s through RCH. Fake audio clock proves controller/transport semantics; it is not native audio evidence.
- [x] Reproduce armed/disarmed loop mismatch through the actual controller and transport. Baseline: 14/17 pass, three new regressions fail.
- [x] Verify section-loop boundaries, out-of-range starts and chart end without clamping through the same focused suite.
- [x] Reproduce the visible cue defect against committed bytes; retain diagnostics/hashes. Corrected-observer baseline: 1/5 pass, two pointer obstructions and two exact cue failures (desktop and touch): scrubber beat 2 versus Cmaj7 at beat 1. First Chromium baseline: 0/4; two load timeouts, one real 320×568 Play obstruction, one obsolete scheduled-source observation against the default Concert Grand worklet. No browser cue proof from that run.
- [x] Repair the confirmed phone Focus transport overflow and verify actual Chromium pointer reachability; observe default-instrument destination PCM and explicitly select the oscillator for the existing scheduled-source test.
- [x] Repair controller pending-start/source binding, ready-position and ready-loop cues; preserve live/paused audio authority, exact notes/time, source staleness, history and bounded cached selector work.
- [x] Finish Chromium Focus layout proof: corrected two-row phone controls passed ready-cue/native PCM checks, but the first broad geometry check found laptop Mute beyond the viewport. Add the existing Sound sheet at compact Focus widths and restore 44px coarse-pointer controls. Keep that failing 4/5 run. Final Chromium: all 24 browser tests pass with zero unexpected/flaky/skipped cases; its outer wrapper alone exits 1 because it mistakenly expected 26, corrected to the actual full 24-case collection. No test failure is relabeled.
- [x] Correct the new test’s mistaken count-in receipt assumption and typed exact-beat expectations. Keep the failed 92/93 run, type/lint diagnostics and earlier 14/17 negative proof.
- [x] Complete the frozen 1,802-file remote input snapshot including the existing iOS theory bridge; native-observer type/lint checks pass. Local hashes and mtimes still match.
- [x] RCH final static: 97/97 tests across eight files, 760 assertions, 56.48s; all three intentional source mutants fail their named witnesses and restored source passes 26/26. Full four-project typecheck, source boundaries/lint and guarded build pass; artifact `fa2657e865e093d2529a15b4e1aaa617a7b0ee7bd606b77256ae11416d03ff9a`, 8,642,945 bytes. Retain all prior failures.
- [x] Complete the sequential 72-case native matrix: Chromium 24/24 (102.292s), Firefox 24/24 (164.288s), WebKit 24/24 (216.525s); every final wrapper exits 0, each test has one passed attempt, zero unexpected/flaky/skipped. Covers 320px fine/coarse pointers, reduced motion, keyboard, Stop, exact cues, native PCM, storage/download and no forbidden requests. All 1,802 source hashes/mtimes still match. Evidence: `test-results/play-along-focus-{chromium-final,firefox,webkit}-20260916/`. Retain the earlier Chromium 24/24 run whose scratch wrapper incorrectly expected 26; the corrected wrapper has now passed without changing assertions, deadlines or product bytes.
- [x] Fresh-review final controller, CSS and test changes against original acceptance. Committed source/tests/contract/artifact are in `d5bc152`; tracker commits `5e8f4a3`/`6fd1d24` are pushed. Preserve these existing commits and all three peer files. Every one of the 1,802 checked inputs matches Git HEAD and local mtimes.
- [x] Complete RCH committed-artifact predeploy: model acceptance 11/11, instrument quality 9/9 with zero fail findings (16 warnings retained), native Chromium playback 15/15. Recovery is enforced but vacuous because no refusal is reproducible. Guarded rebuild at the end matches committed/browser-tested HTML `fa2657e865e093d2529a15b4e1aaa617a7b0ee7bd606b77256ae11416d03ff9a`, 8,642,945 bytes. Evidence: `test-results/play-along-committed-20260916/`. No aggregate `bun run verify`, human listening or exercised-refusal claim.
- [x] Deploy committed bytes to Cloudflare Pages `91021eac` and Vercel `dpl_96zaD7sgAiABwF37kbj6N65EY7hD` (READY). At 2026-09-16 01:10:48–50 UTC both public hosts return that HTML hash and social-image hash `b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12`; all four first-poll responses are HTTP 200 and exact matches.
- [x] Diagnose the first live Cloudflare run: 14/15 pass; Firefox 320px times out at Playwright click completion although the button is actionable and the retained trace proves native voices and Cmaj7 → Bm7#5, with zero application errors and only the known CSP-blocked Cloudflare beacon. Keep `test-results/play-along-live-20260916/`; this run is not green. Explicitly foreground the native browser before audio actions; repeat both full host matrices with unchanged assertions and deadlines. The trace does not prove a specific scheduler/root cause.
- [x] Preserve the second live Cloudflare run (13/15, 313.200s): explicit foreground fixes the first phone click, but Firefox desktop stalls on Cmaj7 and a default-instrument phone case reports zero PCM. No app errors; no asserted root cause. Shared hz3/hz4 I/O pressure is 68–85% full; do not count available build slots as a quiet browser platform. Prepare quiet `ovh-a` (near-zero I/O pressure) with the same copied browser/toolchain/dependency bytes in private RAM and a private PulseAudio service; preserve all test limits and the two failed matrices.
- [x] Complete both full live matrices on quiet RCH `ovh-a` with private RAM/browser cache and a private real PulseAudio sink: Cloudflare 15/15 (119.904s), Vercel 15/15 (102.342s), all one passed attempt, zero unexpected/flaky/skipped, zero application errors and forbidden requests. All six Chromium desktop/fine/coarse-phone screenshots inspected. Source/HTML unchanged. Evidence: `test-results/play-along-live-ovh-a-20260916/`. Cloudflare records its 15 known blocked-beacon CSP errors separately; Vercel records none. Three pre-assertion cue snapshots show the prior UI poll; the subsequent unchanged exact-label assertions all pass. Preserve the raw timing evidence, the two failed host matrices and the corrected post-run parser interpretation.
- [x] Record exact commands, failures, final proofs and deployment disposition on the original verification leaf; return `jcpe-6ujg.1.3` to open/unassigned.
- [ ] Obtain original independent-person and applicable physical-phone/U4 acceptance. The original leaf and package remain open; author-run automation does not discharge those obligations.

## MIDI source-note review — jcpe-upbz (CyanCove, 2026-09-15)

Consumer: the requested granular implementation list. Retire this section after original M1 acceptance; preserve results in the Bead.

- [x] Reconcile tracker and live source; key ambiguity is already fixed in 97aed83. Preserve that implementation and original acceptance.
- [x] Release note-first verify for its remaining human/device work; claim the stale ready M1 build leaf and reserve exact files.
- [x] Specify bounded source-note inspection, half-open overlap, source fidelity, exclusions, short-note disclosure, stale selection and preview ownership.
- [x] Author independent positive/near-miss/transposition/bounds fixtures before production code.
- [x] Implement an on-demand application selector over retained decoded notes; retain complete counts and all pitch keys while bounding detail rows.
- [x] Connect ambiguous and unwritten passages to the review; preserve default one-file/Add flow and all overrides.
- [x] Wire individual/source-set pitch previews into the existing owner, with explicit limits, timing disclaimer, Stop and replacement handling.
- [x] Prove native desktop/320px review, sound/retirement, empty passage, override/new-file reset and unchanged chart/history. Final full groups: Chromium run4 16/16 (61.850s), Firefox run3 16/16 (76.457s), WebKit run2 16/16 (138.500s); zero unexpected/skipped/flaky, all 48 single-attempt passes. Each group binds the same 1,708 inputs and HTML SHA256 `3f9f77ff5ad7b04ae86f95efc0c43cbed575317d764e7743a2d649d36a0be449`. Six source-review receipts have native sound, zero sounding/future nodes after Stop and no errors or requests. Evidence: `test-results/m1-source-browser-{chromium-run4,firefox-run3,webkit-run2}/`.
- [x] Run focused M0/M1, static and guarded-build gates through RCH; retain all failures and exact inputs. Final full M0 evidence: 163/163 tests, 18/18 browser cases (6 per engine), zero findings; final M1 evidence: 92/92 tests, zero findings. Final E2E types and scoped lint pass on 1,757 frozen web/DSP/test/contract inputs in `test-results/m1-source-m0-final-run2/`.
- [x] Fresh review caught overlap-vs-full-duration context labeling; correct it and add the crossing-boundary negative case.
- [x] Give MIDI source/groove preview a scoped generation owner, so stale import release spares newer note-first/pad owners; author positive and replacement/cancellation proof.
- [x] Resolve the existing 513-bar segmentation gate's repeated 5-second timeout by reducing redundant full-file scans: 6,663/6,761ms before, 785ms after in the existing regression. All 207 focused tests pass (7.41s), including scoped source/groove release; M0/M1 contracts and full typecheck pass. This is one measured regression case, not a general performance guarantee. Original timing/mass laws and gate limits are unchanged.
- [x] Verify scoped release and segmentation through the original tests, source mutations and native adapters. First static run: 202/204 unit tests, two failures; second: 204/205, the long-grid timeout remains. New tests passed in both. TypeScript harness/duplicate-import integration errors were corrected; first output-directory mismatch was recovered by explicit rsync. No failed run counts as green.
- [x] Complete static run 3: 207/207 focused tests, 3,201 assertions, M0/M1 contracts, all TypeScript projects, full lint/source policy and guarded build. Artifact: 8,641,021 bytes, SHA256 `3f9f77ff5ad7b04ae86f95efc0c43cbed575317d764e7743a2d649d36a0be449`.
- [x] Repeat the expanded evidence/mutation gate with all 1,708 tracked source inputs: E2E typecheck, scoped lint, official M1 evidence, all four detected source mutants and restored five-test suite pass in `test-results/m1-source-mutations-run2/`. The first RAM copy incorrectly filtered every path containing `ios`, omitting `StudioShell.tsx` and `StudioShellNotice.tsx`; E2E typecheck and M1 evidence refused. All four source mutants were detected and restored tests passed, but the overall run is red. Remove substring filtering; preserve `test-results/m1-source-mutations-run1/`. The final guarded reproductions below cover the restored inputs too.
- [x] Complete the native matrix after diagnosing the retained failures. Chromium run 1 passes 14/16 in 70.060s: organ groove scheduling count remains 24 over 700ms (the trace shows 24 future sources and zero sounding at both samples), and desktop source-review element screenshot times out waiting for stability after accessibility/audio assertions pass. Retain `test-results/m1-source-browser-chromium-run1/`; the clock/foreground corrections and final complete groups below resolve this without relaxed assertions or deadlines.
- [x] Diagnose repeated Chromium organ timing failure with a native constructor/clock probe: `running` clock stalls at 0.01161s while 24 sources remain future. Removing headless mute does not cure it. Strengthen the existing test to require actual sounding sources before its unchanged 700ms groove-motion assertion; preserve the original 5-second expectations, 30-second test deadline and zero retries. Restore foreground after axe and capture full viewport in the new source-review test. Original failed runs and one temporarily capacity-refused RCH request are retained.
- [x] Prove the strengthened audition gate rejects a real one-sonority-per-bar source mutation: guarded-built mutant SHA256 `f958c47640fad56dd7437bc8215f9198ae1d5ee94a1ceb2bbcba8a3c77c3653d` fails the unchanged native motion assertion after sound starts. Final Chromium full group passes 16/16 in 105.241s, zero unexpected/skipped/flaky; source hashes unchanged. Receipts: `test-results/m1-source-native-negative-run1/` and `test-results/m1-source-browser-chromium-run3/`.
- [x] Finish final full groups after WebKit diagnosis; final 48/48 results are recorded above. WebKit run1: 13/16; manual screenshot preparation injects `body {}` (confirmed in Playwright 1.61.1 coreBundle.js) and violates the artifact CSP, so take visual captures only in Chromium while preserving every engine’s assertions/error gate. Concert-grand had four sounding sources at both wall-clock samples; measure the same 700ms using the actual native audio clock, with the original five-second deadline and greater-source-count assertion. The native one-chord-per-bar negative control was repeated for this exact clock correction (run2 below).
- [x] Final WebKit run2 passes 16/16 in 138.500s, zero unexpected/skipped/flaky. The exact final audio-clock test rejects the guarded-built one-chord-per-bar mutant again (`test-results/m1-source-native-negative-run2/`); canonical runtime/HTML hashes are unchanged. Chromium run4, Firefox run3 and full M0 evidence subsequently passed as recorded above.
- [x] Complete full M0 evidence and final reproduction; deployment checks are tracked below. Final types, scoped lint and M1 evidence pass, but M0 run1 stopped before testing because the RAM copy lacked `dsp/concert-grand/src/smf.rs`. Extend the M0/release input set with all 49 tracked DSP files (1,757 total); keep the 1,708-input browser receipts unchanged and accurate. The unchanged full M0 gate subsequently passed on that expanded copy (run2).
- [x] Fresh-review the original build acceptance, address defects, reproduce the artifact and commit explicit owned paths. The first final build refused because the worker’s shared dependency directory no longer contained Preact; do not treat that as an application compile pass. Rebuild and release checks use the same intact frozen RAM dependency copy as the native gates, with toolchain preflight, while preserving the shared directory.
- [x] Final guarded RCH reproduction with the intact frozen dependencies: Bun 1.3.14 / Node 26 doctor and build pass in 3.884s, all 1,757 input hashes and local mtimes match; root/dist/candidate bytes equal SHA256 `3f9f77ff5ad7b04ae86f95efc0c43cbed575317d764e7743a2d649d36a0be449` (8,641,021 bytes). Receipt: `test-results/m1-source-release-rebuild-20260915/`.
- [x] Push production/tests in `5850f48` and generated artifact in `7c4db18`, preserving prior source-review commits `d9e4056`/`cc908f0`. The first push refused after another contributor pushed iOS-only `2ac50f2`; rebase only the two unpublished commits, verify identical web bytes and preserve all four iOS changes. Five owned-file mtimes changed during rebase; capture fresh committed mtimes after verifying content. No force push or unrelated-file staging.
- [x] Correct the first isolated predeploy copy, which refused the missing checked-in model acceptance ledger before running its gate. Expand to all 1,801 committed web/assets/evidence inputs (excluding iOS, mutable operator README/TODO/Beads and separately staged HTML). Keep the original refused run. With Bun 1.3.14 / Node 26, actual `bun run predeploy:check` passes: 11 shipping models, nine instrument-quality subjects, zero fail findings and 16 warnings retained. Actual native Chromium playback passes all 15 instruments; recovery is enforced but vacuous because no refusal reproduces. This is mechanical/audio-adapter proof, not quality or human listening. Only User-Agent and equivalent resolved admission-import adaptations were made in the scratch playback runner; tracked gate SHA256 `601bb5ecd0cdf1eee4a69645d654426e95e8c1fc24993cc21223a635c3ba3ba2`. Evidence: `test-results/m1-source-committed-hz4-run2-20260915/`, 193.840s remote.
- [x] Rebuild immediately before upload through RCH with frozen dependencies and all 1,801 source hashes: doctor/build pass in 4.094s, root/dist/candidate/staged/Git artifact match the 8,641,021-byte SHA256 above. Recheck every committed input hash and mtime before upload. Evidence: `test-results/m1-source-preship-rebuild-20260915/`.
- [x] Deploy committed `7c4db18` HTML and social image to Cloudflare Pages (`15526ed2`) and the Vercel mirror (`dpl_4Y3ookSzXW7Q5txxUMh87RkZs7x5`, READY). At 22:49:54–55 UTC both canonical hosts return HTTP 200 and match the committed HTML and image SHA256 `b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12` on the first complete poll. Actual CLI versions: Wrangler 4.132.0, Vercel 59.17.0, Node 22.22.1. Exact CLI/unmodified-response receipts remain in `.tmp/m1-source-review-20260915/`.
- [x] Verify deployed review/focus/exact source details, real sounding preview/Stop and unchanged revision on both hosts at 320/1280px in Chromium/Firefox/WebKit: JazzChords.org 6/6 in 51.913s, mirror 6/6 in 40.877s, all single-attempt passes, zero unexpected/skipped/flaky. Inspect all 12 native receipts: four sounding sources before Stop, zero sounding/future sources after, no console/page/CSP errors or extra requests. All four Chromium screenshots show visible Stop focus and controls fitting the viewport. The required User-Agent run does not prove Cloudflare analytics injection is globally gone. Evidence: `test-results/m1-source-live-20260915/`; all 1,801 source hashes/mtimes and Git HTML remain unchanged.
- [x] Record final live evidence and close original M1 build leaf `jcpe-upbz` at 22:54:33 UTC with exact commands/results and a solo-review disposition. Commit/push only owned README/TODO/Beads metadata; preserve the three untracked application UI copies and all original independent/human obligations.
- [ ] Complete original independent M1 proof (`jcpe-qbvz`), including independent real-song expectations and comparison; obtain original human listening (`jcpe-wyw5`). Keep these and package `jcpe-765f` open. This build slice does not supply independent-person, physical-device or human musical acceptance.

## Note-first verification — jcpe-6ujg.3.3 (CyanCove, 2026-09-15)

Consumer: the user's requested granular work list and the agent executing this leaf. Retire this section after the original acceptance is complete; preserve command/results in the Bead. This is the existing checklist, not an additional reporting system.

- [x] Read the full repository agreement, README and architecture; reconcile current tracker, source wiring, peer ownership and original note-first contracts.
- [x] Claim the ready verification leaf and reserve only its touched files; preserve the three untracked application UI copies.
- [x] Trace typed/tactile draft editing, plural naming, exact Manual insertion, source staleness and audio ownership.
- [x] Reproduce keyboard-focus loss after successful Add in native Chromium at 320px against committed SHA256 `350487f6b881cf7c65824334ee1e3023a2acf49017317ca5b3d702eaba4daece`. Desktop case hit its unchanged 30-second deadline; retain that separate failure.
- [x] Reproduce Custom-label truncation at both 320/1280px: browser accepts 32 supplementary characters although application accepts 64 code points. Initial four-case run is red, not a pass; RCH result retrieval also timed out, then explicit rsync recovered the retained reports under `test-results/note-first-review-red-20260915/`.
- [x] Fix both UI defects without changing exact-note storage, naming theory, preview ownership or command history: focus the retained note field after successful Add; permit 128 UTF-16 input units while preserving application validation's 64-code-point limit.
- [x] Verify Custom and acknowledged enharmonic insertion, literal order/doubles/spelling, one Undo/Redo and persisted recovery through native downloads/storage in all three engines.
- [x] RCH Bun 1.3.14 / Node 26.0.0: corrected explicit 15-file run passes 180 tests and 2,293 assertions; M0 contract, all four TypeScript projects, full lint, 348-file source policy and guarded build pass. A final E2E typecheck and owned lint pass after the test delta. The initial 145-test selection omitted four note-first files due to the remote Git index; retain that incomplete result. Evidence: `test-results/note-first-review-{static,focused}-20260915/`. A second worker reproduces SHA256 `8d3dd95ef258f1358a179390ff208525ea246c04a2d0dd14324e0eed6a9a1802`, 8,633,118 bytes, with all 1,617 frozen input hashes unchanged (`test-results/note-first-hz3-rebuild-20260915/`).
- [x] Execute and retain the first sequential native matrix: Chromium 11/14 (408.414s), Firefox 6/14 (485.881s), WebKit 6/14 (440.689s), zero skipped/flaky cases. All six changed Chromium cases pass, but the complete 42-case gate is red. The 19 failures include action/whole-test deadlines and a Firefox guitar preview count of zero before its deadline. Reports, traces, 270 worker-pressure samples and the unchanged 1,617-input receipt are in `test-results/note-first-review-browser-20260915/`; explicit rsync recovered the RCH result-transfer timeout. Do not describe this as a green matrix or as proven infrastructure-only failure.
- [x] Resolve the red browser gate and pass each complete 14-case engine group with unchanged deadlines/assertions. Initial hz3 admission refusal remains recorded: free space fell to 56.8 GiB, no expired compiler caches or ballast, and no cleanup/admission override applied there. The admitted vmi1264463 had substantial CPU/I/O contention. Complete final groups now pass on hz4; retain all earlier failures without claiming each was proven infrastructure-only.
- [x] Fresh-review the final UI, application boundary and test diff; retain the original human/player/physical-device acceptance on `jcpe-6ujg.3.3`. Do not close the leaf or its parent on these partial automated results.
- [x] Rebuild immediately before committing the reviewed repair and regenerated artifact as `8f50175`, with the red browser gate explicitly recorded; all 1,617 source hashes/mtimes and root/dist bytes match. The first final-build helper omitted its expected-hash input after a successful build; preserve that failure, supply the input and rerun the unchanged helper. Completed RCH rebuild: 14.589s, SHA256 `8d3dd95ef258f1358a179390ff208525ea246c04a2d0dd14324e0eed6a9a1802`, receipt in `test-results/note-first-final-rebuild-20260915/`. All three peer files remain untracked and untouched.
- [x] Push repair `8f50175` to `origin/main`; record exact verification and recovery details in Bead comment 458, preserving the original open acceptance obligations.
- [x] Resume without source changes; retain the `/run` executable-copy failure (`noexec`, no application tests ran) and the corrected vmi1264463 Chromium group (10/14, four desktop timeouts, 393.307s). Reports: `test-results/note-first-resumed-{browser,native-browser}-chromium-20260915/`.
- [x] Restore hz4 admission using `rch cache clean --older 24h --project <exact-source-content-id> --execute` for two staging copies idle over 29 hours, after privileged process/cwd/open-FD checks found no references. Preserve canonical trees and recent pooled caches. Cleanup receipts record 16,345,073,315 bytes removed; free space rises to about 67 GiB and one slot is admitted. Exact paths, previews and results remain in `.tmp/note-first-review-20260915/hz4-staging-*.json`.
- [x] Preserve the first hz4 setup failure: Playwright was launched before its dependency copy completed. Wait for copy completion, verify CLI 1.61.1 and add a toolchain preflight before the unchanged full group. Final Chromium passes 14/14 in 89.489s, zero unexpected/skipped/flaky, same artifact and all 1,617 frozen source inputs (`test-results/note-first-native-hz4-browser-chromium-20260915/`).
- [x] Restore hz4 admission a second time after free space fell below its threshold. Supported RCH cleanup removes four more non-pooled staging copies idle over 32 hours, with no process/cwd/open-FD references: 22,593,340,498 bytes. Verify exact targets absent and about 75.8 GiB free; no canonical checkout, active/recent pooled cache or policy changed. Preserve the refused WebKit launch separately from the admitted run.
- [x] Complete all full groups on hz4: Chromium 14/14 in 89.489s, Firefox 14/14 in 114.697s, WebKit 14/14 in 216.138s. Every case has one passed attempt, zero unexpected/skipped/flaky; all 42 native diagnostics match the artifact and have no console/page errors or forbidden requests. Permitted file navigation/reload is distinguished from runtime network. All 1,617 source hashes match; inspect all four Chromium phone/desktop keyboard/guitar screenshots. Reports: `test-results/note-first-native-hz4-browser-{chromium,firefox,webkit}-20260915/`.
- [x] Rebuild immediately before release: RCH guarded build and final comparison pass in 3.657s, root/dist/staged/Git bytes equal the same 8,633,118-byte artifact. Recheck all 1,678 release inputs and source mtimes. Preserve `rebuild-hz4.log`: the first reused helper referenced an old expected-hash filename after build. Correct the path, validate/read the expected input before build, and complete the unchanged build/comparison (`rebuild-hz4-complete.log`, `test-results/note-first-release-rebuild-20260915/`).
- [x] Pass actual committed-byte predeploy gates: 11 shipping models; nine instrument-quality subjects with zero fail and 16 warning findings retained; 15/15 native Chromium playback cases. Recovery gate is enforced but vacuous because no refusal reproduces, not human listening or full-register quality acceptance. All 1,678 release input hashes match; tracked playback gate SHA256 `601bb5ecd0cdf1eee4a69645d654426e95e8c1fc24993cc21223a635c3ba3ba2`. RCH helper runs actual `bun run predeploy:check` and native Node playback against the staged Git artifact, with only the required User-Agent/equivalent admission import adaptation. Evidence: `test-results/note-first-committed-hz4-20260915/`, 189.465s remote.
- [x] Upload committed `cf898ef` HTML/social bytes to Cloudflare Pages (`cedac559`) and Vercel (`dpl_G9msUMbhdQ1TaCZnQjduYe9gSQMZ`, READY). Both canonical hosts return HTTP 200 and match Git on the first complete poll at 2026-09-15 20:26:37–38 UTC: HTML SHA256 `8d3dd95ef258f1358a179390ff208525ea246c04a2d0dd14324e0eed6a9a1802`; image SHA256 `b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12`. Retain CLI and unmodified-response receipts in `.tmp/note-first-release-20260915/`.
- [x] Verify live focus, complete 64-code-point Unicode label and exact Manual JSON download: six cases on JazzChords.org (46.299s), six on the Vercel mirror (30.381s), Chromium/Firefox/WebKit at 320/1280px. All 12 pass once, zero unexpected/skipped/flaky. Native diagnostics record no console/page/CSP errors and only the host navigation in this run using the required User-Agent; this does not prove Cloudflare analytics injection is globally gone. All 12 downloaded documents retain the full label, double accidentals, order and duplicate notes. Inspect all four Chromium phone/desktop screenshots: retained note input has visible focus and fits the viewport. Worker screenshots lack emoji glyphs in the chart background, so they do not establish system-font glyph coverage. Reports and screenshots: `test-results/note-first-live-20260915/`.
- [x] Record release evidence in the original Bead and requested TODO inventories; commit/push only owned metadata after final source checks and guarded reproduction. Preserve all three untracked application panel copies.
- [ ] Obtain the original independent musician/player and applicable physical-device acceptance; keep `jcpe-6ujg.3.3` and its parent open. Automated browser views are not physical-phone, font-coverage or human-listening acceptance.

Consumer: the user and Codex executing the 2026-09-04 request to implement the reality-check backlog and GitHub voice-leading issue #2. Gate: do not declare this campaign complete while a requested item remains unresolved. Observed defect: the audit found 51 closed records with unfulfilled obligations. Retirement: retire this checklist when the campaign is complete; lasting commands/results belong on the Beads. This inventory is process and earns no implementation credit.

## Canonical project name — jcpe-0b8b (CyanCove)

### Native display-name regression — 2026-09-16 UTC

- [x] Reopen and claim the original naming task after incoming commit `c4ecc52` reintroduced the rejected name in two native display-name fields. Read the original task's explicit FrankenJazz sibling exception and the Apple app contract; preserve web JazzChords.org branding.
- [x] Restore `FrankenJazz` in both `ios/project.yml` and `ios/Sources/Info.plist`, retaining the new icon, build version 2, bundle identifiers and all unrelated native work.
- [x] Parse the entire YAML and plist, verify every generated property agrees, and compare with committed native metadata: only the two display-name scalars differ. Icon, build version 2, project settings, bundle IDs and all 1,801 verified web inputs remain unchanged. Inspect all 30 capitalized residuals across 1,867 tracked/first-party untracked files: ordinary music/editing text plus the historical regression receipt; no remaining product-name misuse. `git diff --check` passes. Receipt: `.tmp/next-ready-20260916/native-metadata-proof.json`.
- [x] Commit and push only the two corrected native paths as `368699b`. At 02:33:04–05 UTC both web hosts again return HTTP 200 and HTML SHA256 `389490f6600c5237228d7e5e00f782e7106025e50d2268ee65ecef4e298fda92`, identical to Git HEAD; no web rebuild or upload is needed for these native metadata edits. Receipt: `.tmp/next-ready-20260916/final-host-receipts.json`.
- [ ] Complete the Apple plan's clean generated-project rebuild through an admitted RCH Mac worker before re-closing the task. The current 15-worker RCH roster has no Apple-tagged worker, so no admitted Xcode lane was established. Source metadata validation alone does not establish the name in a built or installed Apple app; keep the original task open for that gate.

### Completed web naming correction — 2026-09-15 UTC

- [x] Audit all 1,862 tracked files for product-name, slug, native-name and filename occurrences; classify ordinary musical/editing uses separately.
- [x] Claim the owner-directed correction and reserve exact branding paths; keep unrelated contributor changes intact.
- [x] Correct unambiguous product prose, UI/header, social metadata, QR instructions, import/export descriptions, MIDI instrument metadata, package metadata and matching fixture text.
- [x] Preserve saved charts/recovery by retaining persisted schema, model and storage identifiers plus supported file-format extensions. Apply the naming correction to the web product and its references; the separately requested FrankenJazz native sibling keeps its own name.
- [x] Replace and visually inspect the social share image; correct export filenames, print font/page identifiers, test serving paths and the native lab display label. Unnamed illustration and historical screenshot remain unchanged.
- [x] Audit all 1,862 tracked and three first-party untracked files again, including generated HTML and tracker JSON text. Remaining capitalized occurrences describe music or editing; technical schema/model/storage IDs and existing hosting addresses retain compatibility. Do not rewrite prior test evidence or Git history.
- [x] Reconcile naming-dependent fixture text and byte/semantic fingerprints from literal authored data; use Cadence for the sample chart to retain its storage boundary sizes. Independently reconstruct all seven prior MIDI hashes before the seven-byte instrument-metadata change; preserve the one-byte-over mutation.
- [x] Rebuild DSP with pinned nightly/Binaryen through RCH; remap the worker Rust source path to the canonical compiler path. Actual WASM remains 268,222 bytes, SHA256 `f505035be85ea98cc6c6c71374a84448c65851aa7a4ca31e5720e71f336a6ca9`; generated source ledger updated.
- [x] RCH full typecheck/lint, licenses, eight affected contracts and 772/773 focused tests pass. Correct the remaining A0/E0 aggregate fingerprint; its validator and complete 10-test static suite then pass (49 assertions). All prior failed runs retained; deadlines, boundary cases and mutation assertions unchanged.
- [x] RCH `bun run verify:standalone`: 36/36 native cases across Chromium/Firefox/WebKit, including file/HTTP boot, accessibility and no-network negative controls. Earlier missing-browser setup report retained.
- [x] Preserve the first affected browser run (85 pass, 9 fail, 164 unrun after deliberate interruption). Eight import failures followed a fixture export that cleared the confirmation prerequisite; explicitly establish an unexported revision through real title edits without changing document bytes. Settle the automatic gesture-less delivery probe before the positive trusted click. Keep all original assertions and deadlines.
- [x] Preserve the second affected run: RCH killed it at its 1,800-second SSH limit before all 258 cases completed. Chromium completed 86/86; Firefox exposed asynchronous starter-seeding setup in WAV fixtures, and WebKit exposed the finite preview gate being consumed by mouse actionability waits. Wait for the starter chart and activate the same import controls by keyboard; retain all original assertions, native sounding checks and deadlines.
- [x] Preserve the vmi1264463 Chromium attempt (20 passed, two 30-second exact-sharing timeouts, 64 unrun after interruption). Native traces show slow completed actions rather than a value assertion failure; observed CPU pressure was high. Move to newly admitted vmi1149989 with verified identical binaries, fresh profiles and per-run CPU/I/O/memory pressure diagnostics.
- [x] Preserve the full vmi1149989 Firefox run: 82 passed, four finite-preview fixture failures, zero skipped/flaky, 1,164.872 seconds. The keyboard change was insufficient. Batch only those DOM/file setup steps inside one browser evaluation, using visible real controls and a native File, and record importSetupMs; do not change the audio graph/clock, preview duration or sounding/retirement/document assertions.
- [x] Final batched fixture passes E2E TypeScript/scoped ESLint and both desktop native preview cases. Its next vmi1264463 run was interrupted after four passes/four timeouts; 20 cases were unrun or interrupted. The timeouts were unchanged owner-loss/native-clock scenarios that passed on vmi1149989. Preserve the raw report and the interrupted next case; this is not a green import gate.
- [x] Preserve the resumed vmi1264463 Firefox attempt: five passed, nine timed out, one interrupted and 13 unrun; the JSON groups the latter 14 as skipped. Native traces show completed ownership-loss/replacement operations followed by the whole-test deadline during final export/Undo; live diagnostics record heavy swapping and CPU/I/O contention. No partial report counts as acceptance.
- [x] Restore hz4 through guarded RCH cache maintenance without changing admission limits: 31 expired compiler caches, then two staging copies idle over 24 hours with no process/FD references. Preserve recent staging and compiler pools. Free space rises to about 76 GiB and RCH restores two slots above its 50 GiB reserve; SBH's ambiguous candidates remain untouched.
- [x] Finish the full revised Firefox import suite on hz4: 28 passed, zero unexpected/skipped/flaky, one attempt each, 259.319 seconds. All 1,647 frozen inputs and artifact SHA256 remain identical. Four real preview-retirement attachments show four sounding sources at confirmation, zero sounding/future sources afterward, and 80–110 ms import setup. Evidence: `test-results/branding-ready-firefox-imports-20260915/{firefox.json,receipt.json,source-inputs.json,worker-pressure.json}`. Deadlines, audio clocks, assertions and admission remain unchanged.
- [x] Finish Chromium/WebKit coverage in bounded sharing (30), export (28) and import (28) groups. Preserve the 58 green cases from the eight unchanged Firefox suites, bound to identical artifact/source/test inputs; never count its red import suite as passed. Inspect native print, MIDI, WAV and sharing diagnostics.
- [x] Chromium sharing passes 30/30 (134.234s). Preserve the first export report: 27 pass/one Poppler `pdftotext` timeout. The original 45,561-byte PDF passes unchanged native geometry/font/49-duration proof in 85.6 ms; a diagnostic helper's initially omitted A4 argument is separately corrected and its failed log retained. Put generated native outputs in per-run RAM storage and archive them after each group. Full Chromium exports then pass 28/28 (128.369s), and imports pass 28/28 (136.315s), with no skipped/flaky/retried cases. Reports: `branding-ready-chromium-sharing-20260915` and `branding-ram-chromium-{exports,imports}-20260915` under `test-results/`.
- [x] Resolve the remaining WebKit groups. Preserve the first sharing report: 29 pass/one 5-second Play-click timeout in the first full-chart/file/1280px case. Its CSP errors begin after the timeout during failure capture; later equivalent playback cases pass. A progress update incorrectly inferred no earlier failures from the log tail and was explicitly corrected after reading the full report. Repeat complete groups with the native cache produced by that run retained, fresh browser profiles, RAM output and unchanged deadlines; no cold-start or independent-person claim. Original report: `test-results/branding-ram-webkit-sharing-20260915/webkit.json`. Complete warm-cache runs pass sharing 30/30 (159.649s), exports 28/28 (211.789s) and imports 28/28 (306.044s), each with zero unexpected/skipped/flaky cases and one passed attempt. The four preview-retirement receipts retain four sounding sources at confirmation and zero afterward (100–110 ms setup). `test-results/branding-native-coverage-20260914.json` reconciles 258 distinct cases, 86 per engine, to the same artifact and 1,647 frozen inputs. Original red reports remain intact.
- [x] Rebuild canonical HTML through the guarded RCH build: 8,633,114 bytes, SHA256 `350487f6b881cf7c65824334ee1e3023a2acf49017317ca5b3d702eaba4daece`, byte-identical to dist; source digest `ae0744ae80975a452fd9618c4d57c90fdf1e0e4cf82d2a7abbf05569206317a5`. Recheck frozen inputs and rebuild immediately before commit.
- [x] Commit explicit owned test fixes as `bbde8ee` and branding/source/fixtures/artwork/generated HTML as `e523cff`, with outstanding browser gates stated in both messages. Keep the corrected obsolete recovery snapshot on disk, untrack it and add an exact ignore rule in `4460362`; no local bytes deleted. Preserve all three peer panels. Final RCH guarded rebuild reproduces SHA256 `350487f6b881cf7c65824334ee1e3023a2acf49017317ca5b3d702eaba4daece`; all 1,647 frozen hashes/mtimes match.
- [x] Push the reviewed implementation/cleanup commits to `origin/main`; record exact commands, reports and remaining gates in Bead comment 454. Keep `jcpe-0b8b` in progress; no deployment or full acceptance claimed.
- [x] Run both predeploy gates against committed bytes through RCH: model acceptance 11/11, instrument quality 9/9 (zero fail findings; warnings retained) and native Chromium playback 15/15 pass. Recovery is enforced but vacuous because no refusal is reproducible; no exercised-refusal or human listening claim. Evidence: `test-results/branding-ready-committed-20260915/`.
- [x] Deploy the exact committed artifact and social image: Cloudflare `741483fa`, Vercel `dpl_CyQvV2KgCAz4yVc6GnEwxtn2smJu` READY. At 2026-09-15 03:58:49 UTC both hosts return unmodified HTML SHA256 `350487f6b881cf7c65824334ee1e3023a2acf49017317ca5b3d702eaba4daece` and image SHA256 `b70c9e3cd1f0d56579f7ec1f913a5c239c6b27fa6b67c5202091eb5ecf56af12`. Preserve the first Cloudflare image cache mismatch and successful next poll.
- [x] Finish real desktop/phone boot, naming, metadata, Play/Pause/Stop, unchanged revision and error/request checks on both live hosts; inspect screenshots before closing. The initial Cloudflare matrix passed 5/6; desktop WebKit hit the existing 10-second load timeout. Its trace received HTTP 200 in 424 ms and the failure screenshot shows the app before starter seeding; no application error was recorded. Preserve `test-results/branding-live-20260914/host-0.json`. Repeat the complete host matrices using the native cache from the passing focused suite, with fresh profiles and unchanged assertions/deadlines. Cloudflare then passes 6/6 (43.301s). Vercel passes 5/6; desktop Firefox times out during Play click although the error snapshot is already Playing, with no application errors. Preserve `test-results/branding-live-warm-20260915/host-{0,1}.json`. Move generated outputs and native caches into per-run RAM, archive all artifacts, and repeat the complete Vercel matrix without changing tests/deadlines. Final Vercel matrix passes 6/6 (41.543s) in `test-results/branding-live-ram-20260915/host-1.json`. Together with Cloudflare, all 12 cases have one passed attempt, zero unexpected/skipped/flaky, zero console/page errors, zero beacon exceptions and exactly one host request. Visually inspected all four final desktop/phone Chromium screenshots; canonical name and layout are correct.
- [x] Final guarded RCH reproduction matches the same deployed HTML; all 1,647 source hashes/mtimes remain unchanged. Update README's stale deployment/size claims. Close only `jcpe-0b8b` with exact commands/results in Bead comment 456 after fresh solo verification; keep unrelated release, physical-device and human acceptance obligations open.

## Exact keyboard seeking — jcpe-exact-keyboard-seek-8vjx (CyanCove)

- [x] Trace U4 §5.2: UI half-beat rounding changes fractional seek targets and spoken slider values.
- [x] Capture a failing native regression against committed production bytes: Chromium expected 353/80, observed 9/2.
- [x] Dispatch keyboard intents to application-owned exact rational arithmetic and retain the accepted playhead fraction.
- [x] Prove fractional Arrow/bar/Page steps, exact Home/End and clamps, ready pending starts, refusal and document/history preservation: 4 tests, 123 assertions.
- [x] Run RCH focused tests, typecheck/lint, native regression (3 browsers), existing U4 evidence (27 tests, 753 assertions and 3 browsers), and guarded build. Retain earlier disk-stalled browser attempts and original U0 token failures.
- [x] Fresh-review and commit explicit owned paths as `a41a7e1`; preserve incoming iOS changes in merge `863df84`, reconcile stale web changes, reproduce the same artifact and push.
- [x] Run both predeploy gates against committed bytes: model 11/11, instrument quality 9/9 and real playback 15/15 pass. Quality warnings remain observations; human listening is not claimed.
- [x] Deploy committed 8,632,936-byte artifact to Cloudflare Pages and Vercel; both public hosts repeatedly match SHA256 `2e9c10e4cfb6f3df6a8a5059450578dd15b566cbeac3e45d68f45afeefadd84f`. Cloudflare deployment `4b5f0b70`; Vercel `dpl_4mhtfjGvGDvyAyjc8EnkBaTJQ496` is READY.
- [x] Inspect initial live desktop/phone diagnostics on both hosts: Vercel 6/6 pass; Cloudflare 5/6 pass, with WebKit 320px timing out at initial Play actionability before seek. Preserve `test-results/keyboard-live-ram/host-{0,1}.json` and every earlier failure; that initial matrix was not green.
- [x] Investigate the remaining failure with a bounded passive frame/visibility diagnostic. Cloudflare WebKit 320px completes exact 27/8→35/8→27/8 seeking, Space containment and unchanged revision, with no runtime errors or extra requests. The page stays visible/focused and the button stationary, but frames stall for seconds; concurrent worker full I/O pressure reaches 85%. This supports a scheduling problem without proving its cause. Diagnostic 1/1 is not a replacement for the failed matrix.
- [x] Recover a usable RCH worker and native browser/audio prerequisites. Preserve topology, ZIP-symlink, slot-refusal and cold-cache failures; never bypass browser admission or change deadlines.
- [x] Fix the observed test setup race: wait for enabled Play before recording revision, so asynchronous starter seeding cannot turn the baseline from Revision 0 into Revision 3. Keep document-immutability and seek assertions intact. E2E TypeScript and scoped ESLint pass.
- [x] Preserve normal Mesa shader/GStreamer caches between scratch invocations while keeping browser profiles fresh. This is evidence with persistent native caches, not a cold-driver performance guarantee. The SVG-grain diagnostic did not isolate a cause; no CSS changed.
- [x] Complete the fresh native regression: 3/3 in 29.683s. Complete the original live matrix: Cloudflare 6/6 in 86.625s and Vercel 6/6 in 60.135s, Chromium/Firefox/WebKit at 320/1280px; no failed, skipped, flaky or retried cases. All 15 attachments confirm exact fractional seeking, decimal accessibility values, Space containment, unchanged revision and zero console/page errors. Evidence: `test-results/keyboard-live-persistent-cache-20260914/{native,host-0,host-1}.json`.
- [x] Recheck all 1,597 frozen input hashes/mtimes and both hosts against committed bytes; unchanged artifact SHA256 above. The test/docs-only continuation needs no redeployment.
- [x] Close only this defect with exact evidence in Bead comment 450, after fresh solo verification. Preserve U4 ownership, the original red U0 CSS-token gate and human acceptance obligations. Setup mistakes and failed runs remain recorded; no independent-person signoff claimed.

## M1 audition lifecycle and matched groove — jcpe-qyyn (CyanCove)

- [x] Trace current source-note timers, controller preparation generations and isolated X1 preview ownership.
- [x] Prove Stop retires sounding audition voices and cancels preparation in flight; recorded real adapter calls contain no cancelled audition start. A newer keyboard preview survives stale cancellation. Revision/ready transport remain unchanged; no playing-band acceptance claimed.
- [x] Wire cancellation into Stop, replacement, overrides, Add, Discard, normal completion and component cleanup using the existing exact-owner release path.
- [x] RCH hz2, Bun1.3.14/Node26:38 focused tests/621 assertions,73 M1 evidence tests, full typecheck/lint/guarded build pass. Native import suites:36 pass across Chromium/Firefox/WebKit,112389.469ms, each one attempt; zero skips/flakes/failures. Browser checks prove UI lifecycle, not acoustic output. Missing remote TypeScript cache and test closure-flag lint failures were fixed; original logs retained.
- [x] Extract at most4 bars using real T0 measure ranges, import them in a private composition, canonicalize ephemeral IDs through F2/F3 and compile the selected performance groove. Source meter/rounded import tempo survive; the destination never enters this path.
- [x] Schedule the immutable groove through the existing preview owner and audio-clock lookahead; preserve onset/gate/velocity and prepare matching physical gestures. Bound256 events/64 beats; refuse invalid sequences before replacement. UI reports preparation and refusal, and polls completion without scheduling notes.
- [x] Focused regression23/23: all12 roots, exact tick/velocity assertions, independent voiced fixture, malformed inputs, concurrent band preservation, natural end, interruption/disposal and stale replacement. Preserve the initial fixture/type/formatter failures under `.tmp/cyancove/groove-*`.
- [x] RCH hz2, Bun1.3.14/Node26:87 M1 tests/10 files/11 pins/6 goldens/0 findings;49 transport interruption/replacement/Stop/engine/late-attack tests with428 assertions. Full typecheck/lint and guarded build pass; final E2E tsc/lint and M1 contract pass. Missing public bounds exports and duplicate-import lint corrected without weakening tests.
- [x] Native Automatic+Advanced:39 passed in133945.02ms across Chromium/Firefox/WebKit, each one passed attempt, zero skipped/flaky/unexpected. Real Organ and Concert Grand sources start within the groove, reach zero sounding/future sources after Stop/Discard, and have no later Stop attacks; destination digest and tempo unchanged. Existing320px/axe/console/page gates pass.
- [x] Final guarded reproduction matches browser-tested artifact:8448887 bytes, SHA256 `f429c6da1371ede22015a8c95742f2795f96e30b7c79a2e6ebfee4e91b7ede66`; all320 source hashes/mtimes unchanged. Pre-feature3f3c118 plus independent test overlay fails on old fixed gate. Fresh solo acceptance review complete, not independent-person signoff; raw failures/results and bounded honesty review remain in ignored `.tmp/cyancove/groove-*`. Original implementation scope complete; human listening/release acceptance remain separate.

## M1 import placement — jcpe-qyyn continuation (CyanCove)

- [x] Confirm live ownership, read envelope/atomic insertion laws and reserve affected paths.
- [x] Specify stable before-section placement, unchanged default, stale-target refusal and import-local continuation of chunks.
- [x] Pre-feature f7b669a with only the three independently specified placement tests fails3/3 (wrong position, trailing-chunk destination, missing target wrongly committed) on RCH hz3; no production changes in that overlay.
- [x] Wire automatic and retained manual placement through existing atomic commands, import-local chunk continuation, and Advanced selection/reset/stale-target feedback without changing source music.
- [x] RCH Bun1.3.14/Node26:35 focused tests/596 assertions,70 M1 evidence tests,11 pins/6 goldens; full typecheck/lint/build pass. Placement covers manual and automatic paths, every boundary,640-bar chunks and refusal rollback.
- [x] Native Automatic+Advanced:36 passed in112178.65ms across Chromium/Firefox/WebKit; zero skips/flakes/retries. Covers stale target disappearance/Redo restoration and exact existing chord IDs. Browser-discovered lost section names fixed by committing on input; retained the original failing reports and strengthened the name assertion.
- [x] Prepare verified source/artifact for commit: final M1 evidence70/70, E2E type/lint, M1 contract10 families/0 findings and guarded rebuild pass. All319 source hashes/mtimes unchanged; root/dist identical at8441177 bytes, SHA256 `5932358f69d7d70de47a2be7fe51e82995af4ba331508b89feb1a42f16539e47`. Preserve concurrent spec/fixture commit135f99a.
- [x] Matched-groove audition completed with the proof recorded above. No deployment or human-listening acceptance claimed.

## M1 chord-change detail — jcpe-qyyn continuation (CyanCove)

- [x] Reconcile ownership and read M1 midpoint/mass laws; reserve only affected files.
- [x] Specify explicit depth caps0/1/2 with exact midpoint timing, unchanged Automatic, no forced splits, bounded work and refusal recovery.
- [x] Author independent four-triad SMF and pure span fixtures; reproduce ignored grid on pre-featurec5fe12f.
- [x] Wire retained-model grid planning/trace and Advanced control, preserving other overrides and allowing Automatic after a coarse refusal.
- [x] Pass RCH grid/override/audition/automatic regression (31 tests, 522 assertions), expanded M1 evidence gate (66 tests, 11 pins, 6 goldens), full type/lint and guarded build checks with Bun 1.3.14/Node 26 on hz2. Added previously omitted M1 suites to the evidence input closure; no gate was weakened.
- [x] Prove native grid/name/refusal/new-file/Add/Undo behavior and existing Automatic/Advanced desktop/320px/a11y cases: 33 passed across Chromium/Firefox/WebKit in 101353.897 ms; zero skips, retries, failures or flaky cases. Logs: `.tmp/cyancove/grid-browser*`.
- [x] Prepare verified source/artifact for commit: final RCH rebuild and M1 contract (10 families, zero findings) passed; all319 source hashes/mtimes unchanged, root/dist identical at8439813 bytes, SHA256 `ae777eb05de83a16f48c5e774d04c7bef5c475b7d939f96da31bd8b69205cf3b`. Raw failures and solo review retained under `.tmp/cyancove/grid-*`.
- [x] Matched-groove audition completed with the proof recorded above. No deployment or human-listening acceptance claimed.

## M1 section naming — jcpe-qyyn continuation (CyanCove)

- [x] Recover current ownership and reserve source/test paths; no peer Agent Mail registrations or messages.
- [x] Specify bounded per-section names keyed by start measure, exact preservation/escaping, invalid/stale/duplicate drops, and clear/reset behavior.
- [x] Wire retained-model planning, chunk headers, trace decisions and Advanced section-name fields; preserve names across other overrides and temporary all-excluded refusal.
- [x] Add actual-decoder transposition, invalid-name, clear/restore and two-marker boundary tests, plus native rename/clear/file-reset/Add/Undo proof.
- [x] Final RCH Bun1.3.14/Node26:27pass/0fail/383assertions, full typecheck/lint/build pass. Pre-feature8081514 plus new independent tests:3 expected behavioral failures (8 unrelated tests explicitly filtered). Bound64/256 tests and original overrides/audition/commit regressions pass.
- [x] Native Advanced/Automatic:30pass/0fail/0skip/0retry in94.699s across Chromium/Firefox/WebKit, including320px/no-overflow/unique-ID/axe and actual rename/clear/new-file/reset/exclusion/Add/Undo. Original27pass/3fail setup run retained: replacement file collapses Advanced; reopen it before typing. No product/assertion/timeout relaxation.
- [x] RCH final e2e TypeScript/ESLint check passes after setup correction; M1 contract10families/0findings and guarded rebuild pass. Artifact8438628bytes SHA256 `80da06bdf62bbe1ff314895080dcf12ce77bacacad713ab3d87fc7768fa99d29`, root/dist identical; build manifest source digest independently matches local source, and319 source hashes/mtimes remain unchanged.
- [x] Prepare verified source and generated artifact for commit; preserve initial748991c from the concurrent process. Grid/destination placement and matched-groove audition remain open on the original Bead. Fresh solo review; raw logs and all failures retained under ignored `.tmp/cyancove/sections-*`.

## M1 excluded-track audition repair — jcpe-qyyn (CyanCove, reopened)

- [x] Reconcile Agent Mail, current Beads database, recent commits and CASS availability before claiming. No other registered peers; CASS lexical fuel exhausted and no matching workspace in its local archive.
- [x] Reproduce excluded upper-track notes still sounding through the real decoder/service. Pinned RCH Bun1.3.14 reproduction fails with four unwanted upper notes.
- [x] Carry sanitized immutable exclusions in the automation plan and apply them to source-pitch audition without changing original classifications or decoded notes.
- [x] Prove lower/upper exclusion, duplicates and invalid indices, all-excluded refusal, re-inclusion and determinism across all12 transpositions: RCH `bun test tests/unit/m1-import-audition.test.ts`,6pass/0fail/142assertions.
- [x] RCH Bun1.3.14: audition/overrides/automatic integration23pass/0fail/278assertions; full `bun run typecheck`, `bun run lint`, `bun run build` exit0 on real Node26. Guarded artifact8436713bytes, SHA256 `2705cc512a1a7f4edf5853e70a2283b11d2376c88b79f494143b65757f375fa9`, root/dist identical;319 source hashes and mtimes unchanged.
- [x] Native `m1-midi-import-auto.spec.ts`:12pass/0fail/0skip/0retry across Chromium/Firefox/WebKit in42.1s, actual generated artifact; unchanged Add/Undo/settings/refusal/audition lifecycle assertions and clean page/console diagnostics. This suite proves UI lifecycle, not exact audible exclusion pitches; those have real-decoder/service proof above.
- [x] Matched-groove audition completed with the proof recorded above. No deployment or human-listening acceptance claimed.

Final pre-commit RCH contract validation:10 families/0findings; guarded rebuild reproduces the exact browser-tested artifact above. The first repeat build stopped on a missing worker dependency symlink; task-local setup restored it and the unchanged build passed. Fresh solo review; no independent-person signoff.

Raw reproduction, failed SSH attempt and pinned regression logs: ignored `.tmp/cyancove/`. No test assertions, timeouts, retries or release gates were relaxed. M1 build `jcpe-upbz` remains assigned to its existing owner pending clarification; no deployment or full acceptance is claimed.

## M1 tied-key semantics — jcpe-ionn (CodexRuntime, closed; implementation `97aed83`)

- [x] Recover live ownership and reproduce the uniform-mass counterexample through production inference; preserve zero-mass and unique-winner controls.
- [x] Specify full tied evidence, separate effective selection, explicit Key override, trace/card wording, occupied-chart preservation and bounded work before production changes.
- [x] Author additive independent symmetric, cross-mode, unique and one-unit near-tie fixtures; preserve every pre-existing family and trace-golden byte pin.
- [x] Validate every fixture through all12 rotations and inverses, with corrupted-evidence and selection mutation controls; run contract statics/type/lint.
- [x] Bind full evidence and effective key selection into actual M1 planning; preserve M0 fallback for unresolved ties.
- [x] Wire application transfer and UI Key choice/clear, honest ambiguity card and trace; preserve unrelated overrides and reset on file/batch changes.
- [x] Prove decoded-file pipeline, starter/occupied settings, clear/replan, Add/exact Undo and unchanged M0/M1 behavior.
- [x] Prove actual browser trace/card/override paths in three engines and desktop/phone layouts; inspect errors, requests and retries.
- [x] Run named complete gates, update actual runtime audit, review changes and close only the fulfilled phase; build/deploy remain separately gated.

Evidence (2026-09-09 UTC): specification `9ff4de5`, implementation `97aed83`. `bun scripts/validate-m1-contract.ts`:10 families,0 findings; all10 preceding fixture/trace pins unchanged. `bun scripts/verify-m1-evidence.ts`:35 tests,0 failures/skips,11 pins,6 deterministic decoded goldens and unchanged input closure. The expanded import regression command (M0/M1 unit, automatic integration, batch integration and new ambiguity contract/integration suites) passes184 tests with2382 assertions. New production proof includes all12 actual rotated SMFs, full tied evidence, withheld/explicit key transfer, invalid override drop, occupied-chart preservation and exact document Undo.

`bun scripts/run-playwright.ts test tests/e2e/m1-key-ambiguity.spec.ts tests/e2e/midi-import-batch.spec.ts tests/e2e/m0-midi-import.spec.ts tests/e2e/m1-midi-import-auto.spec.ts tests/e2e/m1-midi-import-advanced.spec.ts`:57/0 in170433.391ms, no skips/retries/flaky results; all57 result records and six new request/console/page-error attachments inspected. Initial new-six run failed only because its post-Add fixture did not reopen Advanced; those failed traces are retained and the reset assertion remains. Corrected new-six run6/0 in22906.622ms. Four isolated actual pipeline-source mutants (drop ties, persist first tie, ignore explicit key, admit pc12) fail the designated unchanged assertions; control2/0. Six independent fixture-corruption controls also reject lost/extra keys, mode, scores and ordering.

Full `bun run typecheck` and `bun run lint` pass. `bun run verify:standalone`:36/0 in48248.934ms, no skips/retries; all1712 frozen inputs remain unchanged throughout this final gate (digest `7b7226817288b3c2c19d7542277d734cbb384147f4196d8fd7453c0cce83d92e`). During the preceding57-browser run, only the M1 evidence runner's command-list cleanup changed; application, test and artifact bytes stayed fixed, and that named evidence gate was rerun. Final guarded rebuild/static check reproduces the tested8436607-byte artifact SHA256 `dc69260de2b5a2befa7a0c1b2ff37ab8adfe807c2c874dd8b24705667435b3c3`, byte-identical to dist. Raw ledgers, mutation copies, diagnostics and failures are retained in ignored `.tmp/m1-key-*` plus the ordinary named-gate output. Only the tied-key/specification task closes: parent M1 build/acceptance, source-note review and human listening remain open. No deployment or later aggregate-gate pass is claimed.

## M0 setup timeout — jcpe-1416 (CodexRuntime)

- [x] Inspect original saved native trace: three Undo stability waits consume532ms,4377ms,1650ms; teardown begins during setup before file selection. A quiet passing rerun is not the fix.
- [x] Claim the ready leaf; preserve original parent contract, authored MIDI bytes, starter witness, empty chart/history, import/refusal/recovery assertions and30s/5s deadlines.
- [x] Scope keyboard activation of the same real Undo button to M0 evidence; leave shared U1 mouse setup unchanged. Record boot/Undo phase timings, never use them as musical cutoffs.
- [x] Prove the mechanism in three native engines: continuously moving Undo rejects ordinary mouse actionability, while actual keyboard activation establishes the same empty prerequisites and malformed/valid-file recovery. A wrong-key mutation must fail setup.
- [x] Run the original complete M0 evidence gate (163 production tests,18 native cases) with input hashes and complete diagnostics.
- [x] Run relevant type/lint/static checks and the unchanged aggregate; report its next actual gate, including human requirements without waiver.
- [x] Freshly review gate diffs, record exact results, close only with original acceptance fulfilled, and commit/push explicit paths. This is verification setup repair, not a new musical feature or deployment acceptance.

Accepted at `a83ac7a` (2026-09-09 UTC), fresh solo verification: three real moving-button keyboard controls complete the original malformed-file refusal and good-file recovery; three mouse counterparts fail the stability check and three wrong-key counterparts fail the unchanged empty-chart prerequisite. All nine native executions and exact errors inspected; no retries/skips. The deliberate button animation reproduces the actionability mechanism, not arbitrary machine contention or all startup latency.

Original `bun scripts/verify-m0-evidence.ts`:163/0 production,18/0 native in38523.338ms,28 refusal codes,2304 mutations,581 truncations,1920 fuzz cases,24576 resolution comparisons and3 round-trip charts. All18 setup attachments prove the actual seeded studio was unwound by three Undo gestures to empty chart/history. Authored corpus remains SHA256 `55cd4644460f644181b3d2791d622e99997fe85634048ee2e3af854f7ac168ea`; every original MIDI assertion, timeout and shared U1 mouse-helper byte is preserved. Scoped `tsc -p tsconfig.e2e.json --noEmit --pretty false` and typed ESLint on the changed spec exit0; guarded build and standalone static check pass with unchanged8434061-byte artifact SHA256 `d5a109ace6c77cd84f6a17ec86baf8d4bec5528e837367384c383fa590ae23f5`.

Unchanged `bun run verify` independently repeats M0:163/0 and18/0 native in38457.476ms. It completes46 gates, including model acceptance and instrument quality, then exits1 at `x1-evidence` solely for `X1_EVIDENCE_LISTENING_MISSING` (`release-evidence/audio/listening/x0-listening-v1.json`); native X1 cases3/0. Later aggregate gates remain unverified. All1706 inputs unchanged through this run, digest `9090d229fb10b3902aacaf3c280b8105c360047305a1997ff528740cd108c353`. Raw positive/negative traces, original and aggregate M0 ledgers, timings, audit and output are retained under ignored `.tmp/jcpe-m0-keyboard-mechanism/`. CASS older-history query again failed its lexical fuel limit; no older-history audit or independent-person signoff claimed. This closes the evidenced setup mechanism, not a universal30s guarantee under arbitrary host overload, a new musical feature, or deployment acceptance.

## Batch local MIDI comparison — jcpe-zw47 (CodexRuntime)

- [x] Claim the independent ready leaf and read existing M0/M1 contracts and composition.
- [x] Implement application-owned local reads: one to five files, 4 MiB per file, 8 MiB total, serial decode, at most five bounded M0 previews retained; no auto-commit.
- [x] Deterministic score: baseline30; tempo40–320 +15/otherwise−25; sounding duration≥150s +15/≥60s +10/shorter−15; ≥5 sounding tracks +10; ≥3 channels +5; percussion +8; ≥20 low notes +10; ≥half attack groups with three pitch classes +7. Exact duration tiers, first-to-last-note span, stable picker-order ties. These are arrangement heuristics, not musical quality or proof of completeness.
- [x] Wire multi-file picker, recommendation, selectable alternatives, per-factor explanations, cancellation, and stale-result suppression into the existing preview/Add workflow.
- [x] Author and run independent byte fixtures: score thresholds, tempo changes, leading silence, transposition, invalid candidates, duplicate names, stable ties, all-invalid, sequential work and byte/count bounds.
- [x] Prove cancellation before read, during read and during decode; late results never publish or launch the next file.
- [x] Run unchanged M0/M1 regressions, complete type/lint/build/static checks, and actual source mutation checks.
- [x] Exercise generated artifact in Chromium/Firefox/WebKit at desktop and phone widths: multi-file recommendation, alternate choice, Add/Undo, limits, and clean diagnostics with network blocked.
- [x] Record exact results in Beads, close only on passing feature gates, rebuild/recheck source snapshot, and commit explicit paths. Deployment remains subject to the separate human listening gate.

Evidence (2026-09-09 UTC): implementation `630ae06`/`b82bc8e`, final cancellation/UI proof `1bd322d`, stale audition witnesses corrected in `23dd4ad` against X1 §8 (16 pitches and first-use initialization); no production audio change. Final `bun test tests/unit/midi-import-*.test.ts tests/unit/studio-midi-import-automation.test.ts tests/unit/m1-import-*.test.ts tests/integration/m1-automatic-import.test.ts tests/integration/midi-import-batch.test.ts`:172pass/0fail/1791assertions, including nine batch cases. The first broader run was169/2; its failures were retained, not hidden. The independent multitrack fixture initially repeated tempo metadata across tracks and refused; corrected to one tempo track before proof.

`bun run typecheck`, `bun run lint`, guarded `bun run build`, and `bun run verify:standalone` pass. Four actual source mutants (tie ordering, sixth-file admission, exact60s boundary, cancellation removal) each fail their designated original assertions; control9/0. `bun scripts/run-playwright.ts test tests/e2e/midi-import-batch.spec.ts tests/e2e/m0-midi-import.spec.ts tests/e2e/m1-midi-import-auto.spec.ts tests/e2e/m1-midi-import-advanced.spec.ts`:51/0 in144898.204ms. Final standalone36/0 in49685.454ms. Final batch-only rerun after lint-correcting the delayed-read fixture:6/0 in24643.585ms; no retries/skips/flaky cases, all six explicit diagnostics/request records clean. Full final typed lint passes; scoped final e2e typechecking passes. All1706 frozen inputs remained unchanged through final36+6 proof, digest `b22329534455bb4b48e4ccd45d50568e3d1340aff24e81b69ed278c9eff03f75`.

Root/dist/immediate-precommit rebuild:8434061bytes, SHA256 `d5a109ace6c77cd84f6a17ec86baf8d4bec5528e837367384c383fa590ae23f5`. Raw reports/logs and consolidated diagnostics remain under ignored `.tmp/jcpe-batch-*`; mutation copies never edited production source. This is solo verification, not an independent-person signoff. No deployment, full-project acceptance, M1 owner-listening, or completion of the broader15-idea campaign is claimed.

## Admission and discovery polling repaired; DSP source drift next

- [x] `jcpe-browser-suite-admission-0nyi`: atomic admission and the follow-up Playwright1.61 worker classification are proved. Aggregate release acceptance remains separate and red.
- [x] Implement Node-owned atomic admission, real-runner classification and orphan detection.
- [x] Prove20 focused tests, including six native process/browser cases and two actual source mutants before adding the direct CLI integration case.
- [x] Reproduce/fix pending-bootstrap misclassification; waiting wrappers must not refuse each other.
- [x] Correct an additional CLI test fixture that omitted `outputDir`. Its actual Playwright run cleaned the default `test-results/` directory, removing ignored audit/loop/admission logs and the locally pinned toolchain. This was this implementation session's harness error, not an external cleanup; repository source and tracked evidence were untouched.
- [x] Isolate that fixture's output, move admission evidence and executable bootstraps under ignored `.tmp/`, outside default Playwright cleanup.
- [x] Recover the exact Bun1.3.14 executable from the still-running owned process.
- [x] Restore pinned Chromium149.0.7827.55/Firefox151/WebKit26.5 outside Playwright output; retain archive URLs, sizes and hashes.
- [x] Full seven-scenario native proof passes:20 Bun tests/0fail,32 assertions, including two actual production CLI bootstraps admitting exactly one real browser suite. Actual no-lock and cache-path source mutants fail the independent oracles.
- [x] Final focused21/0/32 Bun assertions plus all seven native assertion scenarios pass; full `bun run typecheck` and canonical `bun run lint` exit0. Preserve `.tmp/jcpe-admission-recovery/evidence/{focused-final,typecheck-final,lint-final}.log` and `process-run-MnTJuI/`. No lint rule or tested timeout was relaxed.
- [x] Recheck all1701 frozen non-Beads inputs unchanged: `b91be4d83e60db9ffcec8c51e105d88a18dc54d3778905a0f5dadf57a5108842`. This checklist closeout follows the gates; executable and fixture inputs remain identical.
- [x] Fresh unchanged aggregate passes admission and stops at discovery production:121/1/1427 assertions; original maximum-node witness11695.54ms versus5000ms. Run `40f26f14-87bc-4b0d-9a78-36f8e279c643`,1565 stable inputs. The30-case browser matrix was not reached. Retain `verify-final.log` and safe `final-aggregate-discovery/`; this is the exact next failure permitted by the admission leaf's original step4, not an aggregate PASS.
- [x] Restore all six pinned UIowa recordings with exact sizes/SHA256 to the required loader path and a second copy outside Playwright output. Retain `reference-corpus-restored.json`.
- [x] `jcpe-discovery-capture-overhead-luv3`: discovery polling overhead repaired; passive-data production source and all its laws remain unchanged.
  - [x] Preserve the original failing node/depth/text witnesses and profile actual capture. Baseline ten-run mean7.338s, median7.233s, range5.197–8.846s; canonical SHA256 `6c4a9981b89abdfca94f3fdfc55a14db5f18a8c5978ad4a823892c0f1316ff9d`, graph2097176 bytes, reservation6292660 bytes, zero retained after release. This is the finite maximum-node workload, not a general latency percentile claim.
  - [x] Compare native array operations before editing production. Immutable-property construction is faster in Bun but materially slower in Chromium/Firefox; do not adopt a browser regression merely to satisfy Bun's timer. Preserve `array-*-mechanics.jsonl`, `array-engines.jsonl` and baseline profile/benchmark files under the recovery evidence directory.
  - [x] Reassess timing after heavy static jobs finish: the unchanged original boundary test passes in2147.55ms (1pass/0fail,25 filtered diagnostic cases,9 assertions). No production edit, assertion change or timeout increase caused this result. Execution conditions confound the earlier timing; the individual filtered diagnostic is not release proof.
  - [x] Complete isolated aggregate trial `c2171a2f-b9b6-4f02-8490-42f6a2d9ee05`: original122/0/1427 production passes in8.96s; browser29pass/1fail. Preserve the entire failed run under safe `isolated-aggregate-discovery/`. Execution load confounded the earlier production timeout; no claim that a green isolated result erases those failures.
  - [x] Reject the speculative production optimization after all three browser comparisons. Preserve `discovery-data.ts`, every original data fixture, canonical byte/graph/reservation golden and all original hostile-shape/accounting/mutation assertions. Additional decoder cases/decoder mutations are unnecessary because no decoder path changed.
  - [x] Diagnose the actual remaining Firefox observer defect from the trace: calls111/113 already hold `ok=true`, `stale`, `finished`, but transfer2837222/2837220 bytes in6915.684/4436.230ms, exhausting the unchanged10s predicate deadline. The retained final journal has8106 entries; exact edit revision6/history3 and zero errors are present.
  - [x] Move scalar predicates into the browser and evaluate the compound predicate in one snapshot. Keep every full named observation/journal, all original comparison values,30 native cases, timeouts and zero-retry policy. No runtime source or music behavior changes.
  - [x] Real Node `tsc -p tsconfig.e2e.json --noEmit --pretty false` and typed ESLint for the changed spec exit0. Full typecheck/lint previously passed on all other unchanged paths. Original12 actual source-mutation pairs pass in the final gate; no production fault controls were weakened.
  - [x] Final discovery run `9fe88a2e-bea3-4810-abd4-34ddddc0f57d`:122/0/1427 assertions;30 browser passes/0unexpected/0skip/0flaky/0retry in96870.021ms. All30 complete resource/error records independently inspected; full verifier PASS,1565 stable inputs, digest `a8a89a176c823efba79ff6bd9e0e7fe73ab9cbf1bdcf688d57928bfc69f8fed5`. Safe raw copy `observer-final-discovery/`.
  - [x] Guarded build and standalone static pass with unchanged8,431,411-byte root/dist/HEAD artifact SHA256 `c20c0b1f029e3abd3d6eb03f4e7c374a607fb4a7b00fdbd29bd369eb94f316b3`. All1701 frozen inputs unchanged through aggregate, digest `00d9532aed13d3ebe8adab08e51f7c5171425e5b3c8793f4c5313e3a072d871a`; this checklist closeout follows that proof.
  - [x] Record next unchanged aggregate failure: M0 evidence, after27 successful gates. M0 production163/0; browser17pass/1fail, WebKit B06 truncated-header refusal not visible before30s test deadline. Preserve `verify-observer-final.log` and `m0-next-blocker/`. No whole-project, musical-consumer, human or deployment PASS claimed.
- [x] Admission0nyi follow-up proved after the completed discovery leaf: live M0 worker PID3146839 executes `playwright/lib/worker/workerProcessEntry.js`; the original orphan fixture covered only historical `common/process.js`.
  - [x] Add the exact live modern-worker command and a path-as-data negative before repair:21pass/1fail, reproducing the missing classification.
  - [x] Recognize modern and historical worker paths in the shared predicate; retain idle-browser negatives and atomic ownership. Focused/native proof passes with23tests/0fail and both source faults detected; exact logs `modern-worker-{before,focused}.log`.
  - [x] Final23/0/34 assertions plus7native scenarios and2actual source faults; full typecheck/lint exit0. Fresh unchanged aggregate passes43 gates then refuses predeploy model source drift. Discovery122/0 +30native; M0 163/0 +18native; M1 and A0/E0 bridge pass. Safe `verify-modern-worker-final.log`, `modern-worker-final-{discovery,m0,m1}/`, `process-run-y1cgf1/`. Admission source SHA256 `ebe7c8df71241d299d5829c1d10786a23342564829d1096d4f5918adc38b4521`;1701 frozen inputs unchanged, digest `05e31970cf295a559aa841454f5345f992d3aa7760860a5b80e1bf494c0e85a0`.
- [x] `jcpe-w7jf`: independent four-voice render-ahead witness restored and verified.
  - [x] Reproduce unchanged six-case suite: five pass, one fails because the starter performance emits triads rather than the required four-note boundary.
  - [x] Replace only the boundary case's incidental starter input with a hand-authored literal six-event Manual score, validated through F2/F3 and the real controller. Preserve `[1,4,1,4]`, ten leading notes, complete chronological/deferred membership and readiness assertions; add exact full-plan pitch expectations.
  - [x] Correct the initial authored custom chord declaration after semantic validation refused its incomplete pitch-name set. Preserve that failed diagnostic separately. All original six cases now pass with127 assertions.
  - [x] On isolated55863b5, original suite reproduces5/1; with only authored witness,6/0. Actual production truncation mutant fails5/1 at original complete-event count4vs3; source/spec hashes and logs in `render-ahead-proof.json`.
  - [x] Final real Node test-project typecheck and typed lint pass; final integration/preparation suite passes. Initial branded-number expectation compile diagnostic retained; compare numeric pitch values without casting or changing expected pitches.
  - [x] Final focused9/0/138assertions, types/lint/build/static PASS. Unchanged aggregate passes discovery122/30,12source faults, MIDI and bridge gates,11models/9instrument-quality cases; X1 package82/0/773 and3native engines PASS. Aggregate remains incomplete solely at its first outstanding gate, TR-X1-LISTENING (human task `jcpe-60xy`); later aggregate gates are not inferred green. Safe `verify-render-ahead-final.log`, `render-ahead-final-{discovery,x1}/`.1702 inputs unchanged through gates,6c5899a3a4f94ac32a84b4290d3979c5f5de4889f552ca0c50cf0939369fce92. Root/dist remain4ce4ad5b; no deployment.
- [x] `jcpe-1416`: intermittent M0 WebKit startup budget remains open. Original trace reaches30s during starter Undo, before file selection; subsequent fresh original gate passes163/0 +18native, including actual B06 refusal/recovery in4538ms. Preserve both packets and every original deadline/assertion; a quiet pass is not an implemented timing fix. New chart preserves history and is not a valid empty-history substitute.
- [x] `jcpe-playback-gate-ship-failures-u90y`: source/payload drift and deterministic clock-origin playback refusal repaired and proved. Aggregate/deploy acceptance remains separate.
  - [x] Claim the existing P0 leaf atomically; preserve committed native cancellation and pinned nightly source changes. Verify Binaryen131 archive against the official release SHA256.
  - [x] Reproduce generator metadata defect: cargo uses crate pin908501772 (2026-08-30), while the root rustc query reports cea272fa3 (2026-09-07).
  - [x] Query compiler metadata in the same crate directory; pinned nightly/Binaryen131 build and `--check` reproduce268222 WASM bytes SHA256 `f505035be85ea98cc6c6c71374a84448c65851aa7a4ca31e5720e71f336a6ca9`; `--sources` matches current closure399235e4. Preserve candidate/baseline payloads and toolchain observation in `dsp-repin/`.
  - [x] Replay exact candidate: flute8admitted PASS/4declared reference-unavailable, clarinet12/12, plucked12/12 +6/6 pairwise. All32 available PCM digests match prior reviewed evidence. Regenerate all3 reports from their original semantic drivers; model ledger/routing unchanged. `predeploy:check` passes11shippingmodels and9instrument quality rows/0fail findings.
  - [x] Migrate the explicitly stale pre-ledger assertion after reproducing3pass/1fail: preserve a separate authored ledger-absent negative and require exact live source closure for the embedded module. Related focused run41pass/1fail/429assertions; the unrelated starter voice-count witness remains red in `jcpe-w7jf`, unchanged. Full typecheck and changed-path typed lint pass. Guarded root/dist8427927bytes SHA256 `7e863b4654af0d1f2e8d132343420c93c5d2e5f49ff72802af9388ab899990d3`; static standalone PASS.
  - [x] Original native gate over7e863b46:14/15; blues-guitar reaches an engine refusal after audible/pitched buffers, recovery passes. Preserve `playback.{log,json}`; do not promote offline PASS to working playback.
  - [x] Diagnose with isolated build exposing only audio inspection/cache misses: ordinary and exactly tapped full-chart controls pass; full instrumented15/15 passes. This does not prove the original transient's cause or certify shipping bytes. Scratch diagnostics never enter production.
  - [x] Independently reproduce clock-origin cache defect with real WASM: unchanged0.4s gate is prepared, but0.7/1.01s clock origins yield a floating-point overshoot into the next duration bucket and refuse. Keep original0/2 proof and scratch-clock controls; initial diagnostic invalid-future-time/fixture disposal errors are retained separately.
  - [x] Fix only bounded attack cache lookup: adjacent bucket is eligible only for the identical rounded PCM frame count at the cached buffer's actual sample rate. Preserve all other key fields, exact attack/release timestamps, cache-only attacks and genuine longer-window refusals.
  - [x] Final65/0/1640 assertions across6related files, including24real-WASM cases for one/four voices, all12transpositions,7valid clock origins and longer-window refusal/recovery. Actual no-fallback and over-permissive source mutations each fail24/24; raw `clock-*.log` and exact mutation source files retained.
  - [x] Full typecheck/typed lint/build/static PASS; root/dist8428308bytes SHA256 `4ce4ad5b513eb2adae9b1f8a294f8174b657db70b53dc74ea439478ca9e63d60`. Final model/quality PASS and actual unchanged native15/15 with0console/pageerrors, enforced recovery vacuous. Frozen1702inputs unchanged through gates, digest `612657e0ad919723c78004a2bed10be392c7c3048dee3320b3422a0fa35a7554`.
  - [x] Fresh aggregate records the next separate failure: discovery122productionPASS/29nativePASS/1fail, Chromium390 edit-during-search sees running before first expansion. Retain original>0assertion; safe run16ae9748 in `dsp-repin/final-aggregate-discovery/`. No aggregate or deployment PASS.
- [x] Reopen and claim `jcpe-discovery-capture-overhead-luv3` for the observation race after u90y closure.
  - [x] Add a scalar positive-expansion barrier after running and before typing; preserve all existing input/responsiveness/resource/cancel/Undo assertions and original deadlines.
  - [x] Final discovery run5eab9d33-a8f7-4abe-8dd9-f881ab7119fb passes122production/1427assertions,12source-mutation controls and30native/0skip/0retry/0flaky in64939.115ms;1566 stable inputs24ca354a4680069edca677762a65e9cd52ac63422915b6864fdc4889f093b672. Typed checks, lint, guarded build and standalone static pass; root/dist remain4ce4ad5b. Safe raw `discovery-startup-final/`.
  - [x] Unchanged aggregate reaches X1:82owner tests/773assertions and3native engines PASS, but required human TR-X1-LISTENING evidence is incomplete. Preserve `verify-discovery-startup-final.log` and `discovery-startup-final-x1/`; no human or aggregate PASS.1702 frozen inputs unchanged through gates,04028fb0e64090ef3c09254732be82c4623b4cb0355888a683ba7482f4e59207.

- [x] Ordinary-loop acceptance completed (`jcpe-rehearsal-session-6y2b.5`, fresh solo verification of committed runtime `a50e471`).
  - [x] Recover the committed implementation and retained original failure descriptions; preserve the evidence-loss disclosure above.
  - [x] Re-execute the five files containing reported timeout cases on a quiet supported toolchain:64/0/2750 assertions in33.47s. No runtime edit, timeout change, retry or fixture reduction; this is fresh verification, not an optimization claim.
  - [x] Broader unchanged regression run286/0/13800 assertions across28files,66.00s. V1 oracle/laws plus ten real source faults29/0/831 across6files,20.27s; seven22/0 and three5/0 baselines all pass and every mutant fails its designated assertion. Exact commands/logs/bundles are under ignored `.tmp/jcpe-loop-recovery/`.
  - [x] Full typecheck/lint, guarded build/static PASS. Original native12/0/0skip/0flaky/0retry in106529.954ms, Chromium149.0.7827.55/Firefox151.0/WebKit26.5 viafile/HTTP at1280/390. Harness8278199bytes SHAfea2881c276bb2ea309b1117ccf2f7e235f66b40fa7d7b9b1d36cddf1ae66dad.1702inputs unchanged during native proof,6308d6da9d0e59993b531590d6619aa845e96083d308e9e26519f6cef8e15fa0.
  - [x] Inspect all12 full reports: each15-event arrangement has at least3actual passes,8stages/live changes, strict Stop receipt and0post-Stop starts; one closed context,0retained voices/references/edges/listeners,0console/pageerrors/forbidden requests. Raw `native.json`, `native-stages.json`, `native-inspection.json` and logs remain under `.tmp/jcpe-loop-recovery/`.
  - [x] Additional P0 sweep catches the missed projector source inventory:291/1/3718, failing before ownership checks. Add the reviewed module to the exact inventory, preserve all guards, and add3source-injection negative controls for runtime import/private type import/generation call. Final295/0/3729 across17files,24.10s; changed test-project types/lint pass. This is the only test/code change; production source and native harness remain byte-identical to the proved runtime.
  - [x] Fresh solo review and honesty inventory record no new runtime or performance claim, no assertion/deadline relaxation, and no independent-person certification. Historical timing failures and missing raw logs remain disclosed. Human listening, full rehearsal/UI and deployment remain separate; known X1 human gate is not bypassed or needlessly rerun.

Earlier `test-results/reality-check-2026-09-08/` and `test-results/loop-final-evidence-2026-09-08/` raw paths below now refer to historical observations whose files were removed by that fixture. The checked-in reports/Bead comments and this conversation retain their summarized outcomes. Do not call those raw packets currently retained, invent replacement logs, or count the interrupted/lost runs as fresh proof. New evidence belongs in `.tmp/jcpe-admission-recovery/evidence/` until a durable closeout records its exact commands/results.

## Fresh reality check — 2026-09-08

The current assessment is [REALITY_CHECK.md](REALITY_CHECK.md), based on
`9964f76`. This section supersedes stale status summaries below without deleting
historical evidence. The audit changes no production code and closes no runtime
or human-acceptance task.

- [x] Read complete AGENTS/README and principal vision plans; map24 vision goals,
  all15 discovery systems and all15 September workflow ideas to current code and Beads.
- [x] Execute the29-case independent semantic probe:10pass/19fail. Credit the
  literal-facts and web-containment repairs; preserve every remaining counterexample.
- [x] Execute the focused H0/cast/memo packet:84pass/1fail,4,497 assertions;
  the failure is the same29 publication-policy findings, not a timeout.
- [x] Execute aggregate once under supported tools:2 gates pass, discovery browser
  admission refuses idle foreign browser cache paths. No later-gate pass inferred.
- [x] Execute mandatory predeploy check: source/WASM drift in two committed DSP
  files blocks release; the chained instrument-quality command never runs.
- [x] Inspect8 declared local/host desktop/phone workflow cases:4 local pass,
  4 host new-feature journeys fail (one includes an explicit harness timing limitation).
  Separately inspect4 hosted boot/native Play/Stop cases:4pass; do not erase original failures.
- [x] Compare both public payloads to Git: both match11ef504, not9964f76.
  Preserve the expected Cloudflare blocked-analytics console error.
- [x] Reuse existing coverage, add only `jcpe-browser-suite-admission-0nyi`,
  and reopen the false-closed U4 parent while preserving its children's owners.
- [x] Apply three ambition rounds and six refinement passes; correct README's
  U2/U5/F0-only claims,71-gate count and8,431,411-byte artifact size.
- [ ] Reconcile intentional DSP changes and embedded payload under u90y.
  - [ ] Review the pinned Rust toolchain and plucked_v2 cancellation changes with the owner.
  - [ ] Build the correct WASM and refresh every affected exact-input acceptance replay.
  - [ ] Pass unchanged model, instrument-quality and real playback/recovery gates.
- [ ] Repair actual-suite admission (`jcpe-browser-suite-admission-0nyi`, claimed CodexRuntime).
  - [x] Write independent idle-cache-path/active-suite refusal twins first (15 process vectors, ancestor and orphan cases).
  - [ ] Implement native Node-owned atomic admission shared by supported launchers; remove substring-only checks.
  - [ ] Prove two simultaneous real-process entrants, wrapper reaping, normal/error release and planted negatives.
  - [ ] Pass targeted tests, full typecheck/lint and one honest aggregate invocation.
  - [ ] Preserve foreign processes and prove atomic admission, child lifetime and cleanup.
  - [ ] Run one honest aggregate and retain its actual next failure or complete result.
- [ ] Repair29 publication findings through np17's existing owned leaf and named gates.
- [ ] Finish remaining U4 acceptance. The ordinary-loop13+1 timing cases pass in the fresh unchanged regression run above; no limits were widened.
- [ ] Finish U2 independent proof and H0 contextual/scale runtime after its actual dependencies.
- [ ] Repair H1 and complete explicit-scope audible transposition and U6 comparison.
- [ ] Adopt completed gdcw in real G2/U8 and native/web semantic consumers.
  - [ ] Produce only valid distinct options; retain More and complete constraints.
  - [ ] Prove exact Preview/Apply/Undo/recovery/JSON/MIDI with real adapters and stale refusal.
- [ ] Repair G1 hashing/rights/query; complete reviewed D0 corpus within its reservation.
- [ ] Complete every G3–G9/U9–U11/D1 and remaining September workflow obligation.
- [ ] Complete unchanged Q0/R0, actual Apple/device, human listening/accessibility and
  exact committed-byte deployment/browser gates. Current deployment remains blocked.

## Current order

- [x] Ordinary loop arrangement (`jcpe-rehearsal-session-6y2b.5`, completed by the fresh proof above; historical implementation notes follow): perform the full chart before projecting its range; preserve the selected bass/comp groove through Play, live loop changes, groove replacement and instrument preparation. Full rehearsal build retains U4 verification and additionally depends on this bug.
  - [ ] Finish the final packet in ignored `test-results/loop-final-evidence-2026-09-08/`. An external cleanup removed the earlier `/tmp/jcpe-loop-*` files, pinned Bun directory and browser cache while work continued. Earlier `/tmp` links below now identify historical observations whose raw files are unavailable; the original `/data/tmp/jcpe-loop-arrangement-faults-vx6moY` bundles remain. Do not fabricate the missing failures or call those raw reports retained. Restore Bun1.3.14 from the official archive with SHA256951ee2aee855f08595aeec6225226a298d3fea83a3dcd6465c09cbccdf7e848f, use its absolute path, and restore the exact browser revisions. Recent fallback Bun1.4.2 runs are diagnostic only; pinned reruns below supersede them.
  - [x] Reproduce the pad fallback through the actual controller: pinned Bun1.3.14 returns ten literal pads and fails the new positive band assertion. Preserve the earlier Bun1.4.2 harness timeout separately. Use the existing independent rehearsal interval fixtures, seven style relations, pickup/rest section bounds and twelve spelling/16-note duplicate-preservation cases.
  - [x] Implement bounded immutable full-performance projection and wire ordinary Play, live whole/section loop, groove replacement and instrument preparation. Preserve absolute ticks, source offsets/identity, ordered pitches and 24-tick gate law; include explicit empty bars in section bounds. Existing low-level clipped-plan refusal stays intact. Guarded artifact8430922bytes SHA256006bddb72c68b2619bdf283470e067aa91e30420c4a210ee2a125ce0b9be6085.
  - [x] Initial focused18/0,161 assertions cover all seven styles, whole/section/sub-event ranges, pickup/rest phase and live routing/state. Correct the first pickup fixture missing required expectedDuration/reason through F2, preserving the timing expectation. Full typecheck also catches an invalid `.midi` access in a new fixture: use the total pitch projection result and assert all16 MIDI values are integers in the independent register. That earlier identity-only green is not valid numeric pitch evidence.
  - [x] Kill seven actual source faults with passing baselines: literal fallback, rebased time, lost offset, missing silent-bar phase, stale live groove, recipe-colliding voice identity and unavailable-section expansion. Final7/0/84assertions with22/0 baselines and the designated failure for every mutant; inspect current production-source and retained bundle hashes in `/tmp/jcpe-loop-final-fault-inspection.json`, raw bundles/logs `/data/tmp/jcpe-loop-arrangement-faults-vx6moY`. The initial six-fault proof and later6/1 failure remain retained.
  - [x] Native Chromium desktop and phone-width development checks pass with actual StudioRoot/controller/X0/X1, native starts/frequencies, three repeated bass/comp passes, live section/groove/instrument changes, strict Stop receipt and disposal. Preserve the first invalid Playwright screenshot option and initial exact wall-clock-spacing failure. The latter contradicts X1's already-accepted native late-start compensation; retain raw timing deviations as performance evidence and assert exact plan duration, note occurrences and native frequencies instead. No uniform native-cadence or human-listening claim.
  - [ ] Run unchanged relevant P0/performance/transport gates, full typecheck/lint, guarded artifact checks and one real native browser matrix with repeated bass/comp, live changes, Stop and cleanup.
    - [x] Retain the failed full native matrix:8pass/4fail, zero retries; Firefox and WebKit both expose `audio.voice_id_duplicate` after a live vibraphone swap. Decode actual trace snapshots: canceled bass MIDI33 still owns its ID when the existing vibraphone realization folds its replacement to57. Independent fake-clock real-X0/X1 reproduction1pass/1fail; recipe-specific bounded transport IDs repair it to2pass/0fail/23assertions. Source pitches, folding policy, exact onset and existing sounding voices stay intact. Add a sixth actual source mutant and final-state/instrument-outcome browser diagnostics; the full matrix still needs a clean run.
    - [x] Reproduce an additional ordinary-loop defect: merge the armed section into its neighbor and Play silently accepts a full-chart plan. The new regression fails its required refusal before repair. Refuse the unavailable section before audio initialization, preserve revision/history, and prove Undo restores the exact original passage. Final focused22/0/256assertions, `/tmp/jcpe-loop-focused-complete.log`.
    - [ ] Resolve unchanged regression timing gates: combined16-file suite172pass/34fail/6362assertions, every failure a five-second harness timeout. Isolated live-change file1pass/4fail; the same unchanged test against the prior45bc966 application/audio sources also1pass/4fail. Preserve `/tmp/jcpe-loop-{existing-regressions,isolated-live-ride,prior-live-ride}.log` and prior source hashes. This comparison identifies a pre-existing failure; it does not waive the named gate.
    - [x] Repair full lint discovery of the tracked U2 browser config lacking parser services: include its config/spec in the real Node browser TypeScript project and typed ESLint coverage. Move its spec out of the Bun type project only after adding Node coverage; retain every rule. Full typecheck also catches the new harness mount's closure losing a nonnull narrowing; make the checked mount an explicitly returned element, without a cast. Final gates remain pending.
    - [x] Remove unnecessary rational reconstruction for wholly contained performed events, preserving the same fields and gate formula. A65,536-event positive baseline exceeded its unchanged5s harness guard at6673ms; the production fast path brings the same case to1224ms, with8/0/123assertions. These are observed timings, not a musical cutoff or a statistical performance guarantee. Subsequent full seven-fault proof passes above.
    - [x] Full typecheck passes with pinned Bun1.3.14, real Node26 and12GiB Node heap (`/tmp/jcpe-loop-types-memory.log`). The restored-range test compares all four rational integer fields after TypeScript rejects an unbranded expected object; this is branded-number typing, not a switch to or from bigint. Standalone static verification passes on8431303bytes SHA2569f66b0b5933dfcd699d1f4cd7c34c033efbc9973bec0df91824625874e02582f, with HEAD/root/dist equality. Artifact policy/staleness36/0/100assertions passed; no release-facing aggregate claim.
    - [x] Finish the corrected native matrix:12pass/0fail/0skip/0flaky,424890.626ms in `/tmp/jcpe-loop-native-scalar.json`. Inspect all12 embedded reports: Chromium149.0.7827.55, Firefox151.0 and WebKit26.5, file/HTTP,1280/390 widths, retry0, zero console warnings/errors/page errors/blocked requests. Every case records eight stages, three arranged passes, live section/groove/instrument changes, Stop with no future starts, one closed context and zero retained voices/references/edges/listeners. Harness8277755bytes SHA256cf4a960fdafc449a488210104db1c263e76165e9bde74c6269f5c2a5c59e263f; all1463 input hashes/mtimes unchanged after the run, digest1a502dde7514f9135fb41363392b1c97a2c62b4a85eee54683a66d2613f5634b. This is native browser proof, not human listening or native Apple certification.
    - [x] Preserve `/tmp/jcpe-loop-native-complete.json`:6pass/6fail/0skip/0flaky,748259.745ms. Native traces show successful instrument receipts without the original voice collision; full-snapshot transfers take up to4s and exhaust the unchanged polling/test guards. Correct polling to transfer only the checked scalar, retain full named-stage diagnostics, and avoid a duplicate disposed snapshot. Every behavior/error assertion, timeout and retry count stays intact. Focused e2e project typing and spec lint pass after this observer repair.
    - [x] Finish whole-tree ESLint under real Node26: all files/rules/zero-warning criteria pass with four workers and12GiB heap. Initial default4.1GiB heap run stopped143; next full run found one unused U2 constant; later full run caught four redundant Number conversions in the new restored-range assertion. Remove the unused declaration and explicitly type the four observed integers as a numeric array. No U2 waits, owner, type rule or expected value changes. The last launcher fell back to Bun1.4.2 after cleanup, but the actual ESLint process was Node26; the pinned Bun source-policy component still needs its final replay.
    - [x] Profile and reduce synchronous voice-assignment allocation cost. The startup probe reports most time under realization/V1, not piano preparation; paired temporary builds observe Play returning at4837ms versus3978ms. Hoist the six fixed comparison axes and retain readonly private DP candidate/fact records without freezing each losing temporary; preserve detached frozen output, full-path tie order, costs, work counters and memory limits. This is an observed local speedup, not a release performance guarantee. The first profiling test invocation produced no profile; a standalone probe produced the profile but used a nonexistent controller disposal method, then a corrected probe used the actual transport disposal command. Those earlier `/tmp` files were subsequently removed externally.
    - [x] Run the pinned independent V1 oracle/transposition/immutability/accounting/conformance/mutation cases plus progression/comp continuity and all new loop tests:90pass/0fail/6618assertions across13files,20.60s in `test-results/loop-final-evidence-2026-09-08/focused-and-v1-pinned.log`. The fallback Bun1.4.2 run's71pass/2timeout failures is not pinned release proof. Rebuild with Bun1.3.14:8431213bytes SHA2564704d84eec81be397595b93250e0b616d5bfc6695a1f61b357470e2bda69f52f; native harness8277663bytes SHA2560c7c7eb927f75170ef5ee77c4d9a3c7c7e70a83072a899ed1c95f6d82a5dc509. Capture1480 current inputs, digest396afcd25edaa3bfe688f0c3c69c6413b4f0dd8317196b6c8969f96fe3ce03ce, before final native/mutation gates.
    - [x] Refresh the seven real source faults on pinned Bun1.3.14:7/0/84assertions,265.67s; all seven22/0 baselines and designated mutant failures inspected under `test-results/loop-final-evidence-2026-09-08/jcpe-loop-arrangement-faults-s94fnF/`. `mutation-inspection.json` binds current source and retained bundle hashes. No production file is rewritten by these controls.
    - [x] Finish full Node26 TypeScript/ESLint and the pinned source-policy component305files/0findings. Pinned static standalone inspection passes8431213bytes/4704d84eec81be397595b93250e0b616d5bfc6695a1f61b357470e2bda69f52f, source3ed952b42218f812bfc3a87cfa37fa00322e0bd4d4678388e42c004747a2e7ee. Artifact policy/staleness36/0/100assertions,57.72s. Logs and actual Node process results live in the final packet; the static Node launcher fallback is disclosed above.
    - [x] Finish the isolated native matrix:12pass/0fail/0skip/0flaky,380241.018ms in `native-serial.json`. Inspect all12 reports and record `native-serial-inspection.json`: eight stages per case, successful exact-source handoffs/live changes, three bass/comp passes, Stop with no future attacks, one closed context and zero retained voices/references/edges/listeners. Zero console/page errors, forbidden requests or nonreceipt commands. All1480 source/test/tool hash+mtime pairs remain unchanged. The exact harness, browser versions, source digest and Node26 version are bound in every report.
    - [x] Retain the earlier current-source `native.json`:8pass/4fail/0skip/0flaky,578522.726ms with navigation, whole-case and Stop-action timeouts. I mistakenly invoked the default standalone verifier instead of its `--static-only` mode, creating an overlapping Playwright process; its browser launch failed on the missing default cache, and I terminated only that verifier's process tree. Preserve `standalone-pinned.log`, all12 reports, traces and `contended-native-timing.json`. The clean matrix above ran only after all other gates finished and pgrep confirmed no competing suite, with identical timeouts/assertions/retries. No acceptance criterion was relaxed.
    - [x] Reconcile another external commit/push: d55b251 lands the allocation/observer/typing changes and generated artifact; fd9ba68 records the earlier evidence-loss update. Current HEAD/root/dist bytes match exactly and fetched main equals origin/main. Preserve the shared history; publication does not close a failed gate.
    - [ ] Finish unchanged16-file regression gate, fresh review, remaining exact commits/push and Bead disposition. Preserve the failed old five-second gates until the unchanged tests actually pass; no closure, aggregate release or deployment follows from the90 focused passes.
      - [x] Preserve the final pre-memo pinned run:188pass/18fail/6428assertions,327.52s in `unchanged-regressions-pinned.log`; all18 failures are unchanged five-second timeouts. The clean native result above does not waive this regression gate.
      - [x] Reproduce unnecessary V0/V2 recomputation after playback-only publication: valid new fixtures1pass/3fail before repair,4pass/0fail/32assertions after. Earlier invalid key-mode/Manual fixtures are retained separately and earn no defect proof. Reuse the most recent weakly held realization only when the domain's exact equality oracle matches every field except playback; keep identity memoization, exact negative zero and source/Manual/Frozen invalidation.
      - [x] Finish expanded memo proof: all12 transpositions, Undo rearming and literal Manual/Frozen pitch edits. Three actual faulty cache rules each have5/0 baselines and fail the designated assertion; lossy JSON fails both negative-zero cases. Refresh the seven loop faults with22/0 baselines. Combined10/0/120assertions,79.90s in `memo-and-loop-mutations.log`. Correct one unnecessary optional chain in the new test after full TypeScript passed and full lint caught it; final memo/cache-fault/artifact suite44/0/234assertions,30.69s in `memo-artifact-final.log`. Inspect current source/bundle hashes and all ten controls in `memo-final-fault-inspection.json`; no criterion changes.
      - [ ] Resolve the unchanged16-file replay193pass/13fail/6428assertions,274.30s in `unchanged-regressions-memo.log`. Expanded existing realization/voice-leading plus loop/memo suite38/1/2681assertions,26.65s in `memo-focused-final.log`; Deacon Blues exceeds5s. These are observed local run totals, not a statistical speedup or gate waiver.
        - [ ] Diagnose the owner-directed uptempo chart compilation5465.90ms and expanded Deacon Blues continuity5388.87ms; preserve the original voice-leading oracle and5s guards.
        - [ ] Diagnose fractional seek5004.41ms, including its unconditional3s settlement wait, without dropping the end/negative clamping or history assertions.
        - [ ] Diagnose ten notification-continuity timeouts: interruption/natural-end after volume, instrument, seek and groove, plus prior-tempo projection refusal through instrument and volume. Retain all real X1 identity, revision and stale-authority assertions; the log records each exact case and duration.
        - [ ] Diagnose the guitar hostile-chart range-fold case5237.91ms through its existing production preparation. A timeout alone does not establish a DSP or range-fold defect and does not authorize changing the musical expectations.
      - [x] Refresh full Node26 types and whole-tree typed lint on the corrected inputs: both exit0, source policy305/0. Static standalone and guarded rebuild pass8431411bytes SHA256c20c0b1f029e3abd3d6eb03f4e7c374a607fb4a7b00fdbd29bd369eb94f316b3, sourceb428cfd5efaf8f1e5779e6cab6f01f99462918ff2b60a55155a1486d5f1128d0. `static-memo-final-results.json` records the actual commands/process results; preserve earlier lint failure separately.
      - [x] Finish isolated native matrix12pass/0fail/0skip/0flaky,158303.04ms in `native-memo-final.json`. Inspect all12 raw reports again in `native-memo-final-inspection.json`: repeated native bass/comp frequencies, all eight stages/live handoffs, strict Stop and disposal, zero browser console/page errors/forbidden requests, one closed context and zero retained voices/references/edges/listeners. Bun1.3.14, Node26.0.0, Chromium149.0.7827.55/Firefox151.0/WebKit26.5, file/HTTP at1280/390 widths; retry0. Harness8281318bytes/0e39320ddf4fab707e122ffcd94e9fe8589976a8b66559f34b2ef47269555b21. No physical-phone or human-listening claim.
      - [x] Fresh solo review checks exact domain equality, playback-independent realization inputs, weak retention, source invalidation and actual fault/baseline diagnostics. All1482 captured hashes/mtimes stay unchanged through final gates, digest0bb0e4515a596350b90f7c4eb9acd9a38eca1c7260592b778685c4d87a042c34. Rebuild immediately before commit: every hash still matches; only the generated root's expected mtime changes. Commit explicit source/test/artifact paths as33b3454. This is self-review, not independent-person certification; `.5` remains open for the failed timing gates.
    - [x] Reconcile external commits/push during gates: another process landed the implementation/tests/tracker and subsequent performance/type fixes through3e00bfe. `git fetch origin` confirms main==origin/main; all1463 frozen source/test/tool inputs remain unchanged across that publication. Do not rewrite that history or infer task closure from publication.
  - [ ] Fresh solo review, exact-source commit/push and Bead closure only after named gates; retain full rehearsal, human and deployment obligations.

- [x] Semantic surface specification (`jcpe-semantic-surface-conformance-32w2.1`, closed a4713bc): freeze executable expectations for real engine dispatch and the currently false major-key containment explanation; build/proof retain their engine/UI prerequisites.
  - [x] Read the parent/leaf, architecture, rebuild/theory contracts and actual web/native call sites. Inventory32 public operations,2 additional helpers,4 compatibility operations and7 real UI/Swift consumers. H0 literal facts exist; context/scale operations remain missing. Preserve the Swift store's existing local stale-option guard without calling it a bridge-returned source binding.
  - [x] Freeze adapter/version/selection/boundary contracts and12 independently authored chord rows,4 containment cases in12 pitch-class transpositions, exact Db7/C#7 spelling distinction, altered selections/custom barriers, P/L/R arithmetic,16 stored voices with duplicate unisons and exact7/8/pickup timing. The starter has17 tones,14 pitch-class matches,12 spelling matches and a C/G-major tie; no complete-containment or persisted-key claim.
  - [x] Specify13 actual musician-journey stages,7 trace owners,13 limit edges, original engine caps, real storage/audio/download/Undo invariants and exact source/bundle identity. Native parity remains G2 only with explicit v2 exact-source schema; Apple and human observations retain distinct owners.
  - [x] Author the named validator and49 static tests/136 assertions. All16 corrupted fixture controls fail their designated musical/authority assertion; independent bit-mask membership checks all48 context transpositions. Frozen full typecheck/lint and validator pass; source policy304 files/0findings. All327 input SHA/mtime pairs unchanged; guarded artifact remains8424443bytes/b2a27608. Existing artifact policy/staleness36/0,100 assertions. No runtime or fifth-idea completion is claimed.
  - [x] Finish full named gates and fresh solo original-criteria review, commit the exact specification a4713bc, then close only `.1`. The broad `.2` build retains all original engine/UI/Apple/human dependencies.
- [x] Repair the observed existing-studio continuation regression (`jcpe-semantic-surface-conformance-32w2.4`, closed54f371f): truthful overlap/ties/counterevidence, custom barriers, selected-realization-aware cache and actual desktop/phone visible proof. Keep the complete semantic build open; `.4` is an additional prerequisite and no original obligation moved or gate was waived.
  - [x] Reproduce the actual false containment sentence with an independent positive twin:1pass/1fail before repair. Author the accepted starter/positive/near-miss/tie evidence, all12 transpositions, altered/foreign/missing selection and custom/unsupported barrier regressions.
  - [x] Implement session-continuation@2 evidence and truthful wording: exact tone counts/spellings, all tied major readings, actual work comparisons and source barriers. Preserve existing candidates/ranking for supported unambiguous input. The real selector caches document plus selected T1 IDs; explicit selection changes recompute without an edit, while ambiguous7alt has useful visible guidance. Named49/0 tests,201 assertions; retain first incomplete refactor1pass/10fail/1error (one surviving keyName reference), corrected without weakening expectations.
  - [x] Kill5 actual production source faults with23/0 baselines each and specific failed musical assertions; source hashes differ and all reports inspected in `/data/tmp/jcpe-continuation-context-faults-AVYLEF`. Final5/0 fault suite,60 assertions.
  - [x] Initial actual standalone matrix12/0: three engines at1280/390px via file/HTTP; starter, full-containment twin and altered barrier all visible with no source-view changes, console/page errors or unexpected requests. All1516 source/fixture identities unchanged; exact artifact8428650bytes/efb50e4a. This is visual behavior at phone width, not a human listening or full semantic-conformance claim.
  - [x] Final user guidance explains specific alterations; actual updated-artifact native12/0 and all36 stage observations pass. Full lint and artifact36/0 pass. Full typecheck catches three numeric/readonly Bun assertion typing issues, then a misplaced Playwright reducedMotion option; preserve expected values and move that option into contextOptions, adding an actual media-query assertion. Earlier12-case runs prove default-motion behavior; the corrected matrix below separately proves reduced motion.
  - [x] Corrected full typecheck, full/owned lint and final solo54-test/261-assertion suite pass. Actual reduced-motion native12/0 in92231.686ms, with matchMedia confirmation, all12 evidence attachments/36 stage records inspected and no errors/blocked requests. All1516 SHA/mtime inputs unchanged during that run. Guarded artifact8428775bytes SHA256 `e0d4705cdf548cb85bf3dd17f2bf5f0a922a53823e1258a5dca26a8f4dc8b97b`; root/dist/git-show-HEAD identical. Precommit rebuild refreshed generated-root mtime only: extra assertion tripped, then all1516 hashes/1515 source mtimes and committed bytes were independently checked before push. Commit54f371f; close only `.4`, retaining every full-package obligation. Self-review is solo, not independent-person certification.
  - [ ] Resume full semantic adoption only through original ready engine/UI leaves. Current automated entry points remain behind the owned np17/X0-human chain and other original owners; no aggregate/deployment approval follows from this bounded bug. Campaign remains4/15 complete.
- [x] Shared discovery execution (`jcpe-discovery-execution-proof-gdcw`; spec `.1` closed359b02c; build `.2` closed55608ae; proof `.3` and parent closed6dd9ee3). It unblocks existing H1/G0–G9 proof prerequisites; it does not complete an engine or a fifth September idea.
  - [x] Read live parent/spec, architecture, theory plan, exact G0/G2/G3/G4/G5 caps, and the existing A0 derived-patch/pending-request/semantic-publication path.
  - [x] Freeze domain-bound requests, complete version/constraint/seed identity, selected realizations and source snapshots; distinguish musical proof from application publication authority.
  - [x] Specify measured primitive work and managed allocation accounting, canonical ties/candidate identity, input/output/cache/snapshot bounds, deterministic termination and resumable cursor validity.
  - [x] Specify a capacity-one cooperative application job and publication lifecycle: cancellation/staleness at each yield and retirement await, exact patch revalidation, one A0 transaction and Undo, zero document/history/export-marker effects on refusal.
  - [x] Author independent finite enumeration, limit-edge, schedule-partition, authority corruption, exact-time and Manual/Frozen preservation fixtures, provenance and mutation/trace controls before production.
  - [x] Implement the named specification validator and static checks:22/0,119 assertions, all16 corrupted fixture controls rejected,26 maximum-edge rows,21 authority corruption dimensions and32 exact six-unit schedule partitions. The existing independent Manual/Frozen source is hash-bound; publication/Undo expectations preserve exact pitch/time and markers. Full typecheck and owned ESLint pass. Initial two Bun literal-type assertion errors and one void-arrow lint error are retained; no assertions/rules were weakened.
  - [x] Fresh solo specification review clarified shared source-snapshot accounting, terminal cursor/refusal behavior and cancellation of a prepared result. Final frozen validator/static22/0/full typecheck/owned lint/tools TSC PASS; guarded build/static artifact PASS,311 source identities unchanged and runtime artifact identical to603ef80. Source policy caught a type-only private theory import; publish the types through the existing theory entry and recheck it (298 files/0 findings). No runtime or fifth-idea completion follows from these fixture checks.
  - [x] Commit/push specification359b02c and close only `.1` with exact named gates. Claim the now-ready build `.2`; preserve all engine owners and unchanged runtime artifact.
  - [x] Implement bounded passive-data capture/canonicalization and an actual shared reservation ledger. Reject getters, cycles, holes, invalid Unicode and foreign/double releases. Preserve original data and ordered duplicates; pay conservative capture/owner/wrapper bookkeeping separately from graph/string bytes.
  - [x] Implement the actual FIFO stepper, family-bound ceilings, canonical ties and exact duplicate accounting. All32 six-unit partitions produce identical semantic bytes/counters. Latest-cursor identity, synchronous sink leases, cancel/stale and explicit disposal prevent later work. Result bytes use measured UTF-8 payloads and actual envelope/ordinal fields; escaped large options and exact state-size edges pass.
  - [x] Implement the capacity-one MessageChannel service and real A0/F2/F3 publication path. Bind immutable source/selection/versions and private measured-result identity; validate before and after retirement, preserve explicit timing/scope, and publish one undoable command. A cancelled retirement keeps its source/patch charged until its await settles and blocks a second job. Native audio retirement itself remains a proof-phase obligation.
  - [x] Exercise independent graph/quantum/limit/cursor fixtures plus actual A0 complete and bounded-partial Apply/Undo, stale edits/selections/versions, malformed proof/patches, double clicks, retirement failure, replacement, disposal and subscriber reentrancy. Named final unit/integration42/0,658 assertions. Controlled retirement promises establish await races, not native audio evidence.
  - [x] Pass the unchanged contract validator, named final unit/integration42/0, full typecheck and full lint (source policy303 files/0 findings). Add the real unchanged contract command to aggregate verify; no prior gate removed. Retain initial tuple/type/lint errors and the later bookkeeping/result-byte corrections under `/tmp/jcpe-discovery-build-*`. All316 source identities are frozen for final gates.
  - [x] Commit/push build55608ae and close only `.2` with exact revision-bound results; atomically claim `.3`. Guarded build/root-dist equality8424443bytes SHA25661d6d02d04b552a5ec96025f66e697666f6e5e5dfc6bb3581b9880cb89b9afbf. Fresh solo artifact/A0/static/runtime108/0,1238 assertions; this is a separate self-review pass, not independent-agent or browser certification.
  - [x] Execute `.3` against the original proof criteria; preserve all downstream engine/UI/human/release obligations.
    - [x] Add independent conformance/resource-edge and cancellation/state-replacement properties at every named fixture/phase-gate path. The original overlap/reversed-source law failed against production; repair admission with exact rational end comparisons, preserving valid source gaps. Chronology repair is bd1c57e.
    - [x] Kill12 actual production source faults for fabricated work/state/candidate/quantum/queue counters, memory/handle/cursor identity, selected realization, source chronology, pending-retirement ownership and final patch revalidation. Retain passing baseline, failing mutant, raw logs and changed source hashes for every fault.
    - [x] Run actual P0/StudioAudio/X0/X1/A0 and native MessageChannel through the finite protocol harness:122 Bun tests/0fail/1427 assertions and30 browser cases/0fail/0skip/0retry/0flaky. Run `9d281239-d3d2-4612-8b3d-1bc38671b006` passes the named evidence checker with1,527 unchanged inputs, digest `98949e7d0802b39b598f210df324385862fe99f003484dd0c24aa4b48e6ff214`.
    - [x] Inspect all30 request/console/page-error/resource/transaction records and all6 browser/width screenshots. Chromium149.0.7827.55, Firefox151.0 and WebKit26.5 at1280/390px prove responsive input, exact Apply/Undo, Edit/Stop during search and real retirement. No errors or unexpected requests; contexts, graph references, voices, ports, listeners, intervals, scheduled callbacks and retained job memory are released.
    - [x] Preserve initial harness failures: illegal preview ID, missing existing production embedded-WASM permission and Playwright's inert screenshot CSS. Correct the test harness; retain production CSP and every zero-error assertion. Authorize only the pinned tool's exact `body {}` hash in test-only CSP. Earlier failed/interrupted runs remain failed, not retries or passes.
    - [x] Implement the named checker and connect its actual command to aggregate verify without removing a prior gate. Inspect all expected outcomes, exact input hashes/mtimes,12 mutant pairs and real browser rows; reject missing/duplicate/failed/retried/skipped results. Handle Playwright's omitted optional leaf-suite field without changing the30-result requirement.
    - [x] Pass full typecheck/lint and final changed-path ESLint; fresh solo original-criteria conformance/property replay58/0,650 assertions. Static artifact32/0; guarded root/dist8,424,443 bytes SHA256 `b2a27608ffe539d279851697cc13b8b3a78f28e049a8d3abf3643aa8d3869963`. Recheck all1,527 inputs immediately before commit6dd9ee3. Separate self-review, not independent-person certification; generic accounting is not a physical JS heap claim.
    - [x] Close `.3` only after all named gates, then parent only after all three phases are closed. This is an ENABLER; the campaign remains4/15 complete, with musical consumers, native Apple execution, human listening and aggregate/deployment gates still open.
- [ ] H0 authority repair (`jcpe-milestone-musical-intelligence-qqy.2.1`): the five original contradictions and two additional false ambiguity labels are repaired. Specification closure still requires the unchanged aggregate gate; no contextual-analysis or scale runtime is claimed.
  - [x] Recover latest parent/spec context and the actual request/result types, evidence-tier/ordering rules, T1 extension authority and independent counterexamples.
  - [x] Reproduce five consistency findings (audit exit1) and actual literal-facts50pass/2fail before edits; preserve the original production implementation and valid assertions.
  - [x] Preserve the passing-diminished negative while allowing the exact secondary-leading-tone reading required by its identical musical input.
  - [x] Reconcile A7→Dmaj7 reading order/disposition with unchanged strength-first and ambiguity laws. The stronger audit also caught false ambiguity in012/014; preserve their exact Roman labels, speculative readings and counterevidence while labeling them unclassified.
  - [x] Restore required natural11 and spelling in C13/C13sus4 source/containment fixtures directly from unchanged T1 authority. Retain separate4/11 suspension/extension roles; no production normalization.
  - [x] Replace fixture-only scale evidence IDs with one explicit typed frame and exactly spelled tonic, null for no declaration/Custom, four closed kinds and a typed scale-only refusal. Account for two retained input records without widening any work/memory cap. No persisted mode or inferred key is added.
  - [x] Add fifteen independent declaration boundaries/near misses, four-frame/twelve-written-root checks and semantic mutations. Wire the existing validator to the semantic audit even when whole-packet hashes are disabled; repin only six amended fixture files and their reviewed payloads.
  - [x] Reproduce an audit weakness with an actual public-type mutation: unrestricted string/number tonic fields passed despite correct property names. Check exact step/alteration literal types and reject that mutation; retain the failing probe.
  - [x] Final H0 production/static packet122/0,4680 assertions; actual TypeScript semantic audit PASS, targeted H0 typecheck and owned ESLint exit0. Full project typecheck also passes. Retain initial inventory failures for both refusal counts12→13 and all failing logs under `/tmp/jcpe-h0-repair-*`.
  - [x] Run full lint: source policy passes, then ESLint crashes because newly merged `playwright.u4.config.ts` lacks typed-project assignment. Repair this integration gap separately; do not suppress the rules or ignore the new file.
  - [ ] Aggregate `bun run verify` remains outstanding. Its prerequisite np17 is still open/owned; current unchanged cast-policy test1pass/1fail reports the same29 production findings. Human X0/U0 observations and complete H0 runtime/evidence remain separate obligations. No aggregate rerun or deployment pass is claimed.
  - [x] Commit/push owned corrections in948f24b and reconcile Beads in7c691d4; remote main verified at7c691d452c4efdda6c6b85cab9db8bff6e836787. Preserve later U4 audit reopenings and semantically identical comments despite database-local ID remapping. H0/spec remains blocked.
- [x] Restore complete static coverage for newly merged U4 evidence (`jcpe-u4-typed-project-nx42`): config and browser spec now belong to the existing Node Playwright TypeScript project and typed ESLint override; the spec is excluded from the Bun project. Full `bun run typecheck` and `bun run lint` pass, including all actual newly covered files. Original rules/assertions and the U4 browser matrix remain intact; initial ESLint exit2 retained. This does not close the U4 package.
- [x] Repair `jcpe-7tgc` (closed; implementation603ef80): live instrument/mix/toggle/seek commands can strand the active run's notification identity, leaving Playing visible after interruption or natural end.
  - [x] Read the live bug and both facets, A0/X1/U4 laws and application/audio paths; reproduce real controller/X1 interruption after metronome and instrument with an untouched success twin:1pass/2fail. Correct an initial test-only invented instrument ID using the real catalog; retain both logs.
  - [x] Author the additive A0 current-source contract and independent positive, malformed, stale, foreign-document and pending-command witnesses before production. Default A0 admission remains unchanged. Instrument/volume revision continuity requires an already-authorized immediate predecessor; a prior musical/history/replacement edit breaks it.
  - [x] Carry the exact live service identity into A0 at notification delivery; preserve actual request/revision, generation/sequence and settled status. Keep the original bound plan across instrument/mix rides; no audio scheduler, pitch, timing, graph or document/history code is changed.
  - [x] Prove18 interruption/natural-end combinations over nine controller actions, two stale-tempo predecessor near misses,17 pure source-admission checks and existing stale/settlement/transport regressions. Final15-file focused suite137/0,979 assertions,73.72s; all20 original A0 stale/transport fixture hashes remain unchanged.
  - [x] Kill12 actual application source faults, with45 passing baseline cases and per-source hashes/logs. All12 reports inspected in both `/data/tmp/jcpe-transport-continuity-faults-79kvJ9` and the final campaign `/data/tmp/jcpe-transport-continuity-faults-V7DK5y`; every baseline passes and each mutated source hash differs and fails.
  - [x] Retain initial40pass/4fail new-test expectations: malformed A0 refusals append their existing notice while preserving document/history/transport, so whole-state identity was the wrong new assertion. Retain expanded41pass/20fail composition-reader misuse and46pass/1fail/1error nonexistent ID constructor; correct tests to actual composition/public constructors without changing production expectations.
  - [x] Native first run2pass/1fail at `dff784f3-e6b8-41b8-80ad-c525b14033fc`: WebKit's global Space check lacked an explicit noninteractive focus target. Focus the workspace and retain shortcut/zero-error assertions. Corrected complete three-engine run3/0,33134.08ms at `fd1dad9f-5484-4b62-a665-b1bdddb187e7`; all270 inputs and309 source hashes/mtimes unchanged. All three reports prove interruption after toggles/instrument and real natural end after seek, zero errors/blocked requests/nonreleasing voices. This is desktop native proof, not listening or phone acceptance.
  - [x] Run the existing independent U4 verifier on that exact completed run:PASS. Keep U4's later audit reopening and existing build owner; passing technical evidence does not grant package/human closure.
  - [x] Preserve actual browser console messages and request records. Run `c3e0cfbe-add3-46dd-a8cb-76fe602de3f1` passed3/0 but exposed13 WebKit canvas warnings: the pre-existing harness omitted all production CSS. Earlier runs prove transport behavior through an unstyled harness, not product layout. Retain both evidence classes and the original warnings.
  - [x] Embed the actual12 product stylesheets in source-index order in the same offline hash-bound harness bundle; include index/CSS inputs and reject captured-input drift. Leave production canvas/audio code unchanged. The complete styled native run `b479cc63-805c-4227-86a3-9cc1c0ca5734` passes3/0 with283 exact inputs, zero warnings/errors/unexpected requests and zero nonreleasing voices. All reports, requests and three desktop screenshots inspected; WebKit canvas now256×60 pixels at128×30 CSS pixels. Existing U4 verifier PASS, six verifier unit tests PASS; human listening remains not-assessed.
  - [x] Full lint and full project typecheck pass. After the stylesheet wiring, owned runner/driver ESLint and another full typecheck pass. Guarded build produces8,423,409 bytes, SHA-256 `e3e2781b0dfd99a41c78492d76ca7a11ee0096699aa74e96555cf16fcc1b8ff4`; all309 source hashes/mtimes remain unchanged through native gates.
  - [x] Static artifact and two-root/different-mtime reproducibility checks PASS at the same8,423,409-byte hash. Fresh solo re-execution of the original bug criteria passes37/0,264 assertions; this is a separate self-review pass, not independent agent certification. No release gate weakened; original failed/unstyled runs remain retained.
  - [x] Immediate precommit guarded rebuild confirms the same artifact hash and all309 unchanged source identities.
  - [x] Commit the complete fix as603ef80 and close only7tgc with exact proof. Record the implementation evidence on U4/verify without taking its owner's leaf; retain all aggregate/human/deployment blockers. The honesty audit found and corrected the missing-CSS proof gap; no old test/gate was weakened. A bounded older-session cass extension failed query-fuel limits, so no historical audit claim is made.
- [ ] Complete all fifteen September ideas requested by the owner; full ownership/dependencies remain in `PRODUCT_IDEAS_2026_09.md` and Beads. Entry Repair, Focus, Exact Sharing and My Charts are complete (4/15). Rehearsal specification is complete; its build awaits existing U4 verification, and key sequencing awaits H1 verification. U2 technical checks pass; named human observations remain open/unassigned. Preserve existing engine owners and prerequisites.
- [ ] Rehearsal (`jcpe-rehearsal-session-6y2b`): specification leaf `.1` closed with35/0 packet checks; implementation is also gated by the existing U4 verification, and key sequences require H1 verification. Do not bypass those owners or call the package complete after same-key work.
  - [x] Read all four phase Beads and parent, current P0/X1/U4 contracts, exact range bookmarks, tempo bounds and the actual performance/loop seam.
  - [x] Identify the specific seam: P0 keeps absolute source positions; the performance layer refuses looped input and the application silently falls back to literal chords. Preserve the source bar grid by performing the full chart before projecting its range.
  - [x] Specify exact selected-range authority, pickup/empty/partial-bar behavior, preserved performance articulation and source metadata, and explicit performance refusal.
  - [x] Specify a finite immutable sequence of per-pass plans and tempos, consumed by the existing X1 horizon scheduler; no second timer or repeated UI Play. Distinguish scheduled occurrences from the pass currently audible.
  - [x] Specify64-pass/key-expansion bounds, integer20..400 tempo progression, count-in once, native render preflight, cancellation/staleness, source edits, pause/interruption/resume, final tails and Stop.
  - [x] Define UI/application/audio ports and explicit additive boundaries without weakening old P0/X1/U4 contracts; preserve source document/history/recovery/export and session settings.
  - [x] Author literal three-pass groove/timing/tempo/count-in fixtures, boundary and refusal twins, exact work/state bounds and named source-fault controls before production code.
  - [x] Validate the specification and independent arithmetic:35/0 Bun,326 assertions; application/test TypeScript and owned ESLint exit0. Initial new-checker syntax failure and six Bun assertion-type errors were corrected without changing any literal expected result; failed logs retained in `/tmp/jcpe-rehearsal-spec-*`. This is a solo specification review, not native runtime proof or a fifth completed idea.
  - [x] Close only specification obligations, retaining every original runtime criterion verbatim and unchecked on `.4`; source/artifact hashes and exact commands/results recorded in the close reason.
  - [ ] Resume same-key implementation only after U4 verification; resume key sequences only after H1 verification. No rehearsal runtime is implemented or deployed by the specification packet.
- [x] My Charts (`jcpe-my-charts-zt9z`): specification, build, independent verification and parent complete. Final selection-race fix9ed4925; no deployment claim.
  - [x] Read parent/spec/build context and F1/F2/F3/A0/E0/U5 persistence/publication contracts; distinguish record IDs, musical IDs, recovery and file exports.
  - [x] Specify atomic two-store publication, immutable payloads, exact manifest CAS, cross-tab/ABA refusal, quota/denied/corrupt behavior and limits in `MY_CHARTS.md`.
  - [x] Specify detached A0 rename, complete F1 duplicate remap, explicit Keep/Replace/Remove and existing stop-and-replace Open confirmation.
  - [x] Author independent transaction/restore/identity/boundary/negative/workflow cases; preserve existing manually authored exact-note specimen.
  - [x] Run specification consistency tests and owned types/lint; close specification with exact commands/results, then claim build. Packet3/0,58assertions; tests-project typecheck and focused ESLint exit0. These are specification checks, not runtime proof.
  - [x] Implement bounded backup/document validation, collection merge/conflict planning and detached title/duplicate operations. Exact negative zero and full manually authored document witnesses pass.
  - [x] Implement native IndexedDB snapshot reads and atomic CAS writes; first actual phone control proves two-tab conflict and native abort/quota byte preservation. Wider corruption/resource proof is running, not complete.
  - [x] Implement application-owned current/record/preview identity, cancellation, prepared downloads and existing U5 Open delegation.
  - [x] Add My Charts list/search/actions/restore conflict preview and current JSON escape to the studio; complete native accessibility matrix remains open.
  - [x] Initial focused production checks36/0,234assertions. First full typecheck found seven new-test branded/null comparison errors; use explicit unknown observations without brand casts. Corrected full typecheck passes. First full ESLint found13 new-test capture/rejection/comparison issues; retain logs and fix native capture typing/error values, with final full lint still pending.
  - [x] Retain first native control5/0,14.1s on76a6ee52,1622 frozen inputs, Chromium file390. Actual storage/download/audio workflows pass. Improve the native abort diagnostic to an explicit aborted transaction rather than unavailable storage; rebuild8a0c3985 (8421875bytes). No musical or assertion changes.
  - [x] Inspect the129-case expanded native run on8a0c3985:129/0 in323313.002ms, all1622 input hashes unchanged. All258 JSON reports inspected;18 sources-started/stop cycles,90 created/revoked object URLs,747 opened/closed database connections and183 referentially consistent snapshots. Zero console/page/denied-request/axe violations; incomplete axe rules retained as incomplete. Frozen root `/data/tmp/jcpe-u5-completion.4b74pqhs`.
  - [x] Independently exercise populated-tab CAS, maximum/maximum+1 and generation/empty-ABA boundaries, native source faults and fresh-context restore in the verification phase. Five actual adapter faults killed across all three browsers; six populated real-tab races and18 fresh-context restores pass.
  - [x] Exercise focused production unit/integration fixtures;45/0,279assertions includes9 killed source faults with honest baselines. Expanded existing exact-sharing/U0/U5/lifecycle regressions192/0,1594assertions across13files. Retain every initial type/lint failure without suppression or relaxed assertions.
  - [x] Build frozen inputs; run actual file/HTTP three-engine desktop/phone storage/download/audio matrix without racing Playwright.
  - [x] Inspect exact documents/history, storage generations/records, requests/errors, resources and accessibility reports. Full types/lint/static/distinct-root reproduction exit0. Public standalone36/0 in48037.531ms, all1623 input hashes unchanged; inspect12 positive and24 intentional negative JSON reports. Evidence in `docs/evidence/MY_CHARTS_VERIFICATION_2026-09-07.md`.
  - [x] Close build with192/0 Bun,129/0 native,36/0 public standalone and final type/lint/static/reproducible/guarded-build proof; claim `jcpe-my-charts-zt9z.3` atomically.
  - [x] Execute fresh-context backup/restore and adversarial verification, then close verification/epic only if green.
    - [x] Complete actual production-adapter browser laws for populated CAS, empty ABA, interrupted Remove,128/129 records and maximum generation; kill five real storage-source faults with independent successful contexts. Corrected30/0,25301.095ms at `/data/tmp/jcpe-u5-completion.mkh5hhp9`; all75 JSON attachments inspected,1627 input hashes unchanged, zero errors/forbidden requests. Source hashes distinguish every real fault and match the baseline adapter.
    - [x] Retain first native storage-law matrix15pass/15fail,49185.731ms, `/data/tmp/jcpe-u5-completion.nl748ozb`. Every reported baseline and targeted source-fault law reached its expected outcome, but Firefox's invented favicon and WebKit's context-close screenshot violated the private harness CSP, so the gate failed. Add the studio's inert embedded favicon and explicitly close the positive page before context cleanup; preserve zero-error assertions, traces and the production CSP. Corrected proof passes above.
    - [x] Exercise the actual67,239,936-byte outer backup boundary and boundary+1 with a literal valid envelope;1/0,3assertions,896ms. No production constant generates the expected boundary.
    - [x] Retain fresh-context control1pass/1fail at `887spgkk`: exact backup transfer passed; final Open wrongly expected dirty confirmation immediately after a successful current-chart export. Correct the explicit clean-state expectation. Next2pass/1fail at `df95zp6l` caught a test diagnostic mismatch: actual abort message says cancelled. Neither correction changes a production outcome.
    - [x] Diagnose next2pass/1fail at `j_l8wqam`: native selection followed by immediate typing exposed delayed title initialization overwriting the rename draft. Move initialization to the layout commit; retain the failing trace and unchanged title assertion.
    - [x] Rebuild after the selection-draft fix to e3adae59,8421875bytes. Retain combined183-case run182pass/1fail at `bgvdnbn6`: all129 implementation cases pass; the new WebKit320 transfer case tried to dismiss E0 before its export/recovery receipt. Await the visible settled receipt, preserve the failed trace and zero-error assertions, then run the complete affected suite.
    - [x] Final transfer60/0,269190.558ms at `v93c2cq3`: all120 reports inspected,18 fresh transfers,18 preview/playing/recovery cycles,18 interrupted Remove/Restore successful twins,6 populated real-tab stale writes,72 consistent snapshots. Source/canonical backup bytes retained exactly; no skips/retries/flakes.
    - [x] Final public standalone36/0,48601.924ms at `v93c2cq3`; all1629 input hashes unchanged and36 JSON reports inspected. Final193/0 Bun,1597assertions; full types/lint and changed-test rechecks, static/distinct-root reproduction and immediate precommit guarded build pass. All308 source hashes/mtimes unchanged. Three native visual checks and all six screenshots reviewed at1280/390/320; zero errors/overflow.
    - [x] Commit the selection-race fix and native workflow proof in9ed4925; close all three acceptance items, verification and parent with exact commands/results.
- [x] Entry repair (`jcpe-entry-repair-0h1g`): all three phases and epic closed. Final69 scenarios pass; six focused reduced-motion profile rechecks also pass. This is one of fifteen ideas complete, not an aggregate or deployment claim.
  - [x] Freeze UTF-16 source selection, stale refusal, IME and Escape behavior with independent fixtures (six literal spans, six refusal ranges, twelve interaction obligations; specification-only test 1/0).
  - [x] Share diagnostic prose; retain multiline drafts; add exact token selection and bounded Next error on both entry surfaces.
  - [x] Preserve surrounding text and caret; add explicit Keep/Cancel repair and composition-safe Enter/Escape. First browser run found camel-cased Preact composition handlers did not subscribe to native events; fixed with native lowercase listeners and cleanup. Original assertions retained; command and Library IME/stale tests now pass 2/0.
  - [x] Show exact existing duration/insertion labels; retain all U1 parser/input/render limits and explicit recovery semantics.
  - [x] Prove current controller draft-only edits, Insert/Undo, stale ranges, Unicode and boundary cases: focused unit/controller 44/0 (236 assertions), existing overlay/U1 edit-channel 56/0 (280 assertions); four production-source mutations killed with an honest baseline pass. The first integration test called a nonexistent snapshot property; corrected to the public previewQuickEntryDraft method and manually recounted the UTF-16 offset. No production expectation was regenerated.
  - [x] Replace the raw-barlines pristine insertion workaround with one parsed atomic edit. Retain the first measure ID, comments/quoted barlines, intentional empty bars, exact fractions and one-step Undo/Redo. The committed implementation fails all three independent regression cases; the replacement passes them plus existing atomic/editing laws (334 tests, 7163 assertions).
  - [x] Repair two acceptance-discovered input lifecycle defects: native multiline paste must not erase a limit refusal, and Cancel after a rejected own edit must restore the original while preserving edits accepted from another entry surface. Focused current three-engine proof 6/0; pre-fix Firefox cancellation fails its exact original-draft assertion.
  - [x] Build and run real file/HTTP three-engine desktop/phone/zoom/IME proof; retain failures, diagnostics and artifact hash. The first complete matrix was 34/2: an out-of-band stale-input control could be reset before activation, and WebKit exceeded Clear's real five-second confirmation window. Make the stale control atomic and use real keyboard confirmation; retain every assertion. A subsequent Firefox combined-flow timeout is under investigation; it is not a pass.
  - [x] Keep native IME listeners installed from layout commit until unmount; complete owned lint and application/E2E typechecks. The global typecheck still has 17 pre-existing U2 test type errors; no release claim until aggregate gates pass.
  - [x] Record the completed63-case build matrix:0failures,0skips,0retries;36 file/HTTP diagnostic reports and three native listener traces each[2,4,2,4,2], bound to artifact786aabe1.
  - [x] Independently test phone keyboard space: the first run exposed WebKit fields above the viewport after Cancel (-137/-116px). Explicit repair actions now reveal the field; all six reduced-viewport tests pass, preserving selected source, real chord IDs, revision and Undo/Redo availability.
  - [x] Complete the combined69-case verification packet on artifact462f8155 (8324728bytes):69 pass,0failed/skipped/flaky,one attempt each; six real reduced-motion profile rechecks pass. App/E2E/tools types, owned lint, static standalone and isolated exact build pass. Five selection mutants and a real premature-Insert source fault are killed. Exact reports/results are in the verification Bead; the17 pre-existing U2 test-type errors remain open.

- [x] Chart focus (`jcpe-chart-focus-7iz1`): all three phases and epic closed. Current artifact42c362f7 passes57 Focus/recovery cases plus9 tablet cases, with zero failures/skips/retries. Entry repair and Focus are two of fifteen ideas complete; existing release and human acceptance gates remain separate.
  - [x] Freeze keyboard entry/exit, draft/caret, scroll, transport, recovery and preference-failure semantics; independently calculate ten boundary fixtures (specification1/0; production anchor11/0 including packet check).
  - [x] Add one session-only toggle and preserve existing chart DOM, rails, document/history and audio ownership.
  - [x] Expose existing Library/Harmony sheets at focused desktop widths; retain short-phone transport trigger ownership and focus return.
  - [x] Preserve reading anchor and raw input selection; keep explicit Exit/Stop and title reachable.
  - [x] Verify file/loopback, three engines, desktop/tablet/two touch phones,200% layout and reduced motion with actual adapters and error/request logs.
  - [x] Retain recovery/entry regressions, prove source faults are detected, run owned types/lint/standalone checks, and close only green phases.
  - [x] Retain12 unit/fixture passes (22 assertions) and four killed production arithmetic faults, including a strengthened fractional-output case after the original rounding mutant survived.
  - [x] Preserve the local4/7 first run and subsequent startup-failing/interrupted controls. Both preceding/current artifacts exceeded the same10s startup deadline; exploratory sampling also observed3.369s startup, with no stable code-hotspot or speedup claim.
  - [x] Prepare an isolated hz3 browser copy with exact input manifests, identical Bun1.3.14/Node26/browser binaries, native system libraries/audio and an exclusive browser lock. RCH scheduling refused the generic job's16-slot estimate; direct SSH execution is explicitly not an RCH receipt.
  - [x] Record remote3/0 focused controls (23.4s) and first full32/13 matrix (596451.428ms;0skips/flakes). Reports and before/after hashes are in `/tmp/jcpe-focus-remote-controls` and `/tmp/jcpe-focus-remote-first-full`. Do not treat the focused controls as the full gate.
  - [x] Resolve the earlier failing Focus invocation with an unchanged45-case clean pass on7a5bb2ab in the315-case passing subset; retain its separate3 failed/15 unrun overlay setup cases and every earlier timing failure. Focus diagnostics include exact recovery/downloads, one persistent native context, Stop silence and stable listener samples.
  - [x] Complete current42c362f7 Focus and unchanged recovery-layout regressions:57/0 in208450.985ms at `/data/tmp/jcpe-u2-piano-native.651u8n09/focus/`; independent768x1024 touch matrix9/0 in36354.137ms at `/data/tmp/jcpe-u2-piano-native.v_wciu63/focus-tablet/`. All66 diagnostics match the artifact, with no console/page errors or denied requests;1357 copied input hashes unchanged. Full types/lint pass, focused unit12/0 (22assertions), four current-source anchor mutants killed, public standalone static+36/0 and different-root/mtime reproduction pass. Exact commands and retained failures are on the verification Bead.


- [x] Exact sharing (`jcpe-exact-share-1ric`): all three phases and epic complete; third of fifteen September ideas. Source/artifact committed in3ede8ef/68ed456; verification evidence below, no deployment claim.
  - [x] Freeze complete canonical document fields, compact JSON,8192 encoded characters/6138 UTF-8 bytes, strict parsing, v1 compatibility, disclosure, stale/cancelled ownership and exact JSON fallback in `EXACT_SHARE.md`.
  - [x] Author independent Manual/Frozen/unison/fraction/section/annotation fixture, literal wire vectors and exact boundary arithmetic. Packet2/0,15assertions; real F2/F3 accepts the fixture without repair.
  - [x] Complete U5 build/verification before claiming the exact-share build; package evidence below.
  - [x] Implement bounded canonical UTF-8/base64url v2 transport without losing negative zero, escaped text, exact IDs or ordered repeated pitches; preserve v1 reading.
  - [x] Bind Share preview/copy/cancel and JSON fallback to application-owned revision and dialog lifetime; inject clipboard/URL only at composition.
  - [x] Validate explicit v2 startup through existing E0 import and serialized replacement with one Undo; defer recovery, preserve stale edits and never seed over refusal.
  - [x] Wire the existing Share trigger to an accessible, selectable exact-link dialog; keep focus, phone layout and clipboard refusal usable.
  - [x] Prove independent wire/limit/semantic/stale/cancel fixtures and actual same-build voicing plans through production paths.
  - [x] Complete native file/HTTP three-engine desktop/touch/zoom/reduced-motion copy, startup, fallback, recovery, audio and download proof; retain all diagnostics and failures.
  - [x] Kill ten actual source faults with honest baselines; complete named build gates and close build.
  - [x] Complete separate verification: native late clipboard completion after Cancel/edit/reopen, inherited startup recovery, final source identity and package proof review.
    - [x] Add separate source/recipient contexts, raw HTTP path/User-Agent checks, real object-URL counts and delayed completion from an actual native clipboard write. New native test typecheck/lint pass; cancelled-owner source mutation is killed with a successful baseline (11/0 source-fault suite).
    - [x] Preserve first verification54 result42/12: all12 fresh-context cases failed at redundant manual tracing teardown,36 tracing lifecycle errors. Keep runner tracing and every assertion; remove only duplicate manual tracing calls. No production source/artifact changed.
    - [x] Inspect corrected54 result from `/data/tmp/jcpe-u5-completion.1_maeqz4`:54 pass,0 unexpected/skipped/flaky,273587.43ms; all1,604 inputs unchanged and72 JSON attachments inspected with no errors/requests/axe findings. Twelve truly separate source/recipient contexts preserve complete chart data; six delayed native clipboard receipts remain retired after Cancel/edit/reopen; thirty startup-recovery and six recovery/audio cases pass. All18 sharing downloads create/revoke one object URL, zero outstanding; HTTP paths omit fragments. Final full typecheck/lint and11-fault suite pass. Close verification/epic with exact commands and `docs/evidence/EXACT_SHARE_VERIFICATION_2026-09-07.md`.
  - [x] Wire canonical v2 transport, application-owned sharing and startup, explicit JSON fallback and the dialog. Initial focused codec/legacy/fixture/service suite62/0; added empty-chart and exact playback-plan checks bring the integration file to18/0. The final build gate is177/0 across nine files, including ten actual source-fault baselines/mutants.
  - [x] Preserve first native collection failure (missing Node JSON import attribute; zero cases) and correct the import. First complete54 cases:48 pass/6 WebKit selection failures; exact sharing remains unverified. WebKit's read-only Select All did not select the link, although native select() did; add focus/click selection in production, retaining the assertions. Post-failure screenshot CSP errors remain recorded, with CSP unchanged.
  - [x] Inspect expanded93-case native result on artifactef5a0cfa (8,391,898bytes):93/0,252459.392ms, all1,601 inputs unchanged; all150 JSON attachments inspected with no errors/requests/axe findings,18real sources-started/Stop cycles. Public standalone36/0,49,348.318ms; final typecheck/lint/static/reproducibility pass. First54 used a misplaced reducedMotion option;93 uses contextOptions and checks the actual media query. The first54 is not reduced-motion proof. Build closed with exact commands in its Bead and `docs/evidence/EXACT_SHARE_VERIFICATION_2026-09-07.md`.
  - [x] Implement exact codec and application-owned Share dialog, real clipboard/manual fallback and existing JSON export; preserve marker/audio/history ownership.
  - [x] Open v2 through the existing transactional import authority, preserving explicit-share recovery priority, legacy readability and stale/cancel refusal.
  - [x] Verify round trips, same-build chart pitches/timing, caps, refusals, source mutations and actual desktop/phone file/HTTP browser workflows.


- [x] Restore Cloudflare publication under `jcpe-0bjj`: the committed build is deployed and verified on both hosts.
- [x] Complete the U5 prerequisite for exact sharing/My Charts: all three phases and package complete. Final native package proof is local, bound to exact frozen artifacts; no new deployment is claimed.
- [x] Voice-leading: reproduce raw and actually sounded discontinuities; specify a compatible versioned policy; implement; prove small-oracle, real chart, fixed-voicing, browser audio and MIDI behavior.
- [x] U5: connect and verify lifecycle, recovery, import/export and atomic replacement workflows; exact evidence below.
- [ ] H0: implement all three accepted operations, full scale/readings evidence and independent proof; then close the parent.
- [ ] U2 / idea2: specification and production build requirements pass. Independent verification, human listening/screen-reader observations and release aggregate remain open; no package or deployment claim.
  - [x] Reconcile policy2 with Foundation: 1..16 ordered exact notes, duplicate unisons, MIDI0..127, inert2000-code-point annotations and all21 accepted family/bass combinations. Frozen copies generated pitches with actual provenance; it does not invent metadata for arbitrary Manual notes.
  - [x] Replace hash-only validation with independent arithmetic/preservation/count/reference checks; correct impossible E5/A5 MIDI coordinates and C# pitch class; add16/17, astral2000/2001, unison/order/boundary, and12 revision/preview/Undo workflow obligations.
  - [x] Catch five actual domain source faults (order, dedupe, register clamp, cap12, accept17) and four actual annotation faults (strip markup, UTF-16 counting, erase draft, reject2000), each with a passing baseline. Preserve reports under `/tmp/jcpe-u2-{source,annotation}-mutations/`.
  - [x] Preserve annotation source in the application projection, including literal markup/controls and invalid drafts. Correct contradictory unit expectations under accepted Foundation authority; retain the custom formula/pitch mismatch as a negative publication test. Focused61/0,239assertions; native rendering remains a build/verification obligation.
  - [x] Repair the17 test-type errors using real constructors and valid measure completion; fix the UI contract's private layer import without widening the production entry point.
    - [x] Publish test documents through real F2/F3; remove the17 original type errors and private import. Full refreshed typecheck passes once, then is repeated against the final frozen inputs after annotation cleanup.
    - [x] Final full typecheck/lint pass against1340 unchanged hash/mtime inputs; `/tmp/jcpe-u2-{typecheck,lint}-final.log` and `/tmp/jcpe-u2-gates-identity.json`. Initial failures retained. These gates precede the new build changes and must run again before build closure.
  - [x] Replace inspector placeholder pitches/timing with real V0/V2 playback realizations and exact document timing. Custom-to-Auto requires explicit parsed text and confirmation; exact enharmonic octaves and16-note previews are connected. Full native acceptance remains tracked below.
  - [x] Bind a small choice/hear/keep workflow to the existing V0/X1/application commands; retain advanced structure/timing/exact-note/annotation editing.
    - [x] Add bounded current/balanced/shell proposals from real whole-chart V2 selection; never invent notes for an unavailable family. Service6/0; real-pitch/exact-time projection14/0. No browser proof yet.
    - [x] Connect SetVoicing, SetChord, annotation and exact duration through application commands, with focused no-change, stale source/selection, confirmation and single Undo/Redo regressions.
    - [x] Connect serialized preview release by exact ID,16-note support, preparation cancellation, fatal-adapter recovery and band/newer-preview ownership. Full browser matrix remains open.
    - [x] Mount the inspector from Harmony Lens at desktop/phone widths with staged exact-note/Auto controls, dirty Apply/Discard/Continue, preview feedback and global Stop; native accessibility controls pass on3b7d99ce.
    - [x] Synchronize semantic structure/symbol drafts and project actual Harmony/Motion facts from existing engines.
      - [x] Connect structured slash bass, sixth/extension/alteration/addition/omission controls through the accepted AST and formatter; Symbol/Structure share one unapplied draft.
      - [x] Realize symbol drafts provisionally through F2/F3/V2 and preview their actual notes without chart mutation; invalid drafts have no stale root pitches.
      - [x] Project and highlight exact Manual draft occurrences on the MIDI piano, including enharmonic octave names and all128 coordinates; default viewport36..84.
      - [x] Show both previous→current and current→next V1 assignments with real termination evidence and explicit unsupported comparisons. Broader idea8 remains open.
      - [x] Implement pointer/keyboard hold-release cancellation and band playback continuity; focused stale/failed/pending cases and native source controls pass. Final matrix remains open.
    - [x] Fix preview Stop availability from pending preparation and actual submitted preview ownership. Focused audio23/0 and extended command13/0,66assertions. First native failure retained at `/tmp/jcpe-u2-browser-first`.
    - [x] Fix duplicated dialog focus IDs and move the entry control above theory details. Second control0/2 diagnosed the exact `ui.duplicate_item_id`; revised Chromium controls6/2 confirm actual native preview/release, Keep/Undo and exact-note controls. Both failures are the repeated-dismissal annotation flow, not passes.
    - [x] Finish retained-dialog dismissal handling: allow a dirty editor to explicitly decline a dismissal, then accept a later gesture without duplicate/reentrant delivery. Focused overlay17/0,115assertions; three-engine controls24/0 with zero skips/flakes and native sources released. Permanent-latch source mutant is killed. Subsequent feature changes still require fresh input-bound type/lint and browser proof.
  - [x] Add provisional Symbol and Manual realization through F2/F3/V2, exact draft piano projection, both incoming/outgoing V1 segments, structured bass/modifiers and pointer/keyboard hold release. Focused66/0,376assertions, including exact timing/Undo. These development checks do not close the browser/package gates.
  - [x] Finish expanded native proof on7a5bb2ab:234 inspector cases pass. Preserve earlier failures: artifact5a7c2e13: Chromium desktop/phone11/1; all298 source inputs unchanged. Symbol drafts and pointer/keyboard hold both pass with actual sources and zero sounding/future sources after release. Keep exceeded5s once; retain report `/tmp/jcpe-u2-browser-expanded/results.json`.
    - [x] Profile Keep and reproduce2/20 action deadlines in the same-host baseline. Narrow only the selected-choice computation;14 golden outputs byte-identical. Reports `/tmp/jcpe-u2-keep-{profile,baseline}.json`; code still needs measured and native proof.
    - [x] Complete the same-host20-action after measurement; it does not establish a speedup. Baseline median2.835s with2 deadlines; after median4.404s with11 deadlines and higher host load. Preserve `/tmp/jcpe-u2-keep-after.json`; responsiveness remains open.
    - [x] Complete scoped old overlay/browser regressions and final file/HTTP three-engine inspector matrix without retries or relaxed timeouts:234 inspector cases and18 overlay cases pass in separate input-bound invocations; see final evidence below.
    - [x] Complete native audio-fault recovery, band/preview coexistence, actual storage/JSON download,16-note/MIDI-boundary data and2000/2001-character native cases on7a5bb2ab across all18 engine/transport/viewport combinations.
      - [x] Repair fatal X0 command propagation through X1: typed fault, one failed notification, retired generation, and fresh trusted-gesture reinitialization. A fault never certifies a no-effect retirement. Focused transport/lifecycle36/0,217assertions and draft/adapter21/0,121assertions; the actual source-fault mutant is killed with a passing baseline. Reports `/tmp/jcpe-u2-{fault-regressions,adapter-fault}.log` and `/tmp/jcpe-u2-fatal-mutation/summary.json`.
      - [x] Confirm native injected source-start fault recovery and band/preview coexistence at1280/390px on isolated artifact870c71c6. The complete10-case control is7pass/3fail,0skip/flaky: two duplicate-banner accessibility failures and one desktop initial Stop deadline. Exact MIDI0/127 boundary specimen with16 ordered notes and2000-code-point markup/astral annotation survives native phone preview/recovery/download. Desktop data journey remains unproven. Report `/tmp/jcpe-u2-remote-controls/results.json`; all1348 input hashes unchanged.
    - [x] Run every advanced panel through real accessibility/keyboard/phone checks; keep lossless/stale/dirty decisions explicit. All126 panel Axe checks and18 keyboard journeys pass on7a5bb2ab; human screen-reader observations remain separate.
      - [x] Diagnose both duplicate-banner failures and replace the inspector summary header with an ordinary content group; retain every Axe rule. Use same-page Axe execution only after asserting the standalone document has one frame, avoiding seven blank merge-page launches. Fresh proof remains required.
      - [x] Share one validated provisional document between draft projection and motion, keep actual Auto draft notes visible, and reuse the unchanged saved projection. Add visible piano role badges to the existing accessible labels. Focused and native checks are running; no speedup claim.
      - [x] Recover a clean public standalone gate on42c362f7: `bun run verify:standalone` passes static inspection and36 browser cases in the complete isolated copy, with1357 inputs unchanged. The earlier overlapping invocation remains interrupted and is not a pass.
    - [x] Complete full typecheck/lint, static standalone and distinct-root/mtime reproducibility on artifact3b7d99ce (8381464bytes), with1348 unchanged hash/mtime inputs. Expanded inspector/transport regression matrix167/0,1186assertions. Exact reports `/tmp/jcpe-u2-final-{types,full-lint,static,reproducible,domain-audio}.log` and `/tmp/jcpe-u2-final-gates-identity.json`. These gates precede the draft-reuse change below.
    - [x] Resolve the native duplicate-banner failures, visible piano roles and invalid-draft role clearing; expose exact MIDI order, bar completeness and pitch classes. Preserve altered tension spellings (`b9`, `#11`), with an actual source mutant killed. Add native Custom→parsed→Auto confirmation/Cancel/two-Undo and fractional-duration refusal/Apply/Undo journeys.
    - [x] Run all13 inspector journeys on Chromium desktop/phone using memory-backed browser profiles, tools and reports after the remote disk reported90–94% I/O pressure. Artifact3b7d99ce:25pass/1fail,0skipped/flaky;26 diagnostic attachments, no console/page errors or denied requests,1348 remote inputs unchanged. Accessibility, native fault recovery, both exact-data storage/download journeys and new confirmation/timing/piano cases pass. The remaining failure is a5s desktop inspector-open deadline during band playback. `/tmp/jcpe-u2-memory-control/{results,summary}.json`; no complete-matrix claim.
    - [x] Complete the234-case file/HTTP×1280/390/320×three-engine matrix on7a5bb2ab with0 failures/skips/retries and no console/page/network errors. The first broad run on3b7d99ce finished218pass/16fail,0skipped/flaky:15 action deadlines and one navigation deadline. Its234 diagnostics include12 WebKit stylesheet CSP errors on failed cases and no denied network requests. Installed Playwright screenshot code injects a stylesheet; that is a possible diagnostic cause, not permission to weaken CSP or suppress errors. Preserve `/tmp/jcpe-u2-full-before-cache/{results,summary}.json`. Earlier disk-backed26-case run was interrupted after2 passes and timing failures; unexecuted cases are not passes.
    - [x] Profile the band-open path on3b7d99ce: one sample completed in552.5ms with no error; it does not explain away the earlier5s failure or establish a latency guarantee.
    - [x] Reuse one last validated Symbol draft per immutable document/event/exact text, so display, Hear and Apply validation share the existing V2 document-identity result. Every request still checks stale source, length and stored-note confirmation before lookup; old drafts are replaced and document keys are weak. Focused28/0,152assertions;14 frozen outputs byte-identical; cached-confirmation-bypass source mutant killed. Bun-only20-sample preparation median25.906ms→0.611ms, p9556.701ms→1.988ms. This proves that preparation improvement, not a browser speedup. New artifact1fd11237 (8381669bytes) needs its own final gates.
      - [x] Pass fresh full typecheck/lint on the final source and strengthened tests: `/tmp/jcpe-u2-release-{types,lint}.log`. On1fd11237, focused inspector68/0,387assertions and static/reproducibility pass. Twelve complete before/after draft outcomes remain byte-identical (683465bytes, SHA-256cd04976b…); confirmation-bypass and document-ID cache-key mutants are killed. Full native proof remains open.
      - [x] Complete clean native browser proof on1fd11237:234pass/0fail/0skip/flaky in2026049.913ms,234 artifact-bound diagnostics, no console/page errors or denied requests,1348 inputs unchanged before/after. Exact pinned private-cache browsers: Chromium149.0.7827.55, Firefox151.0, WebKit26.5; Node26/Bun1.3.14. `/tmp/jcpe-u2-cached-local.GwRj6T/results-private/{results,summary,cases}.json`. The preceding missing-shared-cache launch failure remains under `results/`; it is not part of this pass. The subsequent symbol-boundary fix still requires its own native proof.
      - [x] Finish native proof for oversized Symbol drafts borrowing the saved projection after the application refuses their256-code-point bound.
        - [x] Preserve exact text, expose the refusal, clear draft notes/canonical text and disable both audition controls after a refused draft read. Add257-refusal/256-success, no-change Apply and correction recovery assertions to the native structured-edit journey. Guarded artifact7a5bb2ab (8381771bytes); full typecheck/lint pass against1348 unchanged inputs in `/tmp/jcpe-u2-symbol-boundaries-{types,lint}.log`; static/reproducibility pass in `/tmp/jcpe-u2-symbol-limit-{static,reproducible}.log`.
        - [x] Demonstrate the defect against the unchanged1fd11237 build: the new regression fails because the257-code-point input reports `aria-invalid="false"` instead of true. All1348 input hashes retained; no console/page/network errors. `/data/tmp/jcpe-u2-symbol-limit.kpou1tde/results-before/{results,summary}.json` and `/tmp/jcpe-u2-symbol-limit-before.log`. This expected failing control is not a product pass.
        - [x] Correct the independently authored boundary fixture after native tests exposed forbidden trailing spaces. T0§3.2 requires those spaces to be refused; the valid boundary uses spaces between `no5,` and `add9`. All behavioral assertions remain intact. The first333 invocation was interrupted with88pass/6fail, one interrupted and238 unexecuted cases (JSON groups the latter239 as skipped); it is not a gate pass. Preserve `/tmp/jcpe-u2-final-native.ujgncok9/results/{results,summary}.json` and the invalid fixture snapshot.
        - [x] Prove the corrected otherwise-identical256/257-code-point fixture in native Chromium: old1fd fails the257 validity assertion; patched7a passes the full structured-edit journey in6.3s. Both have exact artifact diagnostics and no console/page/network errors. `/tmp/jcpe-u2-corrected-native.wzmbol25/control-{before,after}/{results,summary}.json`. Parser-contract probe: `/tmp/jcpe-u2-valid-symbol-boundary.log`.
        - [x] Complete and retain the fresh333-case7a5bb2ab invocation at `/tmp/jcpe-u2-corrected-native.wzmbol25/results/`:315pass/3fail/15unrun,0flaky in1361219.761ms. All234 inspector,45 Focus and36 standalone/offline/accessibility cases pass; overlay setup fails because the isolated copy omitted checked-in font assets. This combined invocation is not a pass. All1348 hashes/mtimes remain unchanged;234 inspector diagnostics report zero console/page errors or denied requests. Full corrected-fixture typecheck/lint pass at `/tmp/jcpe-u2-corrected-{types,lint}.log`.
        - [x] Repair the isolated overlay environment with the three bundled fonts, two licenses and bunfig.toml; preserve both incomplete-snapshot build results. The complete1354-input copy rebuilds exact7a5bb2ab from its own working directory using the harness invocation. Run `bash /tmp/jcpe-u2-overlays.f2xv6f_c/run-browser.sh`:18pass/0fail/0skip/flaky in23308.908ms,1354 hashes unchanged before/after. Every native attachment names7a5bb2ab and no console/page errors. Reports `/tmp/jcpe-u2-overlays.f2xv6f_c/results/{results,cases}.json`. No product assertion, deadline or retry policy changed.
      - [x] Run the unchanged expanded inspector/transport regressions locally on7a5bb2ab:168pass/0fail,1195assertions32files in13.51s,1348 inputs unchanged before/after. The previously timed-out sample-rate case passes in25.21ms. `/tmp/jcpe-u2-final-native.ujgncok9/results-domain-audio/`. Preserve the earlier remote167/1 deadline failure separately at `/tmp/jcpe-u2-cached-domain-audio-remote/`; it remains a failed invocation.
  - [x] Verify Manual/Frozen order, duplicates,16/17 notes, offscreen MIDI,2000/2001 and astral/markup annotations, mode confirmation, stale/cancelled preview, one Undo and global Stop through168 passing focused tests,234 native journeys and independently authored source mutants.
  - [x] Close U2/build `.11.2` with the exact7a5bb2ab gates above; claim independent verification `.11.3` as CodexWorkflow, then release it open after technical fixes/proof while fixture reconciliation and human observations remain. Build closure does not close the parent or authorize deployment.
  - [ ] Finish independent U2 verification and the defects it exposes.
    - [x] Read all11 trace obligations and12 mutation obligations. The29 exact-note/Auto-policy rows already execute in `tests/static/u2-exact-data.test.ts`; do not duplicate that suite or mistake packet validation for full projection proof.
    - [x] Execute eight additional actual source faults with passing baselines: rootless bass, Custom canonical guessing, lost guide roles, erased structural alterations, accepted2001 annotation, unconfirmed Auto replacement, bass by pitch class and generic tension labels. `/tmp/jcpe-u2-remaining-mutations-complete/summary.json`; the first script invocation stopped on a nonunique mutation site and remains separate.
    - [x] Demonstrate4 failing independent piano cases, then retain written flat/sharp ninth/eleventh names, double-sharp source spelling, exact octaves and the off-formula slash-bass role. Initial57/0 focused pass did not cover combined enharmonic roles.
    - [x] Let the real combined browser case expose pitch-class-only role lookup conflating Db bass with C# tension. Preserve artifact7fb61fa5 and its interrupted36-case run:5pass/4fail/1interrupted/26unrun at `/data/tmp/jcpe-u2-piano-native.wkszv572/piano/`. Fix the production projection to select degree, alteration and bass by each occurrence's spelling; an exact-MIDI key can announce both Db3 Bass and C#3 Sharp Eleventh without changing either note. The initial extra-upper-Db fixture was refused by F3; correct it to a valid lowest-bass/unison pair without relaxing assertions.
    - [x] Demonstrate a Custom G7 label incorrectly receiving canonical G7 (4pass/1fail), then preserve its null canonical projection until explicit parsed conversion. `/tmp/jcpe-u2-custom-label-before.log`; current focused59/0,214assertions at `/tmp/jcpe-u2-verification-fixes.log`.
    - [x] Complete native8cb3a129 piano/panel proof:36/0 in175372.591ms. On final42c362f7518d5030a56366d566851d8b520e4e5757618d17e15f84b68265b881 (8382647bytes), all54 Custom/piano/advanced-panel cases pass in259840.663ms,54 bound diagnostics with no console/page errors or denied requests,0 skips/retries/flakes. Reports `/data/tmp/jcpe-u2-piano-native.651u8n09/piano/{results,summary,cases}.json`;1357 copied inputs unchanged,1356 repository hashes/mtimes unchanged. This scoped invocation is not a repeat of the complete252-case inspector suite.
    - [x] Pass final42c362f7 full typecheck/lint,207 focused tests/1289assertions34files in11.45s, distinct-root/mtime reproducibility and clean `bun run verify:standalone` (static plus36/0 native cases in48s). Logs `/tmp/jcpe-u2-verification-{types,lint,regressions,reproducible}.log`, `/data/tmp/jcpe-u2-piano-native.651u8n09/standalone/`. All eight additional source mutants are killed again with eight passing baselines bound to the final source: `/tmp/jcpe-u2-remaining-mutations-final/summary.json`. Earlier7a/8c and every failed invocation remain separately recorded.
    - [x] Reconcile remaining full inspector fixture projection obligations with actual injected/available analysis inputs; no invented harmony, scales or Frozen provenance to match illustrative data. Policy3 amendment is executed through F2/F3 and all seven projected sections; human observations remain separate below.
      - [x] Reclaim only U2/verify after exact-share closure; read current inherited contract and all11 traces/12 mutation obligations.
      - [x] Demonstrate the scale containment defect: independent12-root keyed/unkeyed suite192pass/193fail against unchanged production. G7b9/#11 incorrectly offered altered despite natural5; other failures expose minor-major seventh, natural9 half-diminished, contradictory colors and explicit slash-bass claims. Preserve `/tmp/jcpe-u2-scale-before.log`.
      - [x] Add one bounded exact-degree containment check to existing chart annotations, preserving compound-degree identity and all T1 alternatives. Prefer actual half-whole for b9+natural5, Lydian dominant for#11, melodic minor for minor-major7 and Locrian natural2 for natural9 half-diminished. Refuse unsupported exact combinations; no H0 completion claim. First after389pass/25fail retained; extended half-diminished recognition fixes24 cases. Correct the single old G7b9→altered assertion using the independently stated fifth law. Corrected414/0; later native/source-fault gates remain open.
      - [x] Bind all four inspector fixture rows to real F2/F3 documents and complete projected sections. Initial1/3 exposed illustrative assumptions; first range64..74 amendment3/1 was rightly refused because fixed rootless-a order is3,7,9,5. Independent range64..79 yields E4,B4,D5,G5 and next Manual Dm9 moves1,1,2,2. Corrected4/0; original policy2 bytes/hash retained in Git3f52b83. Policy3 documents every change and preserves the old Cmaj7-rootless refusal regression.
      - [x] Kill six actual containment/projection source faults with successful baselines: missing-degree bypass, enharmonic degree conflation, lost natural5, omitted slash bass, invented Harmony and invented Frozen eligibility.6/0,30assertions; final packet+fault rerun10/0,65assertions. Native old-byte corrected control reaches the intended missing half-whole scale assertion (0/1), preserving exact semantics. The first native control instead hit a legitimate four-voice Auto realization refusal; correct setup uses explicit G7 Manual notes and symbol confirmations. Native54 now passes all54 cases; detailed final result follows.
      - [x] Finish final gates and checkpoint the verified changes.548/0,1991assertions across19 focused files; final packet+mutations10/0,65assertions. Full typecheck/lint, static and distinct-root/mtime reproduction pass on artifactd67a1cb5 (8393339bytes). Native54/0 in418639.869ms: all90 JSON reports inspected,36 actual sources-started/released cycles, no console/page/denied-request/axe findings,1608 unchanged inputs. Public standalone36/0 in48315.277ms,1608 unchanged inputs;12 positive and24 intentional negative reports inspected. Every earlier fixture/type/lint/native failure remains recorded; no suppression/retry/deadline/CSP waiver. Exact commands and scope in `docs/evidence/U2_SCALE_AND_PACKET_VERIFICATION_2026-09-07.md`. U2/verify stays open for real human observations; program remains3/15.
    - [ ] Record actual human listening/screen-reader observations and remaining release-wide acceptance. Automated accessible-name/Axe/source-retirement checks are not human observations.
  - [ ] Complete real browser/audio/download/storage acceptance and named aggregate gates; close only phases whose requirements actually pass.
- [x] Shared discovery execution: accepted types/fixtures, actual bounded execution and atomic Apply, and native protocol proof complete at6dd9ee3.
- [ ] H1 and G0/G1: exact transforms, plural tonal paths, correct hashes/rights/compiler/query behavior and independent proof.
- [ ] G2/G3/G4: real context/route/constraint search and current web/native wiring, with all-slot constraints and proof.
- [ ] G5/G6/G7/G8/G9: lawful reharmonization, registered guide paths/color evidence, exact rhythm/tension, lawful motifs and correctly graded practice.
- [ ] M1: resolve the symmetric-input key-equivariance contradiction, preserve uncertainty in actual import and prove.
- [ ] U3/U4/U5/U6/U7/U8/U9/U10/U11: complete existing musician workflows in dependency order; preserve active assignees.
- [ ] D0/D1: reviewed Atlas and teaching content with actual compiler/engine proof.
- [ ] Cross-surface conformance: real inspect/audition/apply/undo/recovery/export and scoped native proof.
- [ ] Existing audio/native liabilities and release blockers: preserve ownership, obtain real required acceptance, fix global failures and pass Q0/R0. Human-only gates remain unresolved until real observations exist.

## Voice-leading active subtasks

- [x] Capture baseline raw voicings, alignment/gaps/span and actually sounded comp voices for all nine Deacon transitions and three reference progressions.
- [x] Author independent arithmetic/ordering fixtures and policy amendment; prove legacy specification unchanged.
- [x] Implement version-selected aggregation, identity/resume checks, deterministic bounded ranking and studio binding.
- [x] Compare with exhaustive small independent oracles; test actual V0/V1 candidate chains and all relevant fixed/section/time/spelling invariants.
- [x] Inspect and repair any demonstrated downstream register-placement regression.
- [x] Run actual browser playback/Stop and MIDI notes; verify artifact reproducibility.
- [x] Fresh acceptance review; attach exact results and close only satisfied phases.

## H0 active subtasks

- [x] Claim `jcpe-milestone-musical-intelligence-qqy.2.2`; read the complete H0 contract, public types, and inherited Bead context.
- [x] Inspect the independent corpus before using it as implementation authority. Found identical context inputs with contradictory expected dispositions, reversed tier ordering, and scale evidence absent from the public request.
- [x] Implement the `deriveLiteralFacts` production operation and public export, preserving selected T1 identity, spelling, roles, exact weights, Custom duplicates, immutable snapshots, and typed refusals. This alone does not complete H0/build.
- [x] Exercise literal fixtures, all altered selections, twelve written roots, input mutation, and version/selection precedence against the production callable: 50 pass, 2 fail. Both failures expose H0's omission of degree 11 from C13/C13sus4 despite their cited T1 formula requiring it. Keep those comparisons failing until the authority is reconciled.
- [x] Preserve executable context/scale contract witnesses in `scripts/audit-h0-contract-consistency.ts`: five findings. Reopen H0/spec and mark H0/build blocked by that dependency; retain all fixture assertions and valid T1 laws.
- [x] Run the literal-only honest control and four actual source mutants: all four killed (weights, selection, spelling, mutable alias). Recheck the generated artifact with 6/0 voice-leading browser cases, 36/0 standalone cases, and distinct-root/mtime reproducibility. These checks do not certify the missing context or scale algorithms.
- [ ] Reconcile contradictory H0 authority and publish the missing semantic context-evidence boundary. Independently agree the exact accounting recipe, then prove every reachable valid cap and source mutant.
- [ ] Implement all contextual rules and plural scale mappings after the authority is consistent.
- [ ] Replay all independent H0 corpora, execute production mutants, and record complete package gates; close only satisfied phases.

## Completed Cloudflare delivery: jcpe-0bjj

- [x] Reclaim Cloudflare delivery at the owner's 2026-09-06 request; pause U5 without marking its remaining acceptance complete. Independently query the live API: the shell token is active and names the expected account, but the exact Pages project returns HTTP 403 / `10000`. Stored OAuth cannot refresh. The other available deployment tokens also fail the same project check.
- [x] Repair deployment credential selection: preflight the exact Pages account/project before expensive gates, then check saved OAuth when environment credentials fail. Scope the credential override to Cloudflare subprocesses and preserve the shell token for other projects. Run the complete public `predeploy:check` command, retain the native playback gate, isolate public assets from the playback ledger, allowlist Vercel uploads, and execute staging cleanup on failure.
- [x] Complete the owner-authorized device sign-in and verify saved OAuth against the actual Pages project (HTTP 200). The shell token still lacks Pages access; the repaired command selects the verified login only for Cloudflare subprocesses. Credentials were never printed or committed.
- [x] Verify the repair's local release path: `bun scripts/deploy.ts --check` through the required-UA launcher passes model acceptance for 11 shipping models, mechanical quality for nine instruments, and all 15 real Chromium playback instruments with zero console/page errors. The staged bytes match the current committed artifact and staging is removed on success. The recovery subcontrol is explicitly vacuous, not fault-recovery proof. Full output: `/tmp/jcpe-cf-repair-check-all.log`; native report: `/tmp/jcpe-cf-repair-playback.json`.
- [x] Prove cleanup on a nonzero child exit after real acceptance/quality checks. The repaired command removes its stage; restoring the original `process.exit` failure path in a scratch copy leaves the stage behind and fails the same cleanup oracle. The diagnostic then removes only its own leaked stage. Retain `/tmp/jcpe-cf-repair-cleanup-{exit,mutant}-summary.json`. An earlier missing-executable control covered thrown exceptions only; these paired controls specifically exercise the repaired failure path. Neither control claims native playback or deployment success.
- [x] Commit the deployment repair as `9a6ce4790fb7c43a48364b75d8e98a71bb21bb5a`; strict tools TypeScript, changed-file ESLint, the final real-API refusal check and the immediate precommit build pass their intended assertions. All 295 source/test snapshot entries remain unchanged.
- [x] Publish committed `55ac81e31b281b489d8e9f92a29b5ec0e1fc80aa` at 2026-09-06 19:54 UTC: `bun /tmp/jcpe-ship-deploy-entry.ts` runs the canonical production script with the required user agent, Bun 1.3.14 and real Node 26. Exit 0; 11 accepted shipping models, nine quality instruments with zero failing findings, and all 15 native playback instruments with zero console/page errors. Existing warnings and the explicitly vacuous recovery subcontrol remain recorded.
- [x] Verify Cloudflare deployment `8f5d110b-8b89-4601-a5ca-9186448cfd4d` has a successful deploy stage and Vercel `dpl_CJrmKLvJZABYeJfxo5UQLXNuxcrV` is production READY. Both hosts serve SHA-256 `f7685e9ebf752fb8ce83ef907648690ed768a05839b39860e1490675cab15b3e` (8,319,874 bytes); the new stage was removed. Receipts/logs: `/tmp/jcpe-cf-after-auth-{receipt,playback,pages-status}.json`, `/tmp/jcpe-cf-after-auth-deploy.log`, `/tmp/jcpe-cf-after-auth-vercel-status.log`.
- [x] Independently compare both public HTTPS responses with `git show HEAD:jazz_chord_progression_editor.html`: exact byte/hash matches at 20:20 UTC. Preserve `/tmp/jcpe-cf-after-auth-final-host-hashes.json`. A different client receives the identical application plus exactly 367 bytes of the documented Cloudflare analytics script; preserve the insertion-only diff and raw response. Real browsers block that script under the unchanged CSP. Do not claim every response variant is byte-identical or that the host injects no script.
- [x] Prove 40 distinct real Chromium live cases: 28 recovery/layout/native-audio/import-owner-loss cases, eight Organ/Ukulele admission-delay cases and four replacement/Undo cases across the two hosts. All pass with zero page errors or unexpected console errors. The 40 expected beacon CSP messages in the 28-case matrix remain in its diagnostics. Retain eight viewport screenshots and inspect desktop/phone views on both hosts. Reports: `/tmp/jcpe-cf-after-auth-live/{results,summary}.json`, `/tmp/jcpe-cf-after-auth-live-clock-results.json`, `/tmp/jcpe-cf-after-auth-live-replacement-results.json`.
- [x] Recheck the playback failure mechanism on both hosts at 1280/390px: all eight Organ/Ukulele cases survive one real 40 ms busy wait at native engine admission (observed clock advances 32–42.67 ms), start native sources, reach zero sounding/future sources after Stop and allow fresh Play/Stop. New/lesson Cancel/Confirm restores focus and preserves exact JSON through Undo/Redo; confirmed New during playback retires the transport.
- [x] Correct and retain verification failures: the first temporary launcher failed module resolution before running any tests; the first actual run passed 25 and failed three layout cases because Escape preceded export-marker settlement. Trace evidence shows the dialog still busy. Test commit `d567bc7` adds completed-handoff and dialog-absent assertions before the existing chart hit test; all original checks and deadlines remain. E2E TypeScript and changed-file ESLint pass. Preserve `initial-module-resolution-*`, `first-browser-*` and `adaptations.json` in the live report directory.
- [x] Distinguish browser results from wrapper status: the corrected full live run has 28 passes, zero failures/skips/flakes (353,746.47 ms), but its wrapper exits 1 because invocation without explicit spec filenames requests unrelated standalone evidence. Preserve `unrequested-offline-merge-failure.json`; no F0/aggregate pass is claimed. The public runner with the two explicit live layout specs and separate `focused.config.ts` passes eight cases with zero failures/skips/flakes, 92,089.452 ms, exit 0. These repeated cells add no unique-case credit.
- [x] Rebuild before committing the test correction: root/dist/committed HTML remain identical at the hash above. Of 295 source/test snapshot entries, only the explicit four-line test correction changed; production sources are unchanged. Close `jcpe-0bjj` with the exact commands and completed live obligations recorded in its final comment. U5, aggregate findings, independent verification and human acceptance remain open. This is solo verification, not independent certification. Older artifact attempts below are historical and superseded by this deployment.

- [x] Confirm restored Vercel authentication and reproduce the committed `4887a39` artifact with unchanged source hashes/mtimes. Cloudflare account identification alone did not establish Pages authorization; the separate project access failure is recorded below.
- [x] Run the unchanged production gate: 13 instruments pass; Organ and Ukulele refuse with `transport.engine_refusal`. Preserve `/tmp/jcpe-u5-production-playback-failure.json`; no upload occurred.
- [x] Read and atomically claim the existing bug; release the U5 claim while preserving all unfinished acceptance.
- [x] Diagnose against a scratch copy of the real engine: a single 40 ms admission delay produces `audio.start_time_invalid` for both instruments; no-delay controls play. Diagnostic globals exist only in that scratch build.
- [x] Specify an explicit bounded engine-entry catch-up policy that preserves gates and leaves strict absolute-time requests unchanged.
- [x] Author independent positive, strict-negative, malformed, future-boundary, transposition and transport/preview/click regression cases. Corrected pre-fix control: 14 pass/22 fail; restored implementation: 36 pass/0 fail, 172 assertions. Initial invalid test inputs and phone selectors remain recorded with their corrections.
- [x] Implement engine admission and X1 opt-in, preserving strict absolute-time admission outside the explicit policy. Source/contracts/tests and guarded artifact are committed in `8caf56e`.
- [x] Fix the independently exposed Stop-in-ready shortcut: an active preview is retired before Stop promises no future attack. The regression exercises public release-preview refusal and repeated idle Stop behavior.
- [x] Kill five source mutations (no catch-up, unshifted release, lost strict admission, missing transport opt-in, ready Stop shortcut). Focused matrix: 356 pass/0 fail, 3,533 assertions across 32 files. App/browser types, changed-file lint and X0/X1 contract validators pass; full test types retain exactly the 17 existing U2 errors.
- [x] Prove a 40 ms injected admission-clock advance in Chromium, Firefox and WebKit on desktop/phone: 12 pass/0 fail/0 skipped/0 flaky. All 12 cases observe started native sources then zero sounding/future sources; all 24 diagnostic attachments are checked. The separate scratch diagnostic uses a real 40 ms delay; the checked-in cross-browser regression injects the clock value. The original artifact fails all four corrected Chromium counterfactual cases.
- [x] Verify the static standalone envelope and different-root/mtime reproducibility: SHA-256 `7206a79d2c5bc8227311628c058282299c42516d1181845823e93621b2f58010`, 8,318,570 bytes. All 289 source hashes/mtimes remained unchanged through native verification.
- [x] Complete the serial 204-case lifecycle matrix: 204 pass, zero unexpected/skipped/flaky, 1,757,756.944 ms. Every attached top-level console/page-error array is empty. All 289 source hashes/mtimes remain unchanged; report `/tmp/jcpe-0bjj-browser-lifecycle-serial-results.json`. The first attempt was interrupted after an accidental overlapping `verify:standalone` browser launch; both reports remain under `/tmp/jcpe-0bjj-overlap-retained`. Its initial file-navigation timeout predates that overlap and remains unexplained, not attributed to it. Static-only verification uses the documented `--static-only` argument.
- [x] Rebuild immediately before deployment: unchanged `7206a79d` bytes. The original deployment script at `a2ac0bd` passes model acceptance and all 15 real playback instruments with zero console/page errors. Its recovery control is explicitly vacuous; the separate native failure cases supply actual reconciliation proof. Preserve `/tmp/jcpe-0bjj-production-playback.json` and `/tmp/jcpe-0bjj-production-deploy.log`.
- [x] Publish the exact gated stage to Vercel after Cloudflare stops the combined script: deployment `dpl_AgnrMbV34A2bZ98UHuj2YBBQrWGt`, production READY. The mirror returns HTTP 200 with committed SHA-256 `7206a79d2c5bc8227311628c058282299c42516d1181845823e93621b2f58010`, 8,318,570 bytes. The staging upload allowlist contains only `index.html` and `og-image.png`; link-created local environment files and diagnostics are excluded. No gate was skipped or source changed.
- [x] Verify Vercel in real desktop/phone browsers: four Organ/Ukulele cases survive a real 40 ms busy wait at native engine admission, with started sources followed by zero sounding/future sources and fresh playback. Two replacement cases prove New/lesson Cancel/Confirm focus and exact Undo/Redo. Two import/recovery cases prove all six owner-loss paths, exact JSON preservation/download/Undo, and cold Keep/Discard. All eight cases have zero console/page errors. Both screenshots are inspected and neither viewport overflows. Reports: `/tmp/jcpe-0bjj-live-vercel-{clock,replacement,import-recovery}-results.json`.
- [x] Resolve the delivery obligation from the failed 02:08 UTC attempt by publishing the current artifact above. At that earlier attempt Pages returned `10000`, saved OAuth was expired, and the custom domain served `5b8678cde3e005f522d96fab1e3eacffa43f22b66501a0aa2fbf324d4db2b475` (8,204,122 bytes). That historical `7206a79d` target is superseded, not retroactively deployed to Cloudflare. Old Vercel-linked stages remain excluded from uploads.
- [x] Record exact results and push implementation/proof commits through `a2ac0bd`. At that historical point this Bead, full U5, aggregate-gate findings and independent/human acceptance stayed open. The delivery obligation is now closed above; full U5 and the other acceptance work remain open. The scoped Vercel receipt is `/tmp/jcpe-0bjj-partial-deploy-receipt.json`; the combined production script exited 1 at Cloudflare authentication, while Vercel's continuation exited 0 and is independently verified.
- [x] Resolve the automatic-recovery delivery obligation with the current artifact above. The `675fe5a` target (`b6105ad410c5e70bd9d1959683b462e93dcc517452b68af8849bf66b10da10b1`, 8,319,722 bytes) passed its gates but failed Pages authorization at 05:25 UTC; the 05:27 UTC custom-domain read still returned `d7a2cb2f8ab61573d9a9566b2d8ec38ca6d150425576a63fc23b08918e1c1bbb` (8,203,755 bytes). That target is superseded, not retroactively deployed. The successful fresh stage and automatic-recovery live proof are recorded above.

## U5 implementation and completed package verification

- [x] Complete the existing U5/build leaf (`jcpe-milestone-reliable-studio-l3a.13.2`, closed2026-09-07T12:22:51Z). All required production surfaces exist; type/lint/focused tests, static artifact and two-root reproducibility pass. The verification leaf and parent package are now complete; final scoped evidence follows.
  - [x] Preserve nested duplicate-key diagnostics before host JSON parsing, including escaped aliases, array nesting and exact UTF-16 ranges. The old code already refused publication but hid the conflict as an unrecognized shape; seven lexical regressions now pass, with inert cancellation and a fresh valid import through the real service.
  - [x] Register cold-preview initialization with A0's real X1 epoch; retire application preview ownership atomically when replacement publishes. The first native control failed8/10 on stale generation; the next exposed stale Stop availability. Both production defects are fixed without weakening retirement checks. Final30/0 native cases cover New/lesson/canonical/legacy during actually sounding previews and duplicate import at desktop/phone widths in all three engines, with zero skips/retries/errors. Artifact `bd4d00fd`, report `/data/tmp/jcpe-u5-completion.9t4vink8/complete/results.json`; all1362 copied inputs unchanged. Held-preview Cancel/Undo and cancellation during real instrument preparation also pass in integration tests.
  - [x] Finish active-preview cancellation/failure and broader lifecycle/inspector regressions: expanded411 native cases pass. All failed/interrupted controls and exact source mutations remain recorded.
  - [x] Exercise the real 16,777,216-byte history boundary with 4,096- and 6,144-event charts, each storing sixteen spelled notes per event. Twelve native component/service cases pass across desktop/phone and all three engines: inert Cancel, fresh acknowledgement on reopen, exact retained Undo below the cap, explicit non-Undoable replacement above it, and truthful export-first refusal at the separate 2 MiB JSON ceiling. A small-chart native download is the positive export twin. The reduced host renders the real dialogs/controller/adapters; it does not prove rendering performance for 6,144 chart cards.
  - [x] Preserve complete C0 rejected-event/section totals beyond the first-256 report projection; show detected format, retained group counts, and an explicit loss warning in preview and confirmation. A 300-chord legacy file with rejected material after the retained rows now discloses both losses. Six native desktop/phone cases pass.
  - [x] Clear stale title/format/report data after oversized-paste refusal; keep import-preview, import-confirm and history-limit kinds correct in the existing A0 stack, including Back, without an intermediate publication.
  - [x] Route unfinished JSON objects to the syntax diagnostic; preserve decoded duplicate-key identity/ranges at nested array/object depths. Literal positive and malformed/duplicate negative controls remain independent of production output.
  - [x] Refuse replacement while a real cold initialization is awaiting resume: locked transport with queued work cannot certify vacuous retirement. The current chart survives; after resume, a fresh confirmed New retires actual sounding sources and supports exact Undo. Six native cases use a real suspended AudioContext with controlled resume acknowledgement; six integration cases cover all replacement origins and pending preparation.
  - [x] Complete the latest 33-case focused native invocation: 33 pass, zero unexpected/skipped/flaky, 135,034.030 ms; artifact `0c15728fa1343416af44f1a9fc67bcb698b882630225c617769190f7688df54e` (8,385,030 bytes). Report `/data/tmp/jcpe-u5-completion.ovt0vgvf/build-proof/results.json`.
  - [x] Kill nine actual production-source faults with passing baselines, including premature queued initialization retirement and lost legacy totals. Exact sources, hashes, commands and failures: `/tmp/jcpe-u5-all-final-mutations/summary.json`.
  - [x] Restore the immutable E0 v1 document to its accepted bytes; preserve the correct annotated-text export rule in the E0 v2 production amendment. No pin, validator, golden, negative case or runtime behavior changed. The previously red A0/E0 archive check now passes in the unchanged 521-test, 3,472-assertion focused suite.
  - [x] Repair the U2 contract's domain-only type boundary with an application-owned presentation record and bidirectional structural type proof; the unchanged static source-policy assertion passes.
  - [x] Finish lifecycle/inspector browser verification with the green expanded411-case invocation below. Historical failures remain: The complete 1,587-input frozen 375-case invocation finished 374 pass / 1 failure / no skips or retries: all 303 U5 cases passed, but Firefox HTTP 390px lost the Harmony Lens click when playback auto-follow moved its control. This is a failed combined gate, not a passing aggregate. The earlier 279-case invocation was 270 pass / 3 setup failures / 6 unrun because the copy omitted tracked tsconfig.json; it is not a passing aggregate. Preserve all original failures and the corrected Firefox file-navigation observation (actual loaded URL plus no unexpected requests).
  - [x] Reconcile lifecycle/import/export/focus fixtures with actual production paths; all ten trace mappings and concrete proof are in the final U5 evidence record.
  - [x] Complete named build gates: 521/0 focused tests, full typecheck/lint, static standalone and reproducibility exit0. Claim `jcpe-milestone-reliable-studio-l3a.13.3` only after build closure; its first375-case browser invocation later exposed the mobile auto-follow interaction defect recorded below.

- [x] Complete U5/verify and the package before starting dependent exact-share/My Charts implementation. All named package gates pass; global cast policy and X0 human listening remain separate open liabilities.
  - [x] Add direct real-composition checks for MIDI delivery preserving canonical export/recovery markers, retained legacy refusal codes/paths, editable quota/denied states, no publication before pending retirement, and the visible Undo selector with a real undoable edit before a committing export.
  - [x] Kill the production-source counterfactuals for thirteen of the fourteen U5 laws (fourteen variants including both silent-fallback branches), with passing positive baselines. `bun test tests/conformance/u5-production-mutations.test.ts`:14/0,70 assertions,11.41s. This checked-in suite rebuilds the actual source under isolated faults and invokes existing independently authored behavioral assertions; it does not return a fixture's expected result. The separate native focus-restoration control below completes the fourteen-law mutation set.
  - [x] Kill U5-MUT-013 against the actual generated application: six native cases fail at the intended exact-trigger `toBeFocused` assertion across three engines and two widths, with all six positive twins passing in the375-case baseline. The isolated fault replaces only `target.focus()` with `document.body.focus()`; guarded build exit0, artifact7eb57adc. It was never installed in the working product or deployed. Report: `/data/tmp/jcpe-u5-completion.lj7yxuwp/focus-control/results.json`.
  - [x] Inspect the complete375-case JSON report and all embedded JSON diagnostics: 374 expected / 1 unexpected, no skipped/flaky cases, no console/page errors or unexpected requests in diagnostic attachments. Preserve the failing browser trace and its actual cause; verify the copied input manifests independently.
  - [x] Fix playback auto-follow moving a mobile panel control during pointer/keyboard interaction; preserve ordinary playback following and modal focus. Twelve native controls fail on the old artifact at actual control displacement. The repair passes33/33 across three engines, including those12, all18 original preview/band cells and3 normal phone-following cases. Commit `6bd8c93`; artifact8aac97ce,8,385,260 bytes. All1,589 copied inputs unchanged; report `/data/tmp/jcpe-u5-completion.wbjj8fsh/gesture-proof/results.json`.
  - [x] Complete and inspect the expanded411-case native invocation:411 pass,0 unexpected/skipped/flaky,1,895,472.066ms; zero console/page/request/axe findings in JSON attachments; all1,590 copied inputs unchanged before/after. Repaired artifact8aac97ce; runner `/data/tmp/jcpe-u5-completion.hur4al6q/run-verification.sh`. The subsequent export-feedback change is covered by the separate final108-case invocation below.
  - [x] Add24 real-artifact accessibility cases: New, Import, JSON/text export at two widths in three engines; named modal, heading focus, forward/reverse Tab containment, native inert background, exact return and malformed-import refusal. All24 pass with zero axe/console/page findings in63,486.701ms. The one shorthand-void lint finding was corrected without suppression.
  - [x] Check inherited A0/U0 dialog/state/overlay/boundary kernels:120/0,756 assertions. Record all ten frozen requirement mappings in `docs/evidence/U5_LIFECYCLE_VERIFICATION_2026-09-07.md`; final proof scopes and limits are explicit.
  - [x] Fix obsolete export-owner refusal surviving a fresh export: all12 native baselines fail at the stale-alert assertion and record a duplicate announcement. Reset local feedback on a new dialog. Final108 native export/recovery/accessibility cases pass on artifact2d9d786d,8,385,312bytes; all1,594 inputs unchanged. Twelve transcripts each observe two heading focuses, one refusal and one handoff; no human speech claim. Public `bun run verify:standalone` passes36/36 on the same artifact; full typecheck/lint/static/reproducibility exit0.
  - [x] Replace the inspector state assertion with an explicit `AppState` annotation:26/0 focused tests,162 assertions; runtime artifact bytes unchanged. The strict global cast-policy test improves from30 to29 findings and remains red under existing owner `jcpe-cast-policy-compatibility-np17`. X0 human listening also remains open; neither is waived by focused passes.
  - [x] Reconcile all ten trace-ledger requirements and parent criteria in `docs/evidence/U5_LIFECYCLE_VERIFICATION_2026-09-07.md`; exact commands, versions, hashes, failure history and scope limits retained. Frozen implementation/human-acceptance flags remain unchanged.

- [x] Claim `jcpe-milestone-reliable-studio-l3a.13.2`; read the accepted U5 contract and inherited context. Basic recovery is already wired; the README's broader disconnected claim is stale.
- [x] Preserve existing recovery bytes throughout startup and the Keep/Discard decision; serialize admitted writes with Discard so recovery cannot resurrect after successful removal.
- [x] Seed recovery's document/revision dedupe without a boot write; focus, export-preview, and other ephemeral notifications must not overwrite stored copies. A subsequent document edit still persists.
- [x] Bind write completion to document ID, revision, and write generation; an older document with the same numeric revision cannot become clean.
- [x] Replace the UI's unknown recovery token and unchecked cast with an application-owned choice; refuse stale Keep after edits and retain actionable failures.
- [x] Render previous-envelope fallback and corrupt startup diagnostics. Await actual Discard, retaining the offer and bytes when removal fails, with a working retry.
- [x] Render pending/current recovery and exact export status; show quota/denied diagnostics with the pending state and Export JSON action. Keep storage failure independent from chart editing.
- [x] Connect canonical JSON preparation, immutable preview, exact browser handoff and A0 marker settlement. Cancellation, malformed/wrong-byte receipts, allocation failure, and stale completion advance no marker.
- [x] Persist A1's complete export binding through the actual storage adapter, including the delivered file hash/length; verify a fresh service reads it. Quota after handoff must not claim durable marker persistence.
- [x] Cover Blob/URL/anchor/append/click failures with exact cleanup accounting, including cleanup refusal. Preserve synchronous gesture activation and the existing CSP.
- [x] Keep the U0 modal/focus lease across dismissibility/committing changes, with A0 history locking and exact focus return.
- [x] Connect bounded canonical/legacy file and paste preview, grouped reports, inert cancel and revision-bound commit through E0/A0/X1.
- [x] Allocate the import preview identity in the composition-owned replacement workflow; do not start retirement on preview or silently rebind a displayed confirmation to a later request identity. Restore the frozen six-member composition return; the unchanged A0/E0 static policy caught the widened boundary.
- [x] Implement bounded File/clipboard reading, independently decoded canonical data, legacy migration with grouped diagnostics, exact non-Undoable acknowledgement and single-use candidate authority. Focused integration proof passes; normal file/clipboard browser acceptance passes. Oversized history has focused proof; its browser matrix remains below.
- [x] Refuse stale previews, late cancelled reads, removed dialog hosts and oversized input without reusing an earlier candidate; clear import draft authority on failure.
- [x] Fix import command logical time against the existing history. The first real-browser run caught a backwards-time refusal missed by the initial zero-clock harness; the harness now uses prior timed edits.
- [x] Prove exact imported document equality through actual browser JSON download, Cancel/Undo, file and native clipboard paths, Manual/Frozen/Custom values and different-ID cold recovery on desktop/phone in all three browsers.
- [x] Prove serialized confirmed replacement during real playing and paused audio, with no publication from file selection or preview. Connect genuine receipt settlement so controls stop too; retain A0's notification-only generation law. Repeated imports use only the adapter's proven epoch chain; unrelated advances still refuse.
- [x] Connect lossy chart-text export, independently compare its supported projection, render loss disclosures, and preserve the canonical marker law.
  - [x] Compare every supported document/chord/timing field against actual reparsed values; retain the reviewed golden bytes and kill semantic mutations.
  - [x] Prepare text through E0, disclose exact loss counts, and hand single-use bytes to the real browser adapter through the application service.
  - [x] Prove success, cancellation, stale preparation, adapter failure and repeated clicks preserve document/history and canonical export/recovery bindings.
  - [x] Fix the encoder's unescaped section names, invalid annotated repeats, and unordered loss rows. Preserve annotated repeats as literal chords under the unchanged T0 grammar; keep the old goldens and all T0 negative cases.
  - [x] Reproduce and fix mutable delivery authority: freeze the text request and binding so the adapter cannot substitute its own expected filename. Retain the failing counterexample and the interrupted browser-run status; repeat the real browser gates on the corrected artifact.
  - [x] Verify real downloads, loss disclosures, errors, focus and phone layout in all three browsers; rebuild and bind final evidence to unchanged source/artifact bytes. Final source: 275 focused tests / 1,899 assertions; 156 broad browser passes plus six standalone passes after correcting the launcher invocation, on the same artifact. The initial broad invocation's six missing-run-ID failures remain recorded; it was not a green aggregate run.
  - [x] Retain seven killed production-source mutants, the literal fractional-duration/annotation control, exact source/hash evidence, and the existing U2/source/cast gate failures. Full U5 completion and independent verification remain below.
- [ ] Complete New/lesson replacement confirmation. Import confirmation, export-first, focus return, and single-commit ownership are connected; oversized-history browser acceptance remains open. Do not route New/lesson through a falsely labeled canonical-import origin.
  - [x] Prepare a complete validated New/lesson candidate before touching the current chart; replace the library loader's clear/stage/insert/title/groove/tempo sequence with one committed replacement. Production source committed in `7f37d88`; integration proof passes. Phone dialog handoff remains below.
  - [x] Carry the exact `new` or `lesson` origin through the application workflow and serialized retirement/publication boundary. The shared adapter preserves the real local origin and the unchanged import source-format contract; New → JSON import → New has focused proof.
  - [x] Bind the displayed candidate, dirty/transport confirmation decision, export-first action and oversized-history acknowledgement to the exact current revision; cancel and stale confirmation preserve authoritative fields in the existing replacement integration cases and real history-boundary tests.
  - [x] Wire New and library gestures to the application-owned dialog, retaining focus and selection without publishing a candidate from a preview or lesson pick; the existing native New/lesson and cold-preview matrices exercise both actual UI gestures.
  - [ ] Prove one Undoable replacement after real retirement in ready/playing/paused/preview states; add cancel, retirement failure, repeated-confirm, stale and oversized browser cases with exact document/history/marker observations.
  - [x] Fix phone lesson confirmation ownership by carrying the launching surface's stable owner through the UI/application boundary. Preserve U0 refusal checks and show their diagnostics. Consume chart-focus requests only when confirmation owns focus; immediate empty-chart replacement retains chart focus. Final expanded matrix: 60 passes, zero failures/skips/flakes across Chromium/Firefox/WebKit, including both viewport widths, exact Cancel/Confirm focus, missing-owner refusal, and the empty-chart path. The earlier nine phone failures remain recorded in the Bead.
  - [x] Keep Export current chart first available even after native recovery finishes; recommendation status controls its explanation, not action availability. The browser case now waits for real recovery before using the action.
  - [x] Fix the audio-clock retirement race: a caller timestamp that elapsed before engine entry retires immediately at the captured engine time. Negative/nonfinite/out-of-horizon values still refuse, and future times remain exact. The restored old guard fails the corrected clock-advance regression; final audio/transport/lifecycle suite: 179 tests / 1,860 assertions. X0 contract validation passes.
  - [x] Complete the New/lesson host-removal, exact no-effect evidence-envelope, and automatic retirement-reconciliation slice. Source/tests are committed in `b02bf6b` and the guarded artifact in `be4cfb4`. The full U5 package remains open.
    - [x] Reject a late publication after its exact A0 confirmation host or captured UI owner is removed or replaced; close the invalidated host, preserve chart/history/bookmarks/markers and allow a new valid request after proven retirement. Also recover the next gesture after a host disappears before Confirm.
    - [x] Require the exact no-effect envelope before unlocking; malformed success/refusal data and thrown readers discard the publication capability and demand reconciliation. Extra fields cannot turn an uncertain effect into a no-effect refusal.
    - [x] Reconcile uncertain New/lesson retirement with one real serialized stop over the current physical epoch. Release the editing lock only after request-bound generation/ready/no-future-attack proof; never publish the failed candidate. Invalid, refused or throwing reconciliation stays visibly locked. A fresh replacement and playback work after successful reconciliation.
    - [x] Prove 196 focused tests / 1,989 assertions, 72 desktop/phone replacement/accessibility/offline browser cases, and six additional instrumented native recovery cases. All final cases pass with zero failures/skips/flakes; each native case observes started sources followed by zero sounding sources and zero future attacks. Three source mutants are killed. App/browser types, changed-file lint, U5/X1 contracts, standalone static and two-root reproducibility pass.
    - [x] Finish the import/recovery reconciliation slice and its final browser/build evidence; full U5 remains open.
      - [x] Reproduce five unsafe unlocks from ambiguous effects, rejected retirement, and throwing evidence readers; preserve the 33-pass/5-fail baseline.
      - [x] Share application-owned E0 failure handling and the real X1 stop adapter. Discard request authority before reconciliation, validate the actual echoed request and generation/ready/no-future-attack receipt, and retain the lock on unproven stops. Accepted E0 driver and fixtures remain unchanged.
      - [x] Keep import Cancel/source changes and recovery Keep/Discard disabled while reconciliation is unresolved. After a proven safe stop, preserve the current chart/history/bookmarks/markers and require a fresh preview or Keep gesture. Focused 301-test/2,994-assertion matrix passes; two source mutants fail the intended assertions and the restored 74-test control passes.
      - [x] Add native clock fault, actual source counts, exact JSON download, fresh replacement/Keep/playback/Undo, and recovery-storage preservation cases for desktop/phone in all three engines.
      - [x] Complete the 186-case U5 import/recovery/local replacement/accessibility/offline browser matrix: 186 pass, zero unexpected/skipped/flaky, 805419.593ms. All 289 source hashes/mtimes remained unchanged. Final native helper correction passes another 18 overlapping cases (123060.283ms), each with real started sources followed by zero sounding/future sources and zero console/page errors. Reports: `/tmp/jcpe-u5-import-reconcile-browser-matrix-results.json` and `/tmp/jcpe-u5-import-reconcile-native-final-results.json`.
      - [x] Verify the phone recovery-layout repair: the first Chromium probe passed five cases and exposed the fixed recovery banner intercepting Export JSON on the sixth. Place recovery inside the shell layout and modal background; retain real pointer/focus assertions and strict zero-error checks.
      - [x] Finish changed-file type/lint, U5/X1 contract, standalone static and distinct-root/mtime reproducibility checks; rebuild immediately before explicit commits. Source/proof commits: `b2cbc17`, `c6fafee`, `169aac3`, `5b6f161`, `2db99d2`; artifact commit `e8df0de`, SHA-256 `ad03e604f90c1a72cb5652a36aa137dc1381ca4ef4b08cbdb83e9a135e5604f1`, 8,316,364 bytes. Full test typecheck retains exactly 17 U2 errors; source policy retains the U2 private import and accepted E0 bridge pin validation still fails. Full U5 and independent verification remain open.
      - [x] Check the committed `e8df0de` artifact through unchanged deployment gates: `jcpe.deploy.v1`, check-only, modelAcceptance=pass, playback=pass for all 15 instruments. Predeploy quality covers nine instruments with zero failing findings and existing warnings retained (evidence `2fd8f75c5e4ddfb1`). The deployment gate's recovery control is explicitly vacuous; the 18 separate native fault cases supply actual reconciliation evidence.
      - [x] Finish custom-domain synchronization through the current `f7685e9e` artifact on both hosts; see the completed `jcpe-0bjj` checklist above. Earlier intermediate artifacts remain historical evidence, not current production targets.
  - [x] Commit and push the unchanged source, regression tests, and regenerated artifact in separate groups (`7f37d88`, `ddb1389`, `1a81d4a`). Focused checks: 51 tests / 513 assertions; app and browser-test typechecks, changed-file lint, static standalone, and two-root reproducibility pass. Artifact SHA-256 `53321c21c6a01d1b9e18c99de9fa03687d58003899f77f3e5892b84e69fed76c`, 8,314,227 bytes. No production upload.
  - [x] Commit and push the phone/focus, export-first, and clock-retirement fixes in logical groups: `aa13c18` (audio and contract), `65a1edd` (studio and regressions), `43f9902` (guarded artifact). Final artifact SHA-256 `b735a5d050272b6841fa35090a52a15b0af6c4578eb3e7a4344bd5a78c08c21d`, 8,314,648 bytes. All 287 source hashes/mtimes stayed unchanged through the final gates. App/browser typechecks, changed-file lint, X0 contract, standalone static and two-root reproducibility pass. Committed-byte deploy check at `43f9902`: model acceptance passes and all 15 instruments pass real playback; the no-refusal recovery control remains explicitly vacuous. Predeploy instrument quality also passes with zero failing findings; existing warnings remain. Exact commands and earlier failed/interrupted runs are recorded in the U5/build Bead.
  - [x] Rebuild the final reconciliation source with Bun 1.3.14: artifact SHA-256 `d853215c84b5409c4bb9e45700b210ba8e4b1e007ce0dffb5c77f0960bc7cd76`, 8,317,523 bytes, byte-identical root and dist. Source hashes/mtimes stayed stable during the 72-case matrix. The subsequent type-only callback correction emits identical bytes; the instrumented native cases and final type/lint/build checks use the corrected source. Committed-byte deploy check at `be4cfb4` passes model acceptance and all 15 real playback instruments. Predeploy instrument quality passes for nine instruments with zero failing findings; existing warnings remain. The separate playback recovery control is explicitly vacuous, while the six new native fault cases provide actual reconciliation evidence.
  - [x] Finish custom-domain synchronization through the current `f7685e9e` artifact on both hosts; see the completed `jcpe-0bjj` checklist above. Earlier intermediate artifacts remain historical evidence, not current production targets.
  - [x] Resolve `BRIDGE_ACCEPTED_E0_DRIFT` by restoring the immutable E0 v1 archive to its accepted bytes and recording the existing T0-compatible annotated-repeat correction in E0 v2. The unchanged archive/bridge/E0-v2 checks and annotated text-export cases pass in the 521-test suite; no accepted pin was rewritten.
- [x] Verify cold startup discovery after replacing with a different document ID, through native browser storage, reload, Keep and exact JSON export.
- [x] Connect a bounded durable location to the actual recovery adapter, preserving imported IDs. Location-write failure must not certify clean recovery; Discard retains location without chart payloads so an older document is not resurrected. Real A1 service and three-browser cold-start/Discard proof pass.
- [x] Complete the auto-open recovery explanation and Discard/New surface. Automatic-current presentation follows the A1 matrix without overwriting edits or unapplied drafts entered while startup was pending.
  - [x] Reconcile A1/U5 startup fixtures with the starter-chart task: seed only when no recovery exists; render immediately and preserve edits during the probe.
  - [x] Capture failing automatic-open/pending-draft controls before changing production behavior: three application regressions fail at baseline. A later native-browser control also proves that an unapplied title draft must downgrade automatic recovery.
  - [x] Defer the demo until an untouched, empty recovery result; preserve explicit shared charts and session changes during the probe. Raw UI input now supplies an application intent because title drafts do not yet advance document revision or application state.
  - [x] Retain a visible automatic-open explanation; wire Discard to local recovery deletion and New to the existing serialized replacement confirmation, including Cancel and focus restoration. Discard leaves the open chart in memory and cancels its queued recovery write.
  - [x] Prove automatic success, conflicting edits/drafts, previous-copy fallback, Discard failure/success, no boot audio, and real reload/storage/download behavior on desktop and phone. All 36 new cases pass across Chromium, Firefox and WebKit; their diagnostics contain no console/page errors and share the tested artifact hash. All six audio cells observe started native sources, then zero sounding sources and future attacks after both Stop cycles.
  - [x] Run focused gates and production-source mutation controls; rebuild, commit explicit paths, push, and record exact results without closing unfinished U5 acceptance.
    - [x] Retain the full 261-case run: 260 passed and one WebKit import/Undo case exceeded its unchanged 30-second deadline. The stylesheet CSP message began after teardown; Playwright's WebKit screenshot preparation inserts a stylesheet. No CSP or assertion was relaxed.
    - [x] Compare the unchanged failing scenario with the default Mesa renderer and four workers on this 128-CPU host: 19.4/20.3s default controls versus 15.5s with four workers; observed llvmpipe workers fell from 32 to 4. The runner now supplies a bounded Linux default while honoring explicit overrides.
    - [x] Complete the full matrix with the documented renderer default: 261 pass (87 per engine), zero failures/skips/flakes, 2190977.417ms. The original full-run timeout remains retained, not silently converted to a pass. Report: `/tmp/jcpe-u5-auto-browser-bounded-renderer-results.json`; summary: `/tmp/jcpe-u5-auto-browser-bounded-renderer-summary.json`.
    - [x] Pass 259 focused tests / 2,033 assertions across 17 files; kill five production-source mutants; pass app/browser/tools typechecks, changed-file lint, A1/U5 contracts, static standalone and two-root/mtime reproducibility. All 289 source files plus the runner retain their hashes and nanosecond mtimes throughout the final browser gate. The 17 existing U2 test-type errors and other aggregate liabilities remain open.
    - [x] Rebuild immediately and push four logical groups: production/application proof `53535e8`, native browser proof `e2fdac0`, measured renderer resource policy `eb079b0`, and guarded artifact/docs/tracker `675fe5a`. Remote main was verified at `675fe5a03843ac0b8dcca4be3b0c102157a68b20`; GitHub reported no Actions runs for that commit. Root/dist/committed artifact bytes match SHA-256 `b6105ad410c5e70bd9d1959683b462e93dcc517452b68af8849bf66b10da10b1` (8,319,722 bytes).
    - [x] Run the unchanged deployment script against committed bytes: model acceptance and all 15 native playback instruments pass; the recovery subcontrol explicitly reports no reproducible refusal and earns no fault-recovery credit. Cloudflare then refuses authentication. Continue Vercel with the exact gated file and an upload allowlist for only HTML/OG assets; production deployment `dpl_7Azk6bKrWHujdWS1hBerXLe8b5FN` is READY and its public HTTPS bytes match the commit. Logs: `/tmp/jcpe-u5-auto-committed-deploy.log`, `/tmp/jcpe-u5-auto-committed-playback-gate.json`, `/tmp/jcpe-u5-auto-vercel-deploy.log`.
    - [x] Verify the deployed HTTPS application at 1280x900 and 390x844: 12 pass, zero failures/skips/flakes, 139441.149ms. All 12 artifact-bound attachments have zero console/page errors; both native audio cases retain one context after trusted Play and reach zero sounding/future sources after each Stop. Exact JSON, Discard failure/retry/durable reload, New/Cancel focus/Undo, delayed native IndexedDB draft preservation and explicit-share priority pass. Both screenshots were inspected; recovery explanations/actions render and neither viewport overflows horizontally. Report and summary: `/tmp/jcpe-u5-auto-live/{results,summary}.json`.
    - [x] Improve chart visibility at 390x844 and 320x568 while preserving the recovery explanation and precise Discard semantics. The original collapsed chart and completed layout/title repairs are recorded below; current live proof covers both hosts.
      - [x] Measure the smaller supported phone before editing: at 320x568 the recovered chart scrollport collapses to exactly zero pixels, and scrolling a chord cannot make it usable. Preserve the native storage/reload measurements and screenshots in `/tmp/jcpe-u5-phone-before*`.
      - [x] Add failing real-browser regression cases for short fine/coarse phones, tall phone and desktop; measure usable chart space and actual pointer hit targets. Corrected Chromium baseline: one pass / three failures; restored control: four pass. The first draft's invalid corner probes on the circular Play button are retained and corrected to interior axis points.
      - [x] Give compact layouts a scrollable shell frame and a minimum usable chart area while preserving all recovery copy/actions and the persistent transport. The short touch scrollport grows from 0 to 265px and remains 213px after selection. Both CSS mutations fail their intended assertion. The first no-scroll mutant survived programmatic scrolling; the strengthened test now requires measured movement from native wheel input, and that earlier survivor remains recorded.
      - [x] Diagnose the first full matrix honestly: 275 passed / 7 failed, zero skips/flakes. Earlier tail-only progress updates missed Firefox failures and were corrected in the operator update and Bead comment. Preserve `/tmp/jcpe-u5-phone-first-matrix-results.json` and its browser artifacts.
      - [x] Repair Firefox's added scroll-wrapper tab stop so the existing skip link remains first. Remove the new optional screenshot call that makes Playwright inject a WebKit animation-sync stylesheet; retain CSP and every behavior/error assertion.
      - [x] Reproduce the startup title overwrite with native input; synchronize external titles before paint and preserve focused text selections. Add controlled real-UI/application tests for untouched, selected and dirty fields with delayed passive effects: nine engine/case controls pass. The first partial fix still changed an active selection and was not accepted; the completed native quota diagnostic preserves all 12 exact edits.
      - [x] Catch and repair the over-broad focus guard: confirmed New deliberately focuses the title field, so the guard retained the old chart's title. Bind preservation to document identity and reset on replacement, including equal-title replacements. The first expanded run was stopped after two New failures (37 passed, two failed, 252 not run); retain `/tmp/jcpe-u5-phone-interrupted-title-replacement*` as an interrupted diagnostic, not a release pass.
      - [x] Prove title scheduling counterfactuals after the document-boundary fix: each removed safeguard fails its intended case (two pass / one fail), with exact source/mtime restoration.
      - [x] Diagnose the next full run (290 passed / one failed, zero skips/flakes): the export-refusal test captured the initial blank title, but the trace shows the demo and revision 3 already published before its first Export click. Add an explicit default-startup precondition before that baseline; preserve all original cancellation/failure/marker/title/focus assertions. All six engine/viewport controls pass. Retain `/tmp/jcpe-u5-phone-export-baseline-failure*` and `/tmp/jcpe-u5-export-baseline-control-results.json`.
      - [x] Complete the fresh 291-case browser gate: 97 pass per engine, zero failures/skips/flakes, 1285188.407ms. All 295 source/test snapshot entries remain unchanged; all 12 viewport diagnostics bind SHA-256 `f7685e9ebf752fb8ce83ef907648690ed768a05839b39860e1490675cab15b3e` with zero console/page errors. Reports: `/tmp/jcpe-u5-phone-final-matrix-{results,summary}.json`. Earlier failed/interrupted runs remain retained.
      - [x] Verify exact recovered JSON, chord selection, recovery action reachability, New/Cancel/Undo, title scheduling and native storage/audio through the responsive/recovery/accessibility/offline matrix. App/browser types, changed-file lint, static standalone and reproducibility pass. The immediate precommit rebuild reproduces root/dist at 8,319,874 bytes and the same SHA-256 above.
      - [x] Commit and push explicit groups: layout `87d7161`, title timing/selection/document boundary `b993806`, export-test startup precondition `1f99b8f`, artifact/docs/tracker `11ef504`. Remote main matched `11ef50462a70f97a5c72c23d7f898d134177bce2`; GitHub reported zero Actions runs (no workflow directory). Ephemeral reports/caches remain untracked.
      - [x] Pass predeploy acceptance (11 shipping models), mechanical quality (nine rendered instruments, zero failing findings) and native playback (15 instruments) against committed bytes. The playback recovery subcontrol is explicitly vacuous and earns no fault-recovery credit. The immediate predeploy rebuild and gated stage match `git show HEAD` exactly. Logs: `/tmp/jcpe-u5-phone-{predeploy-check,committed-deploy}.log`; native ledger: `/tmp/jcpe-u5-phone-committed-playback-gate.json`.
      - [x] Deploy the same gated stage to Vercel: `dpl_FEpsdUV6mKzhhu9dKWJqCj4vNMsv` is production READY; public HTTPS returns the exact committed SHA-256 above. Upload allowlist includes only HTML/OG assets; the new stage's CLI-created token file was removed afterward. The combined deploy command failed at Cloudflare authentication `10000`, and the authorized mirror upload was continued separately.
      - [x] Verify six live Chromium cases (four viewport/input cells plus desktop/phone New/Cancel/Undo): six pass, zero failures/skips/flakes, 27357.244ms. All six diagnostics bind committed bytes with zero console/page errors; phone and desktop screenshots were inspected. Reports: `/tmp/jcpe-u5-phone-live/{results,summary}.json`. The first live launcher failed before running tests because its Git output buffer was too small; the temporary adapter's capture bound was corrected, with the original setup failure retained and all assertions unchanged.
      - [x] Complete Cloudflare publication under `jcpe-0bjj`: both hosts serve the committed `f7685e9e` artifact, with successful provider receipts and live desktop/phone/recovery/audio proof recorded above. The former `10000` blocker is resolved; full U5 acceptance remains separate.
  - [x] Validate checksum-accepted recovery candidates through F2/F3 before offering them, in current/previous order; retain slot-specific refusal details and never modify storage during selection.
  - [x] Prove structurally/semantically invalid current, invalid previous, both invalid, valid-current precedence, no automatic previous opening, stale Keep and actual successful previous Keep. The 19-case orchestrator suite passes; five temporary production-source mutants fail the intended assertions and the unchanged control passes.
  - [x] Verify native browser storage, checksum-valid semantic corruption, exact recovered JSON, pending-offer byte preservation and cold reload in all three browsers. Final matrix: 123 pass, zero failures/skips/flakes, including all 12 new cases; final focused suite: 92 pass / 761 assertions. Artifact SHA-256 `bc42718621e69cb42f3261ce836a44405fe57f6f6b67c53157ce7e5e0f433c81`; source unchanged throughout, standalone/static/reproducibility checks pass. Full U5 and the existing aggregate failures remain open.
- [x] Keep chart-text import on the insertion lane; preserve MIDI's separate workflows. Section-headed text reaches the real Quick entry draft on desktop and phone without replacing the document.
- [x] Exercise recovery write/discard races and native browser recovery/JSON downloads at desktop and phone widths across Chromium, Firefox and WebKit, including allocation failure and focus restoration.
- [x] Finish the import slice gates: 113 focused tests / 852 assertions; 144 real-browser cases across Chromium/Firefox/WebKit and desktop/phone; app typecheck, all 24 touched TypeScript files linted, standalone static check and two-root reproducibility. Exact commands, artifact hash and prior failure dispositions are in the U5/build Bead. No deployment.
- [x] Replace Firefox's empty constructed clipboard event with native clipboard/keyboard proof. Fix the observed unsafe ephemeral port in the existing offline negative-control server with bounded high-port allocation; preserve every negative assertion, zero retries and CSP.
- [x] Exercise New/lesson, oversized consent, active-preview replacement, storage fallback and the complete lifecycle matrix; final U5 verification evidence above.
  - [x] Finish the import confirmation-owner lifetime slice and final browser evidence; the complete U5 package remains open.
    - [x] Reproduce removed/replaced A0 hosts and false/non-boolean/throwing UI consent: 24 passing import tests and five new failing regressions before the fix.
    - [x] Bind publication to the exact committing A0 descriptor, captured DOM trigger and dialog, and explicit invalidation. Check consent only at the synchronous real owner port after proven X1 retirement; cancel the actual request and let A0 consume/refuse its stale capability. Preserve the accepted E0 driver and result/receipt protocols.
    - [x] Preserve document/history/bookmarks/export/recovery state and the next valid import. A failed old attempt cannot close/update a later same-ID host; unknown retirement still requires real reconciliation. Pristine direct import uses the same guard, and busy reopening cannot replace its consent.
    - [x] Pass 309 focused tests / 3,057 assertions across 28 files. Kill three temporary source mutants (omit request revocation, loosen exact host identity, ignore host invalidation); restore bytes/mtimes and pass the 55-test/397-assertion control.
    - [x] Add real playing-audio browser cases for hidden/replaced triggers and hidden dialogs, exact downloads/Undo, visible focus, actual native source retirement and a fresh import. Initial Chromium desktop/phone probe: six passes.
    - [x] Complete the final 144-case import/recovery/accessibility/offline matrix: 144 pass, zero unexpected/skipped/flaky, 672755.314ms across Chromium 149.0.7827.55, Firefox 151.0 and WebKit 26.5 at desktop/phone widths. All 18 new owner-loss cases observe started native sources followed by zero sounding/future sources. All 132 attached error diagnostics are clean, and all 289 source hashes/nanosecond mtimes remained unchanged. Report: `/tmp/jcpe-u5-import-owner-browser-matrix-results.json`.
    - [x] Pass app/browser typechecks and changed-file lint after correcting closure-state narrowing and validating the cloned DOM node type. Full test types retain only the 17 existing U2 errors; source policy retains the existing U2 private import. Standalone/static and separate-root/mtime reproducibility pass.
    - [x] Rebuild immediately before logical commits and push application/regression source (`06be5c3`), UI/native-browser proof (`34869aa`) and guarded root artifact (`7f224ee`). Root/dist/committed bytes match SHA-256 `9803b439c4f38ba6405e16b18ba641d294974dabdfd484647215a7cfcdd9b017`, 8,318,093 bytes. The original deploy script's committed-byte `--check` passes model acceptance and all 15 real playback instruments at `7f224ee`; its no-refusal recovery control is explicitly vacuous. Log: `/tmp/jcpe-u5-import-owner-committed-deploy-check.log`. Remote main was verified at the artifact commit before recording this evidence.
    - [x] Finish custom-domain synchronization through the current `f7685e9e` artifact on both hosts; see the completed `jcpe-0bjj` checklist above. Earlier intermediate artifacts remain historical evidence, not current production targets.
- [ ] Resolve the repository-wide gate findings through their owned tasks: 17 U2 test type errors, the existing U2 private application import, and 33 existing cast-policy findings. Focused import evidence does not certify the aggregate gate.
- [x] Complete named build gates and the separate U5 verification phase; record final evidence above. No human acceptance flags changed.

## Complete unresolved tracker inventory

Statuses below are the campaign-start snapshot. Use br show and br ready before claiming; an existing assignee is not an invitation to take over.

- [ ] `jcpe-bass-vibes-temporal-character-9g4t` — Bass+vibes physical models: temporal character rejected by owner (thump/bloom, shimmer/beating) (in_progress; P0; CreamBass)
- [ ] `jcpe-cast-policy-compatibility-np17` — P0 compatibility: restore honest cast-policy conformance (open; P0; ubuntu)
- [ ] `jcpe-darl` — Native Apple correctness: theory closure, audible inversions, and promised platform paths (blocked; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1` — Epic: Release Proof — audited standalone artifact (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2` — R0: Produce the standalone artifact, documentation, and release evidence (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2.1` — R0/spec: Specify interfaces, limits, and independent fixtures (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2.2` — R0/build: Implement the complete production package (open; P0; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.2.3` — R0/verify: Prove conformance, integration, and evidence (open; P0; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a` — Epic: Reliable Studio — author, recover, import, play, and stop (open; P0; unassigned)
- [ ] `jcpe-playback-gate-ship-failures-u90y` — Ship candidate: dreadnought mid-chart refusal + archtop/ukulele no pitch lock (in_progress; P0; unassigned)
- [ ] `jcpe-winds-quality-triangulation-drga` — Winds quality: triangulated fix for owner-rejected flute@2/clarinet@2 timbre (in_progress; P0; unassigned)
- [ ] `jcpe-winds-reference-quality-qb68` — Winds: reference-judged realism gate for clarinet v2 + flute (in_progress; P0; TurquoiseBluff)
- [ ] `jcpe-2nky` — Disable Cloudflare Web Analytics beacon injection for jazzchords.org (CSP correctly blocks it; edge modifies released bytes) (open; P1; unassigned)
- [ ] `jcpe-60xy` — X0 human listening session: author release-evidence/audio/listening/x0-listening-v1.json (open; P1; unassigned)
- [ ] `jcpe-765f` — M1: Idiot-proof automated MIDI import — one gesture from .mid to playable chart with matched groove (open; P1; unassigned)
- [ ] `jcpe-bv2b` — FrankenSim-powered next-gen instrument physics: ultra-realism campaign (owner criteria 2026-08-09) (in_progress; P1; unassigned)
- [x] `jcpe-discovery-execution-proof-gdcw` — Discovery execution: truthful bounded jobs and atomic proof validation (closed;6dd9ee3; all three phases complete)
- [x] `jcpe-discovery-execution-proof-gdcw.1` — Discovery execution specification (closed;359b02c; exact gates recorded in Beads)
- [x] `jcpe-discovery-execution-proof-gdcw.2` — Discovery execution build (closed;55608ae; exact gates recorded in Beads)
- [x] `jcpe-discovery-execution-proof-gdcw.3` — Discovery execution verification (closed;6dd9ee3; exact named gates in Beads)
- [ ] `jcpe-espf` — HUMAN: audition Upright Bass + Concert Vibes on the live studio (jcpe-1miv acceptance) (open; P1; unassigned)
- [x] `jcpe-ionn` — M1/spec: Freeze automated-import laws and reconcile tied-key evidence (closed; P1; CodexRuntime; `9ff4de5`/`97aed83`)
- [ ] `jcpe-ios-quality-verification-uo47` — FrankenJazz iOS: quality, honesty, and recorded verification (in_progress; P1; unassigned)
- [ ] `jcpe-j4hj` — Instrument selection requires manual refresh: newly selected physical instruments do not take effect live (in_progress; P1; SwiftBridge)
- [ ] `jcpe-milestone-advanced-craft-ulj` — Epic: Advanced Craft — routes, constraints, reharmonization, and practice (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy` — Epic: Musical Intelligence — explainable continuation and Harmonic Atlas (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2` — H0: Implement evidence-tier context readings and chord-scale tables (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2.1` — H0/spec: repair five executable authority conflicts (reopened; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2.2` — H0/build: Implement the complete production package (blocked by H0/spec; P1; CodexImplementation; literal projection implemented, full package incomplete)
- [ ] `jcpe-milestone-musical-intelligence-qqy.2.3` — H0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3` — H1: Build proof-carrying transformation laws and spelled transposition (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3.1` — H1/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3.2` — H1/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.3.3` — H1/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4` — G0: Implement phrase analysis and k-best Tonal Journey paths (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4.1` — G0/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4.2` — G0/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.4.3` — G0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5` — G1: Build Atlas schema/compiler, provenance, facets, and fingerprints (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5.1` — G1/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5.2` — G1/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.5.3` — G1/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6` — G2: Implement the explainable Contextual Continuation Engine (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6.1` — G2/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6.2` — G2/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.6.3` — G2/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7` — D0: Author and compile the reviewed 250-seed Harmonic Atlas (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7.1` — D0/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7.2` — D0/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.7.3` — D0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8` — U3: Build the evidence-first Harmony Lens and motion explanations (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8.1` — U3/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8.2` — U3/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.8.3` — U3/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9` — U8: Build Continuation, Atlas, Similarity, and Tonal Journey UI (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9.1` — U8/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9.2` — U8/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-musical-intelligence-qqy.9.3` — U8/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1` — Q0: Run the complete regression, accessibility, visual, and performance proof (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1.1` — Q0/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1.2` — Q0/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-milestone-release-proof-pv1.1.3` — Q0/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11` — U2: Build semantic chord inspector, exact piano, and real preview (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11.1` — U2/spec: Specify interfaces, limits, and independent fixtures (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11.2` — U2/build: Implement the complete production package (in_progress; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.11.3` — U2/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.12` — U4: Build transport controls and truthful audio status (open; P1; unassigned)
- [ ] `jcpe-milestone-reliable-studio-l3a.12.2` — U4/build: Implement the complete production package (in_progress; P1; IvoryBluff)
- [ ] `jcpe-milestone-reliable-studio-l3a.12.3` — U4/verify: Prove conformance, integration, and evidence (open; P1; unassigned)
- [x] `jcpe-milestone-reliable-studio-l3a.13` — U5 lifecycle package (all three phases complete; final evidence above)
- [x] `jcpe-milestone-reliable-studio-l3a.13.2` — U5/build: Implement the complete production package (closed; verification complete)
- [x] `jcpe-milestone-reliable-studio-l3a.13.3` — U5/verify (complete; scoped native and mutation proof above)
- [ ] `jcpe-mnsc` — Epic: Gesture-driven physical instrument realism (open; P1; unassigned)
- [ ] `jcpe-mnsc.4` — PHS2: Clarinet v2 dynamic reed, tongue, tone holes, and continuous bore (open; P1; unassigned)
- [ ] `jcpe-mnsc.4.3` — PHS2/verify: Prove conformance, integration, and evidence independently (in_progress; P1; ubuntu)
- [ ] `jcpe-mnsc.5` — PHS3: Flute v2 embouchure, tone-hole lattice, fingering, and continuous air column (open; P1; unassigned)
- [ ] `jcpe-mnsc.5.2` — PHS3/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.5.3` — PHS3/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.6` — PHS4: Coupled guitar family: strings, bridge/body, excitation, pickups, and amps (open; P1; unassigned)
- [ ] `jcpe-mnsc.6.2` — PHS4/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.6.3` — PHS4/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.7` — PHS5: Trumpet outward-striking lips, valves, bore, bell, and brass nonlinearity (open; P1; unassigned)
- [ ] `jcpe-mnsc.7.2` — PHS5/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.7.3` — PHS5/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.8` — PHS6: Physical or hybrid vibraphone: bars, mallets, resonators, fans, and pedal (open; P1; unassigned)
- [ ] `jcpe-mnsc.8.2` — PHS6/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.8.3` — PHS6/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.9` — PHS7: Whole-system physical synthesis acceptance, documentation, and release evidence (open; P1; unassigned)
- [ ] `jcpe-mnsc.9.2` — PHS7/build: Implement the complete production package (open; P1; unassigned)
- [ ] `jcpe-mnsc.9.3` — PHS7/verify: Prove conformance, integration, and evidence independently (open; P1; unassigned)
- [ ] `jcpe-mnsc.9.4` — HUMAN: audition the complete gesture-driven physical instrument matrix (open; P1; unassigned)
- [ ] `jcpe-qbvz` — M1/verify: Independent proof over real multi-track MIDI — conformance, ear-check, and one-undo evidence (open; P1; ubuntu)
- [ ] `jcpe-rh3a` — U0 human accessibility session: attach the six manual observations to the evidence ledger (open; P1; unassigned)
- [ ] `jcpe-sample-elimination-physical-qzgo` — Replace CC0 sample recipes with physical wasm models (vibes, bass; scope piano) (in_progress; P1; unassigned)
- [ ] `jcpe-sample-elimination-physical-qzgo.6` — Replace Salamander piano attack with cooperative physical WASM (blocked; P1; unassigned)
- [ ] `jcpe-semantic-surface-conformance-32w2` — Semantic conformance: real engine wiring across studio and native bridge (open; P1; unassigned)
- [x] `jcpe-semantic-surface-conformance-32w2.1` — Semantic conformance specification (closed; a4713bc; exact gates in Beads)
- [x] `jcpe-semantic-surface-conformance-32w2.4` — Existing studio continuation context repair (closed;54f371f; full package remains open)
- [ ] `jcpe-semantic-surface-conformance-32w2.2` — Semantic conformance: real engine wiring across studio and native bridge / build (open; P1; unassigned)
- [ ] `jcpe-semantic-surface-conformance-32w2.3` — Semantic conformance: real engine wiring across studio and native bridge / verify (open; P1; unassigned)
- [ ] `jcpe-upbz` — M1/build: Implement the automated pipeline and the two-mode import UI (Automatic default, Advanced disclosure) (open; P1; ubuntu)
- [x] `jcpe-voice-leading-continuity-0rgn` — Fix audible voice-leading discontinuities and gap-free scoring exploit (GH #2) (closed; P1; unassigned)
- [x] `jcpe-voice-leading-continuity-0rgn.1` — Voice-leading continuity / spec (closed; P1; CodexImplementation)
- [x] `jcpe-voice-leading-continuity-0rgn.2` — Voice-leading continuity / build (closed; P1; CodexImplementation)
- [x] `jcpe-voice-leading-continuity-0rgn.3` — Voice-leading continuity / verify (closed; P1; CodexImplementation)
- [ ] `jcpe-wyw5` — M1 human listening notes: audition the two imported reference charts under the matched groove (open; P1; unassigned)
- [ ] `jcpe-z62r` — What a Fool Believes + syncopated-sixteenths@1 landed; owner listening is the remaining gate (in_progress; P1; unassigned)
- [ ] `jcpe-0bjj` — Playback-gate flake under CPU contention: first instrument (mellow-keys) intermittently refuses (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10` — U6: Progression voice-leading workbench (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10.1` — U6/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10.2` — U6/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.10.3` — U6/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.11` — U7: MIDI export workflow (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.11.2` — U7/build: Implement the complete production package (in_progress; P2; SwiftBridge)
- [ ] `jcpe-milestone-advanced-craft-ulj.11.3` — U7/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12` — U9: Route, Constraint, and Reharmonization workbench UI (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12.1` — U9/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12.2` — U9/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.12.3` — U9/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13` — U10: Guide, color, rhythm, tension, sequence, and nonfunctional discovery UI (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13.1` — U10/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13.2` — U10/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.13.3` — U10/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14` — U11: Practice Laboratory UI (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14.1` — U11/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14.2` — U11/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.14.3` — U11/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15` — D1: Curate lessons, practice prompts, palette, help, and keyboard guide (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15.1` — D1/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15.2` — D1/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.15.3` — D1/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3` — G3: Goal-directed Harmonic Route Planner (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3.1` — G3/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3.2` — G3/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.3.3` — G3/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4` — G4: Constraint Harmonization Workbench engine (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4.1` — G4/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4.2` — G4/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.4.3` — G4/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5` — G5: Proof-carrying reharmonization branches (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5.1` — G5/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5.2` — G5/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.5.3` — G5/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6` — G6: Guide-tone paths and contextual Color Laboratory engine (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6.1` — G6/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6.2` — G6/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.6.3` — G6/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7` — G7: Cadence, phrase, harmonic-rhythm, and tension-curve transforms (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7.1` — G7/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7.2` — G7/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.7.3` — G7/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8` — G8: Motif, sequence, and nonfunctional transformation atlas (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8.1` — G8/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8.2` — G8/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.8.3` — G8/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9` — G9: Deterministic Chart-to-Practice Laboratory engine (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9.1` — G9/spec: Specify interfaces, limits, and independent fixtures (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9.2` — G9/build: Implement the complete production package (open; P2; unassigned)
- [ ] `jcpe-milestone-advanced-craft-ulj.9.3` — G9/verify: Prove conformance, integration, and evidence (open; P2; unassigned)
- [ ] `jcpe-dsp-flute-c7-44k1-regime-c4p1` — Flute MIDI 93/96 at 44.1/48 kHz: sub-14-sample jets refuse fractional tuning (regime flips) (open; P4; unassigned)
- [x] `jcpe-zw47` — Batch local MIDI import: score user-supplied candidate files and auto-select the best (closed; P4; CodexRuntime; 1bd322d)

### jcpe-ev4q — deploy legacy JSON reply normalization

- [x] Claim deployment leaf and preserve unrelated component work.
- [x] Pin committed artifact, 1,842 inputs and source mtimes.
- [ ] Strict RCH guarded build and model/quality gate.
- [ ] Native 15-instrument playback gate.
- [ ] Final guarded rebuild and unchanged-input check.
- [ ] Upload committed bytes to Cloudflare Pages and Vercel; poll HTML/image hashes.
- [ ] Real desktop/phone JSON export checks on both hosts; retain diagnostics.
- [ ] Record exact results, close leaf and sync tracker.
