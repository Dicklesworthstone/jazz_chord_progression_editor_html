# Changes reality check — 2026-09-08

**September 9 M1 repair (`9ff4de5`/`97aed83`):** the tied-key contradiction below is resolved by full-set equivariance and unique-only automatic selection. The actual importer now discloses ties, retains the original presentation counterexample, and offers an explicit Key override; unresolved ties withhold key transfer. Independent rotated fixtures, actual decoder/controller/Undo proof,57 import browser cases and36 standalone cases pass. The M1 specification task closes; broader M1 acceptance, source-note review and human listening remain open. The dated observations below are retained as history; this update is not a new full-audit or release claim.

**Subsequent evidence-retention incident:** during the next implementation task,
an admission test fixture omitted Playwright's `outputDir`, and its default
cleanup removed the ignored `test-results/` tree. Raw A and loop-final packets
referenced below are consequently unavailable; their values remain historical
observations recorded in this report and Beads, not currently retained raw proof.
The fixture is being corrected under `jcpe-browser-suite-admission-0nyi` and its
new evidence/tools are isolated under `.tmp/`. Tracked source and evidence were
untouched. This notice supersedes raw-retention statements in the dated snapshot.

Changes is a useful working studio with substantial editing, playback, recovery,
interchange and voicing foundations. It is not yet the complete, independently
proved harmonic-discovery system described by its plans. Since September 4,
several useful workflows and a shared execution service have landed. The main
remaining gap is musical correctness and actual consumer integration, followed
by complete acceptance. It is not simply a collection of finished engines waiting
for buttons.

The public sites also lag the repository. Both serve the same earlier committed
build. The current candidate fails the mandatory model-source gate; uploading it
again is not the remedy. This audit made no production-code or generated-artifact
change and performed no deployment.

## Scope, authority and method

Assessment Bead: `jcpe-reality-check-september-5h2l`. Production baseline:
`9964f766d7755d548177239819f4a59eebf64de3`. This replaces the September 4 snapshot
in place; its historical findings and tracker changes remain in Git history.

The complete AGENTS.md and README.md were read before assessment. Vision was
extracted from the complete architecture, rebuild, theory-idea, original
idea-wizard, September product-idea, physical-synthesis and Apple plans, plus the
legacy audit. Relevant discovery execution, cross-surface, U2, rehearsal,
deployment and current native/lifecycle evidence were inspected. This is not a
claim to have reread every historical contract/evidence Markdown file or every
line of the codebase. Current tracker records, production call sites and direct
counterexamples take precedence over old summaries.

All reality-check phases were applied: vision extraction, source/test/runtime
assessment, gap classification, dependency-ordered bridge, Phase 3a Beads,
three ambition rounds and six refinement passes. The anti-ceremony creation,
real-work and honesty worksheets apply to this explicitly requested assessment:
the report and tracker corrections earn **zero implementation credit**. This is
solo review using independently authored existing fixtures, not an independent
person's certification.

Evidence is retained locally in ignored
`test-results/reality-check-2026-09-08/` (called **A** below). It includes raw
commands, failed results, semantic expected/actual values, browser requests,
console/page errors, actual download hashes, AudioContext/source counts and
screenshots. The report preserves the decisive values and commands so that loss
of temporary evidence does not turn a historical observation into a fresh pass.
No actual user chart was uploaded; browser cases used the bundled demo and a
synthetic title. All network requests used the required user agent.

The audit used Bun **1.3.14**, real Node **26.0.0**, Playwright **1.61.1** and
Chromium **149.0.7827.55**. The shell's newer Bun was not used for acceptance.
No foreign browser process was killed and no second test suite was raced.

## What changed since the previous assessment

| Change | Current evidence and limit |
|---|---|
| U5 lifecycle | Specification, build, proof and epic are now closed; current local browser exercises real JSON delivery and collection storage. The old statement that U5 is still unimplemented is obsolete. |
| U2 exact voicings | Specification and build are closed; choose/hear/keep, seven inspector views and the 16-note Manual/Frozen editor exist. Independent package proof remains in progress with IvoryBluff. |
| Four September workflows | Entry repair, Focus chart, exact sharing and My Charts have complete package records. Current local artifact exposes the newer workflows; the hosted artifact predates them. |
| Shared discovery execution | `jcpe-discovery-execution-proof-gdcw` and all three phases are closed. A real generic bounded job/application/audio protocol exists. Its proof is not proof that G0–G9 consume it. |
| H0 literal facts | `deriveLiteralFacts` now exists. Previously contradictory H0 fixtures were repaired. Contextual analysis and scale-option callables are still absent; H0 specification/build retain their aggregate blockers. |
| Truthful small suggestions | The false C-major-containment claim no longer reproduces in the live web suggestion API. This does not complete the deeper continuation engine. |
| Runtime integration | Memoization, arrangement-loop and notification-authority fixes have meaningful focused and real-adapter evidence. Their remaining named timing failures stay open. |
| Mechanical hygiene | Retained September 8 typecheck/lint and 12-case native-adapter evidence correspond to the unchanged recorded source inputs. A current cast-policy replay is still red. |

Do not derive a project-completion percentage from closed issue counts. One
closed generic protocol and a complete musical workflow are different outcomes.

## Committed source and public delivery

| Surface | Bytes | SHA-256 | Interpretation |
|---|---:|---|---|
| HEAD root HTML | 8,431,411 | `c20c0b1f029e3abd3d6eb03f4e7c374a607fb4a7b00fdbd29bd369eb94f316b3` | Current repository candidate |
| jazzchords.org, downloader response | 8,319,874 | `f7685e9ebf752fb8ce83ef907648690ed768a05839b39860e1490675cab15b3e` | Exact artifact at `11ef50462a70f97a5c72c23d7f898d134177bce2` |
| Vercel mirror | 8,319,874 | same as custom-domain downloader response | Same earlier committed artifact |

Comparison used Git objects, not a freshly generated working-tree file. No
Cloudflare authentication failure was demonstrated in this audit. The observed
problem is older deployed bytes plus a blocked newer candidate.

Cloudflare's real-browser response also contains its injected analytics script;
its browser-response hash is
`b243b5d6e3a14ec27579f0495f665cc76272dd79193e867f6e775805f97205f8`.
The existing CSP blocks the script and emits the expected console error. Curl
does not reproduce that injection. `jcpe-2nky` remains the removal task; do not
weaken the CSP or describe the custom domain as console-error-free.

The accepted artifact ceiling is **9 MiB**, including a **512 KiB Atlas
reservation**, not the older 1.5 MiB figure in inherited release prose. Current
total headroom is 1,005,773 bytes; only 481,485 bytes remain inside the separate
8,912,896-byte shell allocation. The reviewed corpus must fit its reservation;
this assessment grants no budget increase.

## Vision checklist

WORKING means observed usable production behavior within the stated scope.
PARTIAL means real implementation with missing scope. UNPROVEN means the full
named acceptance is absent. WRONG_APPROACH means source behavior contradicts
the contract, even when the function has a plausible name. STUB includes
fabricated metrics or pass-through success, not every empty callback.

Package abbreviations resolve through the exact-ID table later in this report.

| # | Testable promise and authority | Current classification and evidence | Remaining owner / success boundary |
|---:|---|---|---|
| 1 | One self-contained offline HTML, pinned Preact-only runtime, reproducible build (F0/R0) | WORKING local file/HTTP boot with network denied; generated source architecture exists. Full release reproducibility/source closure UNPROVEN because predeploy rejects DSP drift. | F0/R0, u90y; exact committed-byte rebuild and mandatory gates |
| 2 | Spelling-first identity, stable IDs, exact rational time (F1/F2/T0/T1/F3) | WORKING foundation; PARTIAL system-wide adherence. H1/G7/G8 violate preservation when transforming valid inputs. | Existing engine repairs; positive/near-miss/transposed/mutated law fixtures |
| 3 | Fast paste/type/edit of bar-delimited charts and exact durations (U1) | WORKING editor and diagnostic repair paths. Fresh title edit/Undo/Redo passes; entry repair has completed package proof. | Q0 integrated editing/IME/hostile-input regression |
| 4 | Distinct selection, insertion, playhead, history and transactional publication (A0/U1) | WORKING current editor architecture; later async consumers still need to use it correctly. | Shared application intents and stale Apply/Undo proof |
| 5 | Seven-part inspector and exact 16-note Manual/Frozen choose/hear/keep (U2) | PARTIAL acceptance: spec/build closed, production inspector exists; proof in progress. Duplicate notes and source spelling must survive. | U2 proof; do not advertise the editor as absent |
| 6 | Literal facts, plural contextual readings and explicit scale evidence (H0/U3) | PARTIAL: literal facts implemented; `analyzeChordInContext` and `enumerateChordScaleOptions` NOT_STARTED at public boundary. Existing Harmony Lens is smaller. | H0 spec/build currently BLOCKED, then proof/U3 |
| 7 | Deterministic voicings, optimal assignment, bounded progression optimization (V0/V1/V2/U6) | WORKING foundation engines; complete selection/comparison/voice-motion workbench PARTIAL. V2 has declared beam/window limits, not unrestricted global optimality. | U6 and independent finite-oracle verification |
| 8 | One immutable musical plan shared by audio and MIDI (P0/E1) | WORKING plan compiler and export paths. No permission for suggestions to invent a second note timeline. | U7 plus exact preview/audio/export integration |
| 9 | Persistent graph, serialized transport and immediate Stop ownership (X0/X1/U4) | WORKING basic native Play/Stop in fresh local/host browsers; complete timing/listening acceptance UNPROVEN. | U4, runtime leaves, X0 human listening |
| 10 | Count-in, metronome, loops, seek and live controls preserve arrangement (U4/rehearsal) | PARTIAL: extensive controls/fixes, but current retained regression packets have13+1 timing failures. U4 parent was false-closed while build/proof remained open. | Reopened U4 parent; preserve IvoryBluff leaves and rehearsal .5 |
| 11 | Local recovery with conflict/previous-copy fallback and visible failure (A1/U5) | WORKING completed lifecycle package and real persistence paths. Best-effort storage is not durable Save. | Preserve U5 regressions; Q0 denied/quota/stale-writer cases |
| 12 | Safe New/import replacement and exact canonical JSON/text export (E0/C0/U5) | WORKING lifecycle and download adapters; fresh downloaded JSON parsed with expected title/events. General publication-policy violations remain. | np17 plus exact round-trip/marker/replacement proof |
| 13 | Bounded MIDI parsing, uncertain multi-track interpretation, exact-note preservation (M0/M1) | PARTIAL: real Rust/WASM import and review; M1 unqualified tied-winner equivariance is mathematically inconsistent. | M1 spec/build/proof, owner listening/DAW review |
| 14 | Real MIDI download from the shared plan with truthful losses (E1/U7) | WORKING production workflow and prior package evidence; fresh audit did not decode a new SMF download. | U7/Q0 exact note/time and human external-player acceptance |
| 15 |250 reviewed Atlas seeds/3,000 variants, valid hashes and rights firewall (G1/D0) | WRONG_APPROACH compiler/hash/query behavior; reviewed large corpus NOT_STARTED as a complete deliverable. Small progression library is not D0. | G1 repair, D0 reviewed content, bounded size/provenance proof |
| 16 | All fifteen discovery workflows (§11.9) | PARTIAL/STUB/WRONG_APPROACH as itemized below; no claim of complete full-contract delivery. | G0–G9 plus U8–U11 and D0/D1 |
| 17 | Lossless spelled chart/range/section transposition with explicit exact-pitch policy (H1) | WRONG_APPROACH:6/9 lost and Unicode roots unchanged; the promised web workflow is NOT_STARTED. | H1 then transpose-workflow, never reuse the broken string replacement |
| 18 | Reviewed lessons and accurate practice grading (D1/G9/U11) | PARTIAL starter/help/library; complete lesson corpus absent and grading can contradict itself. | D1/G9/U11; subjective listening separate from objective grading |
| 19 | Accessible keyboard/touch/zoom/reduced-motion desktop and phone UX (U0/Q0) | PARTIAL evidence: fresh1280/390px boot has no horizontal overflow; full browser/device/accessibility acceptance UNPROVEN. | Q0 plus human accessibility; inspect crowded default phone header |
| 20 | Deterministic work/state/memory bounds, cancellation and responsive discovery (§11.9) | WORKING generic protocol; STUB engine counters and missing musical adoption remain. | Completed gdcw consumed by repaired engines and semantic-surface package |
| 21 | Physical-model fidelity, continuous gestures and exact source/WASM/pack binding (PHS) | PARTIAL shipping instruments; predeploy binding REGRESSED/blocked on two DSP source changes; human quality remains open. | u90y and existing PHS/instrument leaves; no duplicate DSP program |
| 22 | Complete retry-free release gates and verified delivery on both hosts (Q0/R0) | UNPROVEN/red: aggregate process guard, cast policy, timing and source-binding blockers; public artifact behind. | Existing release work plus new browser-admission bug |
| 23 | Native iPhone/iPad/Mac editing/playback/recovery/interchange sibling (Apple plan) | PARTIAL substantial SwiftUI/AVAudioEngine implementation and historical Apple proof; full discovery semantics and current release acceptance UNPROVEN. | uo47/native leaves, regenerated semantic bridge, actual Apple execution |
| 24 | No runtime AI/network/telemetry or silent repair (AGENTS) | WORKING local architectural boundary; host injects blocked analytics. Silent musical repairs in transformation code still violate exactness. |2nky and H1/G7/G8 refusal laws; keep CSP strict |

## Every discovery promise

The source audit followed actual exports, the studio controller, application
services and native bridge. A function existing in `src/theory` is insufficient.
The generic execution service is finished; the current studio still calls the
smaller `deriveContinuationSuggestions`. The native v1 bridge directly calls G2
with invented four-beat events and a smaller symbol-only response. Local native
revision guards do not make that response the complete shared semantic protocol.

| HD | User outcome | Current engine gap | Complete delivery path |
|---|---|---|---|
|01 | Contextual next chords | G2 ranks fixed last-chord templates with constant scores; full context/provider/constraint search missing. Web containment explanation was repaired separately. | H0/G2/U8 + semantic-surface |
|02 | Validated progression Atlas/compiler | Nonempty SHA wrong; source-hash validation bypassable; fingerprint-only content retains expression; manifest ignores changed payload. | G1/D0/U8 |
|03 | Goal-directed routes | G3 fixed templates/costs and synthetic visited-state counts do not implement bounded graph search. | G3/U9 |
|04 | Constraint harmonization | G4 varies first slot only, chooses remaining first candidates and accepts contradictory pins; counts/costs fabricated. | G4/U9 |
|05 | Reharmonization branches | G5 limited rule tree lacks complete intermediate-law validation and can report requested depth instead of reached depth. | G5/U9 |
|06 | Multi-hypothesis tonal journey | G0 emits one heuristic path with fixed confidence, lacks complete k-best/no-key/pinned lattice. | G0/U8 |
|07 | Guide-tone line design | G6 greedy pitch-class movement cannot establish global registered, noncrossing, pinned voice paths. | G6/U10 |
|08 | Contextual colors/upper structures | G6 key/role handling and clash evidence incomplete; unsupported spelling can be clamped instead of refused. | H0/G6/U10 |
|09 | Cadence/phrase/approach construction | G0 misses G7→Am deceptive motion; H1/G7 exact construction laws incomplete. | G0/H1/G7/U10 |
|10 | Harmonic-rhythm transforms | G7 delay passes unchanged input as success; unrepresentable diminished durations silently retain input. | G7/U10 |
|11 | Tension/release curve | G7 fixed register/context and index-based voice-motion values are not the promised measured axes. | G7/U10 |
|12 | Fingerprints/similarity | G1 interval substring search matches1 inside11; placeholder degree profiles and incomplete structural equivalence. | G1/D0/U8 |
|13 | Motif/sequence engine | G8 lacks full extraction/landing solver; zero/unsupported sequence interval silently becomes major second. | G8/U10 |
|14 | Nonfunctional/common-tone Atlas | G8 basic triad operations exist but accept added-tone chords and drop notes; full planing/mediant/common-tone laws absent. | G8/U10 |
|15 | Chart-to-Practice | G9 can present identical right/wrong answers; complete reviewed templates/rubrics and ambiguity handling absent. | G9/D1/U11 |

AST inspection covered305 TypeScript/TSX files and6,110 function bodies. Its three
empty/constant candidates were legitimate IndexedDB/form callbacks and an event
counter. That scan did **not** establish an absence of stubs: fixed costs,
fabricated state counts and pass-through success require semantic inspection.
Source findings above include `atlas-compiler.ts`, `atlas-query.ts`,
`spelled-transposition.ts`, `tonal-journey.ts`, `contextual-continuation.ts`,
`route-planner.ts`, `harmonization-workbench.ts`, `rhythm-transforms.ts`,
`phrase-cadence.ts`, `guide-tones.ts` and the public theory/composition boundaries.

## All fifteen September product ideas

These are an additional musician-workflow campaign, not a replacement for the
fifteen discovery systems or the original foundation ideas. Four packages have
completed records; this is **4/15 package completion**, not27% of the project,
and not evidence that the four are publicly deployed.

| Idea | Current state | Existing work and prerequisite |
|---:|---|---|
|1 Play in the key you need | Workflow open; H1 currently corrupts valid input | H1 → `jcpe-transpose-workflow-nazh` spec/build/proof |
|2 Choose, hear and keep a voicing | U2 production implemented; full proof/U6 remains | U2 `.11.3` and U6 |
|3 Practice this passage with the band | Same-key arrangement repair implemented in part, named gates unresolved; complete bounded session open | `jcpe-rehearsal-session-6y2b`, U4 proof; separate key-sequence leaf follows H1 |
|4 My Charts with portable backup | Package complete; fresh local actual collection write; absent on live build | `jcpe-my-charts-zt9z` closed; preserve stale-tab/quota/atomic restore proof |
|5 Three audible next-chord choices | Smaller panel works; full semantics/audible choices incomplete | G2/U8 + existing semantic-surface |
|6 Share the exact chart | Package complete, local dialog present; older hosted share lacks the new workflow | `jcpe-exact-share-1ric` closed; exact round-trip proof remains its package evidence |
|7 Compare Original and Variation | Complete interaction not delivered | G5/U9; identical playback conditions, exact diff and one Apply/Undo |
|8 Hear and see moving voices | Partial inspector/voice engines, complete registered audible comparison missing | U3/U6, G6 as applicable |
|9 Find a fragment by musical goal | Small reviewed library exists; full goal/Atlas search incomplete | G1/D0/U8 |
|10 Fix the exact entry token | Package complete, source-offset/IME repair implemented | `jcpe-entry-repair-0h1g` closed |
|11 Focus chart | Package complete and local button present; live build lacks it | `jcpe-chart-focus-7iz1` closed |
|12 One-minute listening lessons | Complete reviewed collection/workflow open | D1; subjective listening never auto-graded |
|13 Keep bass/top while varying harmony | G4 currently violates pin conjunction | G4/U9; registered constraints at every slot |
|14 Connect sections in the available bars | G3 templates insufficient | G3/U9; exact endpoints/time and honest no-route |
|15 Review uncertain MIDI interpretation | Existing Advanced review is partial | M1 spec/build/proof, source-note ambiguity links and human review |

The original foundation ideas remain covered by the24-row checklist: exact
spelling/time, safe authoring, voicing/voice-leading, playback/export parity,
recovery, contextual education, accessibility, bounded search and standalone
release. No original scope is dropped to make a newer campaign appear complete.

## Fresh checks and their limits

### Semantic counterexamples

With the supported Bun on PATH:

```bash
bun scripts/audit-discovery-reality.ts
```

Result: **10 passing controls,19 failed checks, exit1**,29 declared checks.
This improves on September4's8/21 because literal facts and the web containment
refusal now pass. These are source-level independent law checks, not browser,
listening or release acceptance. Nineteen failed checks do not mean nineteen
independent root causes.

| Counterexample | Required behavior | Actual behavior |
|---|---|---|
| H0 contextual/scale public operations | Both functions callable | Both `undefined` |
| SHA-256 of `abc` | `ba7816bf…15ad` | `a4b8841f…85d7`; empty-string control passes |
| Atlas wrong all-`f` source digest | Reject entry | One accepted entry |
| Synthetic `fingerprint-only` entry | No chord expression retained | `chords` remains in compiled payload |
| Same Atlas IDs, changed musical payload | Changed manifest digest | Same digest |
| Query interval1 against interval11 | No match | Match |
| C6/9/E transposed up major second | D6/9/F# | D6/F# |
| Unicode D-flat major seventh | Transposed spelled root | Root unchanged |
| Three-beat G7 split into ii–V | Total remains3 | Total becomes2 despite balance claim |
| Pinned Cmaj7 with incompatible C-sharp bass | Explicit conflict/refusal | Accepted |
| G7→Am | Deceptive cadence | No cadence |
| Delay four beats by one | Five beats | Four, reported as result |
| Diminish1/960 past supported grid | Refuse unsupported1/1920 | Accept original duration |
| P operation on Cadd9 | Refuse ineligible added-tone chord | Cm, addition dropped |
| Zero-interval sequence from C | C then C | C then D |
| Practice answer options | Semantically distinct right/wrong choices | Same “E and B” is both |
| Reharm tree reaches no children | Reached depth0 | Reports requested depth2 |

The remaining failed check is **M1 specification inconsistency**. A uniform,
nonzero pitch-class histogram is unchanged by rotation. A deterministic
absolute-tonic tie-break returns the same tonic for that same input; it cannot
also rotate that single winner by one semitone. Repair the law to require
appropriate tie-set equivariance, or qualify winner equivariance to unique
optima, with explicit uncertainty. Do not falsify the tie-breaking implementation
or weaken the test to conceal the contradictory requirements. Existing M1
specification/build/proof leaves already carry this work.

### Aggregate, focused policy and model gates

| Actual invocation | Result | What it establishes |
|---|---|---|
| `bun scripts/verify.ts` | Exit1 after toolchain and discovery-execution-contract pass | Discovery-execution-evidence refuses before snapshot/browser cases; the69 remaining gates are not proved by this invocation |
| Focused four-file command below |84pass/1fail,4,497 assertions,85cases,12.59s | H0 consistency/literal/memo pass; sole policy failure lists29 illegal casts |
| `bun run predeploy:check` | Exit1, one blocking finding across11 shipping models | DSP source closure differs from embedded WASM ledger; chained instrument-quality command did not run |

The aggregate contains **71 gates**, counted from actual declarations in
`scripts/verify.ts`; README's64 was stale. A fail-fast runner is not evidence
that its unvisited gates pass.

```bash
bun test tests/static/validated-document-cast-policy.test.ts \
  tests/static/h0-contract-consistency.test.ts \
  tests/unit/h0-literal-facts.test.ts \
  tests/unit/studio-realization-playback-memo.test.ts
```

The29 policy findings are15 in e0-interchange,2 in e0-transaction-driver,7 in
e0-v2-normalization,1 in studio-recovery,3 in export/interchange and1 in
theory/analysis. The same inventory already belongs to `jcpe-cast-policy-compatibility-np17`.
This is an assertion failure, not a timeout. Preserve the sole validated-document
publication boundary rather than waiving the scanner.

The aggregate's process check uses `pgrep -af '[p]laywright'`. It matched two
unrelated idle Rod Chromium roots, PIDs1812159/1850067, because their executable
cache paths contain `ms-playwright`. Both were PPID1 with0.0CPU and foreign
`/data/retained/ftui-*-tmp/rod/user-data` profiles. No active Playwright test
runner was found. The refusal record contains zero browser cases, no commands,
zero captured inputs and empty input/bundle digests. An `inputsStable:true` field
with zero inputs is not source-stability proof. New bug
`jcpe-browser-suite-admission-0nyi` requires real active-suite identification and
race-free admission, preserving the prohibition on concurrent browser suites.
The audit did not bypass this guard and call the aggregate green.

The model-source gate records:

- Embedded closure: `1b5e336f036a14792e4f2ce1d26dde4388ed1258af75280fbeada5fa565d764f`.
- Current closure: `399235e4c09a3dbc62dd7eddf172e60063a9c42e55f0b8d180ccfd74f80e1e3c`.
- Changed files: `dsp/concert-grand/rust-toolchain.toml` (35d648a) and
  `dsp/concert-grand/src/plucked_v2.rs` (87485cf); no added/removed inputs.

The latter change supports native cooperative render cancellation. Reconcile
intentional source changes with their owner, rebuild the correct payload and
refresh the required exact-input evidence. Merely editing recorded hashes,
reverting another contributor's work, or shipping the current HTML would not
resolve the mismatch. Existing u90y already owns residual embed/source drift.

### Real browser journeys

`A/browser.mjs` ran sequentially under real Node with fresh contexts, native
Web Audio, actual IndexedDB and downloads. Eight cases were declared before
execution: file, local HTTP, Cloudflare and Vercel, each at1280/390px. Network
was denied except the top-level navigation. Instrumentation observed real
AudioContext/source methods; it did not replace the renderer with fake audio.

- **Four local cases pass:** boot, no audio at boot, no horizontal overflow,
  title edit/Undo/Redo, parsed canonical JSON download, exact Share dialog,
  actual My Charts write, and Play/Stop. Each used one AudioContext,4–5 native
  source starts and8–10 stop calls. Zero console/page errors or extra requests.
- **Four hosted cases fail to complete the new-feature journey.** Both
  Cloudflare widths and Vercel desktop reach editing/download but cannot find
  the new exact-sharing dialog. Vercel phone instead hits a harness timing
  limitation: Escape arrives before the older export dialog settles, leaving
  that dialog over Share. That fourth failure is not a proven separate product
  defect. All original results remain failed, with screenshots/logs retained.
- A separately scoped, four-case `A/host-playback.mjs` inspection then passes
  boot/layout and actual Play/Stop on both hosts at both widths. It finds no
  My Charts or Focus button. This does not retry or turn the original four
  failures green. The old build works within its older scope.
- Cloudflare emits the expected blocked-analytics CSP console error in every
  inspected context; Vercel emits no console/page error. No horizontal overflow
  was observed. Desktop/phone screenshots were visually inspected; the default
  phone header remains crowded, so no broad usability certification follows.

These12 cases are diagnostic Chromium observations, not the complete release
matrix, native no-future-attack/resource proof, an axe accessibility pass,
end-to-end MIDI semantic parity or a human timbre audition. Presence of Share is
not fresh proof of every bounded-fragment/Manual/Frozen round-trip law. Those
remain attached to their package evidence.

### Retained proof and explicit outstanding acceptance

The separate `test-results/loop-final-evidence-2026-09-08/` packet contains the
prior supported-toolchain typecheck/lint,44-pass memo/artifact packet, ten actual
source-fault checks, and12 passing real file/HTTP adapter cells across Chromium,
Firefox and WebKit at two widths. Its1,482-input manifest digest is
`0bb0e4515a596350b90f7c4eb9acd9a38eca1c7260592b778685c4d87a042c34`.
The audit captured the same hashes/mtimes. This is retained same-input proof,
not a fresh rerun of those full suites and not a complete DSP source closure.

The retained regression runs are **193pass/13fail** (206cases,6,428 assertions)
and **38pass/1fail** (39cases,2,681 assertions). Failures are the unchanged
five-second limits: ten notification-authority cases, up-tempo playback, seek,
guitar pitch-folding and an expanded Deacon Blues fixture. Some include explicit
settling time. A timeout alone does not identify a musical root cause; none is
waived, retried into green, quarantined or represented as completed acceptance.
They remain under ordinary-loop/rehearsal and U4 work.

Native September7 evidence reports72 unit,11 iPhone and1 iPad cases for commit
87485cf. This Linux audit did not execute Xcode, a physical Apple device or an
App Store submission. Native source, generated bridge and actual Apple runtime
must be verified together after semantic changes. Human X0/U0/M1/external-MIDI
and physical-instrument listening leaves remain open. Missing human observation
is neither an automated pass nor a demonstrated sound-quality failure.

## Bridge: smallest useful order without losing the full vision

Effort below is relative scope, not a calendar estimate. Claim only a ready leaf
and retain current assignees. Each existing package keeps specification/fixtures,
production implementation and independent proof in that order. “Close the gate”
means fix the defect and execute the named proof, not modify the threshold.

| Order | Concrete change and value | Dependency / rough scope | Observable success and preserved requirements |
|---:|---|---|---|
|1 | Reconcile DSP source/payload and acceptance inputs so a real candidate can ship | Existing u90y; medium, pinned toolchain/replay work | Correct rebuilt WASM, model and instrument-quality gates, real playback/recovery and planted pitch negatives; no hash-only repin |
|2 | Restore honest automated gate execution; fix29 publication casts and remaining transport timing failures | New admission bug + np17 + existing U4/rehearsal .5; small-to-medium admission, medium policy/runtime | True concurrent-suite refusal and idle-browser success; legal publication; full original regression cases pass with their unchanged thresholds |
|3 | Finish the already implemented U2 experience and H0 authority/context/scale | U2 proof with IvoryBluff; H0 retains aggregate/human dependencies; medium-to-large | Exact choose/hear/keep/Undo and all notes/spellings; all three H0 operations with counterevidence, ambiguous/no-key and scale laws |
|4 | Repair H1 and deliver explicit scope/key transposition, then complete voicing comparison | H0/H1 specs and proof → transpose workflow/U6; medium-to-large |6/9, Unicode, slash/double accidentals, all roots/inverse cases; explicit Manual/Frozen policy, audible preview, one Apply/Undo |
|5 | Turn finished shared execution into three real audible continuation choices | Repaired H0/G2 + gdcw already complete → U8/semantic-surface; large | Whole-context valid results, fewer when necessary, full More view; revision-bound Preview/Insert/Undo with real audio, exact exports and stale rejection |
|6 | Repair G1 hashing/rights/query and author the reviewed Atlas, expose useful goal filters | G1/D0/U8, H1 for variants; large including human content review | Known SHA vectors, tamper refusal, no protected expression in fingerprint-only output,250 reviewed seeds/3,000 lawful variants within reservation |
|7 | Complete route, constraints, reharm, voice/color/rhythm/tension/sequence/practice engines and workbenches | G3–G9/U9–U11/D1, dependency-ordered; large | Every hard constraint honored; independent finite-oracle optimality/limits, real measured counters, all fifteen full workflows retained |
|8 | Complete physical/native and release acceptance; deploy committed accepted bytes | Existing PHS/uo47/Q0/R0/human leaves; mixed engineering and human work | Complete named matrix without skipped/retried gates, actual Apple/audio observations, both hosts hash-match Git and pass desktop/phone feature checks |

Some work can proceed independently, but order is not permission to hoard leaves
or block ordinary runtime repairs behind the entire discovery program. Same-key
rehearsal stays separate from its H1-dependent key sequence. The existing
64-pass rehearsal bound, count-in, tempo interpolation and all-key scope remain;
a repaired ordinary loop is only one prerequisite.

H0/spec's aggregate requirement and np17's explicit human-listening dependency
currently couple foundational work to broad acceptance. This is a real scheduling
constraint. If the owner wants a narrower specification exit gate, it needs an
explicit contract decision; the audit neither removes that dependency nor claims
H0/build is ready.

### Three ambition rounds

1. **Adopt the finished execution service.** The previous audit correctly
   requested gdcw; it now exists. Reuse it for a full G2 musician journey instead
   of building another worker/protocol or declaring a protocol fixture to be a
   musical engine. Compare cached and uncached results under the full immutable
   semantic/algorithm/corpus tuple. Preserve bounded cancellation and real audio
   retirement. This refinement was added to the existing semantic-surface build.
2. **Make exactness observable end to end.** Extend the existing cross-surface
   proof from engine output through preview, Apply, Undo, recovery, exact JSON
   and parsed MIDI. Compare actual registered notes and rational times, including
   duplicate Manual unisons, odd meters, ambiguous keys and stale requests.
   Native bridge payloads must preserve the same semantic authority; SwiftUI
   guards alone do not prove equivalence. Keep all UI and native owners.
3. **Use stronger algorithms with independent finite oracles.** Retain bounded
   k-best tonal paths, lawful graph search with proved dominance/admissible
   pruning, whole-sequence constraint propagation, registered voice-path
   optimization, collision-checked exact fingerprints and unambiguous practice
   rubrics. Costs and visited/retained states must be measured from real work.
   Distinguish a sufficient conflict set from a minimal one, a bounded partial
   result from an optimum, and model scores from calibrated probabilities.
   Original engine Beads already contain these refinements; they remain binding.

No runtime model, network service, extra framework, telemetry or competing chart
schema is introduced. Stronger mathematics must improve a musician's result and
remain executable against an independently authored oracle.

### Refinement passes and convergence

The frozen Phase5 operator below was applied to the current bridge and Beads.

| Pass | Review and actual disposition |
|---:|---|
|1 Scope | Matched24 vision goals,15 discovery systems and15 September ideas to current consumers and owners. Credited completed U5/gdcw/four workflows; retained full unfinished engines. Reopened only the demonstrably false-closed U4 parent. |
|2 Dependencies | Added the observed admission bug as a blocker of Q0/verify and R0/verify. Preserved active owners, existing human constraints and the separate same-key/key-sequence rehearsal graph. |
|3 Proof | New admission leaf requires independent false-positive/refusal twins, real concurrent entrants, child-lifetime proof and planted negatives. Existing semantic-surface build now explicitly consumes completed gdcw and retains exact full-journey proof. |
|4 Release truth | Attached fresh DSP hashes to existing u90y,29-cast replay to np17 and current H0 callable/gate status to its spec. Updated Q0/R0 with exact source/host hashes,71 gates and all failure limits. |
|5 User and data integrity | Corrected README's obsolete U2/U5/F0-only/size/gate claims. Kept all15 advanced requirements, source-note/Manual/Frozen invariants, real downloads, mobile Stop/Exit and human listening. Bound assessment scope to what was actually read/executed. |
|6 Final convergence | Read back changed records; validate dependency cycles, triage and plan, source stability and documentation diff. No additional uncovered scope warrants another package. Graph health is plan evidence, not implementation acceptance. |

Finishing the previously open list would have left the newly observed admission
failure uncovered, and the closed U4 parent misrepresented package completion.
After correction, all **identified** gaps have existing or new implementation
and proof coverage. Finishing those tasks with their actual named gates passing
would close this identified gap. This is not a guarantee against undiscovered
bugs, a feature-completion percentage or a release-date prediction.

## Exact Beads coverage and next actions

| Work | Exact IDs | Audit action / current state |
|---|---|---|
| Assessment | `jcpe-reality-check-september-5h2l` | Reopened/claimed for this request; only this assessment is eligible for closure from the audit |
| Browser admission | `jcpe-browser-suite-admission-0nyi` | One new unclaimed bug with fixtures → implementation → real process proof; Q0/R0 verify depend on it |
| Source/WASM release drift | `jcpe-playback-gate-ship-failures-u90y` | Existing residual scope updated; not a new pitch-failure claim |
| Publication casts | `jcpe-cast-policy-compatibility-np17` | Existing29 findings freshly reproduced; ubuntu and dependencies retained |
| H0 | `jcpe-milestone-musical-intelligence-qqy.2` | Spec `.1` and build `.2` remain blocked; literal operation credited, contextual/scale operations absent |
| H1 / G0 / G1 / G2 | `jcpe-milestone-musical-intelligence-qqy.3` / `.4` / `.5` / `.6` | Existing open spec/build/proof phases retain counterexamples and full scope |
| G3–G9 | `jcpe-milestone-advanced-craft-ulj.3` through `.9` | Existing full algorithms and independent proof retained |
| Shared execution | `jcpe-discovery-execution-proof-gdcw` | Completed epic and three phases remain closed; no duplicate scheduler package |
| Semantic integration | `jcpe-semantic-surface-conformance-32w2` | Spec `.1` closed, build `.2`/proof `.3` open, narrow web repair `.4` closed; updated actual-consumer obligations |
| U2 / U4 / U5 | `jcpe-milestone-reliable-studio-l3a.11` / `.12` / `.13` | U2 proof active; U4 parent reopened without touching children; U5 fully closed |
| UI delivery | `jcpe-milestone-musical-intelligence-qqy.8` / `.9`; `jcpe-milestone-advanced-craft-ulj.10` through `.14` | U3/U8 and U6/U7/U9/U10/U11 respectively; retain existing ownership and full workflow scope |
| Content | `jcpe-milestone-musical-intelligence-qqy.7`; `jcpe-milestone-advanced-craft-ulj.15` | D0/D1 obligations retained, not replaced with the small existing library |
| M1 | `jcpe-765f`, `jcpe-ionn`, `jcpe-upbz`, `jcpe-qbvz` | Existing parent/spec/build/proof open; reconcile tied-key law and source-note uncertainty |
| Rehearsal / transposition | `jcpe-rehearsal-session-6y2b`; `jcpe-transpose-workflow-nazh` | Open; ordinary-loop `.5` handed back open/unassigned so this assessment does not hoard two claims |
| Release | `jcpe-milestone-release-proof-pv1.1` / `.2` | Q0/R0 stay open with fresh release liabilities |
| Native / physical | `jcpe-ios-quality-verification-uo47`, `jcpe-mnsc` | Existing engineering and actual-device/listening scope unchanged |
| Explicit human / host work | `jcpe-60xy`, `jcpe-rh3a`, `jcpe-wyw5`, `jcpe-espf`, `jcpe-mnsc.9.4`, `jcpe-2nky` | No automated substitution or CSP waiver |

The existing [implementation TODO](IMPLEMENTATION_TODO.md) carries the granular
remaining execution checklist. Before any implementation, use `br ready --json`
and `br show <id> --json`; bv's broader actionable ranking is not claim authority.
Every mutation in this assessment used `br` and was followed by
`br sync --flush-only`. Final graph/results are recorded on the assessment Bead.
### Assessment acceptance

Final `bv --robot-triage`, `bv --robot-insights` and `bv --robot-plan` each
exit0. Authority reports526 valid/visible records, zero load errors and no stale
source; `br dep cycles --json` reports zero cycles. Although bv names the
historical-looking `beads.base.jsonl` path, its loaded authority matches the live
526-record workspace; the filename alone is not evidence of staleness.
`br ready --json` returns10 rows, including the new admission bug and ordinary
loop repair. The higher-ranked human listening tasks cannot be replaced with
an automated audition.

The final1,482-input hash/mtime check reports `changed: []` and digest
`0bb0e4515a596350b90f7c4eb9acd9a38eca1c7260592b778685c4d87a042c34`.
HEAD remains9964f76; Git HTML, root HTML and `dist/index.html` are byte-identical.
`git diff --check` passes and all local Markdown link paths in the three edited
documents resolve. Only README, this report, the existing TODO and Beads changed.
No production, test, gate, configuration or generated artifact changed. Assessment
closure records these checks separately from the red project/release gates.

### Frozen Phase 3a operator

```text
OK so please take ALL of that and elaborate on it and use it to create a comprehensive and granular
set of beads for all this with tasks, subtasks, and dependency structure overlaid, with detailed
comments so that the whole thing is totally self-contained and self-documenting (including relevant
background, reasoning/justification, considerations, etc.-- anything we'd want our "future self" to
know about the goals and intentions and thought process and how it serves the over-arching goals of
the project.) The beads should be so detailed that we never need to consult back to the original
markdown plan document. Remember to ONLY use the `br` tool to create and modify the beads and add
the dependencies.
```

### Frozen Phase 5 operator

```text
Check over each bead super carefully-- are you sure it makes sense? Is it optimal? Could we change
anything to make the system work better for users? If so, revise the beads. It's a lot easier and
faster to operate in "plan space" before we start implementing these things! DO NOT OVERSIMPLIFY
THINGS! DO NOT LOSE ANY FEATURES OR FUNCTIONALITY! Also make sure that as part of the beads we
include comprehensive unit tests and e2e test scripts with great, detailed logging so we can be
sure that everything is working perfectly after implementation. Make sure to ONLY use the `br` cli
tool for all changes, and you can and should also use the `bv` tool to help diagnose potential
problems with the beads.
```
