# bv (beads viewer) local config and caches
.bv/

# Dependency and build output
node_modules/
dist/

# Browser, test, and coverage evidence generated by local runs
blob-report/
coverage/
playwright-report/
test-results/

# Tool caches and temporary reproducibility roots
.bun/
.eslintcache
.tmp/
*.tsbuildinfo

# OS and editor residue
.DS_Store
Thumbs.db

# Reference material only: third-party sequences of copyrighted works are
# never committed. Used locally to measure aggregate style statistics.
*.mid
*.midi

# === multi-repo ephemeral patterns 2026-08-05 ===
# Beads machine-local markers (canonical tracker is issues.jsonl)
.beads/.local_version
.beads/.migration-hint-ts
# NTM swarm runtime state
.ntm/
# RCH virtual FS workload workspaces
.rch-vfs-*/

# === multi-repo ephemeral patterns 2026-08-15 ===
# Qwen Code local project settings (machine-specific)
.qwen/
